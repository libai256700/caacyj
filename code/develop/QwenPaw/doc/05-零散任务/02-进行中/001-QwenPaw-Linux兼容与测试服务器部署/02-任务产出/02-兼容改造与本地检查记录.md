# QwenPaw 兼容改造与本地检查记录

## 1. 结论

- 实施日期：`2026-09-03`
- 当前结论：`通过，存在非阻塞测试依赖缺口`
- 改动范围仅为 `.gitattributes` 与 `docker-compose.yml`；未修改核心 Python、`workspaces`、`C:\Users\renquan\.qwenpaw` 或 `uni_modules/**`。
- `.gitattributes` 将全部 `*.sh` 固定为 LF；Compose 镜像改为可通过 `QWENPAW_IMAGE` 注入，默认行为保持不变。

## 2. 正式本地环境

- Conda 根目录：`D:\ProgramData\anaconda3`
- 环境名：`qwenpaw`
- 解释器：`D:\ProgramData\anaconda3\envs\qwenpaw\python.exe`
- Python：`3.12.13`
- 源码版本：`2.2.0`
- 本地 QwenPaw 命令均通过该 Conda 环境执行；未使用系统 Python 冒充项目检查。

## 3. 检查结果

| 检查项 | 命令口径 | 结果 |
| --- | --- | --- |
| Git 远端预检 | `preflight_git_remote.py E:\huiyitechworkspace\QwenPaw` | 通过；`main` 与 `origin/main` 初始差异 `0/0` |
| Git 差异 | `git diff --check` | 通过 |
| Shell 换行 | `git ls-files --eol -- '*.sh'` | 全部为 `i/lf w/lf attr/text eol=lf` |
| Shell 语法 | Git Bash 对全部已跟踪 `*.sh` 执行 `bash -n` | 通过 |
| Compose 展开 | 设置 `QWENPAW_IMAGE=qwenpaw:precommit-check` 后执行 `docker compose config --quiet` | 通过，展开镜像与注入值一致 |
| Python 编译 | Conda `qwenpaw` 执行 `python -m compileall -q src/qwenpaw` | 通过 |
| 源码导入 | Conda `qwenpaw` 通过 `PYTHONPATH=src` 导入 `qwenpaw` | 通过，版本 `2.2.0` |
| Pytest | Conda `qwenpaw` 执行 `python -m pytest` | 未执行；该既有环境未安装 `pytest`，未擅自安装或重建环境 |

## 4. 本地配置保护基线

- 仓库 `workspaces`：44 个文件；按“相对路径、大小、SHA-256”排序聚合摘要为 `2ABF19E4F6EFCFDB8D73DC361CC071A7A6FFA424B73C405CACB5D9BC3D1F4F2E`。
- `C:\Users\renquan\.qwenpaw` 配置集合：460 个文件；仅纳入 `config.json`、`settings.json`、`nm-bridge.json`、`HEARTBEAT.md` 及 `governance/skill_pool/workspaces`，排除运行日志和计量文件；同口径聚合摘要为 `F3B3EBF72DD8251EB76B739A2531354F8B2A248FAA9CE5BBC2A0A8493FFBEF45`。
- 部署结束后必须用同一算法复算并比较；摘要不包含文件内容或敏感值。

## 5. 已知限制

- 本机 Docker CLI 与 Compose 可用，但 Docker Desktop daemon 未运行，因此未在本机构建镜像。
- 采用服务器端从已提交 Git SHA 源码快照构建镜像；镜像标签和 OCI label 必须绑定该 SHA。
- 本地 `pytest` 缺口不替代测试环境中的真实接口、持久化和重启验证；正式验收由独立代理执行。
