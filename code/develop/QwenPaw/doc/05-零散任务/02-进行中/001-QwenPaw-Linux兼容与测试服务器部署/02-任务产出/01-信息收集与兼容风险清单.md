# 信息收集与 Linux 兼容风险清单

## 1. 结论

- 信息收集状态：`DONE_WITH_CONCERNS`。
- 测试服务器标识、操作系统、SSH 别名、登录用户、端口、私钥相对路径、部署目录及当前 QwenPaw 容器事实均有正式记录，且本机私钥文件存在；本阶段未执行 SSH。
- 正式记录显示 QwenPaw 已在测试机运行，不是首次部署。现有实例占用 `127.0.0.1:8088`，后续必须按“备份现有 Compose/配置/数据卷 -> 受控替换 -> 健康与重启复验 -> 可回退”推进。
- QwenPaw 核心 Python 启动、路径解析和容器持久化已具备 Linux 兼容设计。最小必改范围集中在 `.gitattributes` 与 `docker-compose.yml`，不需要修改核心 Python 代码。
- 阻塞性缺口：正式记录没有 QwenPaw 的对外访问 URL、Nginx 代理前缀或对应配置镜像，仅有服务器本机健康接口；若 `BA-T0001-INT-001` 要求从服务器外部访问，必须先由用户确认入口或补齐正式记录。

## 2. 正式记录来源与测试服务器事实

> 下表不复述真实 IP、登录用户或密钥名；实施时必须回到正式记录读取，不得从本文还原连接参数。

| 事实 | 脱敏结论 | 证据 |
| --- | --- | --- |
| 环境与主机 | `TEST` 唯一正式主机为 `TEST-3DAPP-MICRO-01` | `E:\huiyitechworkspace\devcenter-config-center\Operations\Servers\Test\info.md:5-17`；`...\ServerList-01.md:5-14` |
| 系统与能力 | CentOS 7.6；已登记 Docker、Nginx、Java、Node 等能力 | `...\TEST-3DAPP-MICRO-01\server-info.md:5-29` |
| SSH 正式入口 | 使用正式 SSH alias；模板明确主机、登录用户、非默认端口及服务器目录内私钥引用 | `...\Operations\Servers\ssh-config.example:5-9`；`...\TEST-3DAPP-MICRO-01\server-info.md:12-20` |
| 私钥可用性 | 正式相对路径对应的本机文件存在；未读取密钥内容 | 只读 `Test-Path` 核对；引用规则见 `...\Operations\Servers\server-common-config.md:28-44` |
| 通用部署目录 | 应用 `/home/soft/jar/`、日志 `/home/soft/logs/`、脚本 `/home/soft/script/`、备份惯例 `/home/soft/backup/<时间戳>/` | `...\TEST-3DAPP-MICRO-01\server-info.md:31-38`；`...\TEST-3DAPP-MICRO-01\AppList-01.md:98-107` |
| 当前 QwenPaw | 已运行 Docker 容器；部署目录 `/home/soft/jar/qwenpaw`；Compose 位于该目录；使用 ACR 镜像；仅绑定回环地址 `8088`；两个本机运行时接口已有通过记录 | `...\TEST-3DAPP-MICRO-01\AppList-01.md:72` |
| 端口接管与回退背景 | 原 `fly-llm` 已停止并由 QwenPaw 接管 `8088`；正式记录保留原服务备份与恢复口径 | `...\TEST-3DAPP-MICRO-01\AppList-01.md:59` |
| 应用侧连接 | QwenPaw 当前部署不依赖 `ApplicationConnections/Test/applications.md` 内的数据库或应用账号；该文件不是本次 SSH 入口 | `E:\huiyitechworkspace\devcenter-config-center\Operations\ApplicationConnections\Test\applications.md`；服务器连接归 `00-0600` 正式入口管理 |

正式记录完整性判断：服务器连接与部署定位足够；QwenPaw 对外业务入口不完整。`ApplicationConnections/Test/applications.md` 还存在部分中文乱码，但与本次 QwenPaw 容器连接链路无直接依赖，不据此猜测入口。

## 3. 参考工程的 Linux 落地模式

参考工程只用于证明同一测试环境已经采用的落地方式，不把 Java 参数照搬到 QwenPaw。

| 模式 | 可复用口径 | 证据 |
| --- | --- | --- |
| 正式入口与目录 | 本机脚本使用正式 SSH alias，上传到 `/home/soft/tmp`，正式程序与脚本分别落到 `/home/soft/jar/...`、`/home/soft/script/...` | `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji-admin-server\Operations\deploy-test.ps1:3-11,28-35` |
| 先备份再替换 | 远端按时间戳建立 `/home/soft/backup/...`，复制现有程序和启动脚本后才替换 | `...\Operations\deploy-test.ps1:36-47` |
| 权限与换行 | 敏感 env 文件设为 `600`；启动脚本 `chmod +x` 并执行 `sed -i 's/\r$//'` | `...\Operations\deploy-test.ps1:48-72` |
| 进程与日志 | 启动脚本使用固定应用/日志/PID 路径，优先正常停止，必要时强制停止，再以 `nohup` 启动并记录 PID | `...\Operations\yunjikeji-admin-server-start.sh:4-24` |
| 显式运行配置与健康门禁 | 显式选择 `test` profile；部署后轮询 `127.0.0.1` 健康接口，超时则失败退出 | `...\Operations\yunjikeji-admin-server-start.sh:23`；`...\Operations\deploy-test.ps1:74-82` |

QwenPaw 应对应采用 Docker Compose 的 `restart`、容器健康检查和数据卷备份，不应复制 Java 的 `pgrep/java -jar` 细节。

## 4. QwenPaw 启动、依赖与配置事实

| 项目 | 当前事实 | 证据 |
| --- | --- | --- |
| 官方入口 | Python 包命令为 `qwenpaw`；服务命令为 `qwenpaw app` | `E:\huiyitechworkspace\QwenPaw\pyproject.toml:119-122`；`README_zh.md:110-116` |
| Python 版本 | 要求 Python `>=3.11,<3.14`；容器镜像已自行安装 Python、Node、Chromium、Supervisor 等，不依赖 CentOS 7.6 宿主机的 Python/JDK | `pyproject.toml:6-77`；`deploy\Dockerfile:19-95` |
| 监听 | CLI 默认 `127.0.0.1:8088`；容器 Supervisor 显式启动 `qwenpaw app --host 0.0.0.0 --port ${QWENPAW_PORT}`；Compose 再限制宿主机只绑定 `127.0.0.1:8088` | `src\qwenpaw\cli\app_cmd.py:81-94,151-168`；`deploy\config\supervisord.conf.template:14-24`；`docker-compose.yml:17-18` |
| 进程管理 | 容器由 Supervisor 管理 app、D-Bus、Xvfb、XFCE；Compose 配置 `init: true` 和 `restart: always` | `deploy\config\supervisord.conf.template:1-42`；`docker-compose.yml:11-18` |
| 配置目录 | `QWENPAW_WORKING_DIR` 优先，其次兼容旧目录，最终默认 `~/.qwenpaw`；路径统一通过 `pathlib.Path` 展开解析 | `src\qwenpaw\constant.py:101-120` |
| 敏感与备份目录 | `QWENPAW_SECRET_DIR`、`QWENPAW_BACKUP_DIR` 可显式配置；容器分别固定到 `/app/working.secret`、`/app/working.backups` | `src\qwenpaw\constant.py:122-131,285-298`；`deploy\Dockerfile:25-30` |
| 持久化 | Compose 使用三个独立 named volume 保存工作数据、密钥和备份；仓库本地 `workspaces` 不会被 Dockerfile 复制，也不会被该 Compose 挂载 | `docker-compose.yml:3-9,23-26`；`deploy\Dockerfile:97-114` |
| 初始化 | 容器只有在持久卷缺少 `config.json` 时才执行默认初始化；已有配置会跳过 | `deploy\entrypoint.sh:36-44` |
| Linux 浏览器依赖 | 镜像安装系统 Chromium/Xvfb，并显式设置 Chromium 路径、跳过 Playwright 浏览器下载、声明容器态 | `deploy\Dockerfile:62-90` |

## 5. 风险分类与最小建议

### 5.1 必须修改

| 文件 | 风险 | 最小修改 | 验证 |
| --- | --- | --- | --- |
| `.gitattributes`（并规范化相关工作树文件） | Git blob 为 LF，但当前 Windows 工作树中 `deploy/entrypoint.sh`、`scripts/docker_build.sh`、`scripts/docker_sync_latest.sh` 为 CRLF；`.gitattributes` 仅对 `scripts/check-channels.sh` 固定 LF。若直接用当前工作树构建/上传，入口脚本可能出现 `bad interpreter` 或 `\r` 语法问题 | 增加 `*.sh text eol=lf`，重新规范化已跟踪 shell 文件；至少确保 `deploy/entrypoint.sh` 为 LF | `git ls-files --eol -- '*.sh'` 全部显示 `w/lf`；在 Linux/容器执行 `sh -n deploy/entrypoint.sh` |
| `docker-compose.yml` | 当前硬编码 `agentscope/qwenpaw:latest`，与测试机正式记录的 ACR 镜像来源不同，且 `latest` 不能绑定本次 Git 提交，不满足可追溯部署 | 将镜像改为可由环境变量注入且要求部署时使用本次提交对应的不可变 tag 或 digest；默认源需与正式测试环境 ACR 口径对齐。禁止把 registry 凭据写进仓库 | `docker compose config` 展开到预期 ACR 完整镜像及不可变版本；部署记录同时写 Git SHA、镜像 tag/digest |

说明：`docker-compose.yml` 的顶层 `version: '3.8'` 在当前 Compose 中只产生“已废弃、被忽略”的警告，不构成 Linux 启动阻塞，可在同文件修改时顺带移除，但不单独扩大范围。

### 5.2 仅部署配置处理

| 风险/事项 | 处理要求 | 证据 |
| --- | --- | --- |
| 现有实例与端口冲突 | 先备份现有 `/home/soft/jar/qwenpaw`、Compose、容器配置及三个 named volume，再受控停止/替换；不要直接覆盖运行中容器 | 正式记录 `AppList-01.md:59,72` |
| 持久化与本地保护 | 复用服务器现有三个 named volume，升级前后核对 volume；不得复制或修改本机 `workspaces`、`C:\Users\renquan\.qwenpaw` | `docker-compose.yml:3-9,23-26`；任务计划保护约束 |
| 配置与密钥 | API Key 继续使用 secret volume 中的加密态；额外运行时变量通过服务器受控 `.env`/环境注入，文件权限最小化，不提交仓库 | 正式记录 `AppList-01.md:72`；镜像目录定义 `deploy\Dockerfile:25-30` |
| 宿主机权限 | 当前容器内 Supervisor 明确以 root 运行；不要在宿主机对部署目录/数据卷使用 `chmod 777`。仅保证 Docker 可读取 Compose 且 volume 由当前容器用户可读写 | `deploy\config\supervisord.conf.template:1-5` |
| 健康与重启 | 使用正式记录的两个本机接口；增加 Compose healthcheck 或部署脚本轮询，并至少完成一次 `docker compose restart` 后复验 | 正式记录 `AppList-01.md:72`；参考工程 `deploy-test.ps1:75-82` |
| 外部访问与鉴权 | 端口当前只绑定回环地址。若增加 Nginx 代理，必须先确认正式 URL/前缀；对非可信访问启用 `QWENPAW_AUTH_*` 或保持受控网络边界 | `docker-compose.yml:17-22`；`deploy\entrypoint.sh:16-33` |
| 镜像拉取 | 先验证测试机能拉取指定 ACR tag/digest；如需要 registry 登录，正式记录未提供凭据，不得猜测，立即请求用户介入 | 当前服务器镜像来源见 `AppList-01.md:72` |

### 5.3 无需修改

| 项目 | 判断 | 证据 |
| --- | --- | --- |
| 核心路径拼接 | 工作、密钥、备份及媒体目录使用 `pathlib.Path` 和环境变量，不依赖 Windows 绝对路径或反斜杠拼接 | `src\qwenpaw\constant.py:101-131,186-190,285-305` |
| CentOS 宿主机运行时 | 采用容器部署时，Python/Node/Chromium 由 Debian 系运行镜像提供；宿主 CentOS 只需当前已登记且正在使用的 Docker 能力 | `deploy\Dockerfile:19-95`；正式记录 `server-info.md:22-29`、`AppList-01.md:72` |
| 进程常驻 | Supervisor 与 Compose restart 已提供容器内外两层常驻管理，不需要新增 systemd 或 Java 风格 nohup 脚本 | `supervisord.conf.template:1-42`；`docker-compose.yml:14-16` |
| 默认端口与持久化结构 | 8088、三个持久卷和容器目录与服务器现有登记一致 | `docker-compose.yml:3-9,17-26`；正式记录 `AppList-01.md:72` |
| 核心 Python 代码 | 本次最短证据链未发现启动主链路中未受保护的 Windows 绝对路径、反斜杠或仅 Windows 命令；已有 Linux sandbox 和容器运行时实现，不建议为部署扩大改动 | `src\qwenpaw\constant.py`、`src\qwenpaw\cli\app_cmd.py`、`deploy\` |

## 6. 人工依赖与止损点

1. **必须补充：QwenPaw 对外测试入口。** 正式记录只有 `127.0.0.1:8088` 的运行时接口，没有对外 URL、Nginx 前缀和配置文件入口。未补齐前只能完成服务器本机健康验收，不能声称 `BA-T0001-INT-001` 的外部正式入口已通过。
2. **条件性依赖：ACR 拉取认证。** 正式记录证明现有 ACR 镜像在运行，但未记录 registry 登录材料。若指定不可变镜像无法匿名拉取，立即停止并请用户提供正式凭据入口。
3. **无当前 SSH 人工缺口。** SSH alias、登录用户、端口、私钥相对路径均已记录，且本机密钥文件存在；后续连接仍须严格使用正式 alias，不得在产出或命令记录中展开敏感参数。
4. **禁止以参考工程替代现状。** 本阶段未 SSH，不能断言服务器当前 Compose、volume、环境变量和文件权限与仓库模板完全一致；部署阶段必须先只读核对并备份现状，新证据不一致时立即止损。

## 7. 下一阶段最小文件范围

- 必改：`E:\huiyitechworkspace\QwenPaw\.gitattributes`
- 必改：`E:\huiyitechworkspace\QwenPaw\docker-compose.yml`
- 只做换行规范化并验证，原则上不改语义：`E:\huiyitechworkspace\QwenPaw\deploy\entrypoint.sh`、`E:\huiyitechworkspace\QwenPaw\scripts\*.sh`、插件目录内已跟踪的 `*.sh`
- 只读验证：`deploy\Dockerfile`、`deploy\config\supervisord.conf.template`、`src\qwenpaw\constant.py`、`src\qwenpaw\cli\app_cmd.py`
- 不建议修改：核心 Python 业务代码、参考工程、开发中心记录、本地 `workspaces`、`C:\Users\renquan\.qwenpaw`

## 8. 本阶段验证记录

- `docker compose config`：通过；仅报告顶层 `version` 已废弃警告。
- `git ls-files --eol`：确认关键 shell 文件 blob 为 LF，但当前 Windows 工作树为 CRLF，且未命中 eol 属性。
- `sh -n deploy/entrypoint.sh`：本机无可用 POSIX shell，未执行；必须在下一阶段 Linux/容器内补验。
- 未执行 SSH、未修改 QwenPaw 代码/配置、未修改参考工程或开发中心记录、未读取或输出私钥内容。
