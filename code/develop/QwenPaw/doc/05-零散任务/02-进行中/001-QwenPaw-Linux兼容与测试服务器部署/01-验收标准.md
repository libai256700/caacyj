# 验收标准

> 本文只定义正式验收标准；执行证据和结论统一写入 `02-任务产出/`。

## 1. 文档信息

- 任务ID：`T0001`
- 任务名称：QwenPaw与yunjikeji测试环境部署
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0001-INT-001 | 业务-交互级 | 验收人员通过开发中心正式 SSH 入口在测试服务器本机访问 QwenPaw | 从服务器本机真实入口触发访问，完成服务可用性检查和至少一条核心交互链路，结果由本次部署容器承接 | 服务稳定运行；本机入口可访问；核心交互成功；返回与日志能证明请求由本次部署实例处理 | SSH 命令记录、服务器本机访问结果、容器日志、进程与端口证据 | 无法访问、核心交互失败、使用假数据或请求未落到本次部署实例 |
| BA-T0001-INT-002 | 业务-交互级 | 用户重启测试服务器上的 QwenPaw 后再次访问 | 使用正式启停入口完成重启，既有测试配置和持久化目录仍生效，服务恢复可用 | 重启命令成功；服务自动或按文档恢复；重启前后必要配置与持久化数据一致；再次访问成功 | 重启命令、状态、日志、配置/目录核对和再次访问证据 | 重启失败、配置丢失、依赖手工临时命令才能恢复或访问失败 |
| BA-T0001-INT-003 | 业务-交互级 | 验收人员在测试环境访问更新后的 yunjikeji-admin-server | 从正式健康或业务入口发起真实请求，请求由本次更新实例承接且既有核心服务可用 | 服务稳定运行；正式入口成功；日志能对应本次请求；重启后仍可用 | 测试环境请求、进程、端口、日志和重启证据 | 服务无法启动、入口失败、请求未落到本次实例或重启后不可用 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0001-API-001 | BA-T0001-INT-001 | 技术-接口级 | 测试服务器正式启动入口、健康检查或实际访问入口契约有效 | 启动命令退出状态正确；目标进程与端口存在；健康检查或真实入口连续成功；错误时日志可定位 | 正式启动命令、进程、监听端口、HTTP/命令返回与日志 | 启动报错、进程退出、端口未监听、入口非预期错误或无有效日志 |
| TA-T0001-API-002 | BA-T0001-INT-002 | 技术-接口级 | 正式停止、启动和重启入口可重复执行 | 至少完成一次受控重启，重启后健康与实际访问均恢复；命令不依赖当前交互 shell 的临时状态 | 服务管理命令、退出码、重启前后 PID/状态、访问证据 | 无正式启停入口、重复执行失败、依赖临时环境或重启后不可用 |
| TA-T0001-DATA-001 | BA-T0001-INT-001 | 技术-数据级 | Linux 配置、路径、环境变量、权限、换行和日志落位符合正式服务器事实 | 不存在生效中的 Windows 绝对路径或反斜杠拼接；路径由标准 API/环境变量/显式配置解析；脚本可执行；配置和日志位于计划目录且权限最小可用 | 配置差异、路径检查、权限、环境变量、脚本格式与日志文件证据 | 配置依赖本机路径、权限过宽/不足、CRLF/shebang 导致失败、敏感信息入库或日志不可追踪 |
| TA-T0001-DATA-002 | BA-T0001-INT-002 | 技术-数据级 | 本地配置保护、远端持久化、备份和回退链路完整 | 本地 `workspaces` 与 `.qwenpaw` 实施前后逐文件一致；远端替换前完成程序和配置备份；持久化目录不因更新/重启丢失；回退命令与目标明确 | 本地哈希清单、远端备份目录、部署记录、重启后文件/配置核对 | 本地配置被改写、远端无备份、持久化数据丢失、回退入口不明确或不可执行 |
| TA-T0001-CODE-001 | BA-T0001-INT-001 | 技术-代码级 | QwenPaw 实现覆盖 Windows/Linux 跨平台边界，改动范围最小且有测试保护 | 平台相关路径、shell、权限、进程和配置加载均显式处理；本地 Python/项目检查通过 Conda 环境 `qwenpaw` 执行且通过；无与任务无关重构 | Conda 环境与解释器证据、代码差异、静态检查、单元/集成测试和失败分支证据 | 使用错误本地环境、仍存在已识别的不兼容路径、测试失败、用硬编码掩盖问题或改动超出范围 |
| TA-T0001-CODE-002 | BA-T0001-INT-001 | 技术-代码级 | Git 与三方包边界满足治理要求 | 工作区不包含未经授权的 `uni_modules/**` 变更；敏感配置未提交；提交号可追溯；本地目标分支与远端同步 | `git status`、`git diff`、提交记录、远端差异和敏感文件检查 | 存在未授权三方包或敏感配置变更、未提交改动、未推送或部署代码与提交号不一致 |
| TA-T0001-API-003 | BA-T0001-INT-003 | 技术-接口级 | yunjikeji-admin-server 正式启动、健康与重启入口有效 | 启动脚本成功；进程和端口符合正式记录；健康入口连续成功；受控重启后恢复 | 启停命令、退出码、PID/端口、健康返回和日志 | 启动失败、端口错误、健康失败、重启不可恢复或依赖临时 shell 状态 |
| TA-T0001-DATA-003 | BA-T0001-INT-003 | 技术-数据级 | yunjikeji 的 Linux 配置、路径、环境变量、权限和换行兼容 | 测试配置不含生效中的 Windows 路径；配置入口可显式选择 test；shell 为 LF 且可执行；敏感值不入库 | 配置差异、路径/换行/权限检查和启动参数证据 | Windows 路径生效、CRLF/shebang 错误、权限错误、环境串用或敏感值提交 |
| TA-T0001-DATA-004 | BA-T0001-INT-003 | 技术-数据级 | yunjikeji 远端备份、替换和回退完整 | 更新前备份现有 JAR、配置和脚本；更新后配置保持；回退目标与命令明确 | 备份目录、文件清单、部署记录和回退入口 | 无备份、配置丢失、部署来源不可追溯或无法回退 |
| TA-T0001-CODE-003 | BA-T0001-INT-003 | 技术-代码级 | yunjikeji Linux 兼容修改最小且受测试保护 | 只修改有证据的不兼容点；项目静态检查和单元测试通过；无无关重构 | Git diff、构建/测试记录和风险清单映射 | 未解决已确认风险、测试失败或修改超出范围 |
| TA-T0001-CODE-004 | BA-T0001-INT-003 | 技术-代码级 | yunjikeji Git 与部署版本一致 | 敏感配置未提交；提交已推送；服务器运行产物对应提交号 | Git 状态、提交、远端差异、构建产物与部署记录 | 未提交/未推送、敏感信息入库或运行产物与提交不一致 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0001-001 | 测试服务器、连接、目录、端口和部署入口均引用开发中心正式记录；正式记录缺失或冲突时已停止并请求用户介入 | 信息收集记录中的来源路径与核对结论 |
| VC-T0001-002 | 部署前备份、失败回退入口、部署后验证和重启验证均有记录 | 测试服务器部署记录与远端路径/命令证据 |
| VC-T0001-003 | 计划、验收标准、信息收集、实施和独立验收产出均落入当前任务目录 | 当前任务目录与 Git 记录 |
| VC-T0001-004 | 独立验收全部通过后已提交用户审核；仅在用户明确通过后归档 | 独立验收记录、用户反馈与归档状态 |
