# 实施记录

## 已完成

- 在 `yunjikeji-admin-ui/src/views/yj/resource/index.vue` 的通用资源列表操作列中，为 `knowledge-base` 资源增加“测试”按钮。
- 确认“测试”按钮仅在 `currentConfig.resource === 'knowledge-base'` 时展示，不影响原有编辑、关联表、审核和删除按钮。
- 保留一个“知识库测试”弹框，支持输入问题并点击测试，移除重复弹框风险。
- “云技科技原知识库”测试请求：
  - `GET http://127.0.0.1:5001/api/ask?q=encodeURIComponent(问题)`
- “考试知识库”测试请求：
  - `POST http://127.0.0.1:8088/v1/chat/completions`
  - 请求体使用 `model: exam-kb-assistant`、`stream: false`、`messages` 和 `metadata.question`。
  - `session_id` 与 `question_id` 复用同一个请求 id。
- 返回结果使用现有 `useMessage().alert` 弹框展示；失败时使用 `alertError` 展示错误。
- 修复 SQL 产出文件中文编码，确认两条知识库名称为：`云技科技原知识库`、`考试知识库`。
- SQL 资产保留 tenant_id 未确认时主动失败的安全门禁。

## 未执行

- 未执行数据库写入。
- 未修改数据库连接配置。
- 未修改 `uni_modules/**`。

## 限制

- 数据表确认为 `yj_knowledge_base`。
- 正式连接中心缺少 Dev/Test 连接记录，当前不能直接插入数据，避免误写生产库。
- fly-llm 当前默认是否真正请求底层火山知识库，取决于 fly-llm 运行时配置。
