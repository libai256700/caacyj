# 专属页面实现补充记录

## 已调整

- 已恢复公共页面 `yunjikeji-admin-ui/src/views/yj/resource/index.vue`，不在该文件中放入知识库专属测试逻辑。
- 新增专属页面 `yunjikeji-admin-ui/src/views/yj/knowledge-base/index.vue`。
- 专属页面复用 `YjApi` 访问 `knowledge-base` 资源，提供列表、搜索、状态切换和测试操作。
- “云技科技原知识库”测试请求：`GET http://127.0.0.1:5001/api/ask?q=问题`。
- “考试知识库”测试请求：`POST http://127.0.0.1:8088/v1/chat/completions`，请求体使用 `model: exam-kb-assistant`、`messages` 和 `metadata.question`。
- 新增菜单组件路径切换 SQL：`20260615_update_knowledge_base_menu_component.sql`。

## 未执行

- 未直接写入数据库。
- 未执行菜单 SQL。
- 未修改 `uni_modules/**`。

## 限制

- 正式连接中心 `C:\Users\renquan\.devcenter\Operations\ApplicationConnections` 缺失，当前不能直接执行数据写入或菜单切换 SQL。
