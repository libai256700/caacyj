# 协议类型字典下拉对接验收标准

## 业务验收

### A1 业务-交互线：新增协议页面类型字段可从字典下拉选择

- 业务起点：进入协议管理的新增协议页面。
- 触发动作：打开 `type` 类型字段下拉框。
- 关键交互：下拉选项展示培训协议、管理协议、设备协议、考试协议、训练协议。
- 业务终点：选择任一协议类型后，表单展示所选名称，保存请求使用对应 value。
- 结果承接：新增协议记录的类型值可被后续详情、列表或保存链路承接。
- 核对方式：页面实现、字典接口调用或局部运行验证记录。
- 当前状态：已通过；本轮基于代码链路验收，未补运行态截图。

## 技术验收

### T1 技术-接口线：页面复用项目现有字典取值入口

- 正式入口：项目现有字典接口、字典组件或字典工具函数。
- 支撑的业务结果：新增协议页面类型选项来自字典，不再写死散落业务值。
- 技术边界：只改协议新增页面及必要的字典接入逻辑，不扩大到无关页面。
- 通过条件：能明确看到 `type` 字段使用协议类型字典编码取值。
- 证据承接方式：代码变更和实施记录。
- 当前状态：已通过。

### T2 技术-数据线：协议类型字典数据完整落位

- 正式入口：项目管理的 SQL、初始化脚本或字典种子数据文件。
- 支撑的业务结果：字典中存在 5 个指定协议类型。
- 技术边界：不直接改生产库；脚本应符合项目现有数据库资产管理方式。
- 通过条件：字典 code、label、value 与用户指定内容一致，且不存在破坏性变更。
- 证据承接方式：SQL/脚本 diff 和验证记录。
- 当前状态：已通过。

### T3 技术-代码线：协议类型 value 保存语义不变

- 正式入口：新增协议页面表单提交逻辑。
- 支撑的业务结果：选择“培训协议”等中文名称后，保存链路提交 `01` 至 `05` 的 value。
- 技术边界：不修改无关表单字段，不改后端协议保存语义，除非现状定位证明必须调整。
- 通过条件：代码核对或运行验证表明提交值仍是字典 value。
- 证据承接方式：代码变更和验证记录。
- 当前状态：已通过。

## 闭环项

### C1 交付闭环

- 通过条件：相关验证通过或限制已记录；工作区不包含 `uni_modules/**` 改动；任务变更已提交；有远程仓库时已推送；最终等待用户审核。
- 当前状态：已通过；已确认未修改 `uni_modules/**`，全量 `pnpm ts:check` 受既有问题影响失败，未指向本次变更文件。
