# T03 验收记录

- 状态: DONE_WITH_CONCERNS
- 任务: 复验 APP 接口迁移到 `com.huiyitech.app` 的最终状态
- 范围: `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji-admin-server\yunjikeji-admin-server`

## 验收项

### 1. `/app-api` controller 入口归属

- 结果: PASS
- 证据: `rg -n --glob 'src/main/java/**' "/app-api|@RestController|@Validated|@Tag|class (FrontTenantController|FrontComanpanyAuditController|FrontCustomerController|CompanyAuditController|CustomerController)" src/main/java` 仅命中 `src/main/java/com/huiyitech/app/controller/*.java` 下的 `/app-api` 入口。
- 证据: `FrontTenantController`、`FrontComanpanyAuditController`、`FrontCustomerController` 都位于 `com.huiyitech.app.controller`。

### 2. Spring 注册注解

- 结果: PASS
- 证据: `FrontTenantController`、`FrontComanpanyAuditController`、`FrontCustomerController` 均存在 `@RestController` 与 `@Validated`。
- 证据: 启动类 `src/main/java/cn/iocoder/yudao/server/YunjikejiAdminServerApplication.java` 的 `@SpringBootApplication(scanBasePackages = {...})` 明确扫描 `com.huiyitech.app`、`com.huiyitech.customer`、`com.huiyitech.companyaudit`，可覆盖这些 controller 的 Spring 注册。

### 3. 管理端保留原路由

- 结果: PASS
- 证据: `CompanyAuditController` 保留 `PUT /admin-api/yj/enterprise-audit/audit`，且不再包含任何 `/app-api`。
- 证据: `CustomerController` 保留 `PUT /admin-api/yj/student-audit/audit`，且不再包含任何 `/app-api`。

### 4. 关键 diff 行为保持

- 结果: PASS
- 证据: `git diff` 显示迁移主要是把原 `AppTenantController` 重命名为 `FrontTenantController`，并把学生/企业 APP 接口从 `CompanyAuditController`、`CustomerController` 移到 `com.huiyitech.app.controller`。
- 证据: 迁移后的方法仍然调用原 service：`FrontTenantController -> FrontTenantService`，`FrontComanpanyAuditController -> CompanyAuditService`，`FrontCustomerController -> CustomerService`。
- 证据: 迁移后的路径、HTTP 方法、请求/响应类型与原实现一致，例如 `login-or-register`、`refresh-token`、`logout`、`me`、`save`、`submit` 仍保持原有签名。

### 5. 编译复验

- 结果: CONCERNS
- 命令: `mvn -DskipTests compile`
- 结果: 构建失败，失败点在 Maven 依赖/POM 解析阶段，未进入业务源码编译。
- 关键原因:
  - `cn.iocoder.boot:yudao-dependencies:pom:2026.06-yunjikeji-admin-SNAPSHOT` 无法解析。
  - 输出包含 `Unknown host central`。
  - 随后出现缺失 dependency version 的模型错误，但属于父 POM 解析失败后的连锁结果。
- 判断: 该失败不是本次 controller 迁移代码本身导致，而是外部依赖解析阻塞。

### 6. `uni_modules/**` 工作区检查

- 结果: PASS
- 证据: `git status --short -- '**/uni_modules/**'` 无输出。
- 证据: `git diff --name-only -- . ':(exclude)**/uni_modules/**'` 的变更列表未出现 `uni_modules/**`。

## 结论

本次复验确认 APP 接口迁移已经落到 `com.huiyitech.app.controller`，管理端保留路由未被破坏，且关键 service 调用与接口签名保持不变。当前唯一保留项是 `mvn -DskipTests compile` 被外部 Maven 依赖解析问题阻塞，因此整体状态判定为 `DONE_WITH_CONCERNS`。
