# T01 接口盘点

## 结论

只迁移 `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji-admin-server\yunjikeji-admin-server` 下的代码。

当前范围内还需要处理的是 `com.huiyitech.customer.controller.CustomerController`：

- 迁出 6 个 `/app-api/**` 方法到 `com.huiyitech.app.controller.FrontCustomerController`。
- 保留 `PUT /admin-api/yj/student-audit/audit` 在原 `CustomerController`。
- `CustomerService` 与 customer VO 最小方案保持原包，前台 controller 直接引用。

## 已在目标包内的 APP 入口

- `com.huiyitech.app.controller.FrontTenantController`
  - `GET /app-api/yj/tenants`
- `com.huiyitech.app.controller.FrontComanpanyAuditController`
  - `POST /app-api/yj/company-auth/login-or-register`
  - `POST /app-api/yj/company-auth/refresh-token`
  - `POST /app-api/yj/company-auth/logout`
  - `GET /app-api/yj/company-auth/me`
  - `POST /app-api/yj/enterprise-audit/save`
  - `POST /app-api/yj/enterprise-audit/submit`

## 待迁移 APP 入口

来源：`src/main/java/com/huiyitech/customer/controller/CustomerController.java`

- `POST /app-api/yj/customer-auth/login-or-register`
- `POST /app-api/yj/customer-auth/refresh-token`
- `POST /app-api/yj/customer-auth/logout`
- `GET /app-api/yj/customer-auth/me`
- `POST /app-api/yj/customer-info/save`
- `POST /app-api/yj/student-audit/submit`

目标：`src/main/java/com/huiyitech/app/controller/FrontCustomerController.java`

## admin-api 保留入口

来源：`src/main/java/com/huiyitech/customer/controller/CustomerController.java`

- `PUT /admin-api/yj/student-audit/audit`

## 关键适配点

- 新 `FrontCustomerController` 注入 `CustomerService`、`SecurityProperties`。
- 保留 `SecurityFrameworkUtils.obtainAuthorization(...)`、`HttpServletRequest`、`@PermitAll` 用法。
- 继续 import `com.huiyitech.customer.controller.vo.*`。
- 原 `CustomerController` 只保留 admin 审核方法和必要 imports。

## 构建命令建议

在 `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji-admin-server\yunjikeji-admin-server` 执行：

```powershell
mvn -DskipTests compile
```

或在外层 `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji-admin-server` 执行：

```powershell
mvn -pl yunjikeji-admin-server -am -DskipTests compile
```

## 风险

- `FrontComanpanyAuditController` 当前存在拼写错误，本轮不顺带改名，避免扩大改动。
- 若要求 service/VO 也完全归入 `com.huiyitech.app`，改动面会扩大；当前按“业务无变化”的最小迁移执行。
