# 数据库执行记录

## 脚本

- 正向脚本：`yunjikeji-admin-server/sql/mysql/20260612_yj_post_collection_instance_flow.sql`
- 回滚脚本：`yunjikeji-admin-server/sql/mysql/rollback_20260612_yj_post_collection_instance_flow.sql`

## 执行状态

- 当前状态：待执行。
- 原因：按连接管理规则，优先查找 `C:\Users\renquan\.devcenter\Operations\ApplicationConnections\`，当前目录不存在；项目历史任务记录中曾使用 `application-local.yaml` 测试库连接，但本轮尚未获得用户对“以仓库配置替代正式连接记录”的明确确认。
- 目标库口径：测试库 `yunjikeji`。

## 预期校验

- `yj_post` 不包含 `tenant_id`、`company_id`。
- `yj_post` 存在四字段索引：`name, company_name, salary_range, work_area`。
- `yj_post_collection_task_instance` 存在，并包含 `stauts/task_id/collection_count` 与通用审计字段。
- `yj_post_instance` 存在，并包含任务实例 ID、岗位采集字段与通用审计字段。
