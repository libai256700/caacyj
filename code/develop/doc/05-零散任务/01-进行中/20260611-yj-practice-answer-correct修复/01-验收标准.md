# 题目可选项正确答案显示与迁移核对验收标准

- 任务ID：T20260611-002
- 任务名称：题目可选项正确答案显示与是否正确字段修复
- 当前结论：显示修复已通过代码级验收；数据层不执行伪修复，待用户提供普通题缺失选项来源后可继续修复数据

## 业务验收

### 业务-显示线：正确答案列显示业务含义正确

- 业务起点：题目管理中打开题目 `2768` 的“配置可选项”弹窗。
- 触发动作：加载选项列表。
- 业务终点：“正确答案”列显示“是”或“否”，不显示“启用/停用”。
- 通过条件：截图中的选项行不再把是否正确显示成“停用”。
- 当前状态：已通过代码级验收。`is_correct` 字段已改为按字段配置显示“是/否”，不再使用默认“启用/停用”。

### 业务-数据线：每个有选项的题目具备正确答案标记

- 业务起点：已迁移题目数据。
- 触发动作：按题目统计选项正确答案字段。
- 业务终点：客观题、多选题等需要正确答案的题目能够识别正确选项。
- 通过条件：样本题 `2768` 和全量统计能说明正确答案字段已存在；如源数据确实缺失，必须给出缺失范围和修复方案。
- 当前状态：部分通过。已确认样本题 `2768` 属于问卷题，导入时没有标准答案来源；另确认普通题 `28` 缺少正确答案 `C` 对应选项，需要补齐真实 C 选项内容后才能执行数据修复。

## 技术验收

### 技术-接口线：接口字段语义未混用

- 正式入口：配置可选项列表 API 及其响应对象。
- 支撑的业务结果：前端能拿到独立的是否正确字段。
- 技术边界：`status` 仅表示启停状态，不能作为正确答案字段。
- 通过条件：接口响应、前端类型和表格列使用一致字段表示是否正确。
- 当前状态：已通过。后台管理接口返回 `is_correct`，前端显示使用 `switchLabels` 区分业务布尔字段和状态字段。

### 技术-数据线：迁移数据核对可追踪

- 正式入口：测试库只读核对 SQL 或脚本输出。
- 支撑的业务结果：确认问题属于迁移数据、程序映射或两者叠加。
- 技术边界：只读核对可直接执行；写修复必须脚本化并备份。
- 通过条件：产出中记录样本题和全量统计结果。
- 当前状态：已通过。只读核对结果已记录在 `02-任务产出/数据核对记录.md`。

### 技术-代码线：修复范围受控

- 正式入口：Git diff。
- 支撑的业务结果：只修本次正确答案显示/字段问题。
- 技术边界：不修改 `uni_modules/**`；不提交无关既有工作区改动。
- 通过条件：暂存区只包含本次任务相关代码和任务产物。
- 当前状态：已通过。代码改动仅涉及 `yunjikeji-admin-ui/src/views/yj/resource/config.ts` 和 `yunjikeji-admin-ui/src/views/yj/resource/index.vue`；未修改 `uni_modules/**`。

## 闭环项

- 计划与验收文件：已建立。
- 定位记录：已完成。
- 修复与验证：已完成代码级验证；`pnpm ts:check` 因仓库既有大量类型问题失败，但过滤本轮文件无错误。
- 提交推送：进行中。
- 用户审核：未完成。
