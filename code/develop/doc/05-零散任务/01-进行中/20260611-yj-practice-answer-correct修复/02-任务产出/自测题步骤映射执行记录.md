# 自测题步骤映射执行记录

执行时间：2026-06-11 18:37

执行脚本：`execute_yj_practice_assessment_step_mapping_20260611.py`

执行命令：

```powershell
python "E:\huiyitechworkspace\feixingxueyuan\code\develop\doc\05-零散任务\01-进行中\20260611-yj-practice-answer-correct修复\02-任务产出\execute_yj_practice_assessment_step_mapping_20260611.py"
```

执行结果：成功提交，结果写入 `practice_assessment_step_mapping_result_20260611.json`。

备份表：

- `yj_practice_assessment_step_bak_20260611183740`：备份执行前 `tenant_id=1 AND deleted=b'0'` 的步骤行，0 行。
- `yj_practice_assessment_exercise_bak_20260611183740`：备份执行前 `catalog_type=1 AND tenant_id=1 AND deleted=b'0'` 的自测题行，21 行。

回滚入口：`rollback_yj_practice_assessment_step_mapping_20260611.sql`

最终校验：

- `yj_practice_setp` 中 `tenant_id=1 AND deleted=b'0'` 有 4 条步骤，`sort_no` 为 1 到 4。
- `catalog_type=1` 的 21 道题全部已设置 `step_id`。
- `catalog_type=0` 的 1473 道题仍全部 `step_id IS NULL`，本脚本未更新练习题。
- 自测题未映射数量为 0。
