# 修复记录

## 代码修复

已修复后台通用资源页面中 `switch` 字段的显示文案能力：

- 文件：`yunjikeji-admin-ui/src/views/yj/resource/config.ts`
  - `YjField` 增加 `switchLabels` 配置。
  - `is_correct` 相关字段配置为 `是/否`。
- 文件：`yunjikeji-admin-ui/src/views/yj/resource/index.vue`
  - `formatCell` 对 `switch` 字段改为读取字段级 `switchLabels`。
  - 未配置 `switchLabels` 的状态类字段仍显示 `启用/停用`。

## 数据修复

本轮未执行 DML。

原因：

- 样本题 `2768` 属于调查问卷“入行专属评估”，导入时明确没有标准答案，不能把问卷选项伪造成考试正确答案。
- 普通题 `28` 的问题是缺少正确答案 `C` 对应选项，当前数据不足以自动修复。

后续如需修复题目 `28`，需要先取得真实 C 选项内容，再按脚本化流程补齐：

1. 备份 `yj_practice_exercises_answer` 和 `yk_question_option` 中题目 `28` 相关记录。
2. 插入缺失的 C 选项。
3. 将 C 选项 `is_correct` 置为 `1`。
4. 验证后台题库和学员端练习链路一致。

## 验证状态

- 只读数据库核对：已完成。
- 前端类型检查：首次执行 `pnpm ts:check` 超时，需继续复跑或执行更小范围验证。
