# 练习考题 tenant_id 更新验收标准

- 任务ID：T20260610-001
- 任务名称：练习考题及答案 tenant_id 更新为 1
- 当前结论：已通过，待用户审核

## 业务验收

### 业务-交互线：配置可选项可读取已有数据

- 业务起点：题目管理中点击“配置可选项”时，数据库中已有的题目选项应属于当前租户。
- 触发动作：执行正式 SQL 修复脚本。
- 业务终点：练习题目、可选项和二级选项均归属租户 `1`。
- 通过条件：目标表执行后均不存在 `tenant_id <> 1 OR tenant_id IS NULL` 的记录。
- 证据承接方式：`02-任务产出/执行记录.md` 中记录执行前后 SQL 结果。
- 当前状态：已通过。

## 技术验收

### 技术-接口线：执行入口可追溯

- 正式入口：SQL 脚本文件、Python 执行脚本和数据库连接入口。
- 支撑的业务结果：确保本次更新不是口头或临时命令执行。
- 技术边界：只允许执行 `yj_practice_exercises`、`yj_practice_exercises_answer`、`yj_practice_exercises_answer_child` 三张表的 `tenant_id` 更新。
- 通过条件：存在执行脚本、回滚脚本和执行记录，且脚本未包含范围外表的 DML。
- 证据承接方式：`02-任务产出/` 下的 SQL 文件、执行脚本和执行记录。
- 当前状态：已通过。

### 技术-数据线：数据最终状态正确

- 正式入口：测试库 `yunjikeji`。
- 支撑的业务结果：全部当前练习考题相关记录归属租户 `1`。
- 技术边界：不修改其他字段，不修改生产库。
- 通过条件：三张目标表执行后 `COUNT(*) = SUM(tenant_id = 1)`，且非 `1` 或空值计数为 `0`。
- 证据承接方式：执行前后聚合查询结果与执行结果 JSON。
- 当前状态：已通过。

### 技术-代码线：仓库变更风险受控

- 正式入口：本任务产出目录。
- 支撑的业务结果：脚本资产可复核、可追踪。
- 技术边界：不改业务代码，不改三方包 `uni_modules/**`。
- 通过条件：本轮新增或修改内容只包含本任务文档和 SQL/Python 产出；已存在的无关工作区变更不纳入本轮处理。
- 证据承接方式：`git status --short` 结果。
- 当前状态：已通过。

## 闭环项

- 脚本执行：已通过。
- 独立验收：已通过。
- 用户审核：待用户审核。
- 完成归档：未完成。
