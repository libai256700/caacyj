# T0002-Neo4j图谱内容裁剪执行记录

## 1. 执行范围

- 执行时间：2026-07-02 19:28-19:45（Asia/Shanghai）
- 执行角色：实施子代理
- 目标环境：本地 Docker 容器 `yunji-knowledge-graph`
- 目标数据库：`neo4j`
- 连接账号：`neo4j`
- 连接依据：
  - `neo4j-docker/docker-compose.yml` 中 `container_name: yunji-knowledge-graph`
  - `neo4j-docker/docker-compose.yml` 中 `NEO4J_AUTH=neo4j/yj123456`
  - `neo4j/.neo4j_pass` 内容与 compose 密码一致
- 数据库在线确认：
  - 删除前：`SHOW DATABASES` 返回 `neo4j / online / localhost:7687 / primary`
  - 删除后：`SHOW DATABASES` 返回 `neo4j / online / localhost:7687 / primary`

## 2. 删除前备份

- 在线 `neo4j-admin database dump neo4j` 结果：失败，原因是 Neo4j Community 运行态数据库持有锁。
- `STOP DATABASE neo4j` 结果：失败，原因是 Community Edition 不支持该命令。
- 最终备份方式：短暂停止本地容器，使用同一 `neo4j-docker/data` 数据目录通过 `neo4j:latest` 执行 `neo4j-admin database dump neo4j --to-path=/dumps --overwrite-destination=true`，随后重启本地容器。
- 备份文件：
  - `E:\huiyitechworkspace\knowledge-graph\neo4j-docker\dumps\neo4j-t0002-before-trim-20260702-192801.dump`
- 备份结果：dump 完成，日志显示 `Dump completed successfully`，容器重启后数据库恢复 online。

## 3. 删除前核心统计

| 项目 | 统计 |
| --- | ---: |
| 总节点 | 5892 |
| 总关系 | 152379 |
| `Customer` | 585 |
| `Position` | 487 |
| `Company` | 295 |
| `Student` | 61 |
| 首轮删除目标节点 | 1850 |
| 首轮目标涉及关系 | 8317 |
| 保留范围节点 | 3330 |

删除前保留范围包括：理论题库、政策法规、无人机教材/书籍等来源，保留范围节点非空。

## 4. 删除 Cypher 摘要

本次删除分两段执行，脚本均只用于本次任务产出记录，不作为项目生产脚本。

1. `T0002-delete-scope.cypher`
   - 删除标签：`Customer`、`Position`、`Company`、`Student`
   - 删除名称、来源文档、文档名、路径等字段命中以下范围的节点及其关系：
     - `云技科技`、`湖北云技`
     - `员工手册`、`新员工培训`、`新员工面试`、`入职流程`
     - `公司介绍`、`公司产品介绍`、`价格表`
     - `薪酬体系`、`客户管理与跟进制度`、`客户跟进制度`
     - `jobs.csv`、`customers.csv`、`学员信息.csv`、`价格表.csv`、`新员工培训课程表.csv`
     - `人事制度_`、`企业信息_`
   - 执行结果：删除 1850 个节点。

2. `T0002-delete-residual-business-csv.cypher`
   - 补删剩余业务 CSV 来源节点：
     - `courses.csv`
     - `training_tracks.csv`
     - `instructors.csv`
     - `question_banks.csv`
     - `documents.csv`
   - 删除依据限定为 `source_doc` 等于上述 CSV；没有删除 `source_csv/source_table=documents.csv/question_banks.csv` 且 `source_doc` 为理论题库、政策法规或教材文档的保留节点。
   - 执行结果：删除 56 个节点。

## 5. 删除后核心统计

| 项目 | 统计 |
| --- | ---: |
| 总节点 | 3986 |
| 总关系 | 143661 |
| `Customer` / `Position` / `Company` / `Student` | 0 |
| `Instructor` / `TrainingTrack` | 0 |
| 禁删业务标签反查 | 0 |
| 云技/企业/人事来源文档反查 | 0 |
| 业务 CSV 来源反查 | 0 |
| 保留范围节点 | 3329 |

说明：宽泛字段反查如果包含 `source_csv/source_table`，会命中部分保留节点上的导入索引字段，例如保留的理论题库节点存在 `source_csv=question_banks.csv`，保留的法规/教材文档节点存在 `source_csv=documents.csv`。本次未将这类导入索引字段作为删除依据，避免误删保留文档。

## 6. 保留范围样本

保留来源样本仍可查询：

| 来源 | 删除后数量 |
| --- | ---: |
| `理论题库_飞行原理与飞行性能.txt` | 302 |
| `政策法规_中华人民共和国民用航空法_2025修订_2026-07-01施行.txt` | 291 |
| `理论题库_气象.txt` | 261 |
| `理论题库_综合问答.txt` | 204 |
| `政策法规_CCAR-92部.txt` | 194 |
| `政策法规_《民用中小型无人驾驶航空器操控员训练机构规范》.txt` | 177 |
| `理论题库_无人机教员题库.txt` | 162 |
| `理论题库_系统组成及介绍.txt` | 152 |
| `无人机理论书籍_2023_无人机系统结构与设计_机械工业出版社_李宏达.txt` | 119 |
| `无人机理论书籍_2024_无人机技术概论(第2版)_机械工业出版社_贾恒旦.txt` | 85 |
| `政策法规_民用无人驾驶航空器操控员执照考试管理办法_MD-92-FS-02_2024-05-11.txt` | 75 |

保留节点样本：

- `CCAR-92部`
- `中华人民共和国民用航空法_2025修订_2026-07-01施行`
- `民用无人驾驶航空器操控员执照考试管理办法_MD-92-FS-02_2024-05-11`
- `《民用中小型无人驾驶航空器操控员训练机构规范》`
- `飞行原理与飞行性能`
- `气象`
- `综合问答`
- `系统组成及介绍`
- `无人机任务规划`
- `无人机教员题库`
- `2023_无人机系统结构与设计_机械工业出版社_李宏达`
- `2024_无人机技术概论(第2版)_机械工业出版社_贾恒旦`

## 7. 残留风险

- `~/.devcenter/Operations/ApplicationConnections/Local/applications.md` 中未登记本地 Neo4j 连接条目；本次按用户给定上下文、本仓 `neo4j-docker/docker-compose.yml` 和本地容器状态执行。
- Neo4j Community 不支持在线停库 dump；已通过短暂停止本地容器完成可恢复备份，最后数据库已恢复 online。
- 本次删除依据以业务标签、`source_doc`、文档名、名称、路径等字段为准；保留节点上的 `source_csv/source_table=documents.csv/question_banks.csv` 是导入索引字段，未作为删除依据。
