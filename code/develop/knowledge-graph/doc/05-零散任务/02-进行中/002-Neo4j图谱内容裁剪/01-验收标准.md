# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0002`
- 任务名称：Neo4j图谱内容裁剪
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0002-INT-001 | 业务-交互级 | 用户查看本地 Neo4j 知识图谱内容时，只看到保留范围内的无人机理论、政策法规和教材书籍内容 | 本地 Neo4j 图谱删除业务数据和云技科技资料后，仍保留用户指定的无人机理论题库、政策法规和无人机教材/书籍图谱内容 | 删除后 Neo4j 中不存在客户、岗位、公司、学员等业务数据标签；不存在云技科技员工手册、新员工培训、公司介绍、产品介绍、价格表、薪酬体系、客户跟进制度等来源文档；保留范围来源仍有节点和关系 | 删除前后统计、标签统计、来源文档统计、保留范围样本查询、被删除范围反查结果写入 `02-任务产出/` | 业务数据或云技资料仍大量存在；指定保留范围被误删为空；未提供可核对证据 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0002-API-001 | BA-T0002-INT-001 | 技术-接口级 | 所有操作只针对本地 Neo4j 容器 `yunji-knowledge-graph` 的 `neo4j` 数据库执行，并且删除前必须完成可恢复备份 | 可证明目标容器在线、数据库为本地 `neo4j`；备份文件已生成；删除命令或脚本只连接本地容器 | `docker ps`、`SHOW DATABASES`、备份命令输出、备份文件路径、执行命令记录 | 目标环境不明确、没有备份就删除、连接到非本地或非预期数据库 |
| TA-T0002-DATA-001 | BA-T0002-INT-001 | 技术-数据级 | 删除结果必须以真实 Neo4j 查询结果证明：业务数据和云技科技资料已删除，指定保留范围仍可查询 | 删除后 `Customer`、`Position`、`Company`、`Student` 等业务标签计数为 0；云技科技资料相关 `Document/source_doc` 反查为 0；理论题库、政策法规、教材书籍相关来源仍存在节点；总节点和关系统计可解释 | Cypher 查询记录、删除前后计数表、保留样本、删除范围反查结果 | 删除后仍残留明确业务/云技来源；保留来源缺失；统计证据与执行范围不一致 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0002-001 | 正式验收通过后进入用户人工审核，未经用户明确审核通过不归档到已完成 | `00-任务计划.md` 状态、验收记录、用户审核记录 |
