# 2026-09-05 交接包校验与迁移映射

## 1. 校验范围与基线

- 任务：`T0003 / TASK-004C`
- 源目录：`-APP--main (5)/-APP--main`
- 目标工程：当前 `knowledge-graph` 根目录
- 校验时间：`2026-09-05`
- 本次只接入 App 开发所需的技能、契约、测试和交接手册入口；不覆盖既有 `pipeline/`、`rag_*`、`neo4j/` 或运行数据。

## 2. 包哈希证据

在目标工程执行：

```powershell
Get-FileHash '-APP--main (5).zip' -Algorithm SHA256
Get-FileHash 'developer-handoff-kg-cloud-v2-offline-20260905-r2.zip' -Algorithm SHA256
```

结果：

| 文件 | SHA-256 | 结果 |
| --- | --- | --- |
| `-APP--main (5).zip` | `6a2b39572c095e638d29c6853701786f4b95b8e9acca58fc4c1a3a5fc491eeb4` | 通过 |
| `developer-handoff-kg-cloud-v2-offline-20260905-r2.zip` | `b72131c61da816d404118075e8dce92ed6eec1e6c8f32b8a0ce9d317d2e14bd4` | 通过 |

handoff 包隔离解压后再次校验内部两个 ZIP：

| 内部交付物 | SHA-256 | 结果 |
| --- | --- | --- |
| `knowledge-graph-cloud-app-upload.zip` | `326dd2c95981f43e216f149ac4e60f4e7b1c12dc98045acbd4d4b883e471e28f` | 通过 |
| `knowledge-qa-final-delivery-linux-x86_64-20260905-r2.zip` | `cc140e2a3191095ea333c874f559c68043856983ee466392a0cb4e3dc589eaaa` | 通过 |

## 3. 已接入映射

| 源内容 | 目标路径 | 处理 |
| --- | --- | --- |
| `skills/knowledge-graph-cloud/SKILL.md` | `skills/knowledge-graph-cloud/SKILL.md` | 已接入，保留 App 最终回答合同 |
| `skills/knowledge-graph-cloud/references/*` | `skills/knowledge-graph-cloud/references/` | 已接入 `APP_CONTRACT.md`、`HOST_INTEGRATION.md` |
| `skills/knowledge-graph-cloud/scripts/*` | `skills/knowledge-graph-cloud/scripts/` | 已接入客户端、回答适配器、评测和包校验脚本 |
| `skills/knowledge-graph-cloud/tests/*` | `skills/knowledge-graph-cloud/tests/` | 已接入 App 合同和运行契约测试 |
| 强制手册与接收说明 | `docs/knowledge-graph-cloud/` | 已接入迁移手册、同步要求、`USAGE.md`、`DEPLOY.md`、`README.md` |

目标工程原先不存在顶层 `skills/`，因此本次新增目录不覆盖同名文件。已接入文件集合不包含数据库、BM25、向量索引、Neo4j store、日志、凭据、生产配置或服务端 `deploy/`。

## 4. 未接入与禁止覆盖项

- 未将源目录 `deploy/`、`deploy/cloud_v2/`、`deploy/rag_store/`、`operator-companion/` 整体复制到当前根运行链路。
- 未覆盖根 `pipeline/**`、`rag_*`、`neo4j/**`、`rag_chunks.db` 或任何 Release 内派生资产。
- 未修改 `uni_modules/**`、生产配置、身份、secrets，也未连接生产或导入 Neo4j。
- `knowledge_base/` 本轮保持零修改；源文件如有变化，必须按手册从权威源重建全部受影响派生层。

## 5. 回滚与止损边界

本次新增文件均位于 `skills/knowledge-graph-cloud/` 与 `docs/knowledge-graph-cloud/`，以及两份任务产出文档。若独立验收发现问题，只允许按精确路径回退本次新增文件；不得使用宽泛 glob、递归清理或回退用户既有 dirty 修改。Release 压缩包继续视为不可变只读输入。

## 6. 状态边界

```text
github_delivery_complete: true   # handoff README 的发布事实
deployment_handoff_ready: true   # handoff README 的交接事实
runtime_active: false
product_accepted: false
linux_x86_64_usearch_load: pending_receiving_team
neo4j_import: pending_receiving_team
production_config_and_identity: pending_receiving_team
wsgi_startup: pending_receiving_team
product_qa: pending_receiving_team
```

以上状态仅记录交接包声明；本机未冒充 Linux x86_64、生产配置、Neo4j 导入、WSGI 启动或产品 QA。
