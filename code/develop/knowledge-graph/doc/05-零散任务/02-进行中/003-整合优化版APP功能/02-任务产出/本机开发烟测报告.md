# 本机开发烟测报告

> 本报告仅记录 `2026-08-16` 在 `E:\huiyitechworkspace\knowledge-graph` 的本机开发烟测证据，不是正式 Test 环境验收，不替代 `BA-T0003-INT-002`、`TA-T0003-API-001`、`TA-T0003-DATA-001` 的正式结论，也不改变 TASK-005 和正式 Test 验收的 `BLOCKED` 状态。

## 1. 结论

- 本机开发烟测结论：`DONE_WITH_CONCERNS`。
- 支持继续当前整合方向：最新服务可启动；健康检查、真实图谱读取、历史 GET 问答、新 POST 问答、非法 POST 拒绝、`kg_query.py` 客户端和 Playwright 页面加载均建立了本机证据。
- 未发现触发实现回退的结果：没有启动失败、Neo4j/SQLite 不可读、核心接口 5xx、页面空白、主资源失败或 POST 契约错误。
- 关注项：本机未安装或启动 Ollama，`127.0.0.1:11434` 未监听，`bge-m3` embedding 调用降级为 `embedding_failed:URLError`；`/favicon.ico` 返回 404 并形成一条非阻塞控制台错误。
- 正式 Test 环境地址、页面入口、Neo4j/SQLite 测试数据入口和鉴权信息仍缺失，因此正式 Test 验收继续保持 `BLOCKED`。

## 2. 时间、范围与只读边界

- 执行时间：`2026-08-16 08:37-08:45 +08:00`。
- 执行入口：`http://127.0.0.1:5001`。
- 验证层级：代码/单元基线、服务启动、HTTP 接口、真实数据读取、客户端、Playwright 页面、日志与本机依赖。
- 本轮未调用文件导入、Cypher、节点/边新增修改删除等写接口；未修改 Neo4j、SQLite、配置、索引或应用实现。
- 本轮只新增本报告；未修改任务计划、验收标准、正式测试结论、正式独立验收报告或正式验收状态。

## 3. 本机环境与启动结果

| 检查 | 命令/入口 | 结果 |
| --- | --- | --- |
| Docker 版本 | `docker version --format 'client={{.Client.Version}} server={{.Server.Version}}'` | Client/Server 均为 `29.4.0` |
| Neo4j 容器 | `docker ps --filter 'name=yunji-knowledge-graph'` | `yunji-knowledge-graph` 为 `healthy`；7474/7687 已映射 |
| 启动前端口 | `Get-NetTCPConnection -LocalPort 5001 -State Listen` | 启动前未监听 |
| Python | `D:\ProgramData\anaconda3\envs\know-v2\python.exe` | 文件存在 |
| Neo4j 密码文件 | `neo4j\.neo4j_pass` | 文件存在；未输出内容 |
| 启动脚本 | `scripts\start_know_v2_service.py` | 文件存在 |

实际启动命令：

```powershell
& 'D:\ProgramData\anaconda3\envs\know-v2\python.exe' -X utf8 'scripts\start_know_v2_service.py' --skip-neo4j
```

启动命令退出 `0`，输出确认：

- 跳过已运行的 Neo4j 启动。
- API 打开 `http://127.0.0.1:5001`。
- 启动内置健康检查返回 `HTTP 200: {"neo4j":"connected","status":"ok"}`。
- `2026-08-16 08:45:14 +08:00` 复核时，5001 仍由 PID `12904` 监听，健康检查仍为 200。

## 4. 代码与单元基线

语法命令：

```powershell
& 'D:\ProgramData\anaconda3\envs\know-v2\python.exe' -m py_compile rag_store\request_envelope.py rag_store\retrieval_planner.py pipeline\answer_policy.py pipeline\server.py kg_query.py tests\test_ask_optimization.py
```

- 结果：退出 `0`，无语法错误。

单元命令：

```powershell
& 'D:\ProgramData\anaconda3\envs\know-v2\python.exe' -m unittest discover -s tests -v
```

- 结果：`Ran 10 tests in 0.026s`，`OK`，10/10 通过。
- 覆盖：请求信封、非法请求拒绝、确定性检索计划、答案/证据治理、POST 优先与仅 405 回退 GET、原管理路由保留。

## 5. HTTP 与真实数据证据

所有请求均为 GET 或只读问答 POST；未调用数据写接口。

| 项目 | HTTP | 关键结果 |
| --- | --- | --- |
| `GET /api/health` | 200 | `status=ok`，`neo4j=connected` |
| `GET /api/stats` | 200 | `documents=62`，`entities=3924`，`relations=143661`，关系类型和实体类型均非空 |
| `GET /api/graph` | 200 | `nodes=2062`，`edges=30000`，真实图谱非空 |
| `GET /api/ask?q=无人机法规是什么？` | 200 | 历史 GET 正常；答案 346 字符，4 个来源、5 条证据；降级原因 `embedding_failed:URLError` |
| `POST /api/ask` | 200 | 新 POST 正常；答案 345 字符，4 个来源、5 条证据；返回请求信封、检索计划、降级与治理字段 |
| 非法字段 POST | 400 | `forbidden_request_fields`，`request_rejected=true` |
| 内部控制文本 POST | 400 | `internal_context_rejected`，`request_rejected=true` |
| 空问题 POST | 400 | `missing_user_query`，`request_rejected=true` |

POST 请求体使用：

```json
{
  "user_query": "无人机法规是什么？",
  "allowed_sources": ["bm25", "dense", "neo4j"]
}
```

POST 契约证据：

- `request_envelope.schema_version=kg-request-envelope-v1`，`transport=json_post`，包含稳定指纹、来源策略、风险级别和隔离信息。
- `retrieval_plan.schema_version=retrieval-plan-v1`，包含 `plan_id`、工具预算、期望/条件/禁止通道、停止条件和来源拒绝原因。
- 顶层同时存在 `degraded`、`degraded_reasons`，并返回 `deduplicated_sources`、`evidence_bindings`、`governance_degraded`、`review_required`、`conflict_ids` 等治理字段。
- 本次 POST 为 `degraded=true`，唯一降级原因是 `embedding_failed:URLError`；接口仍以 BM25/已有证据成功返回，不是 5xx。

数据只读承接证据：

- 服务启动日志确认 SQLite 为 `2419 chunks / 25 文档`，且明确记录“SQLite 是唯一文本回源”。
- Neo4j 健康检查已连接，统计与图谱接口返回非空真实数据。
- 本轮没有执行数据库写请求，没有调用导入、Cypher 或图谱 CRUD。

## 6. `kg_query.py` 客户端

实际调用项目客户端模块的 `query('无人机法规是什么？')`：

- 进程退出 `0`，日志记录 `QUERY OK`。
- 耗时约 `7.8s`，返回答案 425 字符、4 个来源。
- 正确使用 POST 主通道并透传 `degraded=true`、`embedding_failed:URLError`。
- 未触发 405，因此未执行历史 GET 回退；“仅 405 回退 GET”已由 10 项单元测试覆盖。

## 7. Playwright 页面烟测

执行方式：复用本机已有 `playwright-core 1.62.1` 和缓存 Chromium 1223，未安装依赖、未修改项目；以 headless Chromium 打开 `http://127.0.0.1:5001`。浏览器在 `finally` 中关闭，整个命令 `10.9s` 内退出 `0`，页面加载与等待耗时 `9670ms`，未发生超时。

| 检查 | 结果 |
| --- | --- |
| 导航 | 首页文档 200 |
| 标题 | `云技 · 知识图谱` |
| 页面图谱 | `2050` 节点、`30000` 边，均非零 |
| 页面统计 | `3924 实体 · 143661 关系` |
| 加载遮罩 | `display=none`，加载完成 |
| 图谱画布 | 1 个 canvas，约 `1240 x 852`，非空尺寸 |
| 核心脚本 | `/vis-data.min.js` 200；`/vis-network.min.js` 200 |
| 核心数据 | `/api/stats` 200；`/api/graph` 200 |
| 页面异常 | `pageErrors=[]`，`requestFailures=[]` |
| 控制台 | 1 条资源 404；结合服务日志和单独 HTTP 核对定位为 `/favicon.ico`，非阻塞 |

本页面烟测只加载首页和读取图谱，未点击或调用页面中的导入、编辑、删除等管理动作。

## 8. 日志与缺口

### 阻塞正式验收的缺口

1. 正式 Test 环境仍缺：知识图谱 Test 服务基础地址、健康检查入口、Test 页面地址、Neo4j 正式连接记录、SQLite 测试数据入口、启动/部署入口；如存在鉴权，还需测试账号或 Token 的正式记录。
2. 上述信息缺失前，本机结果不得回写为 `BA-T0003-INT-002`、`TA-T0003-API-001`、`TA-T0003-DATA-001` 正式通过。

### 本机功能降级缺口

1. 服务固定访问 `http://127.0.0.1:11434/api/embeddings`，模型为 `bge-m3`。
2. 本机 11434 未监听，`ollama` 命令不存在，未发现 Ollama 进程。
3. 若要求 Dense embedding 在本机不降级，用户还需安装并启动 Ollama、准备 `bge-m3` 模型，并确认 11434 接口可用。本次未获授权安装，因此只记录缺口。

### 非阻塞问题

1. `/favicon.ico` 返回 404，造成一条浏览器控制台资源错误，不影响页面、图谱和核心请求。
2. 日志有 `jieba` 使用 `pkg_resources` 的弃用警告，不阻塞启动或查询，后续依赖升级时需处理。
3. 当前运行的是 Flask development server，适合本机开发烟测，不应作为生产部署方式。
4. 当前目录及父级未发现 `.git`，本轮无法执行仓库提交或推送。

## 9. 服务保持状态与下一步

- 报告收口时服务仍运行：`http://127.0.0.1:5001`，5001 正在监听，健康检查 200，Neo4j connected。
- 用户当前可直接访问 `http://127.0.0.1:5001` 查看本机页面。
- 本机若只要求核心只读功能可用，无需额外操作；若要求 Dense embedding 完整可用，需补 Ollama + `bge-m3`。
- 正式验收仍需先补齐正式 Test 环境记录，再按接口级、数据级、Playwright 交互级顺序重新执行，不得复用本报告替代正式证据。
