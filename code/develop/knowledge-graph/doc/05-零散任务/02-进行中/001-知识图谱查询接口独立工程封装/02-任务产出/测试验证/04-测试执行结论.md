# 测试执行结论

## 1. 本轮执行时间

- 执行日期：`2026-06-16`
- 测试环境：本机 Windows，Anaconda `know-v2`
- 服务地址：`http://127.0.0.1:5001`

## 2. 分层结论

| 层级 | 对应验收项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 接口级 | `TA-T0001-API-001` | 通过 | `/api/health`、`/api/ask`、`/api/files`、`/api/import/file` 契约检查通过 |
| 数据级 | `TA-T0001-DATA-001` | 通过-有关注 | `/api/ask?q=无人机` 返回真实知识库来源；embedding 服务不可用导致 `stats.degraded=true`，但 BM25 降级结果可用 |
| 代码级 | `TA-T0001-CODE-001` | 通过 | `py_compile`、静态 smoke、test-client smoke 通过 |
| 交互级 | `BA-T0001-INT-001` | 通过 | 调用方配置指向 `http://127.0.0.1:5001/api/ask`，Python 服务真实返回调用方依赖字段 |

## 3. 命令记录

```powershell
D:\ProgramData\anaconda3\envs\know-v2\python.exe -m pip install -r requirements-know-v2.txt
D:\ProgramData\anaconda3\envs\know-v2\python.exe -m py_compile pipeline\server.py scripts\start_know_v2_service.py scripts\smoke_file_import_api.py
D:\ProgramData\anaconda3\envs\know-v2\python.exe scripts\smoke_file_import_api.py
D:\ProgramData\anaconda3\envs\know-v2\python.exe -X utf8 scripts\smoke_file_import_api.py --live
D:\ProgramData\anaconda3\envs\know-v2\python.exe scripts\start_know_v2_service.py --skip-neo4j
```

## 4. 结论

当前实施结果满足进入独立验收的条件；需由独立验收代理复核代码差异、运行态证据和正式验收标准覆盖情况。

## 5. 独立验收结论

- 验收日期：`2026-06-16`
- 独立验收结论：`通过但有关注`
- 覆盖情况：
  - `BA-T0001-INT-001`：通过
  - `TA-T0001-API-001`：通过
  - `TA-T0001-DATA-001`：通过
  - `TA-T0001-CODE-001`：通过
  - `VC-T0001-001`：通过但无法完全核验提交前条件
- 关注项：
  - 当前路径不是 Git 仓库，不能执行提交和推送。
  - 查询接口运行态存在 `embedding_failed:URLError` 降级，但已返回真实来源文档片段与 `sources`。

## 1. 当前结论

- 当前场景：
- 当前测试时间：
- 当前结论：`已通过 / 未通过 / 部分通过`

## 2. 接口级结果

- 已通过：
- 未通过：
- 未执行：

## 3. 数据级结果

- 已通过：
- 未通过：
- 未执行：

## 4. 交互级结果

- 已通过：
- 未通过：
- 未执行：

## 5. 失败记录

- 失败现象：
- 影响范围：
- 重测入口：

## 6. 后续动作

- 
