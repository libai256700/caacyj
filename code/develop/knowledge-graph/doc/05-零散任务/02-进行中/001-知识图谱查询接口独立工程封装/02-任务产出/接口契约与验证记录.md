# 接口契约与验证记录

## 1. 调用方契约

- 调用方项目：`E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji-admin-server`
- 配置入口：`yunjikeji-admin-server\src\main\resources\application.yaml`
- 当前配置：
  - `huiyitech.knowledge.graph.base-url: http://127.0.0.1:5001`
  - `huiyitech.knowledge.graph.query-path: /api/ask`
  - `huiyitech.knowledge.graph.timeout-millis: 120000`
- Java 客户端：`com.huiyitech.knowledge.dal.KnowledgeGraphClient`
- 调用方式：`GET {base-url}{query-path}?q={question}`
- 解析字段：`answer`、`route`、`stats.trace_id`、`sources`、`rawKnowledge`

## 2. Python 服务契约

| 接口 | 方法 | 用途 | 关键返回 |
| --- | --- | --- | --- |
| `/api/health` | GET | 健康检查 | `{"status":"ok","neo4j":"connected"}` |
| `/api/ask?q=...` | GET | 知识库查询 | `answer`、`route`、`sources`、`stats.trace_id` |
| `/api/files` | POST | 新增文件导入 | `ok`、`job.mode`、`status`、`result`、`error` |
| `/api/import/file` | POST | 文件导入兼容路径 | 同 `/api/files` |

## 3. 本轮实现范围

- 补齐 `pipeline/server.py` 的 `/api/files` 与 `/api/import/file` 路由。
- 新增 `scripts/start_know_v2_service.py`，使用 `D:\ProgramData\anaconda3\envs\know-v2\python.exe` 启动当前项目服务。
- 新增 `scripts/smoke_file_import_api.py`，覆盖静态路由检查与 test-client 级导入契约检查。
- 新增 `requirements-know-v2.txt` 与 `doc/知识库服务启动文档.md`。
- 补齐当前项目 `neo4j\.neo4j_pass`，与 `neo4j-docker\docker-compose.yml` 中的 `NEO4J_AUTH=neo4j/yj123456` 保持一致。

## 4. 验证记录

| 验证项 | 命令或入口 | 结果 |
| --- | --- | --- |
| 依赖安装 | `D:\ProgramData\anaconda3\envs\know-v2\python.exe -m pip install -r requirements-know-v2.txt` | 通过 |
| 语法编译 | `D:\ProgramData\anaconda3\envs\know-v2\python.exe -m py_compile pipeline\server.py scripts\start_know_v2_service.py scripts\smoke_file_import_api.py` | 通过 |
| 静态契约 | `D:\ProgramData\anaconda3\envs\know-v2\python.exe scripts\smoke_file_import_api.py` | 通过，发现 `/api/ask`、`/api/health`、`/api/files`、`/api/import/file` |
| test-client 契约 | `D:\ProgramData\anaconda3\envs\know-v2\python.exe -X utf8 scripts\smoke_file_import_api.py --live` | 通过，`/api/files` 返回 `201` 与同步导入契约 |
| 服务启动 | `D:\ProgramData\anaconda3\envs\know-v2\python.exe scripts\start_know_v2_service.py --skip-neo4j` | 通过，服务进程为 `D:\ProgramData\anaconda3\envs\know-v2\python.exe -X utf8 pipeline/server.py` |
| 健康检查 | `GET http://127.0.0.1:5001/api/health` | 通过，返回 `{"neo4j":"connected","status":"ok"}` |
| 查询接口 | `GET http://127.0.0.1:5001/api/ask?q=无人机` | 通过，返回 `answer`、`route`、`sources`、`stats.trace_id` |
| 文件导入异常分支 | `POST http://127.0.0.1:5001/api/files` body `{}` | 通过，HTTP 400，空 `file_path` 被拒绝 |

## 5. 关注项

- 查询接口本轮 HTTP 响应中 `stats.degraded=true`，原因是本机 `127.0.0.1:11434` embedding 服务不可用；服务已降级使用 BM25 并返回真实知识库结果。
- 当前服务已占用 `127.0.0.1:5001`，供调用方本地联调使用。
