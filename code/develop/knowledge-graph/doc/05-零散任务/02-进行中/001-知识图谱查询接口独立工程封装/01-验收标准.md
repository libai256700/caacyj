# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0001`
- 任务名称：知识图谱查询接口独立工程封装
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0001-INT-001 | 业务-交互级 | `yunjikeji-admin-server` 按既有调用方式向当前知识图谱工程发起查询请求，并获得可用于业务展示或后续处理的查询结果 | 当前项目提供与老版本同源接口格式兼容的查询入口；调用方无需理解当前项目内部脚本结构即可通过 HTTP 或既有契约调用；接口对正常查询、空查询或异常输入给出稳定返回 | 本地启动独立工程后，按调用方既有请求格式调用成功；响应字段、状态码和错误分支满足调用方现有契约；接口结果来自当前项目真实查询逻辑而非假数据 | 记录老版本接口格式、调用方调用代码位置、本地请求命令和响应样例到 `02-任务产出/` | 接口无法启动、调用方契约不兼容、返回伪造数据、异常输入导致服务崩溃或响应格式不可被调用方消费 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0001-API-001 | BA-T0001-INT-001 | 技术-接口级 | 当前项目必须提供独立服务入口、查询 API、健康检查或等价可探测入口，并保留与 `knowledge-graph-source` 及 `yunjikeji-admin-server` 已用接口格式兼容的请求和响应契约 | 在 Anaconda `know-v2` 环境中可启动服务；健康检查通过；至少一个正常查询和一个异常/空输入场景返回符合契约的响应 | `conda run -n know-v2 ...` 启动或测试命令、HTTP 请求命令、响应样例、服务入口文件路径 | 只能靠直接执行脚本查询、没有独立服务入口、契约字段缺失、状态码或错误格式与调用方不兼容 |
| TA-T0001-DATA-001 | BA-T0001-INT-001 | 技术-数据级 | 查询结果必须承接当前项目已有数据、索引、Neo4j/RAG/CSV 查询能力或配置；工程配置和运行日志能说明真实数据来源与失败原因 | 查询结果可追溯到当前项目真实查询模块或数据文件；配置项有默认值或 `.env` 示例；日志不泄露敏感信息并能定位启动、查询和错误分支 | 查询模块调用链记录、配置文件或 README、测试响应、日志片段或测试输出 | 返回硬编码示例、依赖绝对私密配置且无说明、缺少配置承接、日志泄露密钥或无法定位错误 |
| TA-T0001-CODE-001 | BA-T0001-INT-001 | 技术-代码级 | 独立工程结构清晰，依赖声明、启动入口、接口实现、测试或验证脚本边界明确；不得破坏既有脚本能力；不得修改 `uni_modules/**` | 代码改动集中在当前项目查询服务和工程化文件；新增/修改文件有明确职责；基础测试或本地验证通过；暂存区和工作区不包含 `uni_modules/**` | `git diff`、测试命令输出、关键文件列表、`git status` 检查 | 改动散乱、缺少依赖或启动说明、旧功能被破坏、测试无法运行且无合理说明、触碰 `uni_modules/**` |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0001-001 | 任务检查通过后提交到当前仓库；如存在远程仓库，继续推送远程；正式验收通过后进入用户人工审核，未经用户明确审核通过不归档到已完成 | `git status`、提交哈希、远程推送结果或无远程说明、最终用户审核记录 |
