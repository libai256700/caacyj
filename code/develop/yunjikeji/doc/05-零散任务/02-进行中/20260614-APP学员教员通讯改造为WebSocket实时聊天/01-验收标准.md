# 验收标准

## 1. 文档信息

- 任务 ID：`T20260614`
- 任务名称：`APP学员教员通讯改造为 WebSocket 实时聊天`
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项 ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T20260614-INT-001` | `业务-交互链` | 学员端或教员端进入聊天页、发送文本/图片/视频消息、接收对端新消息 | 聊天页首次进入仍按现有历史查询逻辑展示聊天记录；会话建立后，新文本/图片/视频消息通过 WebSocket 实时送达并在当前页追加；刷新或重新进入后，历史记录可继续完整展示，且与实时消息不冲突；图片可预览，视频可播放 | 历史查询、实时追加、刷新回放、媒体展示四段链路均成立 | 前端页面联调、接口返回记录、WebSocket 收包记录、媒体展示截图 | 只能看到历史不能实时、只能实时不落库、刷新后记录缺失或重复异常、图片/视频无法展示 |

## 3. 技术验收

| 验收项 ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T20260614-API-001` | `BA-T20260614-INT-001` | `技术-接口链` | 后端 APP 消息接口迁移到 `com.huiyitech.app.message` 后，原 `/app-api/yj/customer-service/**` 与 `/app-api/yj/company-students/**` URL 继续可用；WebSocket 握手走 `/infra/ws?token=...`；文本/图片/视频消息具备统一消息类型与定向推送能力；媒体上传接口可接收文件并返回入库后的消息对象 | 迁包后 REST 接口不回归；WebSocket 可连通并收到客服消息推送；媒体上传接口可成功返回图片/视频消息 | controller/service 代码核对、接口调试、WebSocket 收包日志、上传接口调用记录 | URL 改变、迁包后接口不可用、WebSocket 无法鉴权或无消息下发、媒体上传接口不可用 |
| `TA-T20260614-DATA-001` | `BA-T20260614-INT-001` | `技术-数据链` | 新文本/图片/视频消息发送后必须真实写入数据库 `yj_message_info`，会话快照 `yj_session_message` 同步更新；图片/视频上传后内容字段写入 BOS 地址，`message_type` 分别写入 `image`、`video`；历史查询能够查到 WebSocket 实时阶段产生的新消息 | 实时消息发送后查询历史接口可回放该消息，且媒体消息返回 BOS 地址 | 数据库表更新记录、历史查询结果、会话快照字段变化、BOS 上传结果 | 前端看到消息但数据库无记录，或数据库有记录但历史查询无法回放，或媒体未写入 BOS 地址 |
| `TA-T20260614-CODE-001` | `BA-T20260614-INT-001` | `技术-代码链` | 前端保持“历史查询 + WebSocket 实时追加”双链路衔接；后端 APP 模块迁移遵循仓内 `controller / controller.vo / service` 范式；BOS 工具落在管理平台 `com.huiyitech.utils`；相关改动通过基础构建检查 | 前后端改动结构清晰、职责边界一致、构建或类型检查通过 | 代码审阅、`npm run type-check`、后端编译/测试记录 | 前端只靠轮询或全量刷新伪装实时；后端迁移破坏模块结构；BOS 工具位置不符合要求；基础检查失败 |

## 4. 闭环项

| 闭环项 ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T20260614-001` | 形成前后端实施记录、验证结果与剩余风险说明 | `02-任务产出/` 下的实施与验证记录 |
