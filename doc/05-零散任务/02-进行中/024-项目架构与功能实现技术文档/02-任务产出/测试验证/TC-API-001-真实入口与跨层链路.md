# TC-API-001 真实入口与跨层链路

## 1. 用例信息

- 用例 ID：`TC-API-001`
- 测试层级：`接口级`
- 对应验收项：`TA-T0024-API-001`
- 正式验收入口：`../../01-验收标准.md`
- 执行时间：`2026-08-31`
- 当前状态：`已通过`

## 2. 验证目标

确认技术文档识别了各应用真实启动入口、前后端接口边界和关键跨层调用链，且入口、处理节点、数据承接与异常边界均可沿源码或配置引用继续查证。

## 3. 前置条件

1. `../项目架构与功能实现技术文档.md` 与 `../源码调研记录.md` 已生成并保持待验收版本。
2. 仓库源码、启动脚本、路由或控制器、请求封装及服务实现可只读访问。
3. `05-自动化测试脚本/validate_technical_docs.py` 和 `05-自动化测试脚本/validation-result.txt` 可用。

## 4. 执行动作

1. 运行 `05-自动化测试脚本/validate_technical_docs.py`，检查两份交付文档是否存在、必需章节与术语是否齐全，以及全部 `path:line` 引用是否有效。
2. 从文档描述的真实启动入口和跨层调用链中进行人工抽查，沿启动配置、页面请求、接口入口、服务实现和数据承接文件逐段核对调用方向与职责边界。
3. 汇总人工抽查的 25 条源码引用和 5 条跨层链路，确认引用目标存在、链路节点相互衔接，且不存在虚构入口、方向倒置或层级职责混淆。

## 5. 预期结果

1. 自动校验无失败项，文档引用均能定位到真实仓库路径和行号。
2. 人工抽查的 5 条跨层链路均可从真实入口连续追踪至主要处理节点和数据承接位置。
3. 前端页面、请求封装、后端接口、服务与数据层的职责说明一致，异常或未知边界有明确标注。

## 6. 脚本与数据入口

- 自动化脚本：`05-自动化测试脚本/validate_technical_docs.py`
- 自动化结果：`05-自动化测试脚本/validation-result.txt`
- 测试数据：`03-测试数据模板.json`；本次为技术文档静态校验，无业务测试数据。

## 7. 失败判定

出现任一情形即判定失败：存在无法定位的启动入口或接口；任一抽查链路的调用方向、处理节点或数据承接无法由源码支撑；接口职责与页面、服务或数据层职责混淆；自动校验出现失败项或路径引用失效。

## 8. 执行证据

- 自动证据：脚本执行结果为 `18 pass / 0 fail / 1 warning`；`406/406` 条 `path:line` 引用有效；检测到 `4` 个 Mermaid 图；两份交付文档中的排除项均为零命中；未检测到产品源码及 `uni_modules/**` 变更。唯一警告为任务目录外存在无关工作区变更，不影响本用例验收结论。
- 人工证据：已人工抽查 `25` 条源码引用和 `5` 条跨层链路，入口、调用方向、主要处理节点与数据承接均可沿引用定位，未发现不符合项。
- 执行结论：符合 `TA-T0024-API-001` 的通过条件，状态为 `已通过`。
