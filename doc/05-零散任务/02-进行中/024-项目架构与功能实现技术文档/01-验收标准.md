# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0024`
- 任务名称：项目架构与功能实现技术文档
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0024-INT-001 | 业务-交互级 | 项目研发或维护人员从任务产出目录阅读综合技术文档，理解本轮纳入范围的项目架构、主要功能及其实现链路 | 文档能独立说明项目边界、工程组成、运行架构、主要模块、关键业务链路和维护入口，并让读者能够沿引用路径继续查证；用户明确排除的两个工程不出现在交付正文中 | 规定章节完整；关键结论有最新工作区源码路径或符号支撑；语言面向研发维护场景，结构清晰且无明显自相矛盾；主技术文档中不包含字符串 `yunjikeji-server` 或 `fly-llm` | 阅读 `02-任务产出/项目架构与功能实现技术文档.md`，检索排除工程名称，并抽查架构、功能与实现链路对应最新源码 | 排除工程仍出现在交付正文，缺少核心章节，主要功能仅罗列名称未解释实现，或关键结论无法从仓库中定位依据 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0024-API-001 | BA-T0024-INT-001 | 技术-接口级 | 文档应基于排除两个工程后的最新源码，识别纳入范围应用的真实启动入口、前后端接口边界和至少三条关键跨层调用链，说明入口、主要处理节点、数据承接与异常边界 | 入口和调用链均能从最新源码或配置直接定位；接口职责不与页面、服务或数据层职责混淆；不引用排除工程路径 | 对照启动脚本、路由/控制器、请求封装、服务实现等文件逐项抽查，并检索排除目录引用 | 使用不存在或已变化的入口或接口，调用方向错误，引用排除工程，或未覆盖关键跨层链路 |
| TA-T0024-DATA-001 | BA-T0024-INT-001 | 技术-数据级 | 文档应说明主要持久化模型、缓存/状态管理、配置来源及文档交付位置，并记录仓库证据无法确认的运行事实 | 数据与状态描述均有实体、映射、SQL、存储封装或状态模块依据；产出固定落位于当前任务目录；未知项单独列出 | 抽查实体/模型、数据访问、配置、状态管理和最终 Markdown 文件 | 数据关系凭空推断、混淆前端状态与持久化数据、交付物不在正式产出目录，或未标注未知项 |
| TA-T0024-CODE-001 | BA-T0024-INT-001 | 技术-代码级 | 分析过程只读且覆盖本轮纳入范围的一方工程；文档引用路径有效并反映最新工作区源码，事实、推断、未知项边界清楚；不触碰 `uni_modules/**` | Git 差异仅包含本任务载体与产出文档；路径及行号全量校验有效；文档无排除工程引用、模板残留、空章节或未经支撑的确定性表述 | 使用 Git 状态/差异、全量路径行号校验、排除名称检索和人工审读 | 修改产品源码或三方包、出现排除工程名称/路径、引用失效路径、存在模板占位符，或把推断冒充直接事实 |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0024-001 | 由独立验收子代理逐条检查上述验收项并形成 `02-任务产出/独立验收记录.md`；正式验收通过后提交 Git，存在远程时推送，并进入用户人工审核 | 独立验收记录、Git 提交记录、远程推送结果和用户反馈 |
