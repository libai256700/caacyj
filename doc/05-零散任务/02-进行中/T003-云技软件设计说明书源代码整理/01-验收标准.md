# 验收标准

> 本文只写正式标准定义；首次单文件验收记录保留在 `02-任务产出/04-独立验收记录.md`，补充后的双文档最终结论写入 `02-任务产出/07-双文档独立验收记录.md`。

## 1. 文档信息

- 任务ID：`T003`
- 任务名称：云技软件设计说明书源代码整理
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T003-E2E-001` | 端到端业务验收 / 任务目标结果验收 | 用户以两份旧项目 Word 为样式参考，要求整理当前云技项目源码并成套交付 | 从两份指定参考 DOCX 出发，分别完成模板识别、源码映射、内容替换、文档生成和桌面交付 | 桌面同时存在且精确命名为 `云技_软件设计说明书源代码.docx` 与 `云技_软件设计说明书源代码_1.docx`；两份均可由标准 DOCX 解析器打开；无后缀版沿用第一份参考的逐源码行清单形式，`_1` 版沿用第二份参考的每文件段落形式；内容主体均为云技 | 两份参考文件、两份生成记录、双文档结构化读取结果、桌面文件属性 | 任一文件缺失、命名不符、无法打开、仍以旧项目为主体，或两份目标没有分别对应两份参考的组织方式 |
| `BA-T003-DATA-001` | 数据结果业务验收 | 源文件路径和代码正文写入两卷说明书 | 两份文档中的源码必须来自当前工作区并可回溯；无后缀版重点承接客户端逐行源码，`_1` 版承接已选多模块源码；两份共同覆盖当前项目主要技术组成 | 所有列出的源文件均真实存在；无后缀版每个文件的行序列与真实源码一致，`_1` 版代码正文与真实文件逐字符一致；两份共同覆盖客户端、后台管理端、业务后端、管理后端和 AI 服务；不写入真实密码、令牌、私钥或证书正文 | 两份选取清单、源文件哈希/行数/字符数对照、双文档抽取结果、敏感信息扫描记录 | 存在虚构路径、行序列或正文截断、关键模块无合理覆盖，或任一成品包含敏感凭据 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T003-API-001` | `BA-T003-E2E-001` | 接口级验收 | 以结构化 DOCX 读写作为真实生成入口，让两份输出分别保留对应参考文档的页面、段落、样式、页眉关系和源码组织口径 | 两份 DOCX ZIP 包均完整；主文档 XML、样式、关系文件均可解析；两份文档的段落组织、页面设置、0 原生表格基线及主要样式分别与对应参考模板一致或在生成记录中有明确说明 | 使用 `python-docx` 和 ZIP/XML 对两份输出分别检查；记录段落、表格、页面、样式、页眉、媒体和源码组织摘要 | 任一 ZIP/XML 损坏、关键部件缺失、输出与对应模板的组织方式明显偏离且无说明，或源码内容破坏文档结构 |
| `TA-T003-QUALITY-001` | `BA-T003-DATA-001` | 代码质量验收 | 两份文档生成逻辑均采用结构化解析，源文件选择与内容映射可复核，产出过程不修改业务源码 | 生成前后业务源码 Git 状态无新增修改；两份源文件清单具有路径、语言、行数/字符数、哈希及用途；生成记录包含两份输入、输出和校验摘要；文本保持可读编码、缩进与换行 | `git status --short -- code/develop`、两份源文件清单、双 DOCX 抽取文本、源码哈希/行序列/字符数对照 | 修改业务源码、使用不可复核的手工粘贴、任一文档出现乱码/大量缺行，或缺少可复现脚本与生成证据 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T003-001` | 两份桌面文档经新的独立验收全部通过后再提交用户；仅在用户明确审核通过后归档任务 | `02-任务产出/07-双文档独立验收记录.md`、用户反馈、已回写的 `00-任务计划.md` |
