# T003 云技源代码说明书生成记录

## 1. 生成结论

- 生成状态：`通过`
- 生成日期：`2026-07-24`
- 成品路径：`C:\Users\18163\Desktop\云技_软件设计说明书源代码_1.docx`
- 成品大小：`80,660` 字节
- 成品 SHA-256：`0530f54d3f1936daaaabb62a59c07fca6201461d9b421904f80911e79a27dcb1`
- 成品已由 `python-docx` 重开，并通过 ZIP CRC、Open XML、源码完整性、品牌、媒体、凭据形态和业务源码 Git 状态检查。

## 2. 输入与生成资产

| 资产 | 路径 | 大小 | SHA-256 |
| --- | --- | ---: | --- |
| `_1` 参考模板 | `C:\Users\18163\Downloads\飞书20260724-105710\来绘_软件设计说明书源代码_1.docx` | 144,634 字节 | `3271e0e1a64ad834808723230995948df5b23f9988adf8c0dd96a4b2f16301b2` |
| 源码清单 | `doc/05-零散任务/02-进行中/T003-云技软件设计说明书源代码整理/02-任务产出/02-源码选取清单.md` | 8,767 字节 | `c8eb4956fecc869ca68d82a4849909bdcbf81101af57e3dd5224afc6b8bd940e` |
| 生成脚本 | `doc/05-零散任务/02-进行中/T003-云技软件设计说明书源代码整理/02-任务产出/generate_source_design_docx.py` | 24,639 字节 | `dc0676a0b75d8c01f2be76f2102e1ea578445834fc94573ad2db422897cb71cf` |
| DOCX 成品 | `C:\Users\18163\Desktop\云技_软件设计说明书源代码_1.docx` | 80,660 字节 | `0530f54d3f1936daaaabb62a59c07fca6201461d9b421904f80911e79a27dcb1` |

运行时：`C:\Users\18163\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`；依赖版本为 `python-docx 1.2.0`、`Pillow 12.2.0`、`lxml 6.0.2`。

## 3. 生成方式

1. 从 `02-源码选取清单.md` 解析 Word 顺序、工程、路径、语言、行数、字符数和 SHA-256，逐一以严格 UTF-8 解码复核真实源文件；带 UTF-8 BOM 的文件按文本读取语义剥离 BOM，但原始字节 SHA-256 保持按文件计算。
2. 复制 `_1` 模板的既有文档结构；保留全部 `20` 个正文段落，不重建节、表格或样式体系。
3. 对第 `2` 至 `19` 段仅保留原 `w:pPr`，清除其他段内元素；每段依次写入工程、仓库相对路径、语言和完整源文件正文，使用 `10.5 pt`、西文 `Times New Roman`、东亚字体 `等线`，原文件每个逻辑换行映射为一个 Word 换行。
4. 在页眉文本节点中将 `来绘` 等长替换为 `云技`，不破坏页眉绘图关系。
5. 由 Pillow 生成 `640 x 960` 纯白 RGB PNG，并通过 `zipfile` 重建 `word/media/image1.png`。
6. 先在桌面目录生成临时候选 DOCX；候选通过全部校验后再原子替换正式目标，临时文件已清理。

## 4. 校验结果

| 校验项 | 结果 | 证据摘要 |
| --- | --- | --- |
| DOCX 可打开 | 通过 | `python-docx` 可重开正式成品 |
| ZIP 完整性 | 通过 | CRC 无损坏；实际文件部件 `15` 个，无模板实际文件部件丢失 |
| XML 可解析 | 通过 | `14` 个 `.xml` / `.rels` 部件均由 `lxml` 成功解析 |
| 正文结构 | 通过 | `20` 个正文段落、`0` 个表格、`1` 个节 |
| 首尾边界 | 通过 | 第 1 段为 `程序开始！！！`，第 20 段为 `程序结束！！！` |
| 源码容器 | 通过 | 第 2 至 19 段共 `18` 个源码容器，顺序与清单一致 |
| 路径完整性 | 通过 | `18/18` 条工程与仓库相对路径逐段精确匹配 |
| 源码完整性 | 通过 | `18/18` 个段落正文与源文件规范化换行后的完整文本精确相等；合计 `8,329` 行、`310,972` 字符 |
| 源文件版本 | 通过 | 生成前逐文件 SHA-256 与清单一致；生成后再次复核无漂移 |
| 段落属性 | 通过 | 20 个段落的 `w:pPr` 规范化 XML 与模板逐段一致 |
| 样式与页面 | 通过 | `styles.xml`、`theme1.xml`、`fontTable.xml` 及唯一 `sectPr` 与模板规范化 XML 一致 |
| 页眉品牌 | 通过 | 页眉完成 `1` 处替换；所有 XML 中 `来绘` 为 `0`，`header1.xml` 含 `云技` |
| 背景媒体 | 通过 | `word/media/image1.png` 为 `640 x 960`，RGB 三通道极值均为 `255`，确认纯白 |
| 明显凭据 | 通过 | 私钥头、AWS 访问密钥形态、长 `sk-`、固定 Bearer/Basic 值和敏感字段固定字符串赋值均为 `0` 命中 |
| 业务源码 Git 状态 | 通过 | 生成前后对 5 个 `code/develop` 工程执行限定路径 `git status --porcelain`，结果均为空 |
| 模板保护 | 通过 | 生成前后模板 SHA-256 一致 |

## 5. 普通关键词复核

明显凭据扫描未命中。普通关键词扫描只出现以下非凭据语义：

| 文件 | 行号 | 复核结论 |
| --- | ---: | --- |
| `code/develop/yunjikeji-admin-server/yunjikeji-admin-server/src/main/java/com/huiyitech/app/practice/service/FrontPracticeServiceImpl.java` | 1417-1419 | `tokenTenantId` 表示登录用户租户 ID，用于租户一致性校验，不含令牌正文 |
| `code/develop/fly-llm/src/main/java/com/flyllm/examkb/service/ChatGatewayService.java` | 110-112 | `PromptTokens`、`CompletionTokens`、`TotalTokens` 为模型用量统计，值为数值 `0`，不含访问凭据 |

## 6. 验收支撑

- `BA-T003-E2E-001`：指定模板已生成指定桌面文件，成品可打开，项目主体、页眉品牌和源码内容均已替换为云技当前工程信息。
- `BA-T003-DATA-001`：18 个源码容器均来自清单中的真实文件，路径与完整正文逐段精确校验，无明显凭据。
- `TA-T003-API-001`：DOCX ZIP、主文档 XML、样式、关系、节属性和媒体均可解析；结构保持 20 段、0 表格、1 节。
- `TA-T003-QUALITY-001`：生成逻辑由可复跑脚本承载，清单哈希、源文件哈希、内容、品牌、媒体和 Git 状态均有机械校验；未修改业务源码。
