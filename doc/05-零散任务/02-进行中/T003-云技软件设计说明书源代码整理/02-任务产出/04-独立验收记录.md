# T003 云技软件设计说明书源代码 DOCX 独立验收记录

## 1. 验收结论

- 独立验收结论：`通过`
- 验收时间：`2026-07-24T11:57:16+08:00`
- 独立验收代理：未参与最终 18 文件清单定稿和 DOCX 生成；本回合未导入、调用或重跑生成脚本。
- 正式标准：`BA-T003-E2E-001`、`BA-T003-DATA-001`、`TA-T003-API-001`、`TA-T003-QUALITY-001`。
- 执行结果：独立脚本 `exit 0`，`30/30` 项机械检查通过，四组标准均为 `PASS`。
- 环境口径：本任务是本地 DOCX 制品验收，不涉及服务集成或测试环境，因此 `40-0100` 的测试环境门禁不适用。

## 2. 执行入口与制品指纹

- 验证脚本：`doc/05-零散任务/02-进行中/T003-云技软件设计说明书源代码整理/02-任务产出/verify_source_design_docx.py`
- Python：`C:\Users\18163\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`
- 执行命令：

```powershell
& 'C:\Users\18163\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' 'doc\05-零散任务\02-进行中\T003-云技软件设计说明书源代码整理\02-任务产出\verify_source_design_docx.py'
```

| 资产 | 实际大小 | 独立重算 SHA-256 |
| --- | ---: | --- |
| `C:\Users\18163\Desktop\云技_软件设计说明书源代码_1.docx` | 80,660 字节 | `0530f54d3f1936daaaabb62a59c07fca6201461d9b421904f80911e79a27dcb1` |
| `02-源码选取清单.md` | 8,767 字节 | `c8eb4956fecc869ca68d82a4849909bdcbf81101af57e3dd5224afc6b8bd940e` |
| `generate_source_design_docx.py` | 24,639 字节 | `dc0676a0b75d8c01f2be76f2102e1ea578445834fc94573ad2db422897cb71cf` |
| `_1` 参考模板 | 144,634 字节 | `3271e0e1a64ad834808723230995948df5b23f9988adf8c0dd96a4b2f16301b2` |

## 3. 四项正式验收

| 验收项 | 结论 | 独立证据 |
| --- | --- | --- |
| `BA-T003-E2E-001` | `PASS` | 桌面仅匹配到精确文件名 `云技_软件设计说明书源代码_1.docx`；`python-docx` 重开成功；正文为 20 段、0 表格、1 节，首尾为 `程序开始！！！` / `程序结束！！！`，中间恰为 18 个源码容器；页眉文本为 `云技`，所有 XML 中 `来绘` 为 0 命中。 |
| `BA-T003-DATA-001` | `PASS` | 18 条清单路径全部存在，顺序为 1～18，覆盖 `yunjikeji`、`yunjikeji-admin-ui`、`yunjikeji-server`、`yunjikeji-admin-server`、`fly-llm` 5 个工程；源文件 SHA-256、行数、字符数全部与清单一致，合计 8,329 行、310,972 字符；18 段的工程/路径/语言元信息及完整源码逐字符与真实文件一致，0 不匹配；配置、证书、账号、`tmp`、生成目录路径 0 命中，明显凭据/证书形态 0 命中。 |
| `TA-T003-API-001` | `PASS` | DOCX ZIP CRC 正常，15 个实际文件部件中关键部件齐全，14 个 XML / `.rels` 全部可由 `lxml` 解析；`styles.xml`、`theme1.xml`、`fontTable.xml`与 `_1` 模板规范化 XML 一致，唯一 `sectPr` 及 20 段 `pPr` 一致；页眉保留 1 个 `behindDoc=1` 浮动绘图；唯一背景 PNG 为 640×960 RGB 且三通道极值均为 255；A4 尺寸与页边距均在 0.2 mm 容差内。 |
| `TA-T003-QUALITY-001` | `PASS` | 生成脚本仅做静态 AST 检查，确认使用 `python-docx`、`lxml`、Pillow 和 `zipfile` 结构化处理，未调用脚本；生成记录中的成品大小/哈希、清单哈希和生成脚本哈希与实际一致；`git status --short -- code/develop` 为空，`git diff -- code/develop` 为空；文本替换字符、异常控制字符和源码截断均为 0。 |

## 4. 结构与内容摘要

| 项目 | 独立实测值 |
| --- | --- |
| DOCX 正文 | 20 段：1 开始 + 18 源码 + 1 结束 |
| Word 表格 | 0；与参考 `_1` 真实基线一致，非结构缺失 |
| 节 / 媒体 | 1 节 / 1 张 PNG |
| ZIP / XML | CRC 通过；15 个文件部件；14 个 XML / `.rels` 可解析 |
| 页面 | 209.991 mm × 297.039 mm；上下约 10.001 mm，左右 31.75 mm，页眉页脚 12.7 mm |
| 背景 | `word/media/image1.png`；640×960；RGB；全白；SHA-256 `f6d271423958d5bbe675f7244d1862fb009b8d3ff7fc6b69e87844005e89cffe` |
| 品牌 | 页眉 `云技`；所有 XML 中 `来绘` 0 命中 |
| 源码 | 18 文件 / 5 工程 / 8,329 行 / 310,972 字符；逐容器精确匹配 |
| 敏感 | 高置信凭据、私钥和证书正文形态 0 命中 |
| Git | `code/develop` 限定路径的 status 与 diff 均为空 |

## 5. 独立性与未修改声明

- 独立验证脚本自行解析 Markdown 清单、真实源文件、DOCX ZIP/Open XML 和 `python-docx` 对象，未复用生成脚本的验证函数。
- 本回合未修改或重新生成目标 DOCX，未修改任何 `code/develop` 业务源码，未修改生成脚本、任务计划或正式验收标准。
- 本回合新增内容仅为 `verify_source_design_docx.py` 和本验收记录。

## 6. 残余限制与风险

1. 本机在 `PATH` 及常见安装路径中未发现 Word、WPS 或 LibreOffice 可执行文件，因此未执行真实 Office 分页渲染或逐页视觉审查。正式标准要求的 DOCX 重开、ZIP/XML、结构、样式、节、背景和完整文本检查已全部通过，因此该限制不阻断本次正式验收通过。
2. 参考模板和成品均无显式分页符，实际页数会受 Office/WPS 版本与字体环境影响；不应以 WPS 缓存页数作为实时页数承诺。
3. 敏感检查使用高置信凭据、私钥、证书和固定授权形态，不等价于全面语义密钥审计；但 18 条选取路径已排除配置、账号、证书和临时资产，且文档正文与该安全候选集逐字符一致。

## 7. 门禁结论

- `BA-T003-E2E-001`：`PASS`
- `BA-T003-DATA-001`：`PASS`
- `TA-T003-API-001`：`PASS`
- `TA-T003-QUALITY-001`：`PASS`
- `TASK-004` 的四项正式验收全部通过，可进入 `TASK-005` 人工审核 / 用户验收；在用户明确审核通过前不得归档。
