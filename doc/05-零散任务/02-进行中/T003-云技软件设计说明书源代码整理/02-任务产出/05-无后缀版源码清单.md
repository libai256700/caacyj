# T003 无后缀版源码清单

## 1. 选取结论

- 本清单只服务于 `云技_软件设计说明书源代码.docx`，对应无后缀参考 A 的逐源码行建段形式。
- 按客户端启动、首页、答题核心流程、练习 API 的顺序选取 `yunjikeji` 工程前 4 个真实文件。
- 合计 `4` 个文件、`2,624` 行、`69,824` 字符；所有文件均可按 UTF-8 严格解码，原始字节 SHA-256 已核对。
- 行数使用逐行读取口径，不为文件末尾换行增加虚构空行；字符数使用 `.NET File.ReadAllText(path, UTF8).Length` 口径，包含原始换行并按 UTF-16 代码单元计数。

## 2. 写入顺序

| 顺序 | 工程 | 源文件路径 | 语言 | 用途 | 行数 | 字符数 | SHA-256 | 敏感信息检查 |
| ---: | --- | --- | --- | --- | ---: | ---: | --- | --- |
| 1 | `yunjikeji` | `code/develop/yunjikeji/src/main.ts` | TypeScript | uni-app/Vue 客户端启动入口，创建应用并注册 UI 插件 | 12 | 222 | `0da7e90cace5a2899bf76114164ebc5f61f09c7a111015a36ad86dbd8d10eaeb` | 通过 |
| 2 | `yunjikeji` | `code/develop/yunjikeji/src/pages/home.vue` | Vue SFC / TypeScript | 客户端首页，组织核心内容展示与业务入口导航 | 263 | 11,575 | `5c7f2fa4673844f3f8fc4ad3eafbb527542939a3204170b9c71997e07a4829a3` | 通过 |
| 3 | `yunjikeji` | `code/develop/yunjikeji/src/pages/practice/answer.vue` | Vue SFC / TypeScript | 练习答题核心页面，承载题目交互、提交、进度与 AI 解答流程 | 1,565 | 36,117 | `2575f99d211190b72ed311d2edf0144500cc467e734b14830c58ca22b59d81f9` | 通过 |
| 4 | `yunjikeji` | `code/develop/yunjikeji/src/services/practice.ts` | TypeScript | 练习领域模型与 API 服务，封装练习、记录、知识查询及 SSE 流式请求 | 784 | 21,910 | `f776e1e3352cf8595d407a12e5c17c39899e8f3735c6ea71796a5bf45ee269cd` | 通过 |
| **合计** | **1 个工程** | **4 个文件** | **TypeScript / Vue SFC** | **客户端入口与核心业务** | **2,624** | **69,824** | **逐文件可复核** | **通过** |

## 3. Word 段落映射

每个文件依次写入以下结构：

1. `工程：yunjikeji`
2. `源文件：<仓库相对路径>`
3. `语言：<语言>`
4. `源代码：`
5. 每一行真实源码对应一个独立 Word 正文段落；空行保留为空段落，tab 缩进保留为 `w:tab`。
6. `文件结束：<仓库相对路径>`

四个文件之外，正文首段保留 `程序开始！！！`，末段保留 `程序结束！！！`。因此目标正文段落总数应为 `2 + 4 x 5 + 2,624 = 2,646`。

## 4. 验收支撑

- `BA-T003-DATA-001`：4 条路径均可回溯到当前工作区真实客户端源码，并保留完整行序列。
- `TA-T003-QUALITY-001`：清单提供路径、语言、用途、行数、字符数和 SHA-256，可由生成脚本机械复核。
