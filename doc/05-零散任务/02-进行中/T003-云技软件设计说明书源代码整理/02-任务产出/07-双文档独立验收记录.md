# T003 双文档成套交付独立验收记录

## 1. 验收结论

- 最新独立验收结论：`通过`
- 验收时间：`2026-07-24T12:56:10+08:00`
- 正式结论入口：本文是成套交付的最新结论；`04-独立验收记录.md` 仅保留为首次单文件验收的历史记录，不再作为成套交付结论。
- 独立性：验收代理未参与无后缀版生成，本回合未导入、调用或重跑任何生成脚本。
- 执行结果：独立脚本 `exit 0`，`49/49` 项机械检查通过，`E2E / DATA / API / QUALITY` 四组均为 `PASS`。
- 环境口径：本任务是本地 DOCX 制品验收，不涉及服务集成或测试环境，`40-0100` 测试环境门禁不适用。

## 2. 执行入口

- 独立验证脚本：`doc/05-零散任务/02-进行中/T003-云技软件设计说明书源代码整理/02-任务产出/verify_source_design_docx_pair.py`
- Python：`C:\Users\18163\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`
- 重跑命令：

```powershell
& 'C:\Users\18163\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' 'doc\05-零散任务\02-进行中\T003-云技软件设计说明书源代码整理\02-任务产出\verify_source_design_docx_pair.py'
```

该脚本自行解析两份 Markdown 清单、22 个清单条目对应的真实源文件、两份参考 DOCX 和两份成品 DOCX。生成脚本仅以普通文本读取并做 AST 静态审查，不会被导入或执行；任一正式检查失败时返回非零状态。

## 3. 成套制品与参考映射

| 卷 | 桌面成品 | 大小 | 独立重算 SHA-256 | 对应参考 | 参考 SHA-256 | 组织方式 |
| --- | --- | ---: | --- | --- | --- | --- |
| A：无后缀卷 | `C:\Users\18163\Desktop\云技_软件设计说明书源代码.docx` | 54,286 字节 | `731226e7db3ac32a8f5a0b3f3a29ce6ea78d1fc2487607c1ea693ed0e4ad88ef` | `来绘_软件设计说明书源代码.docx` | `1eb9fef66254c5ec4f894fdf7d005b3446da70b1a94fd5c65ee4a2e00ac2fc82` | 每一源码行一个 Word 段落 |
| B：`_1` 卷 | `C:\Users\18163\Desktop\云技_软件设计说明书源代码_1.docx` | 80,660 字节 | `0530f54d3f1936daaaabb62a59c07fca6201461d9b421904f80911e79a27dcb1` | `来绘_软件设计说明书源代码_1.docx` | `3271e0e1a64ad834808723230995948df5b23f9988adf8c0dd96a4b2f16301b2` | 每一源文件一个 Word 段落容器 |

桌面按 `云技_软件设计说明书源代码*.docx` 匹配时恰好只得到上述两份精确文件名，无缺失或多余同名交付物。

## 4. 四项正式验收

| 验收项 | 结论 | A 卷证据 | B 卷证据 |
| --- | --- | --- | --- |
| `BA-T003-E2E-001` | `PASS` | 精确文件名存在，`python-docx` 可重开；2,646 段、0 表格、1 节，首尾为程序边界，中间按 4 个文件元信息边界和 2,624 个源码行逐段组织；页眉含 `云技`，旧品牌 0 命中。 | 精确文件名存在，`python-docx` 可重开；20 段、0 表格、1 节，首尾为程序边界，中间 18 个源码容器与清单顺序一致；页眉为 `云技`，旧品牌 0 命中。 |
| `BA-T003-DATA-001` | `PASS` | 4 个 `yunjikeji` 客户端文件全部存在，SHA-256、行数、字符数与清单一致；2,624 行 / 69,824 字符可由元信息边界完整重建，行序列 0 不匹配；296 个真实空行全部保留，源文件和文档均为 0 字面 tab。 | 18 个文件全部存在，覆盖 5 个工程；SHA-256、行数、字符数与清单一致；8,329 行 / 310,972 字符的元信息和完整源码逐容器、逐字符一致，0 不匹配。两卷明显凭据、私钥和证书形态均为 0 命中，共同覆盖客户端、后台管理端、业务后端、管理后端和 AI 服务。 |
| `TA-T003-API-001` | `PASS` | ZIP CRC 正常，17 个实际部件与 A 参考部件集一致，16 个 XML / 关系全部可解析；样式、主题、字体表、主文档关系、页眉关系、唯一节与 A 一致；页眉除品牌等长替换外与 A 一致，保留 2 个 `PAGE` 域、2 个 DrawingML 绘图和 1 个 VML 回退对象；中间段的 `pPr/rPr` 与 A 代码原型一致。 | ZIP CRC 正常，15 个实际部件与 B 参考部件集一致，14 个 XML / 关系全部可解析；样式、主题、字体表、主文档关系、页眉关系、唯一节与 B 一致；20 段 `pPr` 逐段与 B 一致，8,401 个普通换行保持每文件段落组织；页眉保留 1 个 `behindDoc=1` 浮动绘图。两卷都是 A4、0 原生表格、1 媒体，背景均为 640×960 RGB 全白 PNG。 |
| `TA-T003-QUALITY-001` | `PASS` | `generate_source_design_docx_part1.py` 仅做 AST 静态审查，确认使用 `python-docx`、`lxml`、Pillow 和 `zipfile`；清单哈希 `27b0510d...`、脚本哈希 `efb156af...`及 `06` 记录的成品/参考/清单/脚本值均与实际一致。 | `generate_source_design_docx.py` 仅做 AST 静态审查，同样确认结构化依赖；清单哈希 `c8eb4956...`、脚本哈希 `dc0676a0...`及 `03` 记录的对应值均一致；现有 `_1` 成品仍为 `0530f54d3f1936daaaabb62a59c07fca6201461d9b421904f80911e79a27dcb1`。两卷文本替换字符、异常控制字符和截断均为 0；`git status --short -- code/develop` 与 `git diff -- code/develop` 均为空。 |

## 5. 关键结构实测

| 项目 | A：无后缀卷 | B：`_1` 卷 |
| --- | ---: | ---: |
| 正文段落 | 2,646 | 20 |
| 源码组织 | 2,624 个逐行段落 + 20 个文件元信息段落 | 18 个源文件容器 |
| Word 原生表格 | 0 | 0 |
| 节 / 媒体 | 1 / 1 | 1 / 1 |
| 实际文件部件 | 17 | 15 |
| 可解析 XML / 关系 | 16 | 14 |
| 页码域 | 2 个 `PAGE` | 0 |
| 页眉图形 | 2 个 DrawingML + 1 个 VML | 1 个 `behindDoc=1` DrawingML |
| 普通换行 `w:br` | 0 | 8,401 |
| 文档 `w:tab` | 0，与本次 4 个源文件字面 tab 数一致 | 0 |
| 空源码行 | 296，全部保留 | 包含在逐字符匹配中 |
| 页面 | 209.991 mm × 297.039 mm；上下 10.001 mm；左右 31.75 mm | 同 A |
| 背景 | 640×960 RGB 全白，SHA-256 `f6d271423958d5bbe675f7244d1862fb009b8d3ff7fc6b69e87844005e89cffe` | 同 A |
| 旧品牌 / 明显凭据 | 0 / 0 | 0 / 0 |

## 6. 首次漏交问题关闭证明

1. 首次验收只检查了 `_1` 卷，桌面当时没有与第一份无后缀参考对应的交付物；因此 `04` 的单文件通过结论无法证明成套交付完整。
2. 本次独立扫描桌面，精确找到 `云技_软件设计说明书源代码.docx` 和 `云技_软件设计说明书源代码_1.docx` 两份文件，无缺失。
3. A 卷的部件集、样式/主题/字体、节、页眉关系、复杂页眉图形和 2 个 `PAGE` 域与无后缀参考 A 一一对应，正文确为逐源码行建段。
4. B 卷的部件集、样式/主题/字体、节、页眉关系和 20 段段落属性与 `_1` 参考 B 一一对应，正文确为 18 个每文件段落容器。
5. 因此“漏交无后缀版”问题在本轮已由成套存在性、一一模板映射和完整源码检查正式关闭。

## 7. 未修改声明

- 本回合未修改或重新生成任一目标 DOCX。
- 本回合未修改 `code/develop` 业务源码，未修改两个生成脚本、任务计划或正式验收标准。
- 本回合新增文件仅为 `verify_source_design_docx_pair.py` 和本 `07` 记录。

## 8. 残余限制与风险

1. 本机在 `PATH` 及常见安装路径中仍未发现 Word、WPS 或 LibreOffice 可执行文件，因此未执行两卷的真实 Office 分页渲染或逐页视觉审查。更新后的正式标准要求的双 DOCX 重开、ZIP/XML、段落组织、页面、样式、页眉、媒体和源码检查已全部通过，该限制不阻断本次验收。
2. 两份参考和两份成品均无显式分页符，实际页数会受 Office/WPS 版本与字体环境影响；WPS 缓存页数不是实时分页承诺。
3. 敏感检查针对高置信凭据、私钥、证书、固定授权值和凭据 URL 形态，不等价于全面语义密钥审计；但两份清单已排除配置、账号、证书和临时资产，成品源码又与安全候选文件完整匹配。

## 9. 门禁结论

- `BA-T003-E2E-001`：`PASS`
- `BA-T003-DATA-001`：`PASS`
- `TA-T003-API-001`：`PASS`
- `TA-T003-QUALITY-001`：`PASS`
- 成套交付的四项最新正式验收全部通过，首次漏交问题已关闭，可重新进入 `TASK-005` 人工审核 / 用户验收；在用户明确审核通过前不得归档。
