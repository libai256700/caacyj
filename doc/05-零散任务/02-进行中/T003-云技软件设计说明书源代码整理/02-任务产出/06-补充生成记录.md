# T003 无后缀版补充生成记录

## 1. 生成结论

- 生成状态：`通过`
- 生成日期：`2026-07-24`
- 无后缀成品：`C:\Users\18163\Desktop\云技_软件设计说明书源代码.docx`
- 成品大小：`54,286` 字节
- 成品 SHA-256：`731226e7db3ac32a8f5a0b3f3a29ce6ea78d1fc2487607c1ea693ed0e4ad88ef`
- 生成基底仅为参考 A：`C:\Users\18163\Downloads\飞书20260724-105710\来绘_软件设计说明书源代码.docx`
- 参考 A 大小为 `211,878` 字节，SHA-256 为 `1eb9fef66254c5ec4f894fdf7d005b3446da70b1a94fd5c65ee4a2e00ac2fc82`；生成前后未修改参考文件。

## 2. 补充资产

| 资产 | 路径 | 大小 | SHA-256 |
| --- | --- | ---: | --- |
| 无后缀版源码清单 | `doc/05-零散任务/02-进行中/T003-云技软件设计说明书源代码整理/02-任务产出/05-无后缀版源码清单.md` | 2,809 字节 | `27b0510d4b0c80e1ab245911fb9e312beac7c455940bb450274c71be88bf026d` |
| 无后缀版生成脚本 | `doc/05-零散任务/02-进行中/T003-云技软件设计说明书源代码整理/02-任务产出/generate_source_design_docx_part1.py` | 32,129 字节 | `efb156af403bc960d0c38b3b34661908da845d95035ac7fd371b5f7ef928f848` |
| 无后缀 DOCX | `C:\Users\18163\Desktop\云技_软件设计说明书源代码.docx` | 54,286 字节 | `731226e7db3ac32a8f5a0b3f3a29ce6ea78d1fc2487607c1ea693ed0e4ad88ef` |

运行时为 `C:\Users\18163\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`，使用 `python-docx`、`Pillow`、`lxml` 与标准库 `zipfile`。

## 3. 源码与逐行段落结果

- 写入工程：`yunjikeji`
- 写入文件：`4` 个
- 源码行数：`2,624`
- 源码字符数：`69,824`
- 元信息段落：每文件 `5` 个，共 `20` 个，依次为工程、源文件、语言、源代码、文件结束。
- 正文总段落：`2 + 20 + 2,624 = 2,646`。
- 首段为 `程序开始！！！`，末段为 `程序结束！！！`。
- 4 个文件均由元信息边界重新定位并抽取，`4/4` 文件、`2,624/2,624` 行与真实源文件逐行精确一致，无截断、错序或跨文件混入。
- 共保留 `296` 个真实空源码行。
- 所选 4 个真实文件使用空格缩进，字面 tab 总数为 `0`；生成器支持将真实 tab 写为 `w:tab`，本次 XML 与抽取结果均验证为精确 `0`，未人为改写缩进。
- A 第 2 段 `<template>` 作为代码段原型；所有元信息与源码行段落复制其 `pPr/rPr`。原型 `pPr` SHA-256 为 `f4d3882b03d18cf1f78e5736e6657d1de0f356f6239260fa7543147ff83b6aa1`，原型 `rPr` SHA-256 为 `71036b9ff11a2a284d3801b749f72cef16bc5aa61536bee293b275e94ad6fb7e`。

## 4. DOCX 与模板保真检查

| 检查项 | 结果 | 证据摘要 |
| --- | --- | --- |
| `python-docx` 重开 | 通过 | 成品可重开，正文 `2,646` 段 |
| 表格 / 节 / 媒体 | 通过 | `0` 个表格、`1` 个节、`1` 个媒体文件 |
| ZIP / XML | 通过 | ZIP CRC 正常；`17` 个实际文件部件与 A 完全一致；`16` 个 XML/关系部件全部可解析 |
| 逐行组织 | 通过 | `2,624` 个源码行分别对应 `2,624` 个正文段落；正文中无 `w:br` 聚合换行 |
| 页面设置 | 通过 | `sectPr` 与 A 一致；约 `209.991 mm x 297.039 mm`，上下边距约 `10.001 mm`，左右边距 `31.75 mm`，页眉页脚距 `12.7 mm` |
| 样式体系 | 通过 | `styles.xml`、`theme1.xml`、`fontTable.xml` 与 A 的规范化 XML 一致 |
| 复杂页眉 | 通过 | 页眉 XML 除品牌等长替换外与 A 一致，页眉关系文件未变，`2` 个 `PAGE` 域保留 |
| 品牌 | 通过 | 页眉完成 `1` 处 `来绘` 到 `云技` 替换；全部 XML 中旧品牌为 `0`，页眉含新品牌 |
| 背景 | 通过 | `word/media/image1.png` 已替换为 `640 x 960` 纯白 PNG，RGB 三通道极值均为 `255` |
| 明显凭据 | 通过 | 私钥头、AWS 密钥形态、长 `sk-`、固定授权值及敏感字段固定字符串赋值均为 `0` 命中 |
| 业务源码 Git | 通过 | 生成前后 `git status --short -- code/develop`、工作区 diff、暂存区 diff 均为空 |

## 5. 两份桌面成品最终清单

| 对应参考 | 桌面成品 | 大小 | SHA-256 | 组织方式 |
| --- | --- | ---: | --- | --- |
| A：无后缀参考 | `C:\Users\18163\Desktop\云技_软件设计说明书源代码.docx` | 54,286 字节 | `731226e7db3ac32a8f5a0b3f3a29ce6ea78d1fc2487607c1ea693ed0e4ad88ef` | 每一源码行一个 Word 段落 |
| B：`_1` 参考 | `C:\Users\18163\Desktop\云技_软件设计说明书源代码_1.docx` | 80,660 字节 | `0530f54d3f1936daaaabb62a59c07fca6201461d9b421904f80911e79a27dcb1` | 每一源文件一个 Word 段落容器 |

现有 `_1` 成品在无后缀版生成前后的 SHA-256 均为 `0530f54d3f1936daaaabb62a59c07fca6201461d9b421904f80911e79a27dcb1`，确认未被修改。

## 6. 验收支撑

- `BA-T003-E2E-001`：桌面现同时存在分别对应 A、B 两份参考组织方式的无后缀版与 `_1` 版成品。
- `BA-T003-DATA-001`：无后缀版 4 个真实客户端文件的 2,624 行可按元信息边界完整重建并逐行匹配；与 `_1` 版共同覆盖主要技术模块。
- `TA-T003-API-001`：无后缀版保持 A 的逐行段落、A4 页面、样式/主题/字体、复杂页眉关系、2 个 PAGE 域、0 表格和 1 节基线。
- `TA-T003-QUALITY-001`：独立清单、可复跑脚本、逐文件哈希、行序列、字符数、品牌/背景/敏感和 Git 检查证据均已落盘；未修改业务源码或现有 `_1` 成品。
