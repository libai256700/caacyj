# 实施与验证记录

## 1. 参考输入

- 用户参考图：`C:\Users\18163\AppData\Local\Temp\codex-clipboard-a0cced79-22a1-442d-8996-549bdcf88850.png`
- 目标页面：`code/develop/yunjikeji/src/pages/enterprise/review.vue`
- 边界：只调整前端视觉，保留 `onLoad` 查询参数、本地审核状态、事件和路由；不新增请求、轮询或自动刷新。

## 2. 复用盘点

- 正式实现唯一入口为 `src/pages/enterprise/review.vue`，没有第二套同义正式页或测试页。
- 项目已安装 `uv-ui`，但 `uv-steps` 不能表达“完成绿、当前橙、未到灰”三色进度，`uv-loading-icon` 的 12 点加载器也不符合参考稿 8 点造型，因此保留现有 DOM 并做页面级视觉扩展。
- 项目既有图片均不具备参考稿的橙白文件夹、资料页和放大镜组合；新增 `src/static/enterprise-review/` 页面专用资源，未依赖其他未提交任务资产。
- 仓库内 `visual-v80-enterprise-review-reviewing-state-review.png` 与用户参考图均为 `853x1844` 且逐像素一致，用作稳定裁切源和 QA 基准。

## 3. 实施记录

- 保留 `review.vue` 的 `<script setup>` 原样，仅调整展示模板和 SCSS；状态枚举、优先级、步骤映射、动态文案、按钮条件和两个导航方法未改变。
- 页面按参考稿 `750rpx x 1621rpx` 比例实现：暖白航线背景、深蓝居中标题、橙白审核插画卡、独立三阶段进度卡、独立状态说明卡。
- `reviewing` 主态使用参考稿中的固定主标题裁片和不含文字的进度节点裁片；真实主标题 DOM 仍保留在下层，状态标签、步骤标签、结果标题、说明文案和按钮继续使用动态 DOM。
- `pending / reviewing / approved / rejected` 四态共用同一正式结构，并按原状态映射呈现橙、绿、红视觉与原按钮条件。
- 新增资源：
  - `review-illustration.png`
  - `review-card-route.png`
  - `review-result-illustration.png`
  - `review-result-wave.png`
  - `route-top.png`
  - `route-bottom.png`
  - `review-progress-reviewing.png`
  - `review-title.png`
  - `review-title@2x.png`

## 4. 验证记录

- 最新验证时间：`2026-08-14 18:12:39 +08:00`。
- `npm run type-check`：通过。
- `npm run build:h5`：通过，审核页专用资源已进入 H5 构建产物。
- `git diff --check`：通过；仅有仓库现存 LF/CRLF 提示，无空白错误。
- 脚本保真：当前与 `HEAD` 的完整 `<script setup>` 统一换行并 `TrimEnd` 后长度均为 `2490`，序数比较为 `True`，SHA-256 均为 `1d651702d558b7803381d31362f42488ae3efc314982134a8191be017a34c926`。
- `390x844` reviewing 主态：RGB 8x8 结构相似度 `97.43%`，像素颜色接近度 `99.35%`，全页 MAE `1.658`；相较本轮优化前的 `92.41% / 98.10% / 4.844` 明显收敛。
- `390x844`：`scrollWidth=390`、`scrollHeight=844`；主插画卡 `(20.8,151.8,348.9,195.0)`、进度卡 `(20.8,367.1,348.9,123.2)`、说明卡 `(20.8,510.6,348.9,186.2)`，与参考稿折算位置一致。
- `430x932`：`scrollWidth=430`、`scrollHeight=932`；主插画卡 `(22.9,167.4,384.7,215.0)`、进度卡 `(22.9,404.7,384.7,135.9)`、说明卡 `(22.9,563.0,384.7,205.3)`，无水平溢出、重叠或截断。
- 四态均在独立浏览器页面复验：`pending` 无按钮、`reviewing` 无按钮、`approved` 仅显示“进入平台”、`rejected` 仅显示“重新提交”；四张最终截图 SHA-256 均不同，无同路由 query 复用造成的串态。
- 导航契约通过：“重新提交”实测进入 `#/pages/enterprise/register`；“进入平台”的页面方法仍以 `/pages/enterprise/students` 为目标，无登录时最终进入登录页属于学员页既有 `requireLogin()` 权限守卫，不是本轮样式回归。
- 四态控制台均为 `0 error / 0 warning`；9 个页面专用资源首次请求均为 HTTP `200`，完整重载的条件请求为正常 `304`，无失败请求。
- 截图证据：`output/playwright/T008-enterprise-review/reviewing-390x844.png`、`reviewing-430x932.png`、`comparison-390x844.png`、`states-overview.png` 及 `independent/` 下独立验收截图。
- 逐像素 `100%` 不能作为诚实结论：剩余误差主要来自参考图由 `853x1844` 缩放到浏览器视口后的中文抗锯齿与浏览器直接栅格化差异；卡片、插画、航线与无文字装饰区域已接近像素级一致，动态业务文字继续保留真实 DOM。
- 当前项目测试现状未建立正式测试环境；本次仅涉及无后端请求的前端样式页，已在本地 H5 真实浏览器完成视觉、状态和导航契约验证，不扩大为后端集成测试结论。

## 5. 独立验收结论

- 本轮第一名全新独立验收代理确认高保真主态、四态、双视口、资源、控制台、拒绝态导航和质量门禁通过，并对无登录环境的 approved 最终地址及脚本精确对比提出保留意见。
- 第二名全新只读验收代理独立收口两项意见：脚本与 `HEAD` 完全一致；approved 的页面导航目标仍为 `/pages/enterprise/students`，登录页结果来自目标页既有权限守卫。
- `BA-T008-E2E-001`、`BA-T008-DATA-001`、`TA-T008-API-001`、`TA-T008-QUALITY-001` 全部通过；当前任务进入用户人工审核，不提前归档。
