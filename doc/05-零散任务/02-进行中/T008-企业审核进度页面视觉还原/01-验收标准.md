# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T008`
- 任务名称：企业审核进度页面视觉还原
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T008-E2E-001` | 端到端业务验收 | 企业提交申请后查看审核进度 | 待审核页面应严格还原用户参考图的暖白航线背景、深蓝主标题、橙白审核插画卡、三阶段进度条和底部审核说明卡，并在常见移动端宽度下保持完整、清晰、无重叠 | 真实 `enterprise/review` 页面包含参考图所有核心视觉区域；暖白、深蓝与橙色层级、卡片尺寸、圆角、阴影、间距、字号和进度节点接近参考；页面无水平溢出、遮挡或异常空白 | 将目标页在 `390x844` 与 `430x932` 视口截图并与参考图并排检查，补充 DOM 边界测量 | 页面仍沿用旧视觉；任一关键区域缺失；比例明显偏离；文字、节点、插画或卡片重叠/截断 |
| `BA-T008-DATA-001` | 数据结果业务验收 | 审核状态与说明内容展示 | 样式改动不得改变现有审核状态、申请信息、结果分支或显示条件，待审核状态按参考图呈现，已通过/已拒绝等既有分支仍由原数据和状态逻辑承接 | 原状态值、计算属性、条件分支和动态文案逻辑保持；`pending`、`reviewing`、`approved`、`rejected` 四种状态仍能按原条件显示；查询参数与本地状态优先级不受影响 | 审查模板绑定和脚本差异；通过四种合法 `status` 查询参数覆盖主要分支并核对渲染 | 任一状态枚举、状态映射、申请数据、显示条件或状态读取行为被视觉改造改变 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T008-API-001` | `BA-T008-DATA-001` | 技术接口验收 | 保留页面路由、`status` 查询参数、`onLoad` 本地状态写入与读取、条件渲染、事件方法及导航目标的原技术入口和契约 | 页面仍按“合法查询参数 > 已持久化企业审核状态 > `rejected`”取值；步骤映射、按钮条件、事件方法和路由目标保持不变；不得新增 HTTP 请求、轮询或自动刷新 | 代码差异审查；浏览器分别打开四种合法状态路由并核对渲染与两种按钮导航 | 查询参数、本地状态、步骤映射、路由目标或事件语义发生非视觉性变化，或新增请求、轮询、自动刷新 |
| `TA-T008-QUALITY-001` | `BA-T008-E2E-001` | 代码质量验收 | 在现有正式页面单一实现上完成，优先复用项目组件、图标和静态资源，页面专用样式响应式稳定且无调试残留 | 类型检查和 H5 构建通过；无第二套同义页面；静态资源均成功加载；两个目标视口无溢出、重叠或文本截断；实现变更限定在前端展示边界 | 审查变更范围；运行 `npm run type-check`、`npm run build:h5`、`git diff --check`；Playwright 截图和资源/控制台检查 | 构建失败、资源 404、重复实现、调试残留、跨视口溢出/重叠，或修改业务脚本与后端 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T008-001` | 独立验收通过后提交用户人工审核；只有用户明确确认视觉结果后才允许归档 | 独立验收记录、最终截图、用户明确反馈与任务计划状态 |
