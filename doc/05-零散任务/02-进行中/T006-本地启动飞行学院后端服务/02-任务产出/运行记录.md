# 本地后端运行记录

## 运行信息

- 记录时间：`2026-07-24 21:08`（Asia/Shanghai）
- 服务模块：`code/develop/yunjikeji-admin-server/yunjikeji-admin-server`
- 构建产物：`target/yunjikeji-admin-server.jar`
- Spring Profile：`local`
- 后端 PID：`20500`
- 后端端口：`48080`
- Redis 版本：`8.8.0`
- Redis PID：`21680`
- Redis 地址：`127.0.0.1:6380`
- 原有 Redis：`6379` 保持运行，未停止、未替换

## 启动方式

Redis 使用 winget 已安装的 `taizod1024.redis-windows-fork 8.8.0`，以独立进程绑定 `127.0.0.1:6380`，关闭持久化：

```powershell
redis-server.exe --port 6380 --bind 127.0.0.1 --protected-mode yes --save "" --appendonly no
```

后端使用现有 JAR 启动，并覆盖本地 Redis 端口：

```powershell
java -jar yunjikeji-admin-server.jar --spring.profiles.active=local --spring.redis.port=6380
```

## 日志入口

- 后端标准输出：`.codex-run-logs/yunjikeji-admin-server.stdout.log`
- 后端错误输出：`.codex-run-logs/yunjikeji-admin-server.stderr.log`
- Redis：`.codex-run-logs/redis-6380.log`

## 自检结果

| 检查项 | 结果 | 证据摘要 |
| --- | --- | --- |
| Redis 版本与端口 | 通过 | `redis_version:8.8.0`，`127.0.0.1:6380` 正在监听 |
| 后端启动完成 | 通过 | 日志出现 `Started YunjikejiAdminServerApplication in 79.024 seconds` |
| 后端监听 | 通过 | Java PID `20500` 监听 `48080` |
| 健康检查 | 通过 | `GET http://127.0.0.1:48080/actuator/health` 返回 HTTP `200` 和 `{"status":"UP"}` |
| APP 业务路由 | 通过 | `GET /app-api/yj/tenants` 返回 HTTP `200`，响应体为未登录业务响应；路由存在且不是 `404` |
| 前端代理 | 通过 | 用户后续要求启动前端；`GET http://127.0.0.1:5173/yj-app-api/yj/tenants` 返回 HTTP `200` 和未登录业务响应，确认代理已抵达本地后端 |
| 后端源码影响 | 通过 | `git status --short -- code/develop/yunjikeji-admin-server` 无本任务新增改动 |

## 本地入口

- 后端：`http://127.0.0.1:48080`
- 健康检查：`http://127.0.0.1:48080/actuator/health`
- 前端代理验证入口：`http://127.0.0.1:5173/yj-app-api/yj/tenants`
