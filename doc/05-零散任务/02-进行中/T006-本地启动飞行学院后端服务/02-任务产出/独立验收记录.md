# T006 独立验收记录

## 1. 验收信息

- 验收时间：`2026-07-24 21:29`（Asia/Shanghai）
- 验收方式：独立代理只读复核
- 验收对象：本地 `yunjikeji-admin-server`、独立 Redis、前端开发服务、运行记录及后端业务目录 Git 状态
- 操作边界：未修改进程、配置或业务源码，仅执行端口、HTTP、日志和 Git 状态检查。

## 2. 独立复核证据

| 复核项 | 独立证据 | 结论 |
| --- | --- | --- |
| Redis 监听 | `127.0.0.1:6380` 处于 `LISTENING`，占用进程 PID 为 `21680` | 通过 |
| 后端监听 | `0.0.0.0:48080` 与 `[::]:48080` 处于 `LISTENING`，占用进程为 Java PID `20500` | 通过 |
| 前端监听 | `127.0.0.1:5173` 处于 `LISTENING`，占用进程 PID 为 `16116` | 通过 |
| 后端启动日志 | 标准输出包含 `Started YunjikejiAdminServerApplication in 79.024 seconds` | 通过 |
| 健康检查 | `GET http://127.0.0.1:48080/actuator/health` 返回 HTTP `200` 和 `{"status":"UP"}` | 通过 |
| APP 后端直连 | `GET http://127.0.0.1:48080/app-api/yj/tenants` 返回 HTTP `200` 和未登录业务响应 `code=401`，路由不是 `404` | 通过 |
| 前端代理运行态 | `GET http://127.0.0.1:5173/yj-app-api/yj/tenants` 返回 HTTP `200` 和同一未登录业务响应 `code=401`，证明请求已由前端代理抵达后端 | 通过 |
| 运行记录一致性 | 模块、JAR、profile、后端 PID、Redis PID、端口、日志入口及 HTTP 自检结果与当前实际状态一致 | 通过 |
| 后端源码影响 | `git status --porcelain=v1 --untracked-files=all -- code/develop/yunjikeji-admin-server` 无输出 | 通过 |

## 3. 正式验收项结论

| 验收项 ID | 结论 | 判定说明 |
| --- | --- | --- |
| `BA-T006-E2E-001` | 通过 | 后端 `48080`、健康检查、前端 `5173` 与 `/yj-app-api` 代理链路均可用，代表性接口获得后端非 `404` 响应。 |
| `BA-T006-DATA-001` | 通过 | 运行记录完整，且记录中的服务模块、配置、PID、端口、日志及自检结果与独立复核一致，进程仍存活。 |
| `TA-T006-API-001` | 通过 | `local` 后端启动日志完整，Actuator 为 `UP`，代表性 `/app-api/yj/**` 路由直连和经前端代理均能响应。 |
| `TA-T006-QUALITY-001` | 通过 | 后端业务目录未发现本任务新增源码、配置或未跟踪文件。 |

## 4. 总体结论

**独立验收通过。**

全部正式验收项均已获得本次最新运行态证据。后端入口为 `http://127.0.0.1:48080`，健康检查为 `UP`；前端入口为 `http://127.0.0.1:5173`，其 `/yj-app-api` 代理已能抵达本地后端。本次验收未改动进程、配置或源码，也未执行归档。
