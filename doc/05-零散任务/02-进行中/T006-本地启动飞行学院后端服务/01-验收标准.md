# 验收标准

> 本文只写正式标准定义，不写执行结果和最终结论。

## 1. 文档信息

- 任务ID：`T006`
- 任务名称：本地启动飞行学院后端服务
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T006-E2E-001` | 端到端业务验收 / 任务目标结果验收 | 本地学员端访问对应后端 | 正确的 APP 后端保持运行，前端经 `/yj-app-api` 可访问本地 `/app-api/yj/**` 接口 | `48080` 监听、健康检查成功、至少一个代表性 APP 接口经前端代理得到后端响应 | 进程、端口、健康检查和代理 HTTP 响应 | 服务退出、健康检查失败或代理无法抵达后端 |
| `BA-T006-DATA-001` | 数据结果业务验收 | 用户复用本地服务 | 服务模块、配置、PID、端口、日志、启动方式和自检结果完整落入运行记录且与实际一致 | `运行记录.md` 完整且进程仍存活 | 记录与系统进程、监听端口、日志对照 | 信息缺失、记录不一致或进程退出 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T006-API-001` | `BA-T006-E2E-001` | 接口级验收 | Spring Boot 使用 `local` 配置成功启动，数据库和 Redis 依赖初始化成功，Actuator 与 APP 路由可响应 | 日志出现启动完成；`/actuator/health` 为 `UP`；代表性 `/app-api/yj/**` 请求不是连接失败或 404 | 启动日志及直接 HTTP 请求 | 启动报错、健康非 UP、依赖初始化失败或 APP 路由不存在 |
| `TA-T006-QUALITY-001` | `BA-T006-E2E-001` | 代码质量验收 | 启动过程不修改后端业务源码、不覆盖用户改动、不新增明文敏感配置 | 后端业务目录 Git 状态无本任务新增源码改动 | 启动前后 Git 状态核对 | 出现无关源码或配置修改 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T006-001` | 独立验收通过后向用户提供服务入口；用户明确确认前保持待人工审核 | 独立验收记录与用户后续反馈 |
