# TC-T0014-API-001 运行入口与分类边界

## 1. 测试层级

`接口级`

## 2. 对应验收项

- 验收项 ID：`TA-T0014-API-001`
- 正式验收入口：`../../../../01-验收标准.md`

## 3. 前置条件

- 环境前置：当前运行 JAR 进程存在；正式 Local/Test 连接记录可读。
- 账号前置：只从生效配置运行时取用，不写入测试资产。
- 数据前置：分类 `13`、租户 `1`。

## 4. 测试数据

```json
{"runtime_port": 48080, "database": "yunjikeji", "category_id": 13, "tenant_id": 1}
```

## 5. 执行动作

| 步骤 | 动作 | 预期结果 |
| --- | --- | --- |
| 1 | 核对运行进程、监听端口和 MySQL 已建立连接 | JAR 监听 48080，并连接正式测试库端点 |
| 2 | 核对 active profile、正式连接记录和数据库会话 | 三方数据库身份一致 |
| 3 | 只读查询分类边界 | `id=13`、`tenant_id=1`、活动记录恰好 1 条 |

## 6. 期望结果

- 运行程序实际数据库、配置和正式记录一致。
- 目标分类边界唯一。

## 7. 脚本入口

- 自动化脚本：`../05-自动化测试脚本/verify_t0014_readonly.py`
- 首次结果：`../t0014-readonly-result.json`
- 最新重跑结果：`../t0014-readonly-result-rerun.json`

## 8. 失败判定

- 无法确认运行程序实际连接库，或分类边界不唯一。
