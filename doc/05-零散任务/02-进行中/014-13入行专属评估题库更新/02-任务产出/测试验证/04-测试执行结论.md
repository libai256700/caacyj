# 测试执行结论

## 1. 当前结论

- 当前场景：T0014 分类13题库词法扫描修复后的最终固定范围独立只读验收
- 当前测试时间：`2026-08-18T15:15:18+08:00`
- 当前结论：`通过（PASS）`
- 直接原因：内置分类器自测 `23` 正例/`77` 反例全部通过；既有固定 `62` 例独立复跑 `failedCount=0`、`overallPass=true`；完整只读复跑退出码为 `0`、`overallPass=true`、`19` 项 checks 全为 true，推导 `checksFailed=[]`。

## 2. 接口级结果

- 已通过：2，`TA-T0014-API-001`、`TA-T0014-CODE-001`。当前 Java PID `23872` 监听 `48080`，有 2 条到正式测试库端点的已建立连接；生效 local 配置、正式 Local 记录和只读会话数据库身份均为 `yunjikeji`。实际校验 SQL、更新/回滚资产静态审查、词法扫描只读审查、自测和固定探针均通过。
- 未通过：0。
- 未执行：0。

## 3. 独立分类器探针

- 最新证据：`t0014-sql-classifier-probes-postfix.json`。
- 最新统计：固定 `62` 例，安全正例 `11`、危险反例 `51`；`failedCount=0`，`overallPass=true`。
- 词法扫描复核：引号内容保持原义；普通块注释及 `#`/合法 MySQL `--` 行注释归一化；可执行注释、优化器提示、未闭合注释和非法 MySQL `--` 形式 fail-closed；分号只在非引号/注释状态切分；受限 `SET @t0014_*` 拒绝嵌套赋值和危险读构造。
- 下列内容为第三次失败历史证据，继续保留且未覆盖：

- 证据：`t0014-sql-classifier-probes-final.json`。
- 统计：`62` 例，安全正例 `11`、危险反例 `51`；错误放行 `10` 例，`overallPass=false`。
- 错误放行完整列表：
  1. `unsafe_for_update_block_comment`：`SELECT id FROM example FOR/**/UPDATE;`
  2. `unsafe_for_share_block_comment`：`SELECT id FROM example FOR/* boundary */SHARE;`
  3. `unsafe_lock_share_mode_block_comments`：`SELECT id FROM example LOCK/**/IN/**/SHARE/**/MODE;`
  4. `unsafe_outfile_block_comment`：`SELECT value FROM example INTO/**/OUTFILE '/tmp/t0014.txt';`
  5. `unsafe_dumpfile_block_comment`：`SELECT value FROM example INTO/* boundary */DUMPFILE '/tmp/t0014.bin';`
  6. `unsafe_into_variable_block_comment`：`SELECT 1 INTO/**/@t0014_probe;`
  7. `unsafe_set_nested_assignment`：`SET @t0014_probe := CASE WHEN (@other_probe := 1) = 1 THEN 'x' ELSE 'y' END;`
  8. `unsafe_mysql_executable_comment`：`SELECT 1 /*!50000 FOR UPDATE */;`
  9. `unsafe_dash_dash_without_mysql_comment_space`：`SELECT 1--1 FOR UPDATE;`
  10. `unsafe_unterminated_block_comment`：`SELECT 1; /* unterminated`
- 已拒绝的必要边界包括：普通大小写/多空格/换行/行注释变体下的锁定读和文件输出，`GET_LOCK`、`RELEASE_LOCK`、`LOAD_FILE`、`SLEEP`、`BENCHMARK`，`COMMIT`、DML、DDL、未知语句、`autocommit`、`sql_log_bin`、错误变量前缀和锁定子查询。
- 已接受的安全边界包括：普通只读 SELECT、实际 `verify-category-13.sql`、只读事务语句、`ROLLBACK`、`SET NAMES utf8mb4` 和受限 `@t0014_*` 安全 SET。

## 4. 数据级结果

- 已通过：1，`BA-T0014-INT-001`、`TA-T0014-DATA-001` 的数据结果均满足：6 步、48 题、243 选项、0 活动子答案，步骤题量 `5/10/1/1/26/5`。
- 已通过：六步名称和顺序正确；逐题题干、题型、sort、step 及选项内容、代码、顺序与 mapping 一致；A3 拆 8 题且各 3 选项。
- 已通过：题目状态全 1、score 全 0、is_correct 全 0；步骤、答案、子答案孤儿均 0；题目和选项排序连续无重复。
- 未通过：0；未执行：0。

## 5. 交互级结果

- 已通过：0；未通过：0；未执行：0。
- 不适用说明：本轮仅为数据兼容更新，动态网页行为不在验收范围，未伪造网页验收。

## 6. 独立机器证据

- 内置分类器自测：`python verify_t0014_readonly.py --self-test`，退出码 `0`，`selfTestPass=true`，`23` 正例、`77` 反例。
- 完整只读复跑：退出码 `0`，`overallPass=true`，`19` 项 checks 全部为 true，推导 `checksFailed=[]`。
- 最新完整结果：`t0014-readonly-result-final2.json`；最新独立探针：`t0014-sql-classifier-probes-postfix.json`。
- 前三次失败/重跑证据 `t0014-readonly-result.json`、`t0014-readonly-result-rerun.json`、`t0014-readonly-result-final.json`、`t0014-sql-classifier-probes-final.json` 均继续保留，未覆盖、未删除。

## 7. 独立数据库统计与哈希

| 范围 | 行数 | SHA-256 |
| --- | ---: | --- |
| 非目标步骤 | 0 | `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945` |
| 非目标题目 | 1535 | `02fa3673246d9bf22c0616c39500cbed0439da79a863bbf9f945ff9c1c7d984b` |
| 非目标选项 | 4630 | `d1d6f71a0ea3dd8e553735facc0ffda97c62bca28644158a8f18ddeae4f190b8` |
| 非目标子答案 | 0 | `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945` |

- 非目标四表行数和哈希与 `backup-before.json` 基线、第二次复跑结果完全一致。
- 目标最终哈希：步骤 `d2434c9ba82834cd61cf63172bdeed8d65ea85712288c7b862d9a0db45692bb1`、题目 `f6ab711102ab9d6ec129119893112b02fef82504afa9c9806699e057a63b301d`、选项 `713c99b83a8d93ee8cc83b6ac1e0b19e3bd9b797ddeda3e72c9f05eb7590b14b`，均与 execution-log 和第二次复跑一致。

## 8. 数据库零写入证明

- 验收脚本连接后先执行 `SET SESSION TRANSACTION READ ONLY` 和 `START TRANSACTION READ ONLY`，数据库身份检查确认 `@@tx_read_only=1`；业务查询均为 SELECT，结束时执行客户端 rollback 并关闭连接。
- 本轮未执行更新 SQL、回滚 SQL、任何 DML 或 DDL；数据库持久化写入数为 0。
- 结果中的 `updateWriteStatementCount=6`、`rollbackWriteStatementCount=6` 仅为两个 SQL 资产的静态语句计数；全部边界、备份范围、幂等和无模糊名称写入检查为 true，不表示实际执行。
- 本次复跑后的目标和非目标全量哈希与第二次复跑及基线保持一致，未出现持久化变化。

## 9. 验收项映射

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| BA-T0014-INT-001 | 已通过 | `t0014-readonly-result-final2.json` 中六步、48 题、243 选项、A3、标准答案和 mapping 逐项校验 |
| TA-T0014-API-001 | 已通过 | PID 23872、48080 监听、远端连接、生效 local 配置、正式 Local 记录和数据库身份一致 |
| TA-T0014-DATA-001 | 已通过 | `t0014-readonly-result-final2.json` 的数据检查、关系完整性及目标/非目标哈希 |
| TA-T0014-CODE-001 | 已通过 | 词法扫描只读审查、自测 `23/77`、`t0014-sql-classifier-probes-postfix.json` 固定 `62/62` 通过及 SQL 静态审查 |
| VC-T0014-001 | 待闭环 | 独立验收职责分离和最新机器证据已成立；提交、推送及用户明确审核由主代理按任务计划继续闭环 |

## 10. 历史失败、修复与重跑记录

- 首次独立验收：`2026-08-18T14:16:22+08:00`，分类器遗漏安全 `ROLLBACK`，完整脚本退出码 `1`、`overallPass=false`；证据为 `t0014-readonly-result.json`。
- 第一次最小修复后重测：`2026-08-18T14:39:47+08:00`，内置自测和完整只读脚本均 PASS；证据为 `t0014-readonly-result-rerun.json`。第二次独立正式验收发现直接 `FOR UPDATE`、`INTO OUTFILE` 和 `INTO DUMPFILE` 仍被放行，正式结论为 FAIL。
- 第二次最小修复后最终重测：`2026-08-18T14:55:40+08:00`，内置自测和完整只读脚本均 PASS；证据为 `t0014-readonly-result-final.json`。新的独立变体探针发现上述 10 个绕过，正式结论继续为 FAIL。
- 第三次最小修复后最终固定范围重测：`2026-08-18T15:15:18+08:00`，词法扫描审查、自测 `23/77`、固定探针 `62/62` 和完整只读脚本 `19/19` 均 PASS；证据为 `t0014-sql-classifier-probes-postfix.json`、`t0014-readonly-result-final2.json`。
- 固定范围正式验收已通过；下一步进入提交、推送及用户审核闭环，不再扩大测试范围。

## 11. Git 范围与后续动作

- 本轮仅新增 `postfix` 固定探针结果、`final2` 完整只读结果并更新当前任务三份测试索引和执行结论；未修改测试脚本、实施资产、业务代码、配置、分类元数据或 `uni_modules/**`。
- 工作区存在其他无关业务代码及版本文档改动，未修改、未回退、未纳入本任务结论。
- 固定范围正式验收已通过；由主代理按全局门禁提交并推送本任务变更，随后进入用户审核，未获用户明确审核前不得归档为已完成。
