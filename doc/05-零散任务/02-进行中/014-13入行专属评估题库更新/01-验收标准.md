# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0014`
- 任务名称：13入行专属评估题库更新
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0014-INT-001 | 业务-交互级 | 维护人员以 `https://caacyj.com/` 当前公开 V3 题目及用户提供的六步定义为依据，更新当前程序实际连接库中的“13入行专属评估”题库 | 在不修改程序的前提下建立“基础画像、核心量表、主要顾虑、报告目标、补充模块、确认提交”六步，并完成 48 道平铺兼容映射：A3 拆成 8 道单选，A-E 全部固定显示，Likert 作为单选，text/textarea 作为文本；标准答案保持空，其他分类结果不变 | 分类 13 有且仅有 6 个启用步骤且名称/顺序正确；48 道题全部关联正确步骤；题目及选项与来源兼容映射清单逐项一致；A3 8 道拆分题各有 3 个等级选项；所有标准答案为空；其他分类全量基线零变化 | 六步定义、带采集时间的来源清单、步骤/题目映射清单、更新前后快照、目标分类逐项校验和非目标分类全量摘要 | 步骤缺失、重名、错序或题目归属错误；来源内容漏项或错项；A3 未正确拆成 8 题；填充任一标准答案；任一其他分类数据发生变化；把无法由四表表达的动态规则伪装为已实现 |
| BA-T0014-INT-002 | 业务-交互级 | 已登录学员从自我测评入口开始分类 13 测评并连续作答 | 自我测评统一使用考试模块的按需单题加载方式，启动后加载当前题，切换时只请求目标题，同时保持六步、题目、选项、作答和报告结果链路一致 | 首屏仅加载当前题；切换其他题目时每次最多一个目标题请求，不发生初始化阶段循环请求；六步名称与顺序、题目顺序、题型交互、进度均正确；提交后答题记录和最终报告仍可查询 | 真实自测入口操作录像或截图、浏览器 Network 请求清单与响应摘要、题目/六步顺序核对、单选/多选/文本题连续作答记录、提交后记录与报告查询证据 | 首屏循环拉取全部题目；切题不请求目标题或重复发起多次请求；题目/选项重复、缺失或乱序；步骤不显示或错序；作答丢失；提交后没有真实答题记录或报告 |
| BA-T0014-INT-003 | 业务-交互级 | 已登录学员从首页点击“开始测评”，且当前用户已有已完成自测记录 | 首页提示“已有测评记录，是否重新测评”；用户可取消并停留当前页，或确认后清空其历史自测并进入新的测评流程 | 无完成记录时直接进入；有完成记录时必有确认；选择否不跳转且零写入；选择是后历史自测不可再查询、新 session 可正常开始；其他用户记录和普通练习记录不变 | 首页交互截图或录像、取消与确认网络记录、重置前后当前/其他用户数据核对、新会话进入证据 | 未提示即覆盖历史；取消仍发起请求或写数据；确认误删其他用户/普通练习；删除成功却不能开始新测评；失败后出现半删除 |
| BA-T0014-INT-004 | 业务-交互级 | 已登录学员启动逐题练习、章节测试、任一考试或自我测评，以及在做题页加载题目 | 加载期间仅显示同一份正式居中遮罩弹层，标题和说明随当前模式变化；不再展示“逐题练习”等整页加载页 | 四类入口启动和做题拉题时均显示居中遮罩；遮罩阻止重复操作；成功后自动关闭并显示正常内容；失败后关闭并进入可重试错误态 | 四类模式入口与做题页截图/录像、成功和失败状态转换记录、页面文案核对 | 任一模式仍显示整页加载页或系统 loading；遮罩未覆盖交互；文案与模式不符；成功/失败后遮罩不退出 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0014-API-001 | BA-T0014-INT-001 | 技术-接口级 | 来源入口固定为 `https://caacyj.com/`；目标数据库必须由当前运行程序的生效 profile、进程网络连接和正式连接记录共同证明；分类入口固定为 `id=13`、`tenant_id=1`，不修改分类元数据 | 来源页面可复核；运行中程序实际库、正式记录和只读查询三方一致；目标分类身份唯一 | 页面访问记录、进程/生效配置核对摘要、正式连接记录引用、只读分类定位日志 | 来源入口、实际数据库或目标分类身份无法确认仍继续执行；改用配置模板、猜测地址或其他环境 |
| TA-T0014-API-002 | BA-T0014-INT-002 | 技术-接口级 | 自我测评复用考试模块现有单题接口与响应契约，启动与切题按 `index` 获取当前题，不得在初始化阶段循环调用单题接口 | 启动只请求当前题一次；切换目标题时请求对应 `index` 一次；响应可唯一关联同一自测会话和分类 13，题目/选项/步骤字段完整；考试模式契约及行为不回归 | 接口契约对照、真实请求与响应脱敏记录、请求次数断言、题目/六步/选项字段断言、考试模式接口回归记录 | 自测仍在启动阶段循环请求全部题目；切题请求缺失、重复或错 index；会话/分类错配；为自测复制第二套接口；现有考试模式因复用发生回归 |
| TA-T0014-API-003 | BA-T0014-INT-003 | 技术-接口级 | 重测查询和重置接口必须基于当前登录用户认证身份确定作用域，不接受用户 ID 作为删除边界；重置只作用于已完成自测 | 已完成状态查询准确；未确认不调用重置；重置接口无权用户不可调用；成功响应后可重新开始；普通练习、其他考试和其他用户不可通过该接口删除 | 接口契约、鉴权测试、当前用户/其他用户/非自测数据请求与响应断言 | 可越权删除；参数可指定其他用户；未完成记录被错误重置；确认后无法重新开始；普通模式受影响 |
| TA-T0014-API-004 | BA-T0014-INT-004 | 技术-接口级 | 加载遮罩只是现有批次创建、批次详情和单题加载请求的页面状态承接，不新增或改变业务接口契约 | 逐题练习、章节测试、考试和自测仍调用原正式接口；请求成功、失败和重试分支均能正确收口遮罩 | 网络请求对照、调用入口差异审查、成功/失败模拟 | 为加载展示新增平行接口；原接口请求参数或跳转契约发生回归；异常后遮罩持续阻塞页面 |
| TA-T0014-DATA-001 | BA-T0014-INT-001 | 技术-数据级 | `yj_practice_step` 和三张题库表的变更必须仅命中分类 `13`、租户 `1`；目标活动数据最终为 6 步、48 题及映射清单所列选项，题目 `step_id` 全部有效且归属正确，子答案不新增，标准答案保持空，并提供执行前备份与可验证回滚 | 六步名称/排序唯一且正确；48 道题全部关联目标六步中的有效主键；目标题目/选项内容、类型、顺序和数量与映射清单一致；子答案活动数为 0；标准答案空值规则满足；非目标步骤及非目标分类题库摘要零变化；无孤儿步骤、答案；回滚可恢复更新前状态 | 更新前后四表快照、步骤/题目映射清单、分类级与非目标分类校验 SQL、标准答案空值校验、孤儿校验、事务执行记录、回滚验证记录 | 任一 SQL 越过目标分类/租户边界；步骤或题目归属不一致；任一标准答案被填充；新增子答案；非目标步骤/题库数据出现变化；关系断裂；缺少备份、事务或回滚证据 |
| TA-T0014-DATA-002 | BA-T0014-INT-003 | 技术-数据级 | 重测确认后清理当前用户已完成自测会话关联数据：题目 batch、选项 batch、batch 主记录、答题主记录及明细、评估报告；所有删除在一个事务内完成 | 当前用户目标自测数据全部清理；其他用户、普通练习及源题库数据零变化；任一删除失败时全部回滚；重新开始后生成独立新 batch 和报告 | 事务测试、删除前后各表计数与主键集合、失败注入回滚、隔离数据对照 | 漏删导致旧会话复用；误删其他用户或普通模式；题库源数据被删；部分成功部分失败；新旧数据串联 |
| TA-T0014-PERF-001 | BA-T0014-INT-001 | 技术-代码级 | 模式1、自测和逐题 batch 生成在不改变题目顺序、选项顺序、分类限定、模式隔离、`custom_account_id` 绑定和结果保存语义的前提下，优先使用数据库侧 `INSERT ... SELECT` 批量复制题目批次与选项批次，并集中处理 `custom_account_id` 绑定 | 模式1完整顺序复制结果与原逻辑一致；自测和逐题如复用同一批次生成链路，生成与保存行为和模式1保持一致；批量插入减少逐行插入次数；`custom_account_id` 正确绑定到本次用户/模式上下文；其他模式实现仍独立，非目标模式不触发自测报告；存在接口/单元或集成级验证覆盖顺序、数量、账号绑定、报告生成和失败回滚 | 代码审查、SQL/Mapper 契约检查、目标测试执行记录、生成前后批次数量与顺序比对、日志或测试证据中的执行耗时对比 | 仍逐题逐选项循环写入导致性能瓶颈；`custom_account_id` 未正确绑定；自测或逐题保存结果丢失；模式1与其他模式串线；题目/选项顺序变化；报告异步生成不再可恢复或可查询 |
| TA-T0014-CODE-001 | BA-T0014-INT-001 | 技术-代码级 | SQL/执行资产按规范组织，对步骤表和题库表均使用明确分类 ID、租户 ID、精确主键集合和任务唯一标记约束，具备重复执行保护、影响行数断言和异常回滚；不得修改业务代码或 `uni_modules/**` | 静态审查确认步骤/题目/选项写操作均有硬边界，无名称模糊批量更新、无全表无边界写操作；实际影响行数与映射清单一致；重复校验不产生额外变化；工作区除任务产出和计划/验收外无本任务改动 | SQL 静态审查、影响行数日志、重复执行/空变更验证、`git diff` 与暂存区检查 | 存在无边界 UPDATE/DELETE、仅按易变名称匹配、影响行数无断言、脚本不可回滚，或触碰业务代码/三方目录 |
| TA-T0014-CODE-002 | BA-T0014-INT-002 | 技术-代码级 | 自测页面复用考试模块已有单题服务与正式组件边界，移除自测初始化时按 index 循环取题的实现；只有考试单题契约经证据确认无法承接时才做最小后端适配，不新增同义实现或宽泛兜底 | 自测初始化只请求第 0 题；下一题/上一题/跳题函数按目标 index 请求单题；同一份页面状态驱动题号、进度、步骤和作答；错误与重试状态明确；目标自动化测试覆盖首题单请求、切题请求、完整顺序、步骤显示、作答提交、报告查询及考试模块回归；`uni_modules/**` 无变更 | 代码差异审查、请求方法调用计数测试、页面状态测试、接口/服务回归测试、`git diff --check` 与 `uni_modules/**` 变更检查 | 保留启动阶段循环请求；切题请求缺失、重复或错 index；复制考试加载逻辑形成第二套实现；用缓存/异常兜底掩盖接口不兼容；题目状态与步骤/作答状态分裂；缺少请求次数或结果保存回归测试；修改 `uni_modules/**` |
| TA-T0014-CODE-003 | BA-T0014-INT-003 | 技术-代码级 | 重测删除逻辑必须集中在后端受鉴权服务层并使用事务，首页只负责状态查询和用户确认；删除顺序满足外键/关联完整性且不新增兜底删除 | 删除条件显式约束当前认证用户、自测模式和已完成状态；删除顺序明确；异常回滚；前端取消不调用写接口；测试覆盖确认、取消、隔离和回滚；`uni_modules/**` 无变更 | 代码审查、服务层事务测试、前端调用次数测试、静态检查 | 前端自行删除/拼用户 ID；无事务；无模式或完成状态边界；全表/宽泛删除；取消仍调用写接口；修改 `uni_modules/**` |
| TA-T0014-CODE-004 | BA-T0014-INT-004 | 技术-代码级 | 做题加载态必须复用单一正式遮罩组件，各页仅传入显示状态和模式文案；不保留同义整页加载实现，不修改 `uni_modules/**` | 同一组件覆盖逐题练习、章节测试、三类考试和自我测评；入口页、批次详情页、做题页无整页 loading 分支；类型检查、H5 构建和差异检查通过 | 组件引用与状态映射审查、全局搜索、`npm run type-check`、`npm run build:h5`、`git diff --check`、`uni_modules/**` 差异检查 | 复制多份同义遮罩；任一模式仍走整页加载或 `uni.showLoading`；加载成功/失败不收口；构建失败；修改 `uni_modules/**` |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0014-001 | 实施与独立验收由不同代理完成；全部正式验收项通过后提交并推送仓库，随后等待用户明确审核；如目标是生产库，仅提供经审批执行的脚本和证据，不由代理直接写库 | 独立验收记录、提交与推送记录、用户审核反馈；生产变更审批/执行记录（适用时） |
