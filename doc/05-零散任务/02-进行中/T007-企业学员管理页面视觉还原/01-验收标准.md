# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T007`
- 任务名称：企业学员管理页面视觉还原
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T007-E2E-001` | 端到端业务验收 | 企业账号登录后进入学员管理页面 | 页面应最大程度还原用户参考图的品牌场景、标题层级、双筛选区、学员审核卡片、状态操作区和底部导航，并在常见移动端宽度下保持完整、清晰、无重叠 | 企业登录后的真实页面在目标移动端视口呈现上述完整结构；主要色彩、间距、圆角、阴影、字体层级和橙色强调与参考图一致；页面可正常滚动且底栏不遮挡内容 | 真实页面截图与参考图并排比对；至少检查一个 390px 级视口和一个更宽移动端视口 | 目标页面仍沿用旧视觉；关键区域缺失；布局明显偏离参考图；文本、控件或底栏重叠/截断 |
| `BA-T007-DATA-001` | 数据结果业务验收 | 筛选、审核状态与学员数据展示 | 本次改动仅改变视觉呈现，既有学员数据、脱敏手机号、申请时间、审核状态、筛选结果和按钮可用性应继续由原状态与数据源承接 | 原有测试数据或接口数据仍按原字段显示；待审核、已通过、已拒绝状态映射不变；姓名与日期筛选、同意、拒绝及底栏导航仍可触发原行为 | 检查目标组件绑定、事件处理和请求调用未被替换；浏览器执行关键交互并记录结果 | 任一数据字段、状态映射、筛选规则、审核动作或导航行为因样式改动发生变化 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T007-API-001` | `BA-T007-DATA-001` | 接口级验收 | 保留企业登录后目标路由、列表查询、姓名/时间筛选、同意/拒绝和底栏导航的现有技术入口与参数契约 | 不新增或修改后端接口；原事件方法、请求方法、参数与路由目标保持不变；交互调用无控制台异常 | 代码差异审查并在真实页面触发可逆交互；必要时检查浏览器网络与控制台 | 接口、参数、请求时机、路由目标或事件语义发生非视觉性变化 |
| `TA-T007-QUALITY-001` | `BA-T007-E2E-001` | 代码质量验收 | 在现有正式页面和组件体系中完成单一实现，优先复用已安装组件、图标及项目资产，样式具备移动端适配且不引入调试残留 | 项目构建或类型/语法检查通过；无重复正式页面实现；无内联说明性调试文案、辅助线或临时色块；关键固定格式元素有稳定响应式约束 | 审查变更范围；运行项目既有检查/构建；在目标视口与相邻视口截图检查 | 构建失败、出现两套同义实现、存在调试残留、使用脆弱固定偏移导致常见移动视口溢出或重叠 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T007-001` | 独立验收通过后提交用户人工审核；只有用户明确确认视觉结果后才允许归档 | 独立验收记录、最终截图、用户明确反馈与任务计划状态 |
