# 实施与验证记录

## 1. 参考输入

- 用户参考图：`C:\Users\18163\AppData\Local\Temp\codex-clipboard-2911ed05-8e77-4699-8a99-1016e1a123a3.png`
- 目标：仅调整企业登录后的学员管理页面视觉，不改变功能与交互。

## 2. 复用盘点

- 正式页面：沿用 `src/pages/enterprise/students.vue` 单一正式实现，未新增同义页面或测试版组件。
- 第三方组件：继续复用 `@climblee/uv-ui` 已有 `uv-icon`，用于筛选和审核状态图标；没有重写基础图标组件。
- 项目组件：复用 `src/components/HomeProfileTabBar.vue`，通过可选 `enterprise-warm` 变体做轻扩展；未传变体的首页、中心、我的等页面继续使用原默认样式。
- 既有交互：完整复用姓名输入框、日期弹层、列表加载、同意/拒绝审核和底栏 `reLaunch` 路由行为，仅调整展示结构与视觉样式。
- 视觉资产：使用页面专属 `/static/enterprise-students/` 暖白无人机场景、五张头像和三张底栏图标；无人机原图底部旧卡片由橙白波浪在页面内裁切遮盖。
- 复用判断：本次采用“正式页原位修改 + 共享底栏可选轻扩展”，无需新增业务组件。

## 3. 实施记录

- `src/pages/enterprise/students.vue`：还原暖白无人机 Hero、深棕标题、橙白波浪、横向双筛选卡、暖白学员审核卡、头像、竖向分隔、状态区、橙色操作按钮及暖色日期弹层主题。
- `src/components/HomeProfileTabBar.vue`：新增仅目标页启用的 `enterprise-warm` 视觉变体，使用暖白浮动底栏和中央上浮 AI 图标；默认变体的原样式、激活色和导航目标保持不变。
- 展示文案仅将状态显示对齐参考图（`待审核 / 已通过 / 已拒绝`），申请时间在卡片中显示到分钟，底层原始时间、状态值、排序及筛选精度未修改。
- 接口、请求参数、登录校验、日期选择状态、审核方法和路由表均未修改。

## 4. 验证记录

- 已通过：`npm run type-check`（`vue-tsc --noEmit`）。
- 已通过：`npm run build:h5`（uni-app H5 构建完成）。
- 已通过：目标两个源码文件 `git diff --check`，无空白错误。
- 待独立验收：390px / 430px 移动端真实页面截图与参考图比对。
- 待独立验收：姓名筛选、日期弹层、同意/拒绝及底栏导航的浏览器交互回归。

## 5. TASK-004 未通过返工记录

- 返工范围仅限 `HomeProfileTabBar.vue` 的 `enterprise-warm` 专用 CSS；未修改卡片、列表、默认底栏样式、交互、路由或接口。
- 底栏盒体 `bottom` 从 `calc(env(safe-area-inset-bottom) + 12rpx)` 调整为 `calc(env(safe-area-inset-bottom) + 8rpx)`，在 390px / 430px 视口分别下移约 2.08px / 2.29px。
- 中央凸起伪元素 `top` 从 `-38rpx` 调整为 `-24rpx`，在 390px / 430px 视口分别下移约 7.28px / 8.03px。
- `.owl-ring` 的 `top` 从 `-50rpx` 调整为 `-34rpx`，在 390px / 430px 视口分别下移约 8.32px / 9.17px；按首轮测量，新的圆环顶部将分别落在约 747.66px / 825.86px，晚于末卡时间文字底部 745.40px / 821.56px。
- 模板、事件绑定、`openTab`、路由表和 `variant` 默认回退均未改动，默认底栏行为与视觉声明不受本次返工影响。
- 返工后已通过：`npm run type-check`（`vue-tsc --noEmit`）。
- 返工后已通过：`HomeProfileTabBar.vue` 的 `git diff --check`；本实施记录另行检查，无行尾空白。
- 返工后需由独立验收重跑 `BA-T007-E2E-001` 与 `TA-T007-QUALITY-001`，以 DOM 实测确认两视口的时间文字与 `.owl-ring` 交集为 `0`。
