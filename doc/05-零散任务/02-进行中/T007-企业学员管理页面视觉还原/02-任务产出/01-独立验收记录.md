# T007 独立验收记录

## 1. 验收结论

- 执行时间：`2026-08-14`
- 执行状态：`DONE_WITH_CONCERNS`
- 总结论：`未通过，需要返工`
- 阻断项：在 `390x844` 和 `430x932` 两个目标视口中，固定底栏中央 AI 助手圆形凸起均遮挡最后一张学员卡的申请时间文字，不满足“底栏不遮挡内容”的正式验收条件。
- 验收边界：只写入 Playwright 证据与本记录，未修改页面、组件、路由、接口、登录或静态资源实现。

## 2. 环境与数据

- H5 服务：`http://127.0.0.1:5180/yunjikeji/`
- 目标路由：`#/pages/enterprise/students`
- Playwright CLI 会话：`t007-acceptance`
- 视口：`390x844`、`430x932`
- 会话：本地注入已登录企业账号，token 为验收专用 mock 值。
- 数据：5 条 mock 审核记录，覆盖 2 条待审核、2 条已通过、1 条已拒绝；审核操作后对应记录转为已通过。

## 3. 逐项验收

| 验收项 | 结论 | 执行结果 |
| --- | --- | --- |
| `BA-T007-E2E-001` | `未通过` | 品牌场景、标题、橙色波线、双筛选区、5 张审核卡、暖色底栏均真实渲染，且两视口均无水平溢出。但两视口的 AI 助手圆形凸起都遮挡末卡时间文字，构成明显布局失效。 |
| `BA-T007-DATA-001` | `通过` | 姓名输入“李欣妍”后列表精确收缩为 1 条；清空后恢复 5 条。日期面板可打开且点击遮罩可关闭。待审核/已通过/已拒绝映射、脱敏手机号和申请时间正确。点击“同意”后状态变为“已通过”。 |
| `TA-T007-API-001` | `通过` | 列表请求为 `GET /app-api/yj/company/student-audit/page?pageNo=1&pageSize=200&auditStatus=1`；审核请求为 `PUT /app-api/yj/company/student-audit/audit`，body 为 `{"id":101,"auditStatus":2}`，并携带 `Authorization: Bearer demo-enterprise-token` 与 `tenant-id: 100`。请求均返回 200，审核后再次查询列表。底栏抽查“首页”和“我的”，分别到达 H5 首页 `#/` 和 `#/pages/profile`。 |
| `TA-T007-QUALITY-001` | `未通过` | 目标页在独立刷新后为 `0 error / 0 warning`，无空白渲染；目标页 hero、五张头像及三个底栏资源均为 200，未发现目标页 404。但固定底栏对目标视口的稳定约束不足，已实际遮挡文字，故不通过。 |

## 4. 重叠测量

### 4.1 `390x844`

- 末卡：`x=18.72..371.28`，`y=673.91..759.18`。
- 末卡时间文字：`x=99.46..214.63`，`y=731.35..745.40`。
- 底栏盒体：`y=757.69..837.76`，与末卡盒体重叠约 `1.49px`。
- AI 圆形图：`x=167.96..222.03`，`y=739.34..793.42`。
- AI 圆形图与时间文字包围盒交集：约 `46.67px x 6.05px`。
- 页面尺寸：`scrollWidth=390/clientWidth=390`，无水平溢出；`scrollHeight=858/clientHeight=844`。

### 4.2 `430x932`

- 末卡：`x=20.64..409.36`，`y=742.74..836.76`。
- 末卡时间文字：`x=109.57..236.72`，`y=806.08..821.56`。
- 底栏盒体：`y=836.83..925.13`，盒体本身与末卡仅有约 `0.07px` 间隙。
- AI 圆形图：`x=185.18..244.80`，`y=816.69..876.31`。
- AI 圆形图与时间文字包围盒交集：约 `51.54px x 4.88px`。
- 页面尺寸：`scrollWidth=430/clientWidth=430`，无水平溢出；`scrollHeight=946/clientHeight=932`。

## 5. 控制台、网络与路由

- 目标页的最终独立刷新检查：`Errors: 0`，`Warnings: 0`。
- 目标页列表与审核 mock 请求：全部 `200 OK`。
- 目标页静态资源：`hero-drone.png`、`avatar-01..05.png`、`nav-home.png`、`nav-ai.png`、`nav-profile.png` 全部 `200 OK`。
- 导航到首页时确认路由已到达，但首页自身另外产生 `/static/home/nav-home-active.png` 404 与未 mock 的练习接口 500；它们不来自目标学员页资源或学员审核接口，本记录作为范围外观察保留。

## 6. 证据

- `output/playwright/T007-enterprise-students/390x844-viewport.png`
- `output/playwright/T007-enterprise-students/390x844-full.png`
- `output/playwright/T007-enterprise-students/390x844-name-filter.png`
- `output/playwright/T007-enterprise-students/390x844-date-picker.png`
- `output/playwright/T007-enterprise-students/430x932-viewport.png`
- `output/playwright/T007-enterprise-students/430x932-full.png`
- `output/playwright/T007-enterprise-students/mock-setup.js`
- `output/playwright/T007-enterprise-students/dev-server.stdout.log`
- `output/playwright/T007-enterprise-students/dev-server.stderr.log`

## 7. 返工与重跑入口

1. 调整学员列表底部留白或底栏/AI 圆形凸起与内容的空间关系，保证末卡中所有文字不落入底栏及其凸起的覆盖区域。
2. 重跑 `390x844` 和 `430x932` 两个视口，重新测量末卡时间文字与 `.owl-ring` 包围盒，交集必须为 `0`。
3. 保留姓名筛选、日期面板开关、审核请求与底栏导航的现有通过结果，修复后再执行一次最小回归。

## 8. 第二轮返工后独立复验

### 8.1 复验结论

- 执行时间：`2026-08-14`
- 执行状态：`DONE`
- Playwright CLI 会话：`t007-recheck-20260814`（全新会话）
- 总结论：`通过`。第一轮失败的底栏遮挡场景已在 `390x844`、`430x932` 两个视口转绿；姓名筛选和一次待审核“同意”操作的最小关联回归通过。
- 验收边界：本轮只写入两张返工后截图并追加本节，未修改页面、组件、静态资源、接口、路由或登录实现。

### 8.2 DOM 坐标与交集

| 视口 | 末卡 | `.student-time` | 底栏盒体 | `::before`（按计算样式还原） | `.owl-ring` | “AI助手”文字 |
| --- | --- | --- | --- | --- | --- | --- |
| `390x844` | `x=18.719..371.281`，`y=673.906..759.177` | `x=99.458..214.625`，`y=731.354..745.396` | `x=18.719..371.281`，`y=759.771..839.844` | `x=149.427..241.240`，`y=748.291..805.114` | `x=167.958..222.031`，`y=749.750..803.823` | `x=176.302..213.698`，`y=815.260..829.823` |
| `430x932` | `x=20.635..409.365`，`y=742.740..836.760` | `x=109.573..236.719`，`y=806.083..821.563` | `x=20.635..409.365`，`y=839.125..927.417` | `x=164.792..265.875`，`y=826.365..888.948` | `x=185.177..244.802`，`y=828.156..887.781` | `x=194.365..235.615`，`y=900.385..916.438` |

| 视口 | 时间 / `.owl-ring` | 时间 / `::before` | 时间 / 底栏盒体 | 末卡 / 底栏盒体 | `.owl-ring` / “AI助手” |
| --- | --- | --- | --- | --- | --- |
| `390x844` | 空交集 `0x0`，面积 `0`；纵向间隙 `4.354px` | 空交集 `0x0`，面积 `0`；纵向间隙 `2.895px` | 空交集 `0x0`，面积 `0`；纵向间隙 `14.375px` | 空交集 `0x0`，面积 `0`；纵向间隙 `0.594px` | 空交集 `0x0`，面积 `0`；纵向间隙 `11.438px` |
| `430x932` | 空交集 `0x0`，面积 `0`；纵向间隙 `6.594px` | 空交集 `0x0`，面积 `0`；纵向间隙 `4.802px` | 空交集 `0x0`，面积 `0`；纵向间隙 `17.562px` | 空交集 `0x0`，面积 `0`；纵向间隙 `2.365px` | 空交集 `0x0`，面积 `0`；纵向间隙 `12.604px` |

- `390x844`：`scrollWidth=390`、`clientWidth=390`，无水平溢出；`scrollHeight=858`、`clientHeight=844`。
- `430x932`：`scrollWidth=430`、`clientWidth=430`，无水平溢出；`scrollHeight=946`、`clientHeight=932`。
- 两视口截图可见中央凸起只进入末卡底部空白区域，不覆盖时间文字；底栏盒体与末卡盒体本身不相交。

### 8.3 最小关联回归

- 姓名筛选：输入“李欣妍”后列表由 `5` 条精确收缩为 `1` 条且仅显示李欣妍；清空后恢复 `5` 条。
- 审核操作：点击张雨辰“同意”后状态由“待审核”变为“已通过”。
- 审核请求：`PUT /app-api/yj/company/student-audit/audit` 返回 `200 OK`，body 为 `{"id":101,"auditStatus":2}`，仍携带 `Authorization: Bearer demo-enterprise-token` 与 `tenant-id: 100`；随后列表 `GET` 再次返回 `200 OK`。
- 控制台：目标页最终结果为 `Errors: 0`、`Warnings: 0`。
- 资源：hero、5 张头像及 3 个底栏图片均加载完成且 `naturalWidth > 0`。

### 8.4 正式验收项

| 验收项 | 第二轮结论 | 复验依据 |
| --- | --- | --- |
| `BA-T007-E2E-001` | `通过` | 两目标视口的第一轮遮挡项均转绿，无水平溢出，返工后截图未见文字、底栏或中央图标互相覆盖。 |
| `BA-T007-DATA-001` | `通过` | 姓名筛选、清空恢复、状态展示及一次待审核“同意”操作均保持原行为。 |
| `TA-T007-API-001` | `通过` | 审核接口、参数、鉴权头、租户头与随后列表刷新均保持原契约并返回 `200`。 |
| `TA-T007-QUALITY-001` | `通过` | 两视口关键布局交集为空、无水平溢出，目标页 `0 error / 0 warning`，目标资源正常。 |

### 8.5 第二轮证据

- `output/playwright/T007-enterprise-students/recheck-390x844-viewport.png`
- `output/playwright/T007-enterprise-students/recheck-430x932-viewport.png`
