# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0022`
- 任务名称：岗位列表查询条件
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0022-INT-001 | 业务-交互级 | 用户进入岗位列表，在列表上方选择关键词、地域和工资区间后查看岗位 | 页面提供关键词 `全部 / 飞手类 / 工程师类 / 教培类 / 其他类`、地域 `全部 / 省份`、工资 `全部 / 0-3000 / 3000-5000 / 5000-10000 / 10000-30000 / 30000-200000` 三个下拉条件；前三个关键词分类分别说明真实查询关键词为 `飞手 / 工程师 / 教培`，其他类表示名称不匹配这三个关键词的岗位；条件可单独或组合使用 | 每次选择后列表从第一页展示满足全部已选条件的岗位；前三类仅返回对应名称关键词，其他类不返回名称包含前三个关键词的岗位；清空条件恢复原列表；翻页与原展示模式保持一致 | 在真实岗位列表分别执行四类关键词、组合条件、清空条件和翻页验证，核对页面结果及请求参数 | 分类标签或真实关键词映射错误；其他类仍返回前三类岗位；缺少任一条件；组合条件未同时生效；筛选后页码或展示模式异常 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0022-API-001 | BA-T0022-INT-001 | 技术-接口级 | 现有岗位分页接口接收关键词分类、地域、工资上下限查询参数，并与原分页参数共同工作 | `name` 传 `飞手`、`工程师`、`教培` 时执行对应名称匹配；`name=__other__`（或等价正式约定）执行排除三类关键词；地域映射 `work_area`，工资档位映射明确的最小值和最大值；不传筛选参数时接口行为兼容原契约 | 接口测试或真实请求记录，覆盖无筛选、四类关键词、组合筛选、第二页请求 | 参数未透传、分类映射错误、其他类未执行排除逻辑、筛选条件覆盖分页参数或无筛选请求发生回归 |
| TA-T0022-DATA-001 | BA-T0022-INT-001 | 技术-数据级 | 后端对岗位记录按名称分类、工作地域和工资范围执行过滤；工资采用区间交集规则 | `name` 为前三个真实关键词时名称包含对应关键词；其他类排除名称包含 `飞手`、`工程师`、`教培` 的岗位；`work_area` 与所选省份匹配；岗位工资最小值不大于所选最大值且岗位工资最大值不小于所选最小值；多个条件使用 AND 组合 | 后端测试数据或可核对查询结果覆盖四类关键词、命中、不命中、排除、边界相等、区间包含、区间被包含场景 | 其他类仍包含任一前三类岗位；使用单点包含代替区间交集；边界相等被漏掉；组合条件使用 OR；查询结果与字段事实不符 |
| TA-T0022-CODE-001 | BA-T0022-INT-001 | 技术-代码级 | 复用岗位列表现有组件与分页请求链路，筛选状态与分页状态职责清晰，不修改 `uni_modules/**` | 前后端构建或静态检查通过；相关自动化测试通过；改动仅覆盖岗位筛选链路；工作区与暂存区不含 `uni_modules/**` | 代码差异、类型检查、后端测试和构建记录 | 出现重复页面/组件、分页回归、编译失败、测试失败或改动三方包 |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0022-001 | 实施结果和独立验收记录写入 `02-任务产出/`；独立验收通过后提交并推送当前分支，等待用户审核 | 任务产出文件、提交号、远程分支和用户反馈 |
