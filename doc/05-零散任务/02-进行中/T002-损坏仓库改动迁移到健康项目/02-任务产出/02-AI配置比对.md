# AI 配置比对

## 1. 比对结论

- 本次不迁移下载目录中的 `application-local.yml` 到健康项目。
- 健康项目 AI 服务端口与调用入口保持原值，不接受下载目录配置反向覆盖。
- 本次仅迁移“AI 回答返回知识库引用”的代码链路，不迁移本地密钥、端口、环境专用配置。

## 2. 健康项目当前正式配置

### 2.1 `yunjikeji-server`

文件：
- `D:\ProjPort\Work\feixingxueyuan\code\develop\yunjikeji-server\src\main\resources\application.yml`

当前有效值：
- `server.port=18080`
- `ai.assistant.base-url=http://127.0.0.1:8088`
- `ai.assistant.model=exam-kb-assistant`

处理结论：
- 全部保留，不修改。

### 2.2 `fly-llm`

文件：
- `D:\ProjPort\Work\feixingxueyuan\feixing-ai\fly-llm\src\main\resources\application.yml`

当前有效值：
- `server.port=8080`
- `assistant.volcengine.ark.*` 走环境变量
- `assistant.volcengine.knowledge-base.*` 走环境变量

处理结论：
- 全部保留，不修改。

## 3. 下载目录配置文件观察结果

来源文件：
- `C:\Users\13174\Downloads\feixingxueyuan\feixingxueyuan\feixing-ai\fly-llm\src\main\resources\application-local.yml`

观察到的特点：
- 含本地明文接入信息。
- `server.port=8080`，与健康项目 `fly-llm` 端口现值一致，但该文件属于本地环境文件，不应直接入健康项目。
- 其接入项更偏本机调试，不适合作为健康项目基线来源。

处理结论：
- 不创建目标侧同名文件。
- 不覆盖健康项目现有 `application.yml`、`application-example.yml`。
- 不挪用其中明文密钥内容。

## 4. 本次允许迁移的 AI 相关内容

- `AiChatCompletionResponse.metadata.citations` 结构定义
- `AiAssistantCompletion` 结果 DTO
- `PracticeService` 中从 AI 响应提取 citations 并回传 references 的逻辑
- 前端 `references` 类型、流式事件处理、引用文件名展示

## 5. 本次禁止覆盖的配置边界

- `D:\ProjPort\Work\feixingxueyuan\code\develop\yunjikeji-server\src\main\resources\application.yml` 中的 `server.port`
- `D:\ProjPort\Work\feixingxueyuan\code\develop\yunjikeji-server\src\main\resources\application.yml` 中的 `ai.assistant.base-url`
- `D:\ProjPort\Work\feixingxueyuan\feixing-ai\fly-llm\src\main\resources\application.yml` 中的 `server.port`
- 健康项目任何正式配置文件中的环境变量占位设计
- 健康项目中已有的 AI/知识库接入边界

## 6. 风险提示

- 健康项目当前 `git status` 显示 `feixing-ai/fly-llm` 已有大量未提交删除/冲突现场，本次未触碰该现场，只做配置边界记录。
- 健康项目 `PracticeServiceIntegrationTests.java` 已存在重复测试方法，本次未追加源侧测试迁移，避免进一步放大编译风险。
