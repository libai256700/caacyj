# [零散任务计划]T002-损坏仓库改动迁移到健康项目

> 本文件是当前零散任务推进的唯一正式计划入口。
> 未先建立或更新本文件前，不得直接开始分析、改写、修复、验证、验收结论回写或归档。
> 本文件统一承载任务目标、正式验收入口、阶段任务总表、任务详情、门禁和人工审核闭环。正式验收标准不在这里定义，统一维护在 `01-验收标准.md`。

## 1. 任务信息

- 任务ID：`T002`
- 任务名称：损坏仓库改动迁移到健康项目
- 创建日期：`2026-05-14`
- 当前状态：`进行中`
- 分支引用：`沿用当前工作分支`
- 合并状态：`不涉及独立分支合并`
- 分支处置：`不涉及独立分支删除`

## 2. 任务目标

1. 将 `C:\Users\13174\Downloads\feixingxueyuan\feixingxueyuan` 中仍可读取的有效改动迁移到 `D:\ProjPort\Work\feixingxueyuan`。
2. 迁移过程中以健康项目现有配置为基线，尤其 AI 服务端口以 `D:\ProjPort\Work\feixingxueyuan` 当前配置为准，不被下载目录中的配置覆盖。

## 3. 任务范围

- 包含：
- 提取损坏仓库中可识别的源码与配置改动范围
- 将有效改动迁移到健康项目对应文件
- 对 AI 相关配置做差异对照并保留健康项目端口基线
- 形成迁移说明与验收证据
- 不包含：
- 修复下载目录损坏仓库本身
- 发布、部署或远程推送
- 将不明确来源或不可验证的临时截图/垃圾文件迁入健康项目

## 4. 统一原则

- 先读任务详情：阶段任务总表只做导航；真正实施前，必须先阅读对应任务详情。
- 计划启动门禁：只要 `01-验收标准.md` 未收口、阶段任务总表未补齐或当前任务没有任务详情，任何实施任务都不得进入 `进行中`。
- 正式验收承载位置：正式验收标准统一写到 `01-验收标准.md`，`00-任务计划.md` 只保留验收入口和状态门禁，不平行定义第二套正式验收标准。
- 未通过场景门禁：只要正式验收项仍有 `未通过`，不得把任务状态改为 `已完成`。
- 人工审核门禁：只有“独立验收通过 + 用户明确审核通过”同时满足后，才允许执行归档和状态回写。
- 配置迁移门禁：涉及 AI 配置时，只迁移目标项目缺失或需要更新的接入项；`D:\ProjPort\Work\feixingxueyuan` 现有服务端口保持优先，不允许被覆盖。

## 5. 正式验收入口

| 入口 | 作用 | 当前要求 |
| --- | --- | --- |
| `01-验收标准.md` | 记录本任务的业务验收、技术验收和闭环项 | 必须在实施前收口完成 |
| `02-任务产出/` | 沉淀迁移清单、配置比对、校验记录和验收前证据 | 必须能定位真实产出和检查证据 |

## 6. 三阶段任务总表

### 6.1 载体与验收入口阶段
| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-001` | `建立任务载体并补齐计划信息` | `-` | `已完成` | [详见任务详情](#task-001-建立任务载体并补齐计划信息) |
| `TASK-002` | `收口正式验收入口` | `TASK-001` | `已完成` | [详见任务详情](#task-002-收口正式验收入口) |

### 6.2 实施与独立验收阶段
| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-003` | `提取损坏仓库有效改动并迁移到健康项目` | `TASK-002` | `已完成` | [详见任务详情](#task-003-提取损坏仓库有效改动并迁移到健康项目) |
| `TASK-004` | `独立验收迁移结果与配置边界` | `TASK-003` | `已完成` | [详见任务详情](#task-004-独立验收迁移结果与配置边界) |

### 6.3 人工审核与归档阶段
| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-005` | `正式验收通过后等待人工审核/用户验收` | `TASK-004` | `进行中` | [详见任务详情](#task-005-正式验收通过后等待人工审核用户验收) |
| `TASK-006` | `归档并回写状态` | `TASK-005` | `未开始` | [详见任务详情](#task-006-归档并回写状态) |

---

## 7. 任务详情

### TASK-001 建立任务载体并补齐计划信息
**实施标准**
- 在 `doc/05-零散任务/02-进行中/` 下建立当前任务目录，并补齐 `00-任务计划.md`、`01-验收标准.md`、`02-任务产出/` 最小结构。
- 将任务ID、任务名称、当前状态、分支引用、合并状态和分支处置回写到本文件“任务信息”。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 当前任务目录
- 本文件“任务信息”与“任务范围”信息块

**关联验收项**
- `-`

**任务产出 / 结果记录**
- 已建立任务目录并回写计划入口

### TASK-002 收口正式验收入口

**实施标准**
- 按 `20-0200-acceptance-standards-documentation` 的零散任务口径写好 `01-验收标准.md`。
- 本文件只保留正式验收入口和门禁，不平行定义第二套正式验收标准。
- 未完成本任务前，`TASK-003` 不得进入 `进行中`。

**对应技能**
- `20-0200-acceptance-standards-documentation`

**交付物**
- `01-验收标准.md`

**关联验收项**
- `-`

**任务产出 / 结果记录**
- 已建立验收标准入口

### TASK-003 提取损坏仓库有效改动并迁移到健康项目

**实施标准**
- 从损坏仓库中仅提取当前仍可读取且有明确目标映射的改动。
- 将有效改动迁移到 `D:\ProjPort\Work\feixingxueyuan` 对应文件。
- 配置对照时，AI 服务端口以健康项目现状为准，不覆盖目标项目端口。
- 将迁移清单、配置差异说明和关键核对结果沉淀到 `02-任务产出/`。

**对应技能**
- `00-0800-repository-management`
- `00-0400-application-connection-management`

**交付物**
- 健康项目中的实际代码与配置修改
- `02-任务产出/` 中的迁移清单与配置比对记录

**关联验收项**
- `BA-T002-E2E-001`
- `BA-T002-DATA-001`
- `TA-T002-API-001`
- `TA-T002-QUALITY-001`

**任务产出 / 结果记录**
- 已由实施子代理完成首轮迁移。
- 已迁移后端 AI `citations -> references` 返回链路、前端引用展示链路，以及两份任务产出文档。
- 已明确未迁移下载目录中的 `feixing-ai/fly-llm/src/main/resources/application-local.yml`、截图/HTML 抓取文件和 `.gitignore` 删除类无效变更。
- 你已新增执行口径：除部署/配置类差异外，`feixing-ai/fly-llm` 中其余具备业务或代码意义的改动继续迁入 `code/develop/fly-llm`。
- 当前已识别新增候选范围：仅迁移 `ChatGatewayService.java` 中的引用/图片去重逻辑，以及配套 `ChatGatewayServiceTest.java`；不整文件覆盖、不迁部署配置。
- 新增迁移已落地到 `code/develop/fly-llm`：`ChatGatewayService.java` 增加 `citations/images` 按 `source` 去重逻辑，`ChatGatewayServiceTest.java` 补入并完成语法收口。
- 迁移清单见 `02-任务产出/01-迁移清单.md`，AI 配置边界见 `02-任务产出/02-AI配置比对.md`。

### TASK-004 独立验收迁移结果与配置边界

**实施标准**
- 由新的独立验收代理按 `01-验收标准.md` 逐条执行正式验收，不复用实施代理自验收。
- 重点验证迁移后的功能/结构是否落到健康项目，以及 AI 相关配置迁移未覆盖目标项目端口基线。
- 如发现问题，先标记正式验收项为 `未通过`，并记录失败现象、影响范围和重跑入口。

**对应技能**
- `40-0100-project-testing`
- `20-0200-acceptance-standards-documentation`

**交付物**
- 正式验收记录
- 未通过场景收口记录（如有）

**关联验收项**
- `BA-T002-E2E-001`
- `BA-T002-DATA-001`
- `TA-T002-API-001`
- `TA-T002-QUALITY-001`

**任务产出 / 结果记录**
- 首轮迁移已由独立验收子代理完成只读核对，正式验收结论为“通过，但带关注项”。
- `BA-T002-E2E-001`、`BA-T002-DATA-001`、`TA-T002-API-001` 通过。
- `TA-T002-QUALITY-001` 通过但带关注：本次未重跑完整构建/测试，且目标仓库存在既有脏现场。
- AI 服务端口边界已确认保留目标项目现状：
- `yunjikeji-server` 保留 `server.port=18080`
- `yunjikeji-server` 保留 `ai.assistant.base-url=http://127.0.0.1:8088`
- `fly-llm` 保留 `server.port=8080`
- 针对新增 `fly-llm` 迁移范围，已重新完成独立验收，结论为“通过但带关注项”：
- 通过点：`ChatGatewayService.java` 的去重逻辑属于局部最小迁移，`ChatGatewayServiceTest.java` 已完成语法收口，未触碰 `uni_modules/**`，未把 `application.yml` / `application-local.yml` 纳入本次有效交付。
- 关注项：项目级 `compile` / `test-compile` 仍被 `PromptAssemblyService.java`、`GroundedPrompt.java` 的既有编译问题阻断；`ChatGatewayServiceTest.java` 当前仍是未跟踪文件，尚未正式纳入仓库。

### TASK-005 正式验收通过后等待人工审核用户验收

**实施标准**
- 只有在 `TASK-004` 已正式验收通过后，才允许执行本任务。
- 向用户提交当前迁移结果、关键证据和验收结论，并等待明确的人工审核/用户验收反馈。
- 没有用户明确确认前，不得把任务状态改为 `已完成`，也不得提前归档。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 用户审核通知记录
- 用户明确反馈记录

**关联验收项**
- `VC-T002-001`

**任务产出 / 结果记录**
- 因新增 `fly-llm` 迁移范围，本阶段已回退，待新增实施与独立验收完成后再重新进入人工审核。

### TASK-006 归档并回写状态
**实施标准**
- 只有在“正式验收通过 + 用户明确审核通过”同时满足后，才允许执行归档。
- 回写本文件中的当前状态、当前交付物、完成证据和下一步，并把任务目录从 `02-进行中` 移到 `03-已完成`。
- 如使用了正式分支，归档前同步回写 `合并状态` 和 `分支处置`。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 已回写的任务计划
- 已归档的任务目录

**关联验收项**
- `VC-T002-001`

**任务产出 / 结果记录**
- 尚未进入本阶段，等待你明确审核通过后再回写和归档。
