# [零散任务计划] T0004-120.79.4.220最后一步接口与权限调研

## 1. 任务信息

- 任务ID：`T0004`
- 任务名称：`120.79.4.220最后一步接口与权限调研`
- 创建日期：`2026-07-03`
- 当前状态：`进行中`
- 分支引用：`沿用当前工作分支`
- 合并状态：`不涉及独立分支合并`
- 分支处置：`不涉及独立分支删除`

## 2. 任务目标

1. 确认 `https://120.79.4.220/` 页面最后一步实际调用的接口、请求方法、关键请求头、请求体和响应特征。
2. 确认该接口是否存在登录态、Token、角色、来源、Referer、CORS 或服务端鉴权拦截。
3. 评估“自测结果”是否可以不接入当前 deepseek 通道，改为接入该接口，并说明可行性、风险和前置条件。

## 3. 任务范围

- 包含：浏览器网络链路取证、接口直连验证、权限/鉴权行为判断、替换 deepseek 的集成可行性结论。
- 不包含：修改项目代码、接入生产链路、绕过或破解鉴权、写入生产数据、对外部服务做压力测试。

## 4. 统一原则

- 只做最小必要请求取证，不做攻击性扫描。
- 涉及凭证、账号、Token、密钥、白名单或权限缺口时立即停止并请求人工提供正式入口。
- 调研结论必须基于网络请求、HTTP 状态码、响应体、页面行为或可复现命令证据，不凭页面观感推断。
- 正式验收入口为 `01-验收标准.md`，过程证据沉淀到 `02-任务产出/`。

## 5. 正式验收入口

| 入口 | 作用 | 当前要求 |
| --- | --- | --- |
| `01-验收标准.md` | 记录本任务业务验收、技术验收和闭环项 | 实施前已收口 |
| `02-任务产出/` | 沉淀网络证据、直连接口结果、结论记录 | 实施后必须可复核 |

## 6. 阶段任务总表

| 任务ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-001` | 建立任务载体并补齐计划信息 | `-` | `已完成` | 任务目录、计划、验收标准已创建 |
| `TASK-002` | 收口正式验收入口 | `TASK-001` | `已完成` | `01-验收标准.md` 已定义本次调研验收项 |
| `TASK-003` | 采集页面最后一步接口证据 | `TASK-002` | `进行中` | 使用浏览器网络记录和必要直连请求确认接口 |
| `TASK-004` | 判断权限拦截与替换 deepseek 可行性 | `TASK-003` | `未开始` | 基于证据形成结论 |
| `TASK-005` | 沉淀产出并提交用户审核 | `TASK-004` | `未开始` | 输出调研结论和证据路径，等待用户审核 |

## 7. 任务详情

### TASK-003 采集页面最后一步接口证据

**实施标准**
- 打开 `https://120.79.4.220/` 并记录页面加载、用户操作链路和最后一步触发的网络请求。
- 至少记录接口 URL、HTTP 方法、状态码、关键请求头、请求体结构、响应体结构和错误分支。
- 如页面需要人工登录或验证码，停止在“权限依赖”结论，不绕过。

**对应技能或责任**
- `60-0500-engineering-issue-troubleshooting-and-stoploss`

**交付物**
- `02-任务产出/接口调研记录.md`

### TASK-004 判断权限拦截与替换 deepseek 可行性

**实施标准**
- 对比有无登录态/Token/Referer/CORS 时的响应差异。
- 判断接口是否为稳定契约、是否可服务端调用、是否有频控/来源限制、是否存在法律或安全风险。
- 给出“可直接接入 / 可接入但需前置条件 / 不建议接入”的明确结论。

**对应技能或责任**
- `60-0500-engineering-issue-troubleshooting-and-stoploss`

**交付物**
- `02-任务产出/接口调研记录.md`

### TASK-005 沉淀产出并提交用户审核

**实施标准**
- 将最终证据和结论写入任务产出。
- 向用户汇报接口、权限、替换 deepseek 的结论。
- 用户未明确审核通过前，任务不归档。
