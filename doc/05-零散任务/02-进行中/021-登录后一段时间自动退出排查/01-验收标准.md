# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0021`
- 任务名称：登录后一段时间自动退出排查
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0021-INT-001 | 业务-交互级 | 已登录用户从原始登录入口进入需要鉴权的页面，等待覆盖问题时间窗口后继续执行原操作 | 登录状态在目标时间窗口内保持有效；若确需重新认证，页面必须明确提示且不出现静默退出、循环跳转或丢失当前业务上下文 | 测试环境按原始路径复现并完成等待窗口后的页面访问/操作，结果与登录后即时访问一致，或有明确可解释的会话过期提示 | 记录复现时间点、页面路径、浏览器网络请求、页面跳转和最终结果，证据写入 `02-任务产出/` | 页面无提示跳转登录、请求批量变为未授权、用户操作中断或修复后原现象仍可复现 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0021-API-001 | BA-T0021-INT-001 | 技术-接口级 | 登录、鉴权校验、令牌刷新/续期和退出跳转所涉及的真实接口入口、关键请求头/参数及未授权分支均已核对 | 真实入口和响应契约明确；正常会话不会被错误判为未授权；刷新失败时有可解释且单次的处理分支 | 浏览器网络记录、接口实现/路由、服务日志或测试请求证据 | 未确认真实入口、请求未携带有效凭证、刷新接口失败被静默吞掉或出现循环刷新 |
| TA-T0021-DATA-001 | BA-T0021-INT-001 | 技术-数据级 | 客户端令牌/会话存储、过期时间、刷新结果、服务端会话状态和日志之间能够相互承接 | 存储值、过期判断和服务端返回在同一时间点一致；关键状态变化有日志或可复核记录 | 存储检查、响应状态、服务日志/配置和 `02-任务产出/` 证据 | 前端使用旧令牌、时间单位/时钟判断错误、刷新后未回写或正式记录缺失导致无法确认 |
| TA-T0021-CODE-001 | BA-T0021-INT-001 | 技术-代码级 | 修复（如有）仅覆盖自动退出根因相关边界，并控制登录、退出、刷新和其他鉴权请求的回归风险 | 代码审查、相关测试或静态检查通过；未修改 `uni_modules/**`；原始路径和至少一个关联鉴权场景回归通过 | 变更 diff、测试命令/结果、静态检查结果和回归记录 | 存在无关大范围改动、回归失败、无法证明修复与根因对应或触及受保护三方目录 |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0021-001 | 实施结果、正式验收和用户审核均已回写；如正式记录或权限入口缺失，明确停在待人工审核而不猜测 | `00-任务计划.md`、本文件验收状态、`02-任务产出/` 证据及用户明确反馈 |
