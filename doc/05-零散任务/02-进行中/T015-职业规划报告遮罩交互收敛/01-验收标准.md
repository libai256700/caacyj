# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T015`
- 任务名称：职业规划报告遮罩交互收敛
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T015-E2E-001` | 端到端业务验收 | 用户打开正在生成的职业规划评测报告 | 页面与自测报告复用同一遮罩样式，点击遮罩空白区域可关闭；页面不再显示橙色“返回首页”按钮 | 使用 `SelfTestLoadingOverlay`；绑定关闭事件并受本页关闭状态控制；模板无 `career-report__home` 和“返回首页”正文按钮 | 源码契约测试；运行页面检查交互和视觉 | 遮罩无法关闭、关闭后立即重现、仍显示橙色按钮或另写一套遮罩样式 |
| `BA-T015-STATE-001` | 状态业务验收 | 失败报告触发重新生成 | 新一轮报告生成时遮罩重新显示，随后仍可点击关闭 | `regenerate` 成功进入加载前重置遮罩关闭状态；轮询和报告结果逻辑不变 | 静态状态流审查；契约测试 | 遮罩保持关闭导致重新生成无反馈，或修改了报告接口和轮询契约 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T015-QUALITY-001` | `BA-T015-E2E-001`、`BA-T015-STATE-001` | 代码质量验收 | 只轻扩展职业规划报告页对共享组件的使用，不修改共享组件或复制样式 | 契约测试、类型检查、H5 构建和 `git diff --check` 通过；差异不覆盖用户无关修改 | 执行自动检查并审查差异 | 测试或构建失败、修改共享弹层造成额外影响、出现无关改动 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T015-001` | 验证通过后提交用户审核；用户明确确认前不归档 | 实施验证记录、用户反馈与任务计划状态 |
