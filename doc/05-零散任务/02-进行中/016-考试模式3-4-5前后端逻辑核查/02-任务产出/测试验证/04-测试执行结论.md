# 测试执行结论

## 1. 当前结论

- 当前场景：`T0016 考试模式3/4/5前后端逻辑核查`
- 当前测试时间：`2026-08-19`
- 当前结论：`部分通过`

## 2. 接口级结果

- 已通过：模式3、模式4、模式5按钮文案到前端 service、controller、service 入口映射一致。
- 未通过：无。
- 未执行：无。

## 3. 数据级结果

- 已通过：模式5教员考试从分类11随机抽100题。
- 未通过：模式3理论考试当前实现为分类10随机抽10题；模式4综合考试当前实现为1至9分类按 `5/13/3/11/7/22/7/6/26` 配额抽100题。
- 未执行：无。

## 4. 交互级结果

- 已通过：已用 Playwright 访问用户提供的 URL，并验证未登录情况下会被路由重定向到登录页。
- 未通过：无。
- 未执行：登录态缺失，未能停留在考试模式页点击三个按钮，因此没有捕获到真实发出的考试创建请求。

## 5. 失败记录

- 失败现象：`mvn test` 与 `mvn -o test` 都因父 POM `cn.iocoder.boot:yudao-dependencies:2026.06-yunjikeji-admin-SNAPSHOT` 未解析成功而无法启动测试；Playwright 访问 `exam-modes` 时被重定向到 `/pages/auth/login`。
- 影响范围：无法取得运行态按钮点击请求样本，也无法在当前环境直接执行定向 Maven 契约测试。
- 重测入口：补齐可用 Maven 依赖后在 `code/develop/yunjikeji-admin-server/yunjikeji-admin-server` 重新执行 `mvn "-Dtest=FrontPracticeBatchServiceContractTest,FrontPracticeServiceImplBatchStartContractTest" test`；提供已登录学员态后重新运行 `05-自动化测试脚本/exam-modes-playwright-check.js`。

## 6. 后续动作

- 当前任务只核查不修复；如按用户规则修正，需交换模式3与模式4的抽题实现或映射。
