# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0018`
- 任务名称：自测报告生成失败排查修复
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0018-INT-001 | 业务-交互级 | 已登录学员完成自测并点击生成/查看报告 | 前端沿真实自测入口提交完成动作，报告生成成功后进入报告页或显示可重试的明确错误；不得出现笼统异常后无状态反馈 | 在测试环境完整完成一轮自测，报告接口成功，页面可展示报告核心内容；失败时页面不假报成功且允许重试 | 浏览器网络请求、页面状态、服务日志和报告查询结果 | 仍出现“自测报告生成失败”且无可定位错误；接口成功但页面不承接；生成了空报告或串用其他用户报告 |
| BA-T0018-INT-002 | 业务-交互级 | 已登录学员进入自测报告失败页并点击“重新生成自测报告” | 失败页提供明确的重新生成入口；点击后以当前用户已有失败记录恢复生成上下文并重新生成，处理中避免重复提交，成功展示对应报告，失败保留可再次操作的明确反馈 | 按钮只在适用失败状态出现；单次点击只发起一次请求；成功后展示同一自测记录的新报告结果；失败后按钮恢复可用且提示准确 | 页面状态、浏览器网络请求、服务日志、报告查询结果和记录主键关联证据 | 无按钮、重复点击产生并发任务、恢复了错误记录、串用户、成功后仍停留失败页或失败后不可重试 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0018-API-001 | BA-T0018-INT-001 | 技术-接口级 | 自测完成后的真实报告生成接口、认证身份、请求参数和响应契约可由源码与运行样本确认；异常必须保留真实失败原因并映射为可重试分支 | 正常样本只触发目标报告接口一次或按契约触发；响应包含可查询的报告标识/状态；失败响应可关联流程标识和服务日志 | 接口调用点、请求/响应脱敏记录、服务日志和异常堆栈 | 接口入口不明、参数串用户/模式、吞异常只返回统一文案、成功无报告标识或重复生成 |
| TA-T0018-API-002 | BA-T0018-INT-001 | 技术-接口级 | 自我测评最后一步进入真实报告生成方法时，日志明确输出入口方法名和实际入参；现有测试工程提供一个可单独执行的 Test 方法直接调用该真实入口 | 日志使用现有日志框架且方法名、参数和值可核对；敏感字段脱敏；指定 Test 方法可被测试命令单独选中并执行，调用参数与真实方法签名一致 | 源码调用点、测试源码、单测命令与输出日志 | 只打印固定说明而非实际入参、敏感信息明文输出、Test 调用伪造入口、测试不能单独运行或未调用真实方法 |
| TA-T0018-API-003 | BA-T0018-INT-002 | 技术-接口级 | 重生成接口由当前登录身份和报告记录标识定位 `yj_assessment_result`，校验记录归属与可重试状态，从该记录恢复参数并复用真实报告生成方法 | 非本人记录、记录不存在和非可重试状态均被拒绝；合法请求复用同一生成方法；接口契约能支撑前端处理中、成功和失败三种状态 | Controller/Service 源码、鉴权与归属测试、接口请求响应和调用日志 | 由前端提交可伪造完整生成参数、未校验记录归属、另写一套报告生成逻辑、重复请求产生重复有效任务 |
| TA-T0018-API-004 | BA-T0018-INT-001、BA-T0018-INT-002 | 技术-接口级 | 首次提交和重生成遇到重复点击、网络重放或并发请求时，必须先获得唯一生成资格再创建或认领 `PENDING` 记录，失败竞争者不得留下可被查询为最新结果的孤儿记录 | 同一 `record_id` 的并发请求最多只有一个有效 `PENDING` 生成任务；竞争失败请求幂等返回或复用现状；报告查询不会被无任务承接的最新 `PENDING` 阻塞 | 并发契约测试、条件写入/去重源码和结果行数/状态断言 | 先插入多条 `PENDING` 再以内存锁丢弃任务、存在无消费者的最新 `PENDING`、页面永久轮询或重复调用 AI |
| TA-T0018-DATA-001 | BA-T0018-INT-001、BA-T0018-INT-002 | 技术-数据级 | 报告生成和重生成过程中的任务状态、报告主记录、明细/结果数据和错误日志必须按同一用户、同一自测批次或流程标识承接；重生成应恢复持久化参数并更新同一条 `yj_assessment_result` 记录，失败不得留下不可消费的半成品 | 成功时同一记录状态完成且报告数据完整可查询；失败时同一记录保留可重试状态和真实失败原因；事务/补偿边界明确，无孤儿、重复有效报告或跨用户数据 | 测试库只读查询、状态字段、报告记录、日志和回滚/失败注入证据 | 新插入无法关联的替代记录、状态成功但报告为空、半写入、跨用户串数据、无流程标识或失败后无法重试 |
| TA-T0018-CODE-001 | BA-T0018-INT-001、BA-T0018-INT-002 | 技术-代码级 | 修复限定在报告生成真实根因、重生成入口及必要调用方，保持现有认证、题目作答、报告查询和共享 AI 客户端其他业务契约；复用现有页面组件与真实生成方法；共享 AI 请求协议必须优先遵循数据库正式 `channel`，仅在缺失时才允许按 URL 兼容推断；Test 只允许构造 DTO、作答、会话和响应等方法入参，不得 mock Service、Mapper、数据库、登录身份、AI 客户端或 HTTP 服务；不得修改 `uni_modules/**`，不得以宽泛 catch/假数据掩盖问题 | 根因有源码证据；前后端最小变更通过输入构造测试、确定性契约测试、类型/构建或正式测试环境验证；真实调用 Test 使用 Spring 真实 Bean，并要求显式提供真实 tenant/user/customer/record/result 标识，缺参数时明确跳过；输入构造测试与真实落库/AI联调结论分开记录；岗位提取、知识库等现有调用方契约不回归；工作区无三方目录改动 | 代码差异、测试输入对象与参数日志、无伪依赖协议/状态机契约测试、相关调用方检查、正式环境真实调用记录（具备参数并获授权时）、`git diff --check`、`git status` 和 `uni_modules/**` 差异检查 | mock Service/Mapper/数据库/认证/AI/HTTP、使用伪依赖冒充真实联调、给真实关联标识设置假默认值、把未执行或仅输入构造写成真实数据库/AI已验证、复制第二套生成逻辑或页面、吞异常、仅按 URL 后缀忽略正式 channel、引入假报告、改动无关模块、测试失败或触碰三方目录 |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0018-001 | 实施与独立验收由不同代理完成；正式验收通过后提交并推送仓库，随后等待用户明确审核 | 计划/验收文件、实施产出、独立验收记录、提交与推送记录、用户审核反馈 |
