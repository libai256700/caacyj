# T0018 自测报告生成失败实施记录

## 结果

- 已按当前测试库只读证据收口为 `yj_assessment_result` 缺 `report_status/failure_reason/idx_yj_assessment_result_report_status`，不再补 `record_id`。
- 已将 `20260819120000-ddl-add_yj_assessment_result_status.sql` 改造成幂等执行脚本，补齐执行前快照、列/索引幂等补齐和历史数据初始化。
- 已同步完善 `20260819120100-ddl-rollback_yj_assessment_result_status.sql` 与 `20260819120200-check_yj_assessment_result_status.sql`，并把三份资产登记到 `050-执行脚本/00-脚本索引.md`。
- 已在当前测试库实际执行前向状态迁移并完成只读校验，`report_status/failure_reason/idx_yj_assessment_result_report_status` 已补齐。
- 已执行 pending insert 事务探针并回滚，插入行可写入 `PENDING` 且无残留。
- 已新增 `FrontPracticeAssessmentPendingInsertContractTest`，覆盖完整列路径和缺 `customer_account_id` 兼容路径。
- 已在 `FrontPracticeServiceImpl.submitAnswer` 的异步评测任务中补上 `TenantUtils.executeIgnore(...)`，让 `assessmentReportExecutor` 内的评测、落库和失败回写在同一无租户上下文中执行。
- 已复跑 `mvn -pl yunjikeji-admin-server -Dtest=FrontPracticeAssessmentPendingInsertContractTest test`，测试通过。

## 变更文件

- `doc/02-版本迭代/03-进行中版本/20260601000000-vphase1-initial-delivery/050-执行脚本/20260819120000-ddl-add_yj_assessment_result_status.sql`
- `doc/02-版本迭代/03-进行中版本/20260601000000-vphase1-initial-delivery/050-执行脚本/20260819120100-ddl-rollback_yj_assessment_result_status.sql`
- `doc/02-版本迭代/03-进行中版本/20260601000000-vphase1-initial-delivery/050-执行脚本/20260819120200-check_yj_assessment_result_status.sql`
- `doc/02-版本迭代/03-进行中版本/20260601000000-vphase1-initial-delivery/050-执行脚本/00-脚本索引.md`
- `code/develop/yunjikeji-admin-server/yunjikeji-admin-server/src/test/java/com/huiyitech/app/practice/service/FrontPracticeAssessmentPendingInsertContractTest.java`
- `code/develop/yunjikeji-admin-server/yunjikeji-admin-server/src/main/java/com/huiyitech/app/practice/service/FrontPracticeServiceImpl.java`

## 当前程序数据库执行证据

- 数据源：`application-local.yaml` 的 `local` profile，`114.111.30.111:13306/yunjikeji`。
- 执行前只读：`record_id`、`customer_account_id` 已存在，`report_status`、`failure_reason` 和状态索引缺失；活动报告记录数为 `0`。
- 已建立备份表：`bak_20260819_yj_assessment_result_status_meta`、`bak_20260819_yj_assessment_result_status_rows`，本次快照行数为 `0`。
- 已执行：补充 `report_status varchar(32) NOT NULL DEFAULT 'SUCCESS'`、`failure_reason varchar(255)` 和 `idx_yj_assessment_result_report_status`；历史活动报告初始化影响 `0` 行。
- 后置校验：状态列与索引均存在，活动报告记录数仍为 `0`。
- 事务回滚探针：按 Java `createPendingAssessmentResult` 的完整字段集合插入一条 `PENDING` 记录成功，随后 `ROLLBACK`；未留下测试数据。
