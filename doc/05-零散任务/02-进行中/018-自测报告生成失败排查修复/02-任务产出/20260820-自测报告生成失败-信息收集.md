# T0018 自测报告生成失败信息收集

## 状态

`DONE_WITH_CONCERNS`

本轮完成源码、附件、迁移脚本和正式测试库连接入口核对；未修改业务源码、未执行数据库变更。测试库只读连接当前不可达，故未取得运行态 `SHOW COLUMNS` 和同一流程数据行证据。

## 真实样本

- 样本附件：`C:/Users/renquan/.codex/attachments/4fa23837-8d13-4e86-9b52-735a6337913c/pasted-text.txt`
- 异常入口：`FrontPracticeController.submitAnswer`
- 请求路径：`POST /app-api/yj/practices/{practiceId}/sessions/{sessionId}/answers`
- 附件堆栈：`FrontPracticeServiceImpl.submitAnswer:563` -> `createPendingAssessmentResult:1992` -> `assessmentReportFailure:1702`
- 附件只包含统一 `ServiceException` 堆栈，没有请求时间、practiceId/sessionId、用户身份、recordId、SQL 根异常或 `Caused by`，不能从附件单独证明具体数据库错误。

## 最短调用链

1. 前端 `code/develop/yunjikeji/src/services/assessment.ts:334-363` 逐题调用 `submitAppPracticeAnswer`；最后一题要求响应 `completed=true` 且必须有 `recordId`。
2. 前端请求后端 `POST /app-api/yj/practices/{practiceId}/sessions/{sessionId}/answers`，对应 `FrontPracticeController.java:152-158`。
3. `FrontPracticeServiceImpl.java:539-570` 完成批次答题后识别自测会话，查询已有结果；没有可复用的 SUCCESS/PENDING 结果时调用 `createPendingAssessmentResult`。
4. `createPendingAssessmentResult` 首次 INSERT 写入 `yj_assessment_result` 的 `user_id/customer_account_id/record_id/report_status/failure_reason` 等字段；失败后兼容 INSERT 只去掉 `customer_account_id`，仍写入 `record_id/report_status/failure_reason`；两次均失败才在 `FrontPracticeServiceImpl.java:1989-1992` 抛出用户看到的统一异常。
5. 只有 pending 记录成功创建后，才会进入 `enqueueAssessmentEvaluation` 异步 AI 生成；当前样本在入队前失败，因此不是 AI 上游超时、报告 HTML 校验或报告页查询主链路。

## 根因候选与证据

### 已由当前程序数据库确认：报告状态迁移未执行

- 2026-08-20 按用户明确指令，改以当前程序 `application-local.yaml` 的实际数据源为准；本机 Navicat 对同一数据库主机端口存在活动连接。
- 运行库只读 `SHOW COLUMNS FROM yj_assessment_result` 结果：`record_id` 和 `customer_account_id` 已存在，`report_status`、`failure_reason` 不存在；`SHOW INDEX` 也没有报告状态索引。
- 当前 `deleted=b'0'` 的报告记录数为 0。由此可以排除“运行库缺 `record_id`”这一候选，直接根因确认为 `20260819120000-ddl-add_yj_assessment_result_status.sql` 尚未在当前库执行。

### 仓库迁移资产仍存在的缺口

- 基础建表脚本 `doc/02-版本迭代/03-进行中版本/20260601000000-vphase1-initial-delivery/050-执行脚本/20260608-yj-admin-business-tables.sql:350-366` 的 `yj_assessment_result` 只有 `id/user_id/assessment_time/result_summary/recommend_direction/report_content/tenant_id/...`，没有 `record_id`、`report_status`、`failure_reason`。
- `customer_account_id` 仅由 `20260609-yj-customer-account.sql:40-41` 追加；`report_status/failure_reason` 仅由 `20260819120000-ddl-add_yj_assessment_result_status.sql:1-15` 追加。
- 全仓 SQL 搜索没有发现任何给 `yj_assessment_result` 追加 `record_id` 的迁移脚本。
- 当前代码在两条 pending INSERT 中都强制要求 `record_id` 和 `report_status/failure_reason`，因此只要运行库缺任一列，两次 INSERT 都会失败并落到附件中的 `assessmentReportFailure`。第二条兼容 SQL 只能兼容缺少 `customer_account_id`，无法兼容缺少这些共同列。
- 该判断与提交 `1930421a` 同步引入的状态迁移脚本和异步报告代码一致；提交说明明确数据库执行仍需独立验收，仓库没有运行态迁移成功证据。

### 已排除的运行态候选

当前库已经有 `record_id`，故附件中的两次 INSERT 不是因该列缺失而失败；共同缺失的 `report_status/failure_reason` 才是本次运行态直接原因。

### 当前不支持的方向

- 不是前端报告页单独故障：异常发生在最后一次答题的提交接口，且在异步生成和报告查询之前。
- 不是 AI 网关/超时的首要证据：附件栈没有进入 `applyAssessmentEvaluation`、上游 HTTP 调用或 `persistSelfReport`。
- 不是鉴权失败：请求已进入 `FrontPracticeServiceImpl.submitAnswer`，并已取得登录用户；若鉴权失败不会到达 `createPendingAssessmentResult`。

## 连接核对修正

- 初次按测试环境正式记录检查了 `127.0.0.1:13306`，该入口不可达。
- 用户随后明确要求使用当前程序正在连接的数据库；检查 `application.yaml` 的 `local` profile、`application-local.yaml` 数据源与本机活动 TCP 连接后，确认当前数据库入口一致，并已完成只读结构核对。

## 建议最小修复/验证

1. 当前库执行前先备份 `yj_assessment_result` 结构与数据；当前活动报告记录为 0，但仍保留可核对备份入口。
2. 执行并校验状态迁移，只新增 `report_status`、`failure_reason` 和状态索引；当前库已有 `record_id`，不得重复新增。
3. 迁移后用同一登录用户完成一次自测，确认最后一条 answers 请求只触发一次、响应含 `recordId` 和 `assessmentReportStatus=PENDING`，随后轮询/详情得到 SUCCESS 和非空报告；失败时核对服务日志中的 `recordId` 和底层 SQL 异常。
4. 代码侧建议补一个针对 `createPendingAssessmentResult` 的集成/契约测试，覆盖完整 schema 和缺 `customer_account_id` 的兼容 schema；当前测试仅覆盖报告读取/重置，未覆盖 pending INSERT 的 schema 漂移。

## 结论

`DONE`：源码调用链与当前程序实际数据库已经形成闭环证据。当前异常由 `yj_assessment_result.report_status/failure_reason` 迁移未执行直接导致，失败点早于异步 AI 入队；后续按备份、状态迁移、校验和原入口复验推进。
