# 测试执行结论

## 1. 当前结论

- 当前场景：`T0018 自测报告生成失败排查修复`
- 当前测试时间：`2026-08-27 13:29`
- 当前结论：`部分通过`

## 2. 接口级结果

- 已通过：`1`
- 未通过：`0`
- 未执行：`1`

## 3. 数据级结果

- 已通过：`3`
- 未通过：`0`
- 未执行：`1`

## 4. 交互级结果

- 已通过：`0`
- 未通过：`0`
- 未执行：`1`

## 5. 失败记录

- 代码级历史回归：`FrontPracticeAssessmentResetContractTest`、首次 PENDING 插入、报告 HTML、前端 `type-check` 与 `build:h5` 的既有结论保持记录；原 `FrontPracticeAssessmentRegenerateTest` 使用替身和内存库所得结果已撤回，不再作为真实入口或真实落库证据。
- 当前真实依赖 Test：`applyAssessmentEvaluation_shouldUseRealSpringDependenciesAndPersistConfiguredRecord` 构造 response/session 和 10 条 answers 方法入参；Service、AI 和同一行落库使用真实 Spring 依赖。该 Test 不从数据库恢复 answers，不能作为“持久化作答恢复”已验证的证据。
- 不传显式 enable/profile/record/result/user/tenant/practice 参数单独执行为 `Skipped: 1`；跳过发生在 Spring 上下文启动前，因此未连接数据库、未调用 AI、未写库，真实集成结论仍为未执行。
- 推送后返工结果：`FrontPracticeAssessmentPendingInsertContractTest` 4/4 通过，证明数据库归属行锁、锁内资格判定和事务提交后入队的代码契约；`DeepSeekOpenAiClientTest` 5/5 通过，证明正式 `channel` 优先和空 channel URL 回退的请求构造契约；模块编译通过。
- 无本地替身检查：`FrontPracticeAssessmentRegenerateTest.java` 不包含 Mockito、MockWebServer、本地 HTTP 服务、内存数据库或固定本机地址；关键词扫描为 0。
- 影响范围：本次未发现 P1/P2；`TC-REG-001` 的邻接回归已收口，未再看到 reset 合同失真。
- 保留项：正式浏览器交互验收未执行，原因是缺少可用登录态与测试环境交互通道，不能把未执行项写成通过。
- 保留项：真实外部 AI HTTP 与岗位提取/知识库完整业务联调未执行；进程在 PENDING 事务提交后直接崩溃时仍缺少陈旧任务超时回收机制，均不得写成已验证。
- 重测入口：`mvn -o -pl yunjikeji-admin-server "-Dtest=FrontPracticeAssessmentRegenerateTest#applyAssessmentEvaluation_shouldUseRealSpringDependenciesAndPersistConfiguredRecord" test`；正式测试环境参数见目标 Test 注释。

## 6. 后续动作

- 若需要把结论提升到 `通过`，还需要用正式测试环境真实登录态和本人 `FAILED recordId` 执行真实集成 Test、核对数据库最终字段，并补做正式浏览器交互验收。
- 共享 AI 请求需要在正式测试环境使用真实配置联调；不得使用本地替身服务代替。
