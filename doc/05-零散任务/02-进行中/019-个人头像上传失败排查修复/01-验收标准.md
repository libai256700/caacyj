# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0019`
- 任务名称：个人头像上传失败排查修复
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0019-INT-001 | 业务-交互级 | 登录用户从个人中心/我的页面点击个人头像，选择一张符合限制的图片并提交上传，最终完成头像更新。 | 上传过程中有明确进行/失败反馈；成功后当前页面立即展示新头像，重新进入页面或刷新后仍展示同一头像；失败时不误报成功且用户可重试。 | 在测试环境按真实页面路径完成一次成功上传和一次可控失败重试，页面结果与资料读取结果一致。 | 记录原始页面操作、网络请求结果、页面截图或录屏及刷新后的展示；不得以静态 mock 或仅修改本地变量作为证据。 | 上传请求未发出、请求失败未提示、成功后头像不更新/刷新丢失、或失败被展示为成功。 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0019-API-001 | BA-T0019-INT-001 | 技术-接口级 | 核对页面实际调用的头像上传入口、请求方法、文件字段/鉴权信息、成功返回和错误分支，并确认前端按真实契约处理结果。 | 成功请求返回可消费的头像地址或等价资料结果；失败响应进入明确失败分支且不更新成功态。 | 浏览器网络记录、接口调用代码和测试环境响应；若真实入口或权限信息缺失，记录为阻塞，不猜测替代接口。 | 请求字段、鉴权、地址或响应解析与真实契约不一致，或失败响应被吞掉。 |
| TA-T0019-DATA-001 | BA-T0019-INT-001 | 技术-数据级 | 核对上传成功后的头像地址/用户资料状态是否回写并被页面重新读取，确认缓存、状态管理或本地持久化没有覆盖新值。 | 上传成功、当前状态、重新进入页面读取到的头像标识一致；失败不写入半成品地址。 | 页面状态、重新加载结果、接口响应及必要日志/存储证据。 | 仅当前组件临时显示，刷新丢失；或失败场景写入无效头像值。 |
| TA-T0019-CODE-001 | BA-T0019-INT-001 | 技术-代码级 | 修复应限定在正式头像上传页面/组件及其请求适配逻辑，保持现有登录、资料更新和其他图片上传流程不回归；不修改 `uni_modules/**`。 | 变更可读、错误分支完整、无调试代码；相关静态检查/构建或最小自动化验证通过。 | 代码 diff、静态检查/构建命令、最小交互验证记录及变更文件清单。 | 引入未处理异常、破坏其他资料流程、残留调试代码，或触碰受保护三方包目录。 |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0019-001 | 实施完成后由不同于实施代理的独立验收代理逐项复核；正式验收通过后提交用户审核，未获用户明确审核前不归档。 | `02-任务产出/实施/`、`02-任务产出/测试验证/`、任务计划状态和用户审核反馈。 |
