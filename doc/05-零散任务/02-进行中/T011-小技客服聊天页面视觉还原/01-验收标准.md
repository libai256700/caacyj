# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T011`
- 任务名称：小技客服聊天页面视觉还原
- 计划入口：`00-任务计划.md`
- 目标视觉：`02-任务产出/00-目标参考图.png`（`853x1844`）
- 当前错误样式：`02-任务产出/01-当前错误样式.png`（`561x1217`）

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T011-E2E-001` | 端到端业务验收 | 用户进入“小技客服”聊天页 | 页面应严格还原图一的暖白背景、居中深蓝标题、左侧返回箭头、白色左向气泡和底部白色胶囊输入栏，并使用深蓝加号、浅灰占位和橙色发送图标 | `390x844` 下主要区域位置、尺寸、色彩、圆角、阴影、字号与图一接近；`375x812`、`430x932` 无水平溢出、遮挡、错位或文字截断；不再保留图二的蓝色背景和蓝色矩形发送按钮 | 真实页面三视口截图与图一归一化并排、叠加和像素统计；补充 DOM 边界测量 | 仍沿用图二蓝色主题；目标导航、气泡或输入栏缺失；关键比例明显偏离；存在溢出、重叠、截断或异常空白 |
| `BA-T011-DATA-001` | 数据与交互业务验收 | 会话加载、错误、空会话、消息、输入与附件操作 | 样式改动不得改变现有业务状态、文案、消息顺序或交互；空会话显示原“您好，欢迎咨询小技！”，错误态保留真实错误文案与点击重试，但使用目标气泡视觉 | `loadError/loading/empty/messages` 条件、消息类型与发送者分支、输入绑定、发送态、附件面板、图片/视频选择、图片预览和返回行为均保留；错误信息不再作为红色居中警告卡展示 | 审查模板与脚本差异；浏览器检查空态/错误态、输入读回、加号面板、发送禁用态、文件选择入口和返回 | 任一业务文案、状态优先级、绑定、条件、事件、上传、预览、发送或导航语义发生变化 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T011-API-001` | `BA-T011-DATA-001` | 技术接口验收 | 保留客服聊天页现有状态、生命周期、HTTP/WebSocket、上传、发送、预览与路由契约 | `<script setup>` 与改动前逐字一致；不得修改或新增请求、状态字段、条件、发送、上传、实时连接、预览或导航代码 | 实施前后脚本原文哈希比对；审查 `git diff` | `<script setup>` 或任何业务接口与交互契约发生变化 |
| `TA-T011-QUALITY-001` | `BA-T011-E2E-001` | 代码质量验收 | 在现有正式页面单一实现上完成，优先复用现有组件能力，响应式稳定且无调试残留 | 类型检查和 H5 构建通过；无第二套同义页面；资源成功加载；三视口无布局异常；控制台无错误；变更限定在前端展示边界 | 审查范围；运行 `npm run type-check`、`npm run build:h5`、`git diff --check`；浏览器截图、资源和控制台检查 | 构建失败、资源错误、重复实现、调试残留、跨视口布局异常，或修改业务脚本/服务/后端 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T011-001` | 独立验收通过后提交用户人工审核；只有用户明确确认视觉结果后才允许归档 | 独立验收记录、最终截图、用户明确反馈与任务计划状态 |
