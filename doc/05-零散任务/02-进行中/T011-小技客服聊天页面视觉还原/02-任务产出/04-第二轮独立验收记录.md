# T011 小技客服聊天页面第二轮独立验收记录

## 1. 验收信息

- 验收阶段：`TASK-004 独立验收重跑`
- 验收日期：`2026-08-14`
- 验收角色：全新独立只读验收子代理
- 唯一正式标准：`../01-验收标准.md`
- 页面入口：`http://127.0.0.1:5173/yunjikeji/#/pages/service/customer-service-chat?conversationId=default`
- 本轮证据目录：`code/develop/yunjikeji/output/playwright/T011-support-chat/acceptance-v2/`
- 最终结果：`DONE`
- 最终脚本结果：`123 / 123` 条断言通过，`fatalError = null`，进程退出码 `0`
- 实施边界：本轮未修改 `customer-service-chat.vue`、SCSS、资源、服务或业务实现；仅新增独立验收脚本、运行证据和本记录。

四项正式验收均已取得全新独立证据并通过。`TASK-004` 的验收门禁已经闭合，可以由主任务推进到 `TASK-005` 用户人工审核；本验收代理未修改任务计划状态。

## 2. 验收项结论

| 验收项 | 结论 | 本轮独立证据 |
| --- | --- | --- |
| `BA-T011-E2E-001` | **通过** | 全新生成 `390x844`、`375x812`、`430x932` 空态截图和 DOM；三视口水平/垂直溢出、文字裁切、关键区域重叠、资源破损均为 `0`；目标图独立归一化并生成并排、叠加、差分和像素统计，人工复核通过 |
| `BA-T011-DATA-001` | **通过** | 全新运行 loading、empty、error、messages、输入、发送禁用/启用、附件开合、图片/视频 file chooser、输入聚焦关闭附件和返回；错误 GET 计数 `1 -> 2`；未点击发送、未选择文件、未上传，所有场景写请求为 `0` |
| `TA-T011-API-001` | **通过** | 当前含标签原文 `<script setup>` SHA-256 为指定值 `9502961067ab4b89ca6dae2fa3a2dee6ea06cb88fa9421f2bf1f659174abb78a`；模板与脚本分别和 `HEAD` 在统一换行后逐字相同；当前 diff 仅位于 SCSS 样式块 |
| `TA-T011-QUALITY-001` | **通过** | `npm run type-check`、`npm run build:h5`、`git diff --check` 均退出码 `0`；浏览器所有场景 console error、pageerror、requestfailed、HTTP `>= 400` 均为 `0`；唯一正式路由和唯一正式页面通过，正式源码无参考图引用 |

## 3. 浏览器与编码入口证明

- 验收脚本：`acceptance-v2/accept.mjs`。
- 脚本逐字节检查结果：非 ASCII 字节 `0`；`node --check` 通过。
- 所有中文期望值和 mock 响应均以 `\uXXXX` 写在脚本源码中；没有通过 PowerShell here-string 或管道传递含中文 Node 程序。
- 页面定位只使用 CSS 选择器，包括 `.message-state`、`.message-input input`、`.media-toggle`、`.media-action:nth-child(...)`、`.chat-back`。
- 最终浏览器启动入口：`chromium.launch({ channel: 'chrome', headless: true })`；实际启动方式为 `channel = chrome`，没有使用默认 Chromium headless shell，也未触发 executablePath 回退。
- 系统 Chrome 路径只读确认：`C:\Users\18163\AppData\Local\Google\Chrome\Application\chrome.exe`；本轮浏览器版本：`151.0.7922.138`。
- 每个场景均创建独立 browser context；`addInitScript` 注入 uni-app 包装格式的 student 本地会话；每个 context 仅有 `1` 个页面。
- 路由层对 `**/*` 的所有非 GET 返回 `405`，对 session GET 按场景返回空态、错误态或消息态；最终没有任何非 GET 请求触发守卫。

## 4. 视觉证据

### 4.1 三视口稳定性

| 视口 | 水平溢出 | 垂直溢出 | 文字裁切 | 关键区域重叠 | 破损图片 | 结论 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `390x844` | 0 | 0 | 0 | 0 | 0 | 通过 |
| `375x812` | 0 | 0 | 0 | 0 | 0 | 通过 |
| `430x932` | 0 | 0 | 0 | 0 | 0 | 通过 |

人工查看三张全新截图，均保留暖白背景、居中深蓝标题、左侧返回、白色左向气泡、底部白色胶囊、深蓝加号、浅灰占位和橙色纸飞机；未出现图二的蓝色背景、红色居中错误卡或蓝色矩形发送按钮。

### 4.2 390x844 关键坐标

| 对象 | 目标坐标/尺寸 | 本轮实测 | 最大绝对差 |
| --- | --- | --- | ---: |
| header | `h=66.8` | `h=67.078` | `0.278px` |
| 空态气泡 | `38.4 / 224.3 / 220.8 / 68.2` | `38.469 / 224.625 / 221.406 / 67.828` | `0.606px` |
| composer | `18.3 / 756.6 / 353.4 / 65` | `18.188 / 757.203 / 353.625 / 64.438` | `0.603px` |
| 加号 | `37.5 / 777.2 / 24.7 / 24.3` | `36.891 / 776.938 / 24.953 / 24.953` | `0.653px` |
| 纸飞机伪元素 | `329.2 / 778.1 / 22.4 / 22.4` | `329.2 / 778.243 / 23.906 / 22.359` | `1.506px` |

关键区域位置均接近目标。纸飞机自身宽度较目标测量值多 `1.506px`，但横纵位置和高度分别只差 `0 / 0.143 / 0.041px`；并排图和叠加图人工复核未形成可见的比例偏离，按正式标准“接近”判定通过。

### 4.3 独立像素比较

- 目标原图尺寸：`853x1844`；使用双线性中心映射独立归一化为 `390x844`。
- 全图 RGB MAE：`2.460 / 255`，即 `0.965%`；MAE 相似度：`99.035%`。
- pixelmatch（threshold `0.1`、includeAA `true`）：匹配率 `98.860%`，差异像素 `3751 / 329160`。
- 分区 MAE：header `4.518%`、气泡区域 `3.282%`、composer 区域 `1.959%`、主体空白背景 `0.165%`。
- 已人工查看 `comparison-side-by-side-390x844.png` 和 `comparison-overlay-390x844.png`，结构、比例、主色和关键位置与图一一致。

## 5. 状态与功能证据

- loading：显示原文“客服消息加载中...”，完成后进入空态；状态优先级正常。
- empty：三个视口均显示原文“您好，欢迎咨询小技！”。
- error：mock 原始响应 `student is not bound to a tenant`，页面保留既有业务归一化文案“请先绑定企业，绑定成功后再联系客服”；错误态为白色深蓝文字气泡，不是红色警告卡。
- retry：点击 `.message-state--error` 前后 session GET 计数严格为 `1 -> 2`。
- messages：两条消息按 service -> student 顺序渲染，行类分别为 `message-row--service`、`message-row--student`，身份文案为“教员”“我”；消息态不再显示 `.message-state`。
- 输入：空值时发送类为禁用，输入 `T011 acceptance-v2 input readback` 后读回一致且禁用类移除，清空并等待 Vue 更新后禁用类恢复。
- 附件：加号第一次打开、第二次关闭、第三次重新打开；附件标题顺序为“图片”“视频”，面板在视口内且不与气泡重叠。
- file chooser：图片和视频入口分别触发真实 file chooser；未调用 `setFiles`，提供文件数为 `0`，没有发送或上传。
- focus：点击原生 `.message-input input` 后附件面板关闭。
- 返回：点击 `.chat-back` 后到达 `http://127.0.0.1:5173/yunjikeji/#/pages/service/customer-service`。
- 七个独立 context 汇总：console error `0`、pageerror `0`、requestfailed `0`、HTTP `>= 400` `0`、写请求 `0`。

## 6. 脚本与资源边界

- 当前 `<script setup lang="ts">...</script>` 含标签原文为 `10341` bytes；SHA-256 与指定值完全一致。
- 当前模板与 `HEAD` 在统一换行后逐字相同；当前脚本与 `HEAD` 在统一换行后逐字相同。
- 当前文件首个样式标签位于第 `474` 行，所有 Git diff hunk 的新文件起始行均不早于 `478`；模板和脚本无差异，样式与 `HEAD` 不同，因此 diff 仅 SCSS。
- `src/pages.json` 中目标正式路由计数为 `1`；目标 `.vue` 实现仅 `src/pages/service/customer-service-chat.vue` 一份。
- 独立扫描 `src/` 下 `52` 个 `.vue/.ts/.js/.json/.scss/.css` 文件，未发现 `00-目标参考图.png`、`01-当前错误样式.png`、目标参考图、当前错误样式或 `T011-support-chat` 引用。
- 模板静态契约再次确认：`loadError -> loading -> empty -> messages` 条件顺序、text/image/video 分支、service/student 分支、输入绑定、focus/confirm、附件、发送、预览和返回事件均保留。

## 7. 质量门禁

| 门禁 | 结果 | 说明 |
| --- | --- | --- |
| `npm run type-check` | 通过，退出码 `0` | `vue-tsc --noEmit` 无错误输出 |
| `npm run build:h5` | 通过，退出码 `0` | uni-app 编译器 `4.84 (vue3)`，输出 `DONE Build complete.`；版本更新提示不属于失败 |
| `git diff --check` | 通过，退出码 `0` | 全工作区执行；只有现有文件的 LF/CRLF 转换提示，无 whitespace error |
| 浏览器质量 | 通过 | 最终 `123 / 123` 断言通过；console/request/pageerror/resource 均无失败 |
| 单一实现与资源边界 | 通过 | 唯一路由、唯一页面、无参考图正式引用 |

## 8. 记录与截图

关键证据文件：

- `accept.mjs`：纯 ASCII 独立验收脚本。
- `acceptance-results.json`：最终完整结果，`overallPass = true`。
- `390x844-empty.png`、`375x812-empty.png`、`430x932-empty.png`：空态三视口。
- `390x844-loading.png`、`390x844-error.png`、`390x844-messages.png`、`390x844-attachment.png`：状态与功能截图。
- `dom-*.json`：各视口及状态 DOM 边界、样式、裁切和资源数据。
- `reference-normalized-390x844.png`：图一独立归一化结果。
- `comparison-side-by-side-390x844.png`、`comparison-overlay-390x844.png`、`comparison-diff-390x844.png`：并排、叠加和像素差分证据。

## 9. 未通过项与剩余风险

- 未通过项：无。
- 环境阻塞：无；第二轮已通过落盘 ASCII 脚本和系统 Chrome channel 闭合首轮环境问题。
- 剩余门禁：仅剩 `VC-T011-001` 要求的用户人工视觉审核；在用户明确确认前不得归档。
- 工作区存在大量与 T011 无关的既有脏改动，本轮保持原样，未将其纳入 T011 产品结论，也未修改或清理。
