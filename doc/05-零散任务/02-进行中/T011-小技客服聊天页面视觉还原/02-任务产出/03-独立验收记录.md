# T011 小技客服聊天页面独立验收记录

## 1. 验收信息

- 验收阶段：`TASK-004 独立验收`
- 验收日期：`2026-08-14`
- 验收角色：全新独立只读验收子代理
- 唯一正式标准：`../01-验收标准.md`
- 页面：`code/develop/yunjikeji/src/pages/service/customer-service-chat.vue`
- 页面入口：`http://127.0.0.1:5173/yunjikeji/#/pages/service/customer-service-chat?conversationId=default`
- 总结论：`DONE_WITH_CONCERNS`
- 实施边界：本次没有修改页面、样式、资源、服务或业务实现。

本轮不能给出正式全通过结论。独立源码与 Git 基线证据已闭合，但独立浏览器证据和质量命令未完成；下表按正式标准将证据未闭合项判为未通过，不复用 `02-实施与验证记录.md` 的自检结论。

## 2. 正式验收项结论

| 验收项 | 结论 | 独立证据 | 判定说明 |
| --- | --- | --- | --- |
| `BA-T011-E2E-001` | **未通过** | 已独立查看 `00-目标参考图.png`，确认图一的暖白背景、居中深蓝标题、左侧返回、白色左向气泡、底部胶囊、深蓝加号、浅灰占位和橙色纸飞机目标；未成功生成本轮三视口当前页面截图、DOM 坐标、并排图、叠加图或 MAE 数据 | 不能以实施截图代替全新独立证据；`390x844`、`375x812`、`430x932` 的溢出、遮挡、裁切和目标相似度均未闭合 |
| `BA-T011-DATA-001` | **未通过** | 当前 `<template>` 与 `HEAD` 基线相同；源码仍保留 `loadError -> loading -> empty -> messages` 条件顺序、消息类型/发送者分支、`v-model="draft"`、发送、附件、预览和返回事件 | 源码静态边界通过，但空态、错误重试、输入读回、附件 file chooser、聚焦关闭和返回未形成成功的本轮浏览器结果，正式标准要求的运行时证据未闭合 |
| `TA-T011-API-001` | **通过** | `git diff` 的首个变更位于第 475 行 `<style lang="scss">`；模板与基线相同；当前含标签、CRLF 原文 `<script setup>` SHA-256 为 `9502961067ab4b89ca6dae2fa3a2dee6ea06cb88fa9421f2bf1f659174abb78a`，与指定值一致；当前脚本与 `HEAD` 脚本在统一换行后逐字相同 | 没有发现请求、状态、生命周期、HTTP/WebSocket、上传、发送、预览或导航代码变更 |
| `TA-T011-QUALITY-001` | **未通过** | 独立搜索确认 `src/pages.json` 仅登记一个 `pages/service/customer-service-chat` 正式路由，正式源码中没有引用图一、图二或 `T011-support-chat`；本轮未完成 `type-check`、`build:h5`、`git diff --check`，也未形成控制台、请求失败和资源状态的成功结果 | 质量门禁只完成了单一实现和资源边界静态检查，构建、类型、diff whitespace、三视口和浏览器质量证据未闭合 |

## 3. 视觉证据

- 独立人工查看目标原图：`02-任务产出/00-目标参考图.png`，原始尺寸 `853x1844`。
- 独立人工查看图二：`02-任务产出/01-当前错误样式.png`，确认其蓝色背景、左对齐标题、红色居中错误卡和蓝色矩形发送按钮属于明确反例。
- 本轮指定证据目录已建立：`code/develop/yunjikeji/output/playwright/T011-support-chat/acceptance/`。
- 本轮没有成功生成可用于正式判定的空态、错误态或对比截图；因此不报告 DOM 坐标、MAE、相似度或“肉眼已通过”。

## 4. 状态与功能证据

静态源码审查确认以下结构仍存在且模板与 `HEAD` 基线相同：

- `v-if="loadError"`，点击事件为 `@tap="loadConversation"`。
- `v-else-if="loading"`，原文为“客服消息加载中...”。
- `v-else-if="!messages.length"`，原文为“您好，欢迎咨询小技！”。
- `v-for="message in messages"`，保留 service/student 行分支以及 text/image/video 分支。
- 输入仍为 `v-model="draft"`，保留 `@focus="closeMediaPanel"`、`@confirm="sendText"`。
- 发送仍为 `@tap="sendText"`，附件仍为 `toggleMediaPanel`、`sendImage`、`sendVideo`。
- 图片预览仍调用 `previewImage`，返回仍调用 `goBack`。

运行时复验未完成，不能确认错误态 GET 计数 `1 -> 2`、无写请求、file chooser、发送禁用/启用、附件开合、聚焦关闭和返回落点。

## 5. 脚本与资源边界

- 当前 `<script setup lang="ts">...</script>` 含标签 CRLF 原文：`10341` bytes。
- 当前脚本 SHA-256：`9502961067ab4b89ca6dae2fa3a2dee6ea06cb88fa9421f2bf1f659174abb78a`。
- `HEAD` 脚本归一为 CRLF 后 SHA-256 相同；当前与 `HEAD` 统一为 LF 后逐字相同。
- 当前 `<template>` 与 `HEAD` 统一换行后逐字相同。
- `git diff -- customer-service-chat.vue` 只显示 `<style lang="scss">` 内变更，没有模板或脚本 diff。
- `src/` 中未发现 `00-目标参考图.png`、`01-当前错误样式.png`、`目标参考图`、`当前错误样式` 或 `T011-support-chat` 引用。
- `focus-atmosphere.png` 仍被其他既有页面使用，但 `customer-service-chat.vue` 当前不再引用它；图一、图二均未被正式页面当背景使用。

## 6. 质量门禁

| 门禁 | 本轮结果 |
| --- | --- |
| `npm run type-check` | 未完成，不能沿用实施记录结论 |
| `npm run build:h5` | 未完成，不能沿用实施记录结论 |
| `git diff --check` | 未完成，不能沿用实施记录结论 |
| 三视口 console/pageerror/requestfailed/HTTP >= 400 | 未完成 |
| 唯一正式页面 | 静态检查通过：`src/pages.json` 仅一个目标路由，目标页面仅一个正式 `.vue` 实现 |
| 图一/图二正式引用 | 静态检查通过：未发现引用 |

## 7. 未通过原因与重跑入口

1. 首次 Playwright Node 运行使用默认 Chromium 时，Playwright `1.61.1` 所需 `chromium_headless_shell-1228` 不存在，浏览器未启动。
2. 改用已安装 Chrome channel 后，PowerShell 将内联 Node 脚本中的中文状态字符串通过管道转成 `?`，状态定位超时；失败发生在首张截图之前，没有形成可用浏览器结果。
3. 按主任务指令停止额外检查并立即收口，因此没有继续调整脚本编码，也没有运行三项质量命令。

重跑时必须由新的独立验收代理执行，并满足以下入口：

- 以 UTF-8 文件或显式 UTF-8 管道执行 Playwright Node 脚本，使用 `chromium.launch({ channel: 'chrome', headless: true })`。
- 每个场景新建 browser context，`addInitScript` 注入 student 的 uni-app 包装会话，拦截 `/yj-app-api/app-api/yj/customer-service/session`，阻断全部非 GET。
- 重跑 `390x844`、`375x812`、`430x932` 空态，生成 DOM 数据、当前截图、图一归一化图、并排图、叠加图和 MAE/相似度。
- 重跑 loading、error、messages、输入、附件、图片/视频 file chooser、聚焦关闭和返回；错误态必须证明 GET `1 -> 2` 且写请求为 `0`。
- 独立运行 `npm run type-check`、`npm run build:h5`、`git diff --check`，并记录控制台、请求失败、HTTP 错误和资源状态。

在上述未通过项全部重跑并通过前，`TASK-004` 不得标记完成，`TASK-005` 和 `TASK-006` 不得启动。
