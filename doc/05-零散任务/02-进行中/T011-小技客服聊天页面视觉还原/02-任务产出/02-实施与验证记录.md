# T011 小技客服聊天页面视觉还原实施与验证记录

## 1. 实施结论

- 实施日期：2026-08-14
- 正式页面：`D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/src/pages/service/customer-service-chat.vue`
- 实施状态：展示层还原和实施侧验证完成；本记录不代替后续独立验收及用户人工审核。
- 页面已由蓝色纹理背景、红色居中错误卡和蓝色矩形发送按钮收敛为暖白聊天界面；正式实现仍为唯一原页面，未新增同义页面、依赖或图片背景。

## 2. 变更边界与复用判断

- 仅修改正式页 `<style lang="scss">`，模板、条件分支、绑定、事件和动态文案均未修改。
- `<script setup lang="ts">` 未修改；会话加载、错误重试、消息分支、输入、发送、上传、图片预览、视频、WebSocket 和返回逻辑保持原实现。
- 已盘点项目依赖 `@climblee/uv-ui`、uni-app 基础组件及项目内同类样式。当前页面已有完整正式 DOM 和业务组合，最小且风险最低的复用方式是直接扩展原页面 SCSS；未引入第二套聊天组件或图标资产。
- 返回箭头、加号和纸飞机均在原展示节点上通过 CSS 绘制；不是整图背景，真实 DOM 交互区域保持可点击。
- 普通消息行样式不在本次空态目标的必要改造范围内，未扩大修改。

## 3. 视觉实现

- 页面背景：`#fbf6f2` 暖白色。
- 导航：129rpx 高，左右等宽槽位保证标题真居中；标题和返回箭头使用 `#07325c`。
- 空态：白色左向气泡、27rpx 圆角、暖灰阴影、深蓝正文；绑定错误沿用同一气泡视觉，保留真实业务文案和点击重试。
- 输入区：35rpx 左右外边距、43rpx 底距、124rpx 外壳高度；内部为 96rpx 白色胶囊，深蓝圆形加号、灰色占位和橙色纸飞机。
- 附件态：沿用同一 composer 展开，暖白面板内保留“图片/视频”真实入口；图片使用深蓝图标，视频使用橙色图标。

### 390x844 关键 DOM 实测

| 元素 | 目标约值（px） | 实测（px） |
| --- | --- | --- |
| header | `x=0, y=0, w=390, h=66.8` | `x=0, y=0, w=390, h=67.078` |
| 空态气泡 | `x=38.4, y=224.3, w=220.8, h=68.2` | `x=38.469, y=224.625, w=221.406, h=67.828` |
| composer 外壳 | `x=18.3, y=756.6, w=353.4, h=65` | `x=18.188, y=757.203, w=353.625, h=64.438` |
| composer 内胶囊 | 内缩约 `7.3`、高约 `49.9` | `x=25.453, y=764.469, w=339.094, h=49.906` |

## 4. 状态与交互验证

- 空态接口：拦截 `GET **/yj-app-api/**/customer-service/session`，返回 `code=0`、`messages=[]`；页面显示原文“您好，欢迎咨询小技！”。
- 错误态接口：返回 `code=500`、`msg=student is not bound to a tenant`；页面保留业务归一化文案“请先绑定企业，绑定成功后再联系客服”，显示为深蓝单行白气泡。
- 错误点击重试：session 请求计数由 `1` 增至 `2`。
- 输入绑定：输入 `T011 input readback` 后读回完全一致。
- 发送状态：空输入时 `send-button--disabled=true`；输入后变为 `false`。
- 附件开合：加号可展开“图片/视频”，输入聚焦后面板关闭；展开态无水平溢出。
- 文件入口：图片和视频入口点击均实际触发 Playwright `filechooser` 事件。
- 返回：点击返回后 URL 为 `http://127.0.0.1:5173/yunjikeji/#/pages/service/customer-service`。
- 隔离策略：所有非 GET 写请求在 Playwright 路由层阻断；验证过程未触发真实写请求。

## 5. 三视口、控制台与资源

| 视口 | 水平溢出 | 请求失败 | HTTP 4xx/5xx | 结果 |
| --- | ---: | ---: | ---: | --- |
| `390x844` | `0` | `0` | `0` | 通过 |
| `375x812` | `0` | `0` | `0` | 通过 |
| `430x932` | `0` | `0` | `0` | 通过 |

- 三个空态视口均无裁切、遮挡、文字截断或异常空白。
- 控制台仅包含 Vite、uv-ui 和 App 生命周期的 debug/log；无 `error`、`warning` 或 `pageerror`。
- 机器可读结果：`D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T011-support-chat/validation-results.json`。
- 可重复执行脚本：`D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T011-support-chat/validate.mjs`。

## 6. 脚本证明与质量门禁

- `<script setup lang="ts">` 含标签原文 SHA-256：`9502961067ab4b89ca6dae2fa3a2dee6ea06cb88fa9421f2bf1f659174abb78a`。
- 脚本换行：`CRLF=344`、独立 `LF=0`；与实施前完全一致。
- `npm run type-check`：通过。
- `npm run build:h5`：通过，输出 `DONE Build complete.`。
- `git diff --check -- code/develop/yunjikeji/src/pages/service/customer-service-chat.vue`：通过，无空白错误。

## 7. 最终截图

- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T011-support-chat/390x844-empty-final.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T011-support-chat/375x812-empty-final.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T011-support-chat/430x932-empty-final.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T011-support-chat/390x844-error-final.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T011-support-chat/390x844-attachment-final.png`

## 8. 已知差异

- 参考图为位图，包含轻微暖白纹理、压缩噪声和平台字体抗锯齿；正式页使用任务指定的纯色 `#fbf6f2` 与系统中文字体，因此逐像素会存在微小差异。
- 未发现 P0 布局差异或已知未通过项；最终独立验收和用户人工确认仍按任务流程另行执行。
