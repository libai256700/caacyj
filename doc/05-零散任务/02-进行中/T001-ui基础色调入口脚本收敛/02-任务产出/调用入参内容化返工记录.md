# 调用入参内容化返工记录

## 返工原因

用户审核指出 `000-基础色调/02-调用入参.json` 中：

1. `overall_design_standard` 不应是 `031-页面设计/00-总体设计标准.json` 这类文件路径，而应是总体设计标准转换后的完整文本内容；
2. `reference_source_images[].path` 不应是相对路径，而应是本机绝对路径。

## 修改结果

已修改 `generate_base_tone_stage.py`：

- 从 `00-总体设计标准.json` 读取 JSON 后，转换为 `key:\nvalue` 形式的完整文本，写入 `02-调用入参.json.overall_design_standard`；
- 扫描 `006-参考图来源/` 时，将参考图 `path` 写为本机绝对路径；
- 校验逻辑新增约束：`overall_design_standard` 不得是 `.json` 路径，参考图 `path` 必须是绝对路径且文件存在；
- 保持正式产物顺序：`01-提示词.md` -> `02-调用入参.json` -> image2 成功后 `03-生成图片.*` -> image2 成功后 `04-图片说明.md`。

已修改 `render_base_tone_prompt.py`：

- `01-提示词.md` 的“总体设计标准”块优先使用 `02-调用入参.json.overall_design_standard` 中的内容文本；
- 不再依赖提示词中出现总体设计标准文件路径。

## 当前产物检查

`000-基础色调/02-调用入参.json` 当前结果：

- `overall_design_standard` 是内容文本，长度为 1489；
- `overall_design_standard` 不匹配 `.json` 路径；
- `reference_source_images[0].path` 是绝对路径；
- `reference_source_images[0].path` 指向的图片文件存在；
- 顶层字段为 `stage`、`overall_design_standard`、`reference_source_images`、`ratio`、`resolution`，没有 `prompt`。

`000-基础色调/01-提示词.md` 当前结果：

- 已包含“总体设计标准”内容块；
- 未出现“以某文件为准 / 以文件为准 / 以 00-总体设计标准.json 为准”等路径依赖表达。

## 验证命令

```powershell
python -X utf8 -m py_compile `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\generate_base_tone_stage.py `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\render_base_tone_prompt.py `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\validate_ui_design_directory.py `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\quick_validate_base_tone_stage.py
```

结果：通过。

```powershell
python -X utf8 D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\quick_validate_base_tone_stage.py
```

结果：通过，输出 `quick validation passed`。

```powershell
python -X utf8 D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\validate_ui_design_directory.py `
  D:\ProjPort\Work\feixingxueyuan\doc\02-版本迭代\03-进行中版本\20260511100000-vinit-project-bootstrap\031-页面设计
```

结果：通过，输出 `{"status": "passed", "errors": []}`。

```powershell
python -X utf8 D:\ProjPort\Work\dev-skills\skills\00-1100-skill-writing-guide\scripts\quick_validate.py `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation
```

结果：通过，输出 `Skill is valid!`。

## 遗留问题

image2 外部渠道仍不可用，真实阶段脚本运行到 image2 时失败；本次返工重点已收口 `01/02` 的入参边界，未生成新的 `03/04`。
