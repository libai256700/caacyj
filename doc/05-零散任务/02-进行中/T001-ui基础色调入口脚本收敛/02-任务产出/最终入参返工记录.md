# 最终入参返工记录

## 返工结论

本次已把 `05-最终入参.json` 收口为 `000-基础色调` 阶段入口脚本直接生成的主视图产物。

`02-调用入参.json` 仍只表示 UI skill 调用 image2 脚本前的脚本级入参快照；`05-最终入参.json` 来自 image2 脱敏 `.evidence/<run-id>/request.json`，表示最终逻辑请求快照，不表示 multipart 二进制传输层。

## 修改范围

- `D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\generate_base_tone_stage.py`
  - 新增 `05-最终入参.json` 生成逻辑。
  - 从 image2 脱敏 `request.json` 派生模型、接口模式、接口路径、prompt、size/resolution、参考图逻辑输入和逻辑请求对象。
  - image2 成功但缺少 `request.json` 时直接失败，避免出现成功但没有 `05` 的状态。
  - image2 失败但已写出 `request.json` 时，仍尽量写出 `05`，不伪造 `03/04`。
- `D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\SKILL.md`
  - 将 `05-最终入参.json` 纳入阶段主视图产物。
  - 明确 `02` 与 `05` 的职责边界。
  - 明确入口脚本顺序：`01 -> 02 -> image2 -> 05 -> 03 -> 04`。
  - 明确 `generate_base_tone_stage.py` 不读取旧 `02` 做整理或修正。
- `D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\quick_validate_base_tone_stage.py`
  - 模拟 image2 写出符合真实结构的脱敏 `request.json`。
  - 校验 `05` 存在、核心字段一致、无敏感字段。
- `D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\validate_ui_design_directory.py`
  - 识别并校验基础色调阶段根部 `05-最终入参.json`。
  - 校验 `05` 不包含 Authorization、API key、base64、binary 等敏感或二进制字段。

## 实际产物

- `D:\ProjPort\Work\feixingxueyuan\doc\02-版本迭代\03-进行中版本\20260511100000-vinit-project-bootstrap\031-页面设计\000-基础色调\05-最终入参.json`
- 对应 evidence：
  `D:\ProjPort\Work\feixingxueyuan\doc\02-版本迭代\03-进行中版本\20260511100000-vinit-project-bootstrap\031-页面设计\000-基础色调\.evidence\20260511-final-input-mainview\request.json`

核心字段核对：

```json
{
  "model": "gpt-image-2",
  "request_mode": "edits",
  "request_path": "/v1/images/edits",
  "size": "780x1387",
  "reference_local_count": 1,
  "prompt_equal_to_request_json": true,
  "has_sensitive_or_binary_field": false
}
```

## 执行命令与结果

```powershell
python -X utf8 -m py_compile `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\generate_base_tone_stage.py `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\quick_validate_base_tone_stage.py `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\validate_ui_design_directory.py
```

结果：通过，exit code 0。

```powershell
python -X utf8 D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\quick_validate_base_tone_stage.py
```

结果：通过，输出 `quick validation passed`。

```powershell
python -X utf8 D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\generate_base_tone_stage.py `
  D:\ProjPort\Work\feixingxueyuan\doc\02-版本迭代\03-进行中版本\20260511100000-vinit-project-bootstrap\031-页面设计\000-基础色调 `
  --run-id 20260511-final-input-mainview
```

结果：image2 外部渠道失败，exit code 1；脚本已生成 `01/02/05` 和 `.evidence/20260511-final-input-mainview/`，未伪造新的 `03/04`。

```powershell
python -X utf8 D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\validate_ui_design_directory.py `
  D:\ProjPort\Work\feixingxueyuan\doc\02-版本迭代\03-进行中版本\20260511100000-vinit-project-bootstrap\031-页面设计
```

结果：通过，输出 `{"status": "passed", "errors": []}`。

```powershell
python -X utf8 D:\ProjPort\Work\dev-skills\skills\00-1100-skill-writing-guide\scripts\quick_validate.py `
  D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation
```

结果：通过，输出 `Skill is valid!`。

## image2 失败原因

本次外部渠道全部失败，evidence 中记录 4 次尝试：

- 429：daily quota hard cap reached
- 429：DAILY_LIMIT_EXCEEDED
- 502：Upstream request failed
- 403：No active subscription found for this group

该失败不影响 `05-最终入参.json` 的生成规则验证；`03/04` 沿用既有正式图片与说明，未在失败调用中改写。

## 禁止项复核

- 未修改 `uni_modules/**`。
- `05-最终入参.json` 不包含 API key、Authorization、base64、binary 或图片二进制。
- 已清理复验过程中误写入真实阶段目录的快验样例夹具，并重新生成正式 `05`。
