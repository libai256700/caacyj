# T001-产物顺序最终验收记录

## TODO

- [x] 检查正式 `01-提示词.md` 写入顺序是否早于正式 `02-调用入参.json`
- [x] 检查机器临时输入是否落在 `.evidence/<run-id>/base-tone-stage-input.json`
- [x] 检查脚本是否读取旧正式 `02-调用入参.json` 做修复输入
- [x] 检查 `03-生成图片.*` 与 `04-图片说明.md` 是否只在 image2 成功后生成或更新
- [x] 检查 `SKILL.md` 口径是否与代码一致

## 验收结论

通过。

## 验收证据

1. `D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\scripts\generate_base_tone_stage.py`
   - 第 288-289 行定义正式 `prompt_path` 与 `input_path`。
   - 第 291-292 行把脚本级临时输入写入 `.evidence/<run-id>/base-tone-stage-input.json`。
   - 第 294-304 行调用 `render_base_tone_prompt.py`，输出正式 `01-提示词.md`。
   - 第 305-307 行仅在提示词渲染成功后写入正式 `02-调用入参.json`。
   - 第 318 行读取正式 `01-提示词.md` 作为 image2 的 prompt 输入。
   - 第 338-356 行仅在 image2 返回成功、stdout 中存在图片且图片文件存在后，才复制生成 `03-生成图片.*` 并写入 `04-图片说明.md`。
   - 全文未发现读取正式 `input_path` 或旧 `02-调用入参.json` 作为修复输入的逻辑；`read_json()` 仅用于读取 `00-总体设计标准.json`。

2. `D:\ProjPort\Work\dev-skills\skills\00-1800-ui-design-generation\SKILL.md`
   - 第 53 行明确要求：先生成机器用临时入参并直接生成正式 `01-提示词.md`，再生成正式 `02-调用入参.json`，不得读取旧 `02` 做自动修复或重写；只有 image2 成功返回可采用图片后，才允许落 `03/04`。

## 遗留问题

无。
