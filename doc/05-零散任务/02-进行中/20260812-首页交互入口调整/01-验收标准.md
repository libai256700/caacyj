# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T20260812`
- 任务名称：首页交互入口调整
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T20260812-INT-001 | 业务-交互级 | 用户从首页进入学习模块并操作自测、测评或错题入口 | 首页展示独立的考题自测、考题测评、错题测评业务块；自测块提供“开始自测”“自测结果”，测评块包含练习数据概览与考题测评入口，错题块包含开始错题练习与错题练习内容；各入口跳转到指定页面并保留可用的题目/列表内容 | 页面结构与文案符合需求，所有入口点击后进入对应页面且无路由错误；自测答题页可展示题目并可操作 | 运行前端后通过页面交互逐项点击，核对 URL、页面标题和内容；截图或浏览器控制台记录作为证据 | 任一业务块缺失、入口跳错、页面空白/报错或常用功能布局仍显示个人资料 |
| BA-T20260812-INT-002 | 业务-交互级 | 用户从首页开始考题测评并选择分类 | 首页“开始考题测评”先进入独立题型分类页；该页只展示截图所示的“题型分类”标题、说明和两列分类卡片，不展示练习页其他区域；点击分类后进入 `/pages/practice/exam-assessment` 单分类详情页，详情页不再展示任何分类导航块 | 分类页数量、名称、图标与现有分类数据一致；点击任一分类后详情页加载对应分类，URL 携带稳定分类参数；详情页仅展示所选分类详情、开始练习和错题复练 | 从首页进入分类页，核对页面内容边界；逐项点击分类，核对 URL、详情分类和操作按钮 | 首页仍直达详情、分类页夹带错题练习等其他区域、点击分类未传参数、详情页仍显示分类滑块/下拉框/图标网格或分类错误 |
| BA-T20260812-INT-003 | 业务-交互级 | 用户从任一带底部导航的主页面点击“我的” | 不再进入原“我的”一级页面，直接进入 `/pages/profile/settings` 设置页面；设置页取代原“我的”主页面并继续展示“首页 / AI助手 / 我的”底部导航，其中“我的”保持选中 | 点击底部“我的”后地址与页面内容均为设置页；设置页底部导航完整可见且“我的”选中；可从设置页继续切换首页和 AI 助手 | 在各主页面点击底部“我的”，核对地址栏、页面标题、设置功能和底部导航；从设置页点击首页、AI 助手核对跳转 | 任一底部“我的”仍进入 `/pages/profile`，设置页缺少底部导航、选中态错误、无法切换主页面或出现循环跳转 |
| BA-T20260812-INT-004 | 业务-交互级 | 用户进入 `/pages/center` 使用 AI 对话 | AI 对话页在任何自测状态下均不展示自测引导、自测完成消息、自测结果提示、自测报告卡片或查看报告入口；自测统一从首页进入，AI 对话的普通消息、输入和学习记录等非自测功能保持可用 | 首次进入、存在自测记录、存在自测报告等状态下，页面均无自测专属内容与入口，且非自测对话功能无回归 | 运行 AI 对话页并核对不同本地/接口自测状态下的初始消息、卡片、按钮和跳转；静态搜索自测条件渲染及入口 | 任一条件下仍显示自测文案、报告卡片、开始/查看自测入口，或清理导致普通 AI 对话功能不可用 |
| BA-T20260812-INT-005 | 业务-交互级 | 用户从首页进入考题自测并完成答题 | 首页自测入口承接原 AI 对话自测的真实业务链路，仅改变入口和呈现方式；题目来源、作答规则、提交结果、自测记录和报告查看保持一致 | “开始自测”加载原自测题目并按原规则作答、提交；提交后生成可在“自测结果”查看的记录和报告；已完成、空题、失败等状态有稳定承接 | 对比迁移前 center 自测逻辑与当前首页自测页面的接口、字段、校验、提交和结果导航，并运行自测完整链路 | 使用不同题源/提交接口、遗漏原有必填或多选规则、提交后记录/报告不一致，或只展示静态/模拟内容 |
| BA-T20260812-INT-006 | 业务-交互级 | 用户从首页点击“开始自测”并填写基本资料 | `/pages/practice/self-test-answer` 首屏以完整页面展示“自我测评”标题、返回入口、“填写基本资料”区域、姓名、手机号、毕业院校（选填）、专业（选填）和底部“下一步”，不展示身份证号、遮罩、弹窗或关闭按钮；资料页不显示题目计数、答题进度或题目卡片。姓名和手机号必填，毕业院校和专业选填；手机号从登录会话或当前学员资料回填并保持不可编辑。页面初次进入仍尝试读取当前学员资料用于回填，读取失败时提示用户并允许手填；点击“下一步”执行资料校验和保存，保存成功后在同一页面直接进入原自测第 1 题，不再要求当前学员资料二次回读成功；保存失败时留在资料页并显示原有错误反馈。点击返回优先回到首页，导航栈不可用时稳定重回首页 | 首页进入后直接看到四字段资料页面；身份证号在展示、校验及保存请求中均不存在；必填/选填、回填与不可编辑状态符合规则；初次资料回填失败后页面仍可操作；不完整或无效资料不能进入题目；资料保存成功后出现第 1 题且题目计数从 `1/总题数` 开始；后续作答、切题、提交与报告跳转无回归；返回可到首页 | 从首页进入页面，分别检查初次资料回填成功/超时、资料缺失、格式错误、保存失败和保存成功场景；核对字段、保存请求、请求时序、提示、URL、题目序号及完整答题提交结果；截图、网络记录或浏览器控制台记录作为证据 | 页面仍展示、校验或提交身份证号；首屏仍弹窗或同时显示题目；初次回填失败后无法操作；手机号可编辑；校验失败仍进入题目；保存成功后仍因二次当前学员资料回读失败而不能进入第 1 题；返回异常，或后续答题提交链路回归 |
| BA-T20260812-INT-007 | 业务-交互级 | 用户从首页点击“自测结果” | 首页复用既有自测记录逻辑查询最新记录；有记录时携带最新记录 ID 直接进入 `/pages/center/self-test-report` 展示对应报告，不再经过 `/pages/practice/self-test-record`；无记录或查询失败时留在首页展示明确提示，不传空 ID、不循环跳转；“开始自测”入口保持原行为 | 有结果时报告页加载显式 ID 对应的最新报告；无结果或失败时首页反馈明确；地址和导航历史不经过记录页；报告页返回可回到首页，显式操作可返回中心；个人页等其他既有自测记录入口继续保留 | 从首页分别在有结果、无结果和接口失败状态点击“自测结果”，核对请求、URL 中编码后的 ID、导航历史、报告/提示和返回行为；静态核对其他记录入口 | 仍先进入记录页、未先查最新记录、无 ID 或空 ID 进入报告页、报告与最新记录不匹配、无结果无提示、返回异常、“开始自测”受影响，或其他既有记录入口被删除 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T20260812-API-001 | BA-T20260812-INT-001 | 技术-接口级 | 首页入口使用项目现有 uni-app 路由，答题页按既有 practice/answer 参数契约承接，自测结果、错题列表和错题复习使用指定路径与参数 | 路由注册、参数编码和页面跳转均可解析，不改动后端接口契约 | 核对 `src/pages.json`、页面导航代码及浏览器地址栏 | 路由未注册、参数丢失或跳转到不存在页面 |
| TA-T20260812-API-002 | BA-T20260812-INT-003 | 技术-接口级 | 共用底部导航的“我的”目标统一为已注册的 `/pages/profile/settings`，原 `/pages/profile` 不再作为底部导航落点 | 所有底部导航实现和调用方使用同一设置页目标；设置页路由有效且返回行为无循环 | 核对底部导航组件、`pages.json` 与页面导航代码 | 存在第二套旧目标、路由未注册或导航方式导致循环/栈异常 |
| TA-T20260812-API-003 | BA-T20260812-INT-007 | 技术-接口级 | 首页“自测结果”先调用现有 `fetchAssessmentRecords` 取得最新记录 ID，再使用 uni-app 路由进入已注册的 `/pages/center/self-test-report?id=...`；报告页继续按显式 `id` 调用 `fetchAssessmentResult` | 首页不经过记录页，只有获得非空 ID 后才编码并导航；报告页按该 ID 加载精确报告；无 ID 报告页兼容逻辑不作为首页正常链路 | 核对首页点击处理、`pages.json`、报告页 `onLoad/loadReport` 和 assessment 服务调用 | 路由未注册、仍指向记录页、未查询记录便导航、传入空/错误/未编码 ID、正常链路依赖无 ID 兜底，或报告页未按 ID 加载 |
| TA-T20260812-API-004 | BA-T20260812-INT-006 | 技术-接口级 | 自测资料页复用现有 `fetchCurrentCustomer`、`saveCustomerInfo` 与 `fetchAssessmentQuestions` 接口：初次 `/me` 仅用于回填且失败不阻断；保存请求不携带身份证号；`saveCustomerInfo` 成功响应即作为进入题目阶段的完成条件，不再追加或等待第二次 `/me` | 页面初始化至多发起一次当前学员资料读取；保存请求仅提交姓名、毕业院校、专业等现有可选字段且无隐藏身份证号；保存成功后直接加载题目；保存失败不加载题目；真实登录态 `/me` 超时仍按真实失败提示，不伪装为接口恢复 | 核对 customer/assessment 服务调用点、保存 payload 与浏览器网络时序；模拟初次回填失败、保存成功和保存失败 | 保存 payload 仍含身份证号；保存成功后再次请求 `/me` 或以其结果决定是否进入题目；初次 `/me` 失败导致表单不可操作；保存失败仍加载题目；吞掉或伪造真实超时 |
| TA-T20260812-DATA-001 | BA-T20260812-INT-001 | 技术-数据级 | 练习数据概览和错题练习卡片复用现有页面/组件的数据承接方式；自测页展示题目状态与答题操作，不伪造后端统计 | 页面在现有数据可用或空数据状态下均有稳定展示，不出现未定义字段错误 | 页面运行、控制台检查和现有数据绑定代码核对 | 统计/错题内容使用硬编码冒充真实结果，或数据缺失导致页面崩溃 |
| TA-T20260812-DATA-002 | BA-T20260812-INT-002 | 技术-数据级 | 独立分类页复用现有 practice 分类数据与卡片样式，详情页按传入 `topicId/topicTitle` 加载单一分类，不维护第二套分类数据 | 分类数量、标题、图标来自现有分类服务与映射；详情页校验参数并只请求对应分类；无参数或无效参数有稳定错误/返回承接 | 核对分类组件复用、分类页数据加载、路由参数和详情页请求 | 硬编码固定分类、复制出不一致数据模型、详情忽略参数加载首项、无效参数导致空白或错误分类 |
| TA-T20260812-DATA-003 | BA-T20260812-INT-004 | 技术-数据级 | AI 对话页不再读取或消费自测结果来生成初始消息、状态卡片或导航入口，自测数据仍由首页及自测专属页面承接 | center 页面不存在自测状态条件渲染、报告数据拼装或自测导航动作；移除无用依赖后无未定义引用 | 核对 center 页面数据加载、消息初始化、模板条件和导航函数，运行类型检查 | center 仍根据自测状态生成内容、残留无效依赖或移除后产生运行/类型错误 |
| TA-T20260812-DATA-004 | BA-T20260812-INT-005 | 技术-数据级 | 首页自测页面复用原 assessment 服务契约和自测结果存储，不新建平行题源、结果模型或模拟数据 | 题目加载与答案提交使用现有 assessment 服务；单选、多选、文本答案映射及报告 ID/记录页承接与迁移前一致 | 对比迁移前后代码与服务调用，核对自测页面、记录页和报告页参数 | 存在平行数据模型、答案映射丢失、结果无法在记录/报告页读取或用模拟数据替代真实机制 |
| TA-T20260812-DATA-005 | BA-T20260812-INT-006 | 技术-数据级 | 基本资料页面化继续使用现有当前学员资料读取、登录手机号回填、资料校验与资料保存机制，但自测资料表单及保存数据不再承接身份证号；资料保存成功前不得加载或展示题目，成功后直接复用现有自测题目加载、答案状态和提交结果 | 页面初始化从现有学员资料与登录会话回填姓名、手机号、毕业院校和专业；姓名按 2-20 位中英文/间隔点规则校验，手机号按 11 位大陆手机号规则校验；院校、专业保持选填；表单状态、校验状态和保存 payload 中均无身份证号；初次资料读取失败保留登录手机号并允许继续填写；保存成功后只初始化一次原题目状态 | 核对 `self-test-answer` 的资料初始化、校验、保存 payload 和阶段切换代码，以及现有 customer/assessment 服务调用；验证重复点击、接口失败和成功后的请求顺序 | 保留身份证号的隐藏状态、校验或提交；新增平行资料接口或字段模型；保存前请求并展示题目；初次回填失败导致表单锁死；保存失败仍切换到题目；成功后重复加载、丢失题目或重置后续答案状态 |
| TA-T20260812-DATA-006 | BA-T20260812-INT-007 | 技术-数据级 | 首页复用 assessment 服务的真实自测记录集合，以现有排序后的首条记录作为最新记录，并只把其 ID 传给报告页；不新增平行记录模型或模拟报告 | 有记录时使用 `records[0].id`；无记录时不导航并提示先完成自测；查询异常时不导航并展示明确错误；重复点击受加载状态保护 | 核对首页 `fetchAssessmentRecords` 调用、最新 ID 选择、空态/异常处理和按钮加载状态；在有记录/无记录/失败状态验证 | 首页硬编码 ID/报告、选择非最新记录、空 ID 仍导航、错误被吞掉、重复请求失控，或改变后端/数据模型 |
| TA-T20260812-CODE-001 | BA-T20260812-INT-001 | 技术-代码级 | 仅修改首页及新增自测页面/路由所需代码，复用现有答题模式与组件；保持 `uni_modules/**` 不变并清理调试残留 | 代码可构建，首页及相关页面无明显样式重叠、控制台错误或回归入口丢失 | 静态检查、项目构建/启动、浏览器交互验证、git diff 复核 | 构建失败、入口回归、布局溢出，或三方包目录被修改 |
| TA-T20260812-CODE-002 | BA-T20260812-INT-006 | 技术-代码级 | 在既有 `/pages/practice/self-test-answer` 正式页面内以“基本资料 / 答题”前置阶段切换完成页面化，不新建第二套自测页面或复制题目逻辑；成组删除身份证号的模板配置、字段类型、响应式状态、触碰状态、校验、回填及保存 payload，并移除保存后的二次当前学员资料回读；保留现有答题、提交、报告跳转和返回逻辑 | 页面只保留一份四字段正式资料表单和一份既有答题实现；不存在身份证号隐藏提交或残留引用；资料保存期间重复点击受锁，保存成功后由保存响应直接触发题目加载；资料阶段与答题阶段互斥渲染，常见手机宽度下无内容遮挡或溢出；无未定义引用、调试标记和 `uni_modules/**` 修改；类型检查及差异检查通过 | 静态搜索身份证号引用，核对页面模板、状态、样式与服务依赖；运行类型检查、`git diff --check`，并在目标页面验证资料回填失败、保存成功/失败和移动端布局 | 保留身份证号字段、校验、隐藏状态或 payload；保存成功仍等待二次资料回读；重复点击产生重复保存；资料与题目同时可见；产生类型/运行错误，或修改三方包目录 |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T20260812-001 | 实施产出、独立验收记录和用户审核反馈均已写入任务目录；正式验收通过后再等待用户确认，不提前归档 | `02-任务产出/`、本计划状态、用户明确审核消息 |

## 5. 自测完成后直达最新报告补充（2026-08-15）

### 5.1 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T20260812-INT-008 | 业务-交互级 | 用户完成自测最后一题并提交 | 页面先保存本次自测，再查询最新自测记录，并携带最新记录的非空 ID 直达 `/pages/center/self-test-report`；报告查询失败时提示“自测已保存，报告加载失败，请重试”并留在当前页，再次点击只重试报告查询 | 正常链路进入本次最新报告；ID 为真实查询参数且未写死；保存失败不进入报告；保存成功后的查询失败或空 ID 不重复提交答案 | 核对末题请求时序、导航 URL、失败提示和重复点击行为 | 使用固定 ID、直接使用提交响应 ID 跳转、先查后存、空 ID 导航、保存成功后重试时重复提交，或失败提示不明确 |

### 5.2 技术验收

| 验收项ID | 关联业务验收项 | 技术验收类型 | 正式入口与技术边界 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T20260812-API-005 | BA-T20260812-INT-008 | 技术-接口级 | 末题保存复用 `submitAssessmentAnswers`，保存成功后复用 `fetchAssessmentRecords`；报告页仅接收最新记录的编码 ID | 首次操作按保存、查询顺序调用；查询失败后的重试不再调用保存接口 | 核对 `self-test-answer.vue` 调用顺序及失败分支 | 新增平行接口、调用顺序错误或重试重复保存 |
| TA-T20260812-DATA-007 | BA-T20260812-INT-008 | 技术-数据级 | 本地状态只记录本次答案是否已成功保存；导航 ID 只取 `records[0]?.id` | 保存成功立即置为已保存；保存失败保持未保存；非空最新记录 ID 才允许导航 | 核对已保存状态写入时机、最新记录选择、空值分支 | 保存状态提前写入、使用提交响应 ID、使用非首条记录、写死 ID 或空 ID 导航 |
| TA-T20260812-CODE-003 | BA-T20260812-INT-008 | 技术-代码级 | 在既有末题提交函数内沿用 `submitting` 锁扩展状态机，不复制答题页或改动三方包 | 重复点击受锁；查询失败可重试且不重复保存；类型检查与差异检查通过；不修改后端及 `uni_modules/**` | 静态核对并运行 `npm run type-check`、`git diff --check` | 重复请求失控、失败后无法重试、类型错误、无关改动或三方包改动 |

### 5.3 人工审核边界

- 在真实登录环境完成末题提交，核对答案保存请求成功后才发起最新记录查询，并确认跳转 URL 中的 ID 对应查询结果首条记录。
- 模拟最新记录查询失败或空 ID，核对页面提示、停留状态和再次点击只重试查询；该浏览器证据补齐前不得把本增量判定为真实接口联调完成。

## 6. 逐题练习与章节测试续作直达补充（2026-08-20）

### 6.1 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T20260812-INT-009 | 业务-交互级 | 用户从首页点击“逐题练习”或“章节测试” | 首页点击对应入口后，先查询当前账号对应类型的最新练习批次；若最新批次不存在或已完成，则继续进入分类页，再按原逻辑进入分类详情页和答题页；若最新批次未完成，则跳过分类页和分类详情页，直接进入答题页继续未完成题目；直接进入的答题页返回后仍回到分类列表页 | 未完成批次可直接续作且题目顺序、已答状态与原批次一致；不存在或已完成时仍保留原分类流程；返回到分类列表页的体验不变 | 从首页分别验证“无批次 / 已完成批次 / 未完成批次”三种场景，核对页面跳转、返回路径和题目恢复位置 | 错接到错误批次、仍强制进入分类页、未完成批次不能续作、返回路径回到首页而非分类列表页，或出现页面空白 / 报错 |

### 6.2 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T20260812-API-006 | BA-T20260812-INT-009 | 技术-接口级 | 首页入口新增“按当前账号和练习类型查询最新批次”的接口，查询条件必须限定当前账号、练习类型及对应练习模式；返回值复用现有批次详情结构，未命中时返回空，不改动原分类页/详情页的开始接口 | 首页仅新增一次查询；未命中时继续原流程；命中未完成批次时才直接进入答题页；接口契约可解析 | 核对首页点击逻辑、批次查询接口、答题页路由参数与后端控制器 / 服务方法 | 仍直接走分类页、查询条件缺失、误命中其他模式批次、返回空值不可处理，或新增平行开始接口 |
| TA-T20260812-DATA-008 | BA-T20260812-INT-009 | 技术-数据级 | 最新批次只按同类练习模式和最新时间顺序命中；未完成时使用该批次的 `practiceId / sessionId / recordId / catalogBatchId` 继续答题；已完成或空值时保持原分类流程 | 续作批次能恢复到未完成题目索引；已完成批次不被误续作；空值不影响原流程 | 核对数据库最新批次筛选条件、答题页恢复位置和返回路径 | 误取其他模式批次、恢复位置错误、空值导致页面异常或原流程被破坏 |
| TA-T20260812-CODE-004 | BA-T20260812-INT-009 | 技术-代码级 | 首页入口、后端批次查询与答题页返回逻辑只做最小增量，复用现有答题页和分类页，不改动 `uni_modules/**` | 类型检查与差异检查通过；直接续作和原分类流程均可运行；答题页返回可稳定回到分类列表页 | 静态搜索、`npm run type-check`、`git diff --check`、首页/答题页交互核对 | 新增多套重复逻辑、返回路径异常、类型错误、构建失败或修改三方包目录 |
