# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T013`
- 任务名称：自测报告生成中样式收敛
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T013-E2E-001` | 端到端业务验收 | 用户打开尚未生成报告的自测报告页 | 从首次请求开始到后端报告生成完成前，页面始终使用图二的暖色生成中视觉，不先显示图一的旧蓝色加载页 | 首屏和轮询等待阶段均显示同一生成中卡片、橙色加载图标、“报告正在生成中”和橙色“返回首页”；不存在“正在加载自测报告...”旧结构或蓝色等待页闪现 | 静态审查条件渲染；运行页面并延迟/挂起报告接口后检查首屏与等待态截图、DOM 和计算样式 | 首次打开仍出现旧加载文案、旧蓝色背景/按钮或另一套加载结构；加载与生成中发生布局跳变 |
| `BA-T013-DATA-001` | 数据结果业务验收 | 报告接口分别返回未生成、已生成、空记录或异常 | UI 收敛不得改变真实报告状态和后续结果承接 | 未生成时继续按既有周期轮询；已生成时展示原报告 HTML；空记录和异常仍进入各自反馈；返回首页和返回操作保持可用 | 审查 `loadReport` 状态赋值和轮询代码；模拟不同接口结果或做静态分支校验 | 轮询停止/重复、已生成报告不展示、空态或错误态被误判为生成中、导航回归 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T013-API-001` | `BA-T013-DATA-001` | 接口级验收 | 本次只调整前端状态呈现，不改变现有报告查询接口、参数和轮询契约 | `fetchAssessmentResult`、`fetchLatestAssessmentResult` 的调用条件、显式记录 ID 和 3 秒轮询保持不变 | 审查目标文件差异 | 修改接口路径、参数、记录选择或轮询周期 |
| `TA-T013-QUALITY-001` | `BA-T013-E2E-001`、`BA-T013-DATA-001` | 代码质量验收 | 首次加载与报告生成中共享现有正式组件，不新建同义页面或复制样式；已有用户改动不被回退 | 旧加载模板和文案被删除；暖色主题覆盖首次加载；类型检查、H5 构建和 `git diff --check` 通过；差异仅包含任务所需代码与记录 | 代码差异审查；执行项目检查与构建；检查工作区改动保护 | 仍保留两套加载实现、引入第二套组件、构建失败、出现格式错误或覆盖无关改动 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T013-001` | 独立验收通过后提交用户审核；用户明确确认前不归档 | 独立验收记录、页面验证证据、用户反馈与任务计划状态 |
