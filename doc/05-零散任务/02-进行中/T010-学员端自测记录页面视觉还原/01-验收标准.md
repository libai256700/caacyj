# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T010`
- 任务名称：学员端自测记录页面视觉还原
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T010-E2E-001` | 端到端业务验收 | 已登录学员从首页“自测结果”或“我的 > 自测记录”进入最近一次自测记录页 | 页面应按批准稿 V015 呈现顶部导航、本次自测信息、回答快照和结果摘要；仅展示最近记录的前三项回答快照，查看报告继续携带该记录 ID | `390 x 844` 主视口及一个相邻移动端视口中，卡片边界、间距、圆角、阴影、象牙白纸纹、橙色强调、深蓝文字和信息层级与批准稿一致；页面无横向溢出、文字重叠或关键内容截断；查看报告进入当前记录报告页 | 真实浏览器页面截图与 `visual-v71-self-test-record-real-details-review.png` 对照；DOM 几何测量；执行入口与报告导航 | 沿用旧蓝色视觉；展示超过三项快照；关键结构、间距或颜色明显偏离；出现重叠、截断、横向滚动或错误记录导航 |
| `BA-T010-DATA-001` | 数据结果业务验收 | 学员完成不同答案的自测后查看记录 | 页面中的完成时间、问题、答案、评分、题数、答对数和结论必须来自最近一次真实记录；年龄区间、职业状态、CAAC 了解程度不得固定为参考图示例值 | 完成时间使用服务端 `assessment_time`；前三项问题和答案来自该记录详情，答案优先展示服务端文案且不混入选项 ID；未评分问卷依据答案结构显示 0 分；题数、答对数及结论按当前记录计算；源码不存在固定日期、`16-25岁`、固定 18 题或固定答案 | 核对记录列表与详情接口响应、数据库结果时间映射、前端计算属性和至少一组真实记录页面；用替代数据测试动态映射 | 使用客户端当前时间、固定示例日期/年龄/答案/题数；展示选项 ID；使用首次答题时间冒充完成时间；当前记录变化后页面仍显示旧值 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T010-API-001` | `BA-T010-DATA-001` | 接口级验收 | 在现有练习记录详情接口上最小扩展真实自测完成时间，不新增平行接口或破坏既有字段 | `/app-api/yj/practices/records/{id}` 返回可选 `assessmentTime`，值来自与记录 ID、当前用户和租户匹配的最新 `yj_assessment_result.assessment_time`，精确到秒；不存在结果行时保持向后兼容；原字段与鉴权不变 | 后端单元/集成测试或接口响应断言；审查 VO、Service 查询条件与前端类型映射 | 时间来自客户端或固定值；查询未限制用户/租户/记录；原接口字段、鉴权或调用路径被破坏；秒级时间丢失 |
| `TA-T010-QUALITY-001` | `BA-T010-E2E-001`、`BA-T010-DATA-001` | 代码质量验收 | 在现有正式页面和服务层单一实现中完成修改，保留既有路由、登录校验、加载/空态/错误态并具备移动端适配 | 类型检查、H5 构建、后端目标测试和 `git diff --check` 通过；无第二套页面、mock、调试文案或临时辅助线；页面在 `onShow` 刷新且不会并发重复请求；现有用户改动未被回退 | 代码差异审查；运行项目既有检查与构建；真实浏览器多视口和控制台检查 | 构建/测试失败；出现重复实现或 mock；错误态、空态、登录或报告导航回归；热重载后布局溢出；覆盖无关工作区改动 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T010-001` | 独立验收通过后提交用户人工审核；只有用户明确确认视觉与真实数据结果后才允许归档 | 独立验收记录、最终截图、接口/测试结果、用户明确反馈与任务计划状态 |
