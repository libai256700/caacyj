# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T012`
- 任务名称：我的页面视觉还原
- 计划入口：`00-任务计划.md`
- 目标视觉：`02-任务产出/00-目标参考图.png`（`853x1844`）
- 当前错误样式：`02-任务产出/01-当前错误样式.png`（`633x1254`）
- 正式页面：`code/develop/yunjikeji/src/pages/profile.vue`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T012-E2E-001` | 端到端业务验收 | 学员进入底部“我的”页 | 页面应严格还原图一的暖白纹理背景、居中标题、白色学员信息卡、八行白色功能菜单卡及底部弧形导航，使用深蓝正文、橙色线性图标、浅灰箭头和红色退出登录 | `390x844` 下标题、信息卡、菜单卡和底栏的位置、尺寸、颜色、圆角、阴影、字号与图一接近；完整展示绑定组织、学习记录、账号安全、注销账号、联系客服、隐私协议、关于我们、退出登录；`375x812`、`430x932` 无水平溢出、遮挡、错位或文字截断；不再显示图二的橙色渐变、练习数据概览、蓝色卡片图标和分散面板 | 真实页面三视口截图与图一归一化并排、叠加和像素统计；补充关键 DOM 边界与色值测量 | 仍沿用图二橙色数据面板；目标标题、信息卡、八行菜单或底栏缺失；关键比例明显偏离；存在溢出、重叠、截断或异常空白 |
| `BA-T012-DATA-001` | 数据结果业务验收 | 页面加载个人资料与用户操作菜单 | 样式和展示结构调整不得改变个人资料、组织状态、菜单动作、资料编辑、客服、协议、退出或底部导航的原有业务语义 | 昵称、脱敏手机号和头像由个人资料承接；“所属组织”必须按最新组织审核记录的 `tenantId/companyId` 匹配已认证组织列表并显示正式组织名称，禁止使用个人资料 `schoolName` 作为组织名称兜底；待审核或驳回时显示审核进度，无有效绑定时显示暂未绑定；目标八行菜单全部复用已有事件处理函数；编辑资料弹层仍可到达；组织绑定、学习记录、账号安全、注销、客服、隐私、关于、退出及底部导航均保持原结果；不发生真实注销、资料保存或上传写操作 | 审查模板与脚本差异；浏览器模拟个人资料 `schoolName` 与已绑定组织名称不一致的数据，确认个人卡片和编辑资料摘要显示审核记录对应的组织名称；逐项检查显示与事件入口，拦截导航、弹窗及写请求并核对目标 | “所属组织”显示 `schoolName`、旧组织或其他非审核记录对应名称；任一状态优先级、事件目标、资料编辑、请求、上传、退出或导航语义发生变化，或已有功能变为不可到达 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T012-API-001` | `BA-T012-DATA-001` | 接口级验收 | 保留“我的”页现有生命周期、个人资料、组织审核、练习数据、认证、客服、协议、退出与路由契约，并复用已认证组织列表解析正式组织名称 | 继续使用 `fetchCurrentStudentAudit` 获取最新审核记录，并仅在审核通过时调用既有 `listApprovedOrganizations`，按 `tenantId/companyId` 精确匹配组织名称；不得修改服务文件、状态仓库、持久化、上传、退出或路由契约；模板事件只引用已有处理函数 | 审查 `git diff` 与浏览器网络请求；模拟 `schoolName` 和绑定组织名称不一致，核对审核与组织列表响应、个人卡片结果和无效状态回退 | 新增或修改后端接口契约；组织名未按审核记录匹配；未审核通过也把 `schoolName` 显示为所属组织；服务、持久化、上传、退出或路由语义发生变化 |
| `TA-T012-QUALITY-001` | `BA-T012-E2E-001` | 代码质量验收 | 在现有 `profile.vue` 和正式底栏组件上完成单一实现复用，响应式稳定且无调试残留 | 先完成第三方/项目组件与资源盘点；无第二套同义页面；类型检查和 H5 构建通过；目标资源加载成功；三视口无布局异常；控制台、页面错误、失败请求和 HTTP 错误均为零；变更限定在前端展示边界 | 审查范围；运行 `npm run type-check`、`npm run build:h5`、`git diff --check`；浏览器截图、DOM、资源、网络和控制台检查 | 构建失败、资源错误、重复实现、调试残留、跨视口布局异常，或修改脚本、服务、状态仓库、后端与无关页面 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T012-001` | 独立验收通过后提交用户人工审核；只有用户明确确认视觉结果后才允许归档 | 独立验收记录、最终截图、用户明确反馈与任务计划状态 |
