# T012 我的页面视觉还原：实施与验证记录

## 1. 实施范围

- 正式页面：`code/develop/yunjikeji/src/pages/profile.vue`
- 新增纯展示资源：
  - `src/static/profile-settings/profile-organization.svg`
  - `src/static/profile-settings/profile-learning-record.svg`
- 复用既有资源：纸纹、上下等高线、账号安全/注销/客服/隐私/关于/退出图标，以及 `HomeProfileTabBar` student 变体。
- 未修改 `HomeProfileTabBar.vue`、服务、状态仓库、后端、企业模板、资料编辑弹层和 `<script setup>`。
- 目标参考图仅用于验证证据，正式页面没有引用目标图。

## 2. 视觉实现

学员页已收敛为图一结构：暖白纸纹与等高线背景、居中“我的”标题、动态学员信息卡、单张八行菜单卡和既有弧形底栏。图二中的橙色渐变、练习数据概览、蓝色卡片图标和分散面板已从学员模板移除。

信息卡继续绑定 `profileState.avatarId`、`profileState.nickname`、`maskedPhone`、`displayOrganizationName` 和现有审核状态；点击卡片仍进入资料编辑，点击组织状态使用 `@tap.stop`，不会误开编辑弹层。

八项动作映射如下：

| 展示项 | 现有动作入口 |
| --- | --- |
| 绑定组织 | `goOrganizationBind` |
| 学习记录 | `handleMenu(recordMenus[0])` |
| 账号安全 | `handleEnterpriseMenu(enterpriseMenuRows[0])` |
| 注销账号 | `handleEnterpriseMenu(enterpriseMenuRows[1])` |
| 联系客服 | `handleEnterpriseMenu(enterpriseMenuRows[2])` |
| 隐私协议 | `handleEnterpriseMenu(enterpriseMenuRows[3])` |
| 关于我们 | `handleEnterpriseMenu(enterpriseMenuRows[4])` |
| 退出登录 | `handleEnterpriseMenu(enterpriseMenuRows[5])` |

## 3. 脚本与功能边界

- 实施后 `<script setup>` SHA-256：`E45D33754CAFDF4CAF5B4B6D1662B2C44E8AA885B2D27AE118C35A3195155F29`
- 与实施前指定哈希逐字一致，长度仍为 `17215` 字符。
- 自动审查确认企业模板、资料编辑弹层、动态头像、卡片编辑入口、组织状态阻止冒泡、八项动作和既有底栏契约均存在。
- 浏览器只读验证捕获 9 个 UI 调用：组织入口 2 次、学习记录 1 次、其余 6 项各 1 次；所有 URL/弹窗标题与原处理函数一致。
- 编辑资料弹层可从信息卡到达；组织状态点击后弹层数量为 0。
- 所有非 GET 请求均由验证守卫阻断；实际捕获写请求为 0，未执行保存、上传、注销或退出确认。

## 4. 三视口自检

| 视口 | 信息卡边界 | 菜单卡边界 | 底栏 DOM 顶部 | 水平溢出 | 页面/控制台/请求错误 |
| --- | --- | --- | ---: | ---: | ---: |
| `390x844` | `x13.516 y64.969 w362.969 h116.984` | `x13.516 y197.547 w362.969 h466.953` | `731.172` | `0` | `0` |
| `375x812` | `x13 y62.5 w349 h112.5` | `x13 y190 w349 h449` | `703.5` | `0` | `0` |
| `430x932` | `x14.906 y71.641 w400.188 h129` | `x14.906 y217.828 w400.188 h514.844` | `807.594` | `0` | `0` |

`390x844` 八行中心为 `227.602 / 285.719 / 343.836 / 401.953 / 460.078 / 518.195 / 576.313 / 634.438`，与目标近似中心最大偏差 `2.44px`。标题 DOM 文本范围为 `x177.313 y25.907 w35.375 h24`，截图中的实际可见字形顶部约为 `y31`。深蓝、红色、灰箭头计算色分别为 `rgb(8, 36, 50)`、`rgb(254, 0, 0)`、`rgb(184, 186, 192)`。

像素统计基于目标图与实际图统一归一化为 `390x844`：

| 区域 | MAE | MAE 占比 | 基于 MAE 的相似度 | 变化像素占比 |
| --- | ---: | ---: | ---: | ---: |
| 全图 | `6.323` | `2.479%` | `97.521%` | `6.452%` |
| 标题与信息卡 | `9.041` | `3.546%` | `96.454%` | `9.994%` |
| 八行菜单 | `5.437` | `2.132%` | `97.868%` | `5.019%` |
| 底部区域 | `5.625` | `2.206%` | `97.794%` | `6.274%` |

## 5. 自动检查结果

| 检查 | 结果 |
| --- | --- |
| `node output/playwright/T012-profile/validate.mjs` | 通过，`48/48`，无失败断言 |
| `npm run type-check` | 通过 |
| `npm run build:h5` | 通过，`DONE Build complete` |
| `git diff --check` | 通过；仅输出工作区既有 LF/CRLF 提示 |
| 三视口资源检查 | 破损图片 `0` |
| 三视口网络检查 | HTTP 错误、请求失败、意外 GET 均为 `0` |
| 三视口运行时检查 | 控制台错误、页面错误均为 `0` |
| 写请求检查 | `0` |

## 6. 证据入口

- 目标归一化图：`code/develop/yunjikeji/output/playwright/T012-profile/target-390x844.png`
- 实际截图：`actual-390x844.png`、`actual-375x812.png`、`actual-430x932.png`
- 目标/实际并排：`comparison-390x844.png`（左目标、右实际）
- 50% 叠加：`overlay-390x844.png`
- 关键 DOM：`dom-390x844.json`、`dom-375x812.json`、`dom-430x932.json`
- 全量验证：`validation-results.json`
- 像素统计：`visual-stats.json`

## 7. 剩余风险

- 浏览器资料使用允许的本地头像和只读模拟 GET；头像人物与目标参考人物不同，因此头像区域仍贡献像素差异，但正式页保持动态头像绑定，没有硬编码测试头像。
- 本记录属于实施与自检，不代替后续独立验收和用户人工视觉确认。
