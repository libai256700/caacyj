# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T005`
- 任务名称：本地启动飞行学院项目
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T005-E2E-001` | 端到端业务验收 / 任务目标结果验收 | 开发者从本机浏览器打开飞行学院 APP | H5 开发服务应保持运行，访问所提供本地 URL 后呈现真实应用页面 | URL 返回成功响应，页面非空且无阻断性启动错误 | HTTP 请求结果、浏览器页面检查、开发服务日志 | 地址不可访问、响应失败、页面空白或构建报错 |
| `BA-T005-DATA-001` | 数据结果业务验收 | 用户接收并复用本地运行入口 | 启动命令、应用目录、访问 URL、监听端口和进程信息应落入正式运行记录 | `02-任务产出/运行记录.md` 信息完整且与实际进程一致 | 运行记录与端口/进程查询结果对照 | 运行信息缺失、记录与实际不符或进程已退出 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T005-API-001` | `BA-T005-E2E-001` | 接口级验收 | 通过项目既有 `dev:h5` 脚本启动 Vite/uni-app 开发入口，并从本机通过 HTTP 访问 | 启动脚本无编译失败，监听端口存在，根路径返回 2xx | npm 输出、`Get-NetTCPConnection`、`Invoke-WebRequest` | 脚本退出、无监听端口或 HTTP 非成功响应 |
| `TA-T005-QUALITY-001` | `BA-T005-E2E-001` | 代码质量验收 | 本次启动不得为绕过问题修改业务源代码，不覆盖用户现有改动 | Git 差异仅包含任务记录；业务代码保持原状 | 启动前后 Git 状态核对 | 为启动而产生无关业务代码改动或覆盖既有文件 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T005-001` | 独立验收通过后向用户提供本地地址，并等待用户明确审核结果；未确认前任务保持“待人工审核” | 独立验收记录及用户后续反馈 |
