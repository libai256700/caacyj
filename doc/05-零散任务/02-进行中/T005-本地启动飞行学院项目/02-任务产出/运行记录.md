# 本地运行记录

## 1. 运行目标

- 应用：飞行学院学员 APP H5
- 应用目录：`code/develop/yunjikeji`
- 执行日期：`2026-07-24`

## 2. 启动信息

- 启动脚本：`npm run dev:h5 -- --host 127.0.0.1 --port 5173`
- 访问地址：`http://127.0.0.1:5173/yunjikeji/`
- 监听地址：`127.0.0.1:5173`
- Node 进程 PID：`16116`
- 进程启动时间：`2026-07-24 21:21:27`
- 本次重启方式：隐藏 Node 进程；原日志文件保留但本次未重定向输出

## 3. 实施验证

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 首次编译 | 通过 | Vite `5.2.8`，`ready in 6999ms` |
| 端口监听 | 通过 | PID `16116` 监听 `127.0.0.1:5173` |
| HTTP 入口 | 通过 | `GET /yunjikeji/` 返回 `200`、`text/html`、响应体长度 `928` 字节 |
| 浏览器渲染 | 通过 | 最终 URL 为 `/yunjikeji/#/`，标题为“首页”，`#app` 存在已渲染子节点 |
| 页面内容 | 通过 | DOM 包含“AI助手”“岗位查询”“联系客服”“首页”“我的”等可见内容 |
| 浏览器错误 | 通过 | 页面加载后控制台错误数为 `0` |
| 源码影响 | 通过 | `code/develop/yunjikeji` 与 `code/develop/yunjikeji-server` 无本任务新增 Git 改动 |

## 4. 环境说明

- 本机 Node.js 为 `v25.2.1`，npm 为 `11.6.2`；现有依赖可正常启动。
- APP 开发配置通过 Vite 代理访问既有远程服务，本次打开主页面不要求本地 Java 后端先运行。
- 本机当前没有全局 Maven；因为本次范围是主 APP H5 启动，未安装 Maven，也未启动后台管理端或 Java 服务。
- `2026-07-24 21:25` 已按用户要求重新启动前端；同时本地 Java 后端已在 `48080` 运行，`/yj-app-api` 代理链路验证通过。
