# 独立验收记录

## 1. 验收信息

- 任务 ID：`T005`
- 验收时间：`2026-07-24 20:27:42 +08:00`
- 验收对象：飞行学院学员 APP H5 本地开发服务
- 正式入口：`00-任务计划.md`、`01-验收标准.md`、`02-任务产出/运行记录.md`
- 验收方式：PowerShell 端口、进程、HTTP、日志和 Git 核对；Playwright CLI 真实浏览器复验
- 总体结论：`通过`

## 2. 正式验收项

| 验收项 ID | 结论 | 独立验证结果与证据 |
| --- | --- | --- |
| `BA-T005-E2E-001` | `通过` | `http://127.0.0.1:5173/yunjikeji/` 返回 `200`、`text/html`、`928` 字节。Playwright 打开后最终 URL 为 `http://127.0.0.1:5173/yunjikeji/#/`，页面标题为“首页”，`#app` 有 `1` 个直接渲染子节点；页面快照包含“岗位查询”“联系客服”“首页”“AI助手”“我的”等真实业务内容。Playwright 控制台共采集 `5` 条消息，其中错误 `0`、警告 `0`，未发现阻断性启动错误。 |
| `BA-T005-DATA-001` | `通过` | `运行记录.md` 已完整记录应用目录 `code/develop/yunjikeji`、启动命令 `npm run dev:h5 -- --host 127.0.0.1 --port 5173`、访问 URL、监听地址、PID `8008`、启动时间及日志位置。独立核对时 PID `8008` 仍存活并监听 `127.0.0.1:5173`，记录与实际一致。 |
| `TA-T005-API-001` | `通过` | Vite 标准输出显示通过项目既有 `dev:h5` 脚本启动、Vite `5.2.8` 且 `ready in 6999ms`，标准错误日志为空。`Get-NetTCPConnection` 确认 PID `8008` 监听 `127.0.0.1:5173`；`Invoke-WebRequest` 对根路径 `/` 与应用路径 `/yunjikeji/` 均返回 `200`，响应体均为 `928` 字节并包含 `#app` 挂载节点。最终稳定性复核仍为 `200`。 |
| `TA-T005-QUALITY-001` | `通过` | `git status --short -- code/develop/yunjikeji code/develop/yunjikeji-server` 返回空，两个业务代码目录无已跟踪或未跟踪改动；未发现为启动服务而修改业务源代码或覆盖既有文件。仓库其他目录存在与本任务无关的既有未跟踪文档和临时产物，不计入本次启动造成的业务代码影响。 |

## 3. 关键证据摘要

### 3.1 监听与进程

- 监听：`127.0.0.1:5173`，状态 `Listen`
- 进程：`node.exe`，PID `8008`，启动时间 `2026-07-24 20:22:27`
- 命令行：项目 `node_modules` 中的 uni-app CLI，参数 `--host 127.0.0.1 --port 5173`

### 3.2 HTTP 与页面

- `GET http://127.0.0.1:5173/`：`200 text/html`，`928` 字节
- `GET http://127.0.0.1:5173/yunjikeji/`：`200 text/html`，`928` 字节
- Playwright 页面：最终 URL `/yunjikeji/#/`，标题“首页”，正文非空
- Playwright 控制台：错误 `0`，警告 `0`

### 3.3 构建与代码边界

- 开发日志：Vite `5.2.8`，`ready in 6999ms`
- 标准错误日志：空
- 业务目录 Git 状态：`code/develop/yunjikeji`、`code/develop/yunjikeji-server` 均无差异

## 4. 结论

`BA-T005-E2E-001`、`BA-T005-DATA-001`、`TA-T005-API-001`、`TA-T005-QUALITY-001` 四项全部通过。当前本地 H5 服务可通过 `http://127.0.0.1:5173/yunjikeji/` 访问，任务可进入等待用户人工审核阶段；在用户明确审核通过前不得归档。
