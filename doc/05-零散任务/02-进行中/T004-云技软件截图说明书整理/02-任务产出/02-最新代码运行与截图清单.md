# T004 最新代码运行与截图清单

## 1. 盘点结论

- 盘点时间：`2026-07-24T17:13:58+08:00`。
- 当前 Git 分支：`feature/20260601000000-vphase1-initial-delivery`。
- 当前 Git HEAD：`df486e2a79b19b0dcdc57945e4c48ebdcaebd9a9`。
- HEAD 提交时间与摘要：`2026-07-19T15:06:31+08:00`，`test: align server tests with current APIs`。
- `git status --short -- code/develop`、未暂存 diff 和已暂存 diff 均为空；本次盘点未修改业务源码。
- 可启动的真实前端入口共 `2` 个：`yunjikeji` H5 客户端和 `yunjikeji-admin-ui` 管理端。两者的依赖目录和本地 Vite 可执行文件均存在。
- 当前没有可证明由本工作区最新云技代码启动的服务。监听 `5175` 的 Vite 页面标题、入口脚本均属于另一仓库，禁止作为本任务启动或截图证据。
- 推荐软件截图候选共 `12` 页：`4` 页可先验证静态展示，`8` 页需要真实登录、后端或业务数据后再采集。不得用历史截图补齐受阻页面。

## 2. 工具与依赖指纹

| 项目 | 当前事实 |
| --- | --- |
| Node.js | `v25.2.1` |
| npm / npx | `11.6.2 / 11.6.2`，`npx` 可用 |
| pnpm | `11.9.0` |
| Playwright 入口 | 按 Playwright CLI 流程执行；不得改用历史测试截图 |
| 客户端依赖 | `code/develop/yunjikeji/node_modules` 存在；Vite `5.2.8`；`package-lock.json` 存在 |
| 管理端依赖 | `code/develop/yunjikeji-admin-ui/node_modules` 存在；Vite `8.0.10`；`pnpm-lock.yaml` 存在 |
| 管理端引擎门槛 | `node >= 20.19.0`、`pnpm >= 8.6.0`，当前版本满足 |

Playwright 技能的前置检查已通过。后续截图必须采用 CLI-first 的 `open -> snapshot -> interaction -> snapshot -> screenshot` 顺序；导航或页面显著变化后必须重新 snapshot，不能使用过期元素引用。

## 3. 启动入口、端口和 URL

| 前端 | 工作目录 | package script | 配置端口与基址 | 建议的可复核启动命令 | 目标 URL |
| --- | --- | --- | --- | --- | --- |
| 云技 H5 客户端 | `code/develop/yunjikeji` | `npm run dev:h5`，实际执行 `uni` | 未写死端口，Vite 默认 `5173`；H5 基址 `/yunjikeji/` | `npm run dev:h5 -- --host 127.0.0.1 --port 5176 --strictPort` | `http://127.0.0.1:5176/yunjikeji/#/pages/...` |
| 云技管理端 | `code/develop/yunjikeji-admin-ui` | `pnpm dev`，实际执行 `vite --mode env.local` | `.env.env.local` 指定 `3000`；基址 `/` | `pnpm dev -- --host 127.0.0.1 --strictPort` | `http://127.0.0.1:3000/...` |

盘点时 `5173`、`5174`、`5175` 均被其他 Node 进程占用，`5176`、`5177` 和 `3000` 空闲。端口状态会变化，实施前必须重查；客户端必须使用 `--strictPort` 并记录实际 PID，避免 Vite 自动换端口后误连其他项目。

当前监听 `5175` 的命令只有相对形式 `node_modules/vite/bin/vite.js --host 127.0.0.1 --port 5175`，但 HTTP 首页标题和 `/src/main.js` 指向另一项目，不属于云技。管理端本地代理目标 `127.0.0.1:48080` 在盘点时未监听。

## 4. 环境、鉴权与数据依赖

### 4.1 H5 客户端

- 开发模式默认把 `/dev-api` 代理到 `https://sit.lai-do.com`，把 `/yj-app-api` 代理到 `https://yunjikeji.lai-do.com`；两者均是外部服务依赖。
- 生产环境变量指向 `https://yunjikeji.lai-do.com/yunjikeji-api` 和 `https://yunjikeji.lai-do.com/yunjikeji-admin-api`。
- 登录会话使用本地存储键 `yunjikeji-user-session`；请求从会话生成 `Bearer` 和 `tenant-id` 请求头，接口返回 `401` 时会清会话并重定向 `/pages/auth/login`。
- `/pages/index` 只是启动重定向页：无会话去登录；企业身份去学员页；其他已登录身份去练习页。该页不适合作为截图。
- `pages/home.vue` 的功能卡片是当前源码内的静态数据，可独立显示；`pages/auth/agreement.vue` 的协议正文也是源码内置内容。
- 练习页虽内置 `12` 个题型样式回退项，但正常流程先执行 `requireLogin()` 和练习接口。回退项只能作为界面容错，不能冒充真实练习数据。
- 登录页的视觉主体可在无后端时展示，但发送验证码和登录动作依赖真实认证服务。未提供测试手机号、验证码或授权会话时，不得伪造登录成功状态。

### 4.2 管理端

- 本地模式为端口 `3000`、基址 `/`、API 前缀 `/admin-api`，代理到 `http://127.0.0.1:48080`；当前后端端口未监听。
- 租户功能开启、验证码关闭。环境文件存在默认租户、用户名和密码字段；本记录不展开敏感值，也不授权自动使用这些字段登录。
- 未登录白名单为 `/login`、`/social-login`、`/auth-redirect`、`/bind`、`/register`、`/oauthLogin/gitee`。业务地址会被重定向到 `/login?redirect=...`。
- 登录后先获取用户信息和角色菜单，再由 `permission` store 动态生成路由并调用 `router.addRoute`。因此云技业务菜单的最终 URL 不能仅靠文件路径猜测，必须在真实登录后的最新 snapshot 中记录。
- 首页会调用 `HomeApi` 获取汇总、趋势和练习数据；`yj/resource/index.vue` 与 `yj/knowledge-base/index.vue` 均直接调用后端 API。管理端没有可替代这些云技数据的本地 mock。
- 资源页覆盖企业审核、学员加入审核、练习分类、练习题目、练习记录、错题、岗位、采集任务、协议、自测题库、自测结果、知识库、智能体和客服会话等配置。截图时应从真实菜单选择一个数据完整且有代表性的资源，而不是手工拼路由参数。

## 5. 当前页面清单

客户端 `src/pages.json` 共登记 `25` 个页面：

| 分组 | 页面路由 |
| --- | --- |
| 入口与公开内容 | `pages/home`、`pages/index`、`pages/auth/login`、`pages/auth/agreement` |
| 练习与测评 | `pages/practice`、`pages/practice/start`、`pages/practice/exam-assessment`、`pages/practice/answer`、`pages/practice/ai-answer`、`pages/practice/record`、`pages/practice/self-test-record` |
| AI 与报告 | `pages/center`、`pages/center/self-test-report` |
| 用户与设置 | `pages/profile`、`pages/profile/phone-bind`、`pages/profile/settings`、`pages/profile/logout` |
| 企业流程 | `pages/enterprise/register`、`pages/enterprise/review`、`pages/enterprise/students`、`pages/enterprise/organization-bind` |
| 岗位与客服 | `pages/jobs`、`pages/service/customer-service`、`pages/service/customer-service-chat` |
| 内部预览 | `pages/home-reference-preview`，仅为参考首页预览，不纳入正式软件截图 |

除首页、登录、协议和部分表单初始态外，主要业务页均调用服务层或要求有效会话。答题、AI 解答、练习开始等页面还要求真实练习 ID、会话 ID 或题目上下文，不能直接打开空参数路由截图。

管理端源码中与云技直接相关的正式页面实现为：

- `src/views/Home/Index.vue`：管理首页与统计趋势，需登录和后端数据。
- `src/views/yj/resource/index.vue`：统一资源管理容器，具体标题、资源和 URL 由配置及后端菜单共同决定。
- `src/views/yj/knowledge-base/index.vue`：知识库列表与查询测试，需登录、菜单权限和后端数据。
- `src/views/Login/Login.vue`：公开登录页；视觉可独立加载，租户识别和登录提交依赖后端。

## 6. 推荐截图范围

建议主视口：客户端 `390 x 844`；管理端 `1440 x 900`。客户端可追加 `430 x 932` 做裁切检查，管理端可追加 `1920 x 1080` 做宽屏检查，但同一说明书内应保持主视口一致。

| 序号 | 端 | 页面 | 真实路由或进入方式 | 依赖与采集门禁 | 优先级 |
| ---: | --- | --- | --- | --- | --- |
| 1 | 客户端 | 登录 | `/yunjikeji/#/pages/auth/login` | 无后端可展示；不得执行未经授权的验证码或登录 | 必选 |
| 2 | 客户端 | 用户服务协议 | `/yunjikeji/#/pages/auth/agreement?type=user` | 完全使用当前源码内置文案与静态资源 | 必选 |
| 3 | 客户端 | 首页 | `/yunjikeji/#/pages/home` | 主要视觉和卡片为本地源码；正常产品流程应在真实会话后进入 | 必选 |
| 4 | 管理端 | 登录 | `/login` | 无会话可展示；允许租户探测失败说明，但截图前应确认无持续遮罩 | 必选 |
| 5 | 客户端 | 练习中心 | `/yunjikeji/#/pages/practice` | 真实登录、租户/企业绑定和练习 API | 条件必选 |
| 6 | 客户端 | 答题 | 从练习中心开始真实练习后进入 | 真实 practice/session/topic 参数和题目数据 | 条件必选 |
| 7 | 客户端 | AI 助手中心 | `/yunjikeji/#/pages/center` | 真实登录；用户资料、测评和知识服务 | 条件必选 |
| 8 | 客户端 | 岗位查询 | `/yunjikeji/#/pages/jobs` | 真实登录和岗位分页 API | 条件推荐 |
| 9 | 客户端 | 我的 | `/yunjikeji/#/pages/profile` | 真实登录；用户、审核和练习统计 API | 条件推荐 |
| 10 | 管理端 | 数据看板 | 登录后 `/index` | 管理后端、授权账号、首页统计 API | 条件必选 |
| 11 | 管理端 | 云技资源管理 | 登录后从后端下发菜单进入 | 菜单权限、管理后端和真实列表数据；优先选择练习题目或企业审核 | 条件必选 |
| 12 | 管理端 | 知识库管理与查询 | 登录后从后端下发菜单进入 | 菜单权限、知识库列表和查询 API | 条件推荐 |

前 `4` 页可作为启动与基础视觉验证；后 `8` 页必须等真实账号、后端和数据可用再采集。若这些条件缺失，应在实施记录标为受阻并向用户要上下文，不得用空状态、错误页、直接写本地 token 或旧截图替代。

## 7. 启动与截图实施建议

1. 启动前把 `git rev-parse HEAD`、`git status --short -- code/develop`、本机时间和目标端口占用写入本次运行记录。
2. 客户端显式使用 `5176 --strictPort`；管理端使用 `3000 --strictPort`。两个服务分别把标准输出写到本次任务产出中的新日志，并记录命令、PID、进程创建时间和首个健康响应时间。
3. 客户端健康入口使用 `/yunjikeji/`，管理端使用 `/login`。HTML 中应出现当前项目入口而不是其他项目标题；仅端口可访问不足以证明服务归属。
4. 使用新的 Playwright CLI 会话打开精确 URL，先 snapshot，再按最新 ref 交互。登录、菜单跳转、弹窗开关和业务页切换后重新 snapshot。
5. 每张截图记录页面名、最终 URL、视口、截图时间、服务 PID、服务启动时间、Git HEAD、PNG 路径和 SHA-256。截图文件创建/修改时间必须晚于本次服务进程创建时间和首个健康响应时间。
6. 截图前确认加载遮罩消失、核心文案和数据已出现、控制台无导致白屏的错误；网络错误或缺数据必须记录，不能通过裁切规避。
7. 客户端按移动视口逐页采集，管理端按桌面视口采集。图片只写入本任务 `02-任务产出/03-截图/`，并在 Word 嵌入前后校验媒体哈希。

## 8. 禁止复用范围与风险

- 禁止复用仓库根 `tmp/**`、旧验收目录、旧 Word 内嵌图片及任何历史截图。
- 特别禁止复用 `code/develop/yunjikeji/tmp/**`；其中已存在旧登录页 PNG 和旧检查脚本，均早于本次启动且不满足最新代码证据门禁。
- 禁止把 `pages/home-reference-preview` 当作正式产品页截图；它的路由名称已明确标注为参考预览。
- 客户端关键业务页目前受真实登录、远程 API、租户绑定和业务数据约束；管理端关键业务页受 `48080` 后端、授权账号、动态菜单和数据权限约束。
- 盘点时未启动服务、未登录任何账号、未生成截图，因此这里只给出可执行入口和候选清单，不宣称关键业务页当前已可截图。
- Playwright 实施阶段如缺少授权测试账号、短信验证码获取方式或可用后端，应回报 `NEEDS_CONTEXT`，不得自行使用环境中的默认密码字段。

## 9. 验收支撑

- `BA-T004-DATA-001`：已给出 Git 指纹、当前工作区状态、12 页候选的真实代码路径/路由、视口、依赖、时间和哈希留证口径，并明确禁止历史资产。
- `TA-T004-API-001`：已确认两个当前代码启动入口、依赖和端口，区分了其他仓库的既有 Vite 服务，定义了 Playwright CLI 的快照与截图顺序及服务归属证明方式。

## 10. TASK-003 本次启动与截图实施结果

### 10.1 启动与代码指纹

- 实施日期：`2026-07-24`。
- Git HEAD：`df486e2a79b19b0dcdc57945e4c48ebdcaebd9a9`。
- 工作目录：`code/develop/yunjikeji`。
- H5 启动命令：`npm run dev:h5 -- --host 127.0.0.1 --port 5176 --strictPort`。
- 服务基址：`http://127.0.0.1:5176/yunjikeji/`。
- 运行日志：`02-任务产出/运行记录/01-h5-stdout.log`、`02-任务产出/运行记录/02-h5-stderr.log`。
- 截图主视口：`390 x 844`；本次 6 张 PNG 均为该像素尺寸和 RGB 模式。
- 实施后 `git status --short -- code/develop` 为空，截图和 Word 生成均未修改业务源码。

### 10.2 本次截图清单

| 序号 | 页面 | 路由 | 截图时间（+08:00） | PNG | SHA-256 | Word 映射 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 登录 | `/yunjikeji/#/pages/auth/login` | `2026-07-24 17:31:49` | `03-截图/01-login.png` | `493a93166a64c3d3145b436fc29aa8417a4cd2ad63a349875da384880a6e5de2` | `word/media/image1.png` |
| 2 | 用户服务协议 | `/yunjikeji/#/pages/auth/agreement?type=user` | `2026-07-24 17:35:41` | `03-截图/02-agreement-user.png` | `980120e2cbf315d9fe97fac7967b216993e61266ddc1a77bf5baa55f843f9826` | `word/media/image2.png` |
| 3 | 首页（未登录直接访问） | `/yunjikeji/#/pages/home` | `2026-07-24 17:36:06` | `03-截图/03-home-before-login.png` | `c5abad07ff2c66f73462566b0bd98e46d94d900579bb61bf8cb725b578b29f8e` | `word/media/image4.png` |
| 4 | 隐私协议 | `/yunjikeji/#/pages/auth/agreement?type=privacy` | `2026-07-24 17:49:50` | `03-截图/04-privacy.png` | `66ba9eed29069dcb68dbfa4adc4b6df18f5cecc94c6ed828fa2b001ea188a8da` | `word/media/image3.png` |
| 5 | 企业注册 | `/yunjikeji/#/pages/enterprise/register` | `2026-07-24 17:50:06` | `03-截图/05-enterprise-register.png` | `3f2f46bafc71db392c1bef92f6d6bcf85322ba25d668edb8b073c6bb5d2fb481` | `word/media/image5.png` |
| 6 | 绑定手机号 | `/yunjikeji/#/pages/profile/phone-bind` | `2026-07-24 17:50:25` | `03-截图/06-phone-bind.png` | `2f007e4a846f185b130bac71fa981422876a7f0f9072f186c83a893f16ad70a2` | `word/media/image6.png` |

### 10.3 Word 落盘与覆盖限制

- 目标文件：`C:\Users\18163\Desktop\云技软件截图.docx`。
- 修正版大小：`933,176` 字节；SHA-256：`243eebd22b34df9436a0cfd63a83209a2b046b335614818e347ea1078869f81e`。
- 生成脚本：`02-任务产出/generate_screenshot_docx.py`；生成记录：`02-任务产出/04-生成记录.md`。
- Word 共嵌入 6 个媒体，包内媒体与上表源 PNG 的 SHA-256 逐一一致。
- 未获取获授权的正式账号、登录会话和完整后端业务数据，因此真实登录后的练习、答题、AI 中心、个人中心及管理端业务页未覆盖；未使用历史图片或伪造数据补齐。

### 10.4 首轮验收返工

- 首轮固定 14.63 cm 宽导致竖屏图片高 31.661 cm，超过 A4 正文可用高度；本轮按宽高双上限重新计算并保持 390:844 比例。
- “访问与协议”现为 3 行单列表格，依次承载登录、用户服务协议、隐私协议；“企业与账号”现为 2 行单列表格，依次承载企业注册、绑定手机号；首页为独立正文图片。
- 两个表格均为 Table Grid、一列，共 5 行，所有行设置不可拆分。表格图片为 `7.855 x 17.000 cm`，首页图片为 `8.780 x 19.000 cm`，均低于 A4 正文可用高度约 `24.620 cm / 697.9 pt`。
- 修正版保持 0 个显式分页符，使用自然分页；6 个媒体哈希仍与原实拍 PNG 一致，未重拍或替换图片。
