# 验收标准

> 本文只写正式标准定义；执行结果与最终结论写入 `02-任务产出/05-独立验收记录.md`。

## 1. 文档信息

- 任务ID：`T004`
- 任务名称：云技软件截图说明书整理
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T004-E2E-001` | 端到端业务验收 / 任务目标结果验收 | 用户要求参照旧项目截图表格，整理当前飞行学院最新代码对应的软件截图 | 从可用参考 DOCX 的截图版式出发，完成模板确认、当前代码启动、真实页面采集、Word 生成和桌面交付 | 桌面存在 `云技软件截图.docx` 且不覆盖两份源码说明书；文档可打开；页面名称、截图顺序、图片尺寸和分页方式与确认后的参考截图版式一致；所有软件界面图均来自本次启动的当前工作区代码 | 模板分析、Git/启动记录、Playwright 截图清单、源图片属性、DOCX 结构化读取结果 | 模板来源无法证明、文件缺失或损坏、复用旧截图、截图并非当前代码运行页面、输出覆盖既有源码文档，或版式明显偏离参考 |
| `BA-T004-DATA-001` | 数据结果业务验收 | 本次截图证据与 Word 媒体内容承接 | 每张截图必须有页面名称、真实路由、视口、采集时间、当前代码指纹和源图片哈希，且 Word 嵌入图片与源图片一一对应 | 截图清单完整；源图片在本次任务目录生成且采集时间晚于服务启动时间；图片非空、尺寸合理、无历史文档来源；DOCX 媒体哈希与对应源图一致；截图覆盖参考模板要求且不存在明显重复或缺页 | 截图清单、文件时间、SHA-256、图像像素统计、Playwright 会话记录、DOCX 关系与媒体哈希 | 缺少路由/时间/视口/指纹，源图早于本次启动，图片空白或重复，媒体无法映射，或存在未说明的页面缺失 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T004-API-001` | `BA-T004-E2E-001` | 接口级验收 | 以当前代码启动命令、真实浏览器页面和 Playwright CLI 作为截图技术入口 | 已记录 Git HEAD/状态、运行项目、启动命令、进程和 URL；每个目标页面通过 Playwright 打开并在最新快照后截图；页面无持续加载遮罩、白屏、严重控制台错误或关键资源失败；截图像素非空 | 服务日志、进程/端口、Playwright 命令和快照、截图像素/尺寸、必要的控制台与网络摘要 | 使用旧构建目录或历史截图、服务并非当前代码启动、页面白屏/未加载完成、关键错误未说明，或截图不是 Playwright 本次采集 |
| `TA-T004-QUALITY-001` | `BA-T004-DATA-001` | 代码质量验收 | 模板解析、截图映射和 DOCX 生成采用结构化 API，可复核且不修改业务源码 | 可用模板及截图版式结论有结构化证据；DOCX ZIP/XML、关系、图片和样式可解析；截图清单、生成脚本和记录完整；`code/develop` 没有本任务新增业务源码修改；正文与图片无明显乱码、拉伸或裁切 | 模板/输出 DOCX 结构统计、图片宽高与纵横比、生成脚本、ZIP CRC/XML 解析、`git status --short -- code/develop` 与 diff | 手工不可复核拼接、文档损坏、图片映射错误或变形、修改业务源码、缺少生成证据，或出现乱码/严重裁切 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T004-001` | 独立验收全部通过后提交用户审核；仅在用户明确确认截图范围、内容与版式通过后归档 | `02-任务产出/05-独立验收记录.md`、用户反馈、已回写的 `00-任务计划.md` |
