# TC-DATA-T0015-001 field_type 契约

## 1. 测试层级

`数据级`

## 2. 对应验收项

- 验收项 ID：`TA-T0015-DATA-001`
- 正式验收入口：`../../01-验收标准.md`

## 3. 前置条件

- 环境前置：只读核对仓库内正式数据库定义；自动化测试不连接真实数据库。
- 账号前置：无。
- 数据前置：`field_type varchar(10)`，综合考试期望值为 `comprehens`。

## 4. 测试数据

- 见 `03-测试数据模板.json` 中 `TC-DATA-T0015-001`。

## 5. 执行动作

| 步骤 | 动作 | 预期结果 |
| --- | --- | --- |
| 1 | 核对正式库表定义 | `field_type` 为 `varchar(10)` |
| 2 | 核对两个综合考试常量 | 值均为 `comprehens` 且长度为 10 |
| 3 | 运行目标 Maven 测试 | 待插入记录的 `fieldType` 等于 `comprehens` 且长度不超过 10 |

## 6. 期望结果

- 错误值 `comprehensive-exam` 不再进入综合考试写入链路。
- 未通过扩大数据库字段约束掩盖错误业务值。
- 本用例验证待插入对象和字段定义，不冒充真实测试库落库结果。

## 7. 脚本入口

- 自动化脚本：`05-自动化测试脚本/run_contract_tests.py`
- 依赖命令：目标 Maven 契约测试命令与只读仓库检索。
- 结果输出位置：`04-测试执行结论.md`

## 8. 失败判定

- 正式字段定义不是 `varchar(10)`，或综合考试常量存在冲突。
- 待插入记录值不等于 `comprehens`，或长度超过 10。
