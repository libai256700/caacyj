# 验收标准

> 本模板只用于零散任务场景 `01-验收标准.md`。
> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T0015`
- 任务名称：综合考试后端报错修复
- 计划入口：`00-任务计划.md`

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| BA-T0015-INT-001 | 业务-交互级 | 学员从前端考试模式页点击“综合考试”，系统创建正式考试批次并进入后续考试流程 | 从真实页面入口触发综合考试创建，页面获得可继续答题所需的成功结果；后端不再出现本次 MySQL 截断异常 | 点击一次“综合考试”后请求成功，页面进入现有综合考试后续流程，且同一入口不出现服务器错误提示 | 浏览器网络请求与页面结果、对应后端日志；优先使用项目既有测试，需页面联调时按规则使用 Playwright | 请求失败、页面无法继续、后端仍抛出 `MysqlDataTruncation` 或产生重复/错误批次 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| TA-T0015-API-001 | BA-T0015-INT-001 | 技术-接口级 | 核对综合考试页面实际调用的创建接口、请求参数、响应契约及异常分支，以真实调用链为准修复 | 综合考试创建接口以现有合法参数调用时返回成功契约；非法输入仍按现有错误契约处理；不再返回由数据截断引发的 5xx | 接口级自动化测试、真实请求响应和后端日志 | 合法请求仍失败、响应契约被破坏、或通过绕开真实入口掩盖异常 |
| TA-T0015-DATA-001 | BA-T0015-INT-001 | 技术-数据级 | 综合考试创建过程中写入的考试模式、批次或关联状态必须与实体映射及 MySQL 字段类型/长度/枚举约束一致 | 合法请求写入的数据值可被数据库完整接收并可被后续流程正确读取；不扩大字段约束掩盖错误业务值，不执行生产写操作 | 数据库定义、实体/枚举映射核对、测试环境或自动化测试的落库结果、异常日志对比 | 仍发生截断、写入错误值/空值、后续读取语义错误，或修改生产数据完成验证 |
| TA-T0015-CODE-001 | BA-T0015-INT-001 | 技术-代码级 | 修改仅覆盖本次根因及必要测试，不覆盖同事改动，不修改 `uni_modules/**`，并保持其他考试模式兼容 | 目标测试、相关模块测试和构建/静态检查通过；差异中无无关删除、格式化或第三方包修改；理论考试与教员考试的既有逻辑不因本次修改改变 | Git 差异、针对性单元/集成测试、相关构建或静态检查结果 | 出现无关文件回退/删除、第三方包修改、相关测试失败或其他考试模式回归 |

## 4. 闭环项

> 没有闭环项时可以写“当前无”；闭环项不属于正式验收分类本身。

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| VC-T0015-001 | 实施代理完成修复并提交证据后，由独立验收代理按上述四项逐条复核；验收通过后提交代码并推送已有远程，任务进入待人工审核但不提前归档 | `02-任务产出/` 中的实施记录、独立验收记录、Git 提交与推送结果 |
