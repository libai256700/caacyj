# 登录与企业注册调查记录

## 1. 当前结论

- 已完成最小代码修复：企业端登录且岗位接口明确返回 `hasWtPost === false` 时，进入既有 `/pages/enterprise/register`。
- 教员人员（`hasWtPost === true`）、岗位身份未知或接口查询失败（`hasWtPost === undefined`）、非企业端仍保持当前 `/pages/home` 行为。
- 未恢复审核中、已审核、企业首页等旧分流；未修改后端、注册页、`pages.json`、依赖或 `uni_modules/**`。
- 静态分支检查、前端类型检查和 H5 构建已通过；正式测试 URL、测试账号、验证码规则及部署提交仍缺失，未执行真实浏览器登录和注册提交，不把运行态链路写成通过。

## 2. 身份字段证据

- 前端企业短信登录在 `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji\src\pages\auth\login.vue:689` 调用企业登录接口，并在 `:695` 调用岗位接口；一键登录在同文件 `:578` 使用同一岗位判断。
- 会话字段 `hasWtPost` 已存在于 `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji\src\stores\appState.ts:27` 和 `:41`，不是本次新增协议字段。
- 后端 `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji-admin-server\yunjikeji-admin-server\src\main\java\com\huiyitech\companyaudit\service\CompanyAuditServiceImpl.java:721` 根据岗位编码是否包含 `WT` 计算 `hasWtPost`，并在 `:727` 返回该字段。
- 本次把岗位查询失败从 `false` 改为 `undefined`，只对服务端明确返回的 `false` 判定非教员，避免把接口异常误导向企业注册。

## 3. 回归与修复证据

- 提交 `aa4a5030452ad59929f538e7224a0fd1398a29c4`（`feat: refine login profile and self-test experience`）删除了企业登录后的 `hasWtPost`、审核状态和注册页分流，并统一跳转 `/pages/home`。
- 当前修复仅在 `E:\huiyitechworkspace\feixingxueyuan\code\develop\yunjikeji\src\pages\auth\login.vue:713` 的登录后跳转中增加 `loginRole === 'enterprise' && hasWtPost === false` 条件，并在 `:718` 跳转既有注册页。
- 保留原有个人端审核检查和 `:735` 至 `:736` 的首页兜底；未从旧提交恢复 `/pages/enterprise/review`、`/pages/enterprise/students` 或审核状态判断。
- 提交元数据显示 `aa4a5030` 的 author 为 `wansiliang <wansiliang@huiyitech.com.cn>`；该元数据只能说明提交记录，不能据此推断具体操作者身份。

## 4. 验证记录

| 验证项 | 命令或证据 | 结果 |
| --- | --- | --- |
| 标准测试资产 | `python3 C:\Users\renquan\.codex\skills\40-0100-project-testing\scripts\init_project_testing_assets.py adhoc <任务目录>` | 通过，现有资产全部保留 |
| 分支静态断言 | PowerShell 只读检查 `redirectAfterLogin` 的必要和禁止路由 | 通过：企业+明确非教员进入注册页，其余保留首页，未恢复旧分流 |
| 类型检查 | `npm run type-check` | 通过 |
| H5 构建 | `npm run build:h5` | 通过；存在既有静态图片运行时解析警告，与本补丁无关 |
| Diff 范围 | `git diff --check`、定向 `git diff` 和 `git status` | 通过；业务代码只修改 `login.vue` |
| 真实浏览器回归 | 正式测试入口、账号、验证码规则和部署提交 | 阻塞，未执行 |

## 5. 待独立验收

1. 复核 `hasWtPost === false` 的企业登录分支进入 `/pages/enterprise/register`。
2. 复核 `hasWtPost === true`、`undefined` 和非企业端仍进入 `/pages/home`。
3. 在人工补齐正式测试入口、账号、验证码规则和部署提交后，执行真实浏览器登录与注册链路回归。
4. 复核最终差异不包含后端、注册页、`pages.json`、依赖锁文件或 `uni_modules/**`。
