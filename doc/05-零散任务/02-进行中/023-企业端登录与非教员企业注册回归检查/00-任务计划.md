# [零散任务计划]T0023-企业端非教员注册分支修复

> 本文档是当前零散任务推进的唯一正式计划入口。
> 未先建立或更新本文前，不得直接开始分析、改写、修复、验证、验收结论回写或归档。
> 本文档统一承载任务目标、正式验收入口、阶段任务总表、任务详情、门禁和人工审核闭环。正式验收标准不在这里定义，统一维护在 `01-验收标准.md`。

## 1. 任务信息

- 任务ID：`T0023`
- 任务名称：企业端非教员注册分支修复
- 创建日期：`2026-08-31`
- 当前状态：`待人工审核`
- 分支引用：`沿用当前工作分支`
- 合并状态：`不涉及独立分支合并`
- 分支处置：`不涉及独立分支删除`

## 2. 任务目标

1. 修复企业端非教员再次登录时的审核状态分流，避免已审核企业重复进入注册页。
2. 企业审核状态优先于岗位身份：已通过进入当前企业端首页，审核中进入审核进度页，未提交或可重新提交时才进入注册页。

## 3. 任务范围

- 包含：企业端登录成功后的非教员身份与企业审核状态组合判断，以及对应首页、审核进度页、注册页跳转。
- 不包含：教员登录行为调整、企业注册表单或后端接口修改、生产数据写入、`uni_modules/**` 变更；运行测试由用户执行。

## 4. 统一原则

- 先读任务详情：阶段任务总表只做导航；真正实施前，必须先阅读对应任务详情。
- 计划启动门禁：只要 `01-验收标准.md` 未收口、阶段任务总表未补齐或当前任务没有任务详情，任何实施任务都不得进入 `进行中`。
- 正式验收承载位置：正式验收标准统一写到 `01-验收标准.md`；`00-任务计划.md` 只保留验收入口和状态门禁，不平行定义第二套正式验收标准。
- 未通过场景门禁：只要正式验收项仍有 `未通过`，不得把任务状态改为 `已完成`。
- 人工审核门禁：只有“独立验收通过 + 用户明确审核通过”同时满足后，才允许执行归档和状态回写。

## 5. 正式验收入口

| 入口 | 作用 | 当前要求 |
| --- | --- | --- |
| `01-验收标准.md` | 记录本任务的业务验收、技术验收和闭环项 | 必须在实施前收口完成 |
| `02-任务产出/` | 沉淀实施结果、比对结果、校验记录和验收前证据 | 必须能定位真实产出和检查证据 |

## 6. 三阶段任务总表

### 6.1 载体与验收入口阶段

| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-001` | `建立任务载体并补齐计划信息` | `-` | `已完成` | [详见任务详情](#task-001-建立任务载体并补齐计划信息) |
| `TASK-002` | `收口正式验收入口` | `TASK-001` | `已完成` | [详见任务详情](#task-002-收口正式验收入口) |

### 6.2 实施与独立验收阶段

| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-003` | `按审核状态修复非教员企业登录分支` | `TASK-002` | `已完成` | [详见任务详情](#task-003-按审核状态修复非教员企业登录分支) |
| `TASK-004` | `独立代码复核与用户运行测试` | `TASK-003` | `待用户测试` | [详见任务详情](#task-004-独立代码复核与用户运行测试) |

### 6.3 人工审核与归档阶段

| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-005` | `正式验收通过后等待人工审核 / 用户验收` | `TASK-004` | `未开始` | [详见任务详情](#task-005-正式验收通过后等待人工审核--用户验收) |
| `TASK-006` | `归档并回写状态` | `TASK-005` | `未开始` | [详见任务详情](#task-006-归档并回写状态) |

---

## 7. 任务详情

### TASK-001 建立任务载体并补齐计划信息

**实施标准**
- 在 `doc/05-零散任务/` 正式状态目录下建立当前任务目录，并补齐 `00-任务计划.md`、`01-验收标准.md`、`02-任务产出/` 最小结构。
- 把任务ID、任务名称、当前状态、分支引用、合并状态和分支处置回写到本文档“任务信息”。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 当前任务目录
- 本文档“任务信息”与“任务范围”信息块

**关联验收项**
- `-`

**任务产出 / 结果记录**
- 已通过标准初始化脚本创建任务目录、计划、验收文件与产出目录。

### TASK-002 收口正式验收入口

**实施标准**
- 按 `20-0200-acceptance-standards-documentation` 的零散任务口径写好 `01-验收标准.md`。
- 本文档只保留正式验收入口和门禁，不平行定义第二套正式验收标准。
- 未完成本任务前，`TASK-003` 不得进入 `进行中`。

**对应技能**
- `20-0200-acceptance-standards-documentation`

**交付物**
- `01-验收标准.md`

**关联验收项**
- `-`

**任务产出 / 结果记录**
- 已按业务交互、接口、数据和代码四类口径收口到 `01-验收标准.md`。

### TASK-003 按审核状态修复非教员企业登录分支

**实施标准**
- 在现有登录成功跳转逻辑中，仅针对“企业端且非教员人员”按企业审核状态分流。
- `auditStatus === 2` 时沿用当前企业端首页；`auditStatus === 1` 或本地状态为审核中时进入审核进度页；其他可提交状态进入注册页。
- 确保刷新令牌后仍保留登录接口返回的 `auditStatus`，不修改后端、注册页或第三方包。
- 本轮不执行测试；只完成实现、独立代码复核和提交推送，运行验证由用户执行。
- 全过程持续使用 `01-验收标准.md` 驱动检查和返工。

**对应技能**
- `60-0500-engineering-issue-troubleshooting-and-stoploss`
- `40-0100-project-testing`
- `00-1600-frontend-ui-implementation`
- `00-1150-main-subagent-governance`

**交付物**
- 企业端登录页审核状态分支修复
- `02-任务产出/登录与企业注册调查记录.md`
- 自动化测试、构建或静态核对证据

**关联验收项**
- `BA-T0023-INT-001`
- `BA-T0023-INT-002`
- `TA-T0023-API-001`
- `TA-T0023-DATA-001`
- `TA-T0023-CODE-001`

**任务产出 / 结果记录**
- 已在短信登录和一键登录的令牌刷新结果中显式保留初始企业登录响应的 `auditStatus`，确保会话持久化及再次 `onShow` 分流可读取该状态。
- 已仅针对 `hasWtPost === false` 的企业端非教员分支按审核状态分流：`auditStatus === 2` 沿用当前首页，`auditStatus === 1` 或本地 `reviewing` 进入审核进度页，其他可提交状态进入注册页；教员、身份未知和非企业端分支未改。
- 本轮按用户要求未执行测试、构建、类型检查、静态断言或浏览器验证；独立代码复核已通过，提交推送后由用户运行验证。

### TASK-004 独立代码复核与用户运行测试

**实施标准**
- 由新的独立验收代理按 `01-验收标准.md` 进行只读代码复核，不得复用实施代理，也不得执行测试。
- 代码提交推送后由用户执行真实登录验证并反馈结果。
- 如果发现问题，先补正式验收标准或把对应验收项标记为 `未通过`，并记录失败现象、影响范围和重跑入口。
- 只有在正式验收通过后，才允许进入 `TASK-005`。

**对应技能**
- `40-0100-project-testing`
- `20-0200-acceptance-standards-documentation`（如需补正式验收标准）

**交付物**
- 正式验收记录
- 未通过场景收口记录（如有）

**关联验收项**
- `BA-T0023-INT-001`
- `BA-T0023-INT-002`
- `TA-T0023-API-001`
- `TA-T0023-DATA-001`
- `TA-T0023-CODE-001`

**任务产出 / 结果记录**
- 独立代码复核已通过：审核状态来源、刷新后会话保留、三类页面分流和修改范围均符合验收口径。
- 未执行测试、构建、类型检查或浏览器验证；等待用户使用真实账号验证。

### TASK-005 正式验收通过后等待人工审核 / 用户验收

**实施标准**
- 只有在 `TASK-004` 已正式验收通过后，才允许执行本任务。
- 向用户提交当前任务结果、关键证据和验收结论，并等待明确的人工审核 / 用户验收反馈。
- 没有用户明确确认前，不得把任务状态改为 `已完成`，也不得提前归档。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 用户审核通知记录
- 用户明确反馈记录

**关联验收项**
- `-`

**任务产出 / 结果记录**
- [完成后填写]

### TASK-006 归档并回写状态

**实施标准**
- 只有在“正式验收通过 + 用户明确审核通过”同时满足后，才允许执行归档。
- 回写本文档中的当前状态、当前交付物、完成证据和下一步，并把任务目录从 `02-进行中/` 移到 `03-已完成/`。
- 如果使用了正式分支，归档前同步回写 `合并状态` 和 `分支处置`。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 已回写的任务计划
- 已归档的任务目录

**关联验收项**
- `-`

**任务产出 / 结果记录**
- [完成后填写]
