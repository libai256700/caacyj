# T014 学员端首页视觉还原 - 独立验收记录

## 1. 验收说明

- 验收角色：新的独立验收代理，只读复核，不参与实施修改。
- 验收日期：`2026-08-30`
- 验收依据：`01-验收标准.md`
- 验收对象：用户目标图、正式首页实现、共享底栏视觉变体、三视口截图、构建产物和真实浏览器运行态。

## 2. 验收结论

| 验收项 | 结论 | 关键证据 |
| --- | --- | --- |
| `BA-T014-E2E-001` | `通过` | `375x812`、`390x844`、`430x932` 均完整展示欢迎区、能力评测、学习成长、难点练习、常用功能和单一固定底栏；body/html 的 `scrollWidth` 等于视口宽，无横向溢出、遮挡、错位、文字截断或重复底栏；`390x844` 归一化视觉相似度为 `97.1148%`。 |
| `BA-T014-INT-001` | `通过` | 运行态识别 8 个业务热点和 3 个底栏入口；三视口下每个入口中心的 `elementFromPoint` 均命中自身，热点相互重叠为 `0`；处理函数、服务调用、弹窗事件和路由目标未改变。 |
| `TA-T014-QUALITY-001` | `通过` | 独立执行 `npm run type-check` 退出码 `0`，`git diff --check` 通过；最终 H5 构建产物包含 `student-home` 视觉变体和新资源；三视口 fresh runtime 的控制台错误、页面错误、失败请求和 HTTP `4xx/5xx` 均为 `0`，全部首页图片资源加载成功。 |

## 3. 变更边界复核

- 正式实现仍由 `home.vue` 和共享 `HomeProfileTabBar` 组成，没有新增第二套页面或导航逻辑。
- `home.vue` 的业务脚本只调整“模拟考试”和“岗位信息”两个展示标签；`startSelfTest`、`openLatestSelfTestReport`、`startAssessmentTraining`、`startWrongReview`、`handleAssistantAction`、组织绑定与自测重置弹窗事件均未改。
- `HomeProfileTabBar.openTab` 未改，默认视觉仍供其他页面使用；学员首页只通过 `student-home` 参数选择新的底栏资产。
- 未修改服务、状态仓库、接口、路由配置或后端代码。

## 4. 辅助证据修正

- 初始 `storage-state.json` 中 uni-app 对象采用裸 JSON，直接加载会被 `uni.getStorageSync` 读成字符串并触发登录重定向。
- 独立验收使用等价的 uni-app `{type,data}` 本地预览状态完成运行态检查，全程未触发真实业务写入。
- 主代理已按该格式修正辅助 `storage-state.json`；此项不影响三项正式验收结论。

## 5. 最终结论

本次独立验收全部通过，任务可以进入用户人工审核阶段；在用户明确确认前不得归档。
