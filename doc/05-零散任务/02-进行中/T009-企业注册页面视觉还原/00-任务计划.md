# [零散任务计划]T009-企业注册页面视觉还原

> 本文档是当前零散任务推进的唯一正式计划入口。
> 正式验收标准统一维护在 `01-验收标准.md`。

## 1. 任务信息

- 任务ID：`T009`
- 任务名称：企业注册页面视觉还原
- 创建日期：`2026-08-14`
- 当前状态：`待人工审核`
- 分支引用：`沿用当前工作分支 feature/20260601000000-vphase1-initial-delivery`
- 合并状态：`不涉及独立分支合并`
- 分支处置：`不涉及独立分支删除`

## 2. 任务目标

1. 将企业端注册正式页面严格还原为用户提供的暖白、深蓝、橙色参考图。
2. 保留现有表单绑定、图片选择、校验、上传、提交、状态写入、返回和跳转行为，不改变任何业务功能与交互。

## 3. 任务范围

- 包含：企业注册页暖白航线背景、顶部导航、基础信息卡、六行表单、营业执照上传卡、上传按钮、提交按钮、移动端响应式和必要的展示性模板结构。
- 不包含：表单字段、数据模型、校验规则、上传与提交接口、状态存储、路由目标、后端代码及任何新交互。

## 4. 统一原则

- 先读任务详情：阶段任务总表只做导航；真正实施前，必须先阅读对应任务详情。
- 计划启动门禁：只要 `01-验收标准.md` 未收口、阶段任务总表未补齐或当前任务没有任务详情，任何实施任务都不得进入 `进行中`。
- 正式验收承载位置：正式验收标准统一写到 `01-验收标准.md`；本文只保留验收入口和状态门禁。
- 未通过场景门禁：只要正式验收项仍有 `未通过`，不得把任务状态改为 `已完成`。
- 人工审核门禁：只有“独立验收通过 + 用户明确审核通过”同时满足后，才允许执行归档和状态回写。
- 样式边界：`<script setup>` 必须保持不变；模板只允许为参考图排版调整展示性节点，既有事件绑定、表单绑定、条件、属性和语义必须保留。

## 5. 正式验收入口

| 入口 | 作用 | 当前要求 |
| --- | --- | --- |
| `01-验收标准.md` | 记录业务验收、技术验收和闭环项 | 已在实施前收口 |
| `02-任务产出/` | 沉淀参考图、复用盘点、视觉比对、构建与交互回归证据 | 实施与验收阶段持续更新 |

## 6. 三阶段任务总表

### 6.1 载体与验收入口阶段

| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-001` | `建立任务载体并补齐计划信息` | `-` | `已完成` | [详见任务详情](#task-001-建立任务载体并补齐计划信息) |
| `TASK-002` | `收口正式验收入口` | `TASK-001` | `已完成` | [详见任务详情](#task-002-收口正式验收入口) |

### 6.2 实施与独立验收阶段

| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-003` | `实施企业注册页面样式还原并沉淀产出` | `TASK-002` | `已完成` | [详见任务详情](#task-003-实施企业注册页面样式还原并沉淀产出) |
| `TASK-004` | `独立验收与未通过场景收口` | `TASK-003` | `已完成` | [详见任务详情](#task-004-独立验收与未通过场景收口) |

### 6.3 人工审核与归档阶段

| 任务 ID | 任务名称 | 前置任务 | 状态 | 任务详情 |
| --- | --- | --- | --- | --- |
| `TASK-005` | `正式验收通过后等待人工审核 / 用户验收` | `TASK-004` | `进行中` | [详见任务详情](#task-005-正式验收通过后等待人工审核--用户验收) |
| `TASK-006` | `归档并回写状态` | `TASK-005` | `未开始` | [详见任务详情](#task-006-归档并回写状态) |

---

## 7. 任务详情

### TASK-001 建立任务载体并补齐计划信息

**实施标准**
- 在 `doc/05-零散任务/02-进行中/` 建立 T009 任务目录和计划、验收、产出结构。
- 明确沿用当前工作分支，不建立独立分支。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 当前任务目录、计划文件与用户参考图副本

**关联验收项**
- `-`

**任务产出 / 结果记录**
- 已建立 T009 正式载体，参考图保存为 `02-任务产出/00-用户参考图.png`。

### TASK-002 收口正式验收入口

**实施标准**
- 沿用项目相邻视觉还原任务的正式口径编写 `01-验收标准.md`。
- 业务验收覆盖参考图视觉和表单行为保真，技术验收覆盖脚本、事件、接口、构建和响应式质量。

**对应技能**
- `20-0200-acceptance-standards-documentation`（当前会话未暴露该技能，沿用相邻正式任务结构）

**交付物**
- `01-验收标准.md`

**关联验收项**
- `-`

**任务产出 / 结果记录**
- 已完成正式验收入口定义。

### TASK-003 实施企业注册页面样式还原并沉淀产出

**实施标准**
- 盘点第三方组件、项目组件、现有正式页面和静态资源后，在 `src/pages/enterprise/register.vue` 单一实现上修改。
- 按原始文件 `853x1844` 参考图还原 `390x844` 主视口，并兼顾 `375x812`、`430x932` 常见移动端视口。
- 保持 `<script setup>` 不变，保留全部 `v-model`、输入属性、条件渲染、事件和按钮状态；把实施与检查记录沉淀到 `02-任务产出/`。

**对应技能**
- `00-1600-frontend-ui-implementation`

**交付物**
- 企业注册正式页面展示层样式
- `02-任务产出/01-实施与验证记录.md`

**关联验收项**
- `BA-T009-E2E-001`
- `BA-T009-DATA-001`
- `TA-T009-API-001`
- `TA-T009-QUALITY-001`

**任务产出 / 结果记录**
- 已在唯一正式页 `src/pages/enterprise/register.vue` 完成暖白橙色高保真展示层还原。
- `<script setup>` 的 CRLF 原文 SHA-256 仍为 `3eb2f5986dafa31f9e821f5a2a3c3b2b012916a2b67c65f72cf1181432c9a5e3`，六个输入绑定、上传、校验、提交、状态和路由逻辑未改。
- `390x844`、`375x812`、`430x932` 三视口截图均已生成，主视口完整高度为 `844px` 且无水平溢出。
- `npm run type-check`、`npm run build:h5`、`git diff --check` 均通过；详细记录见 `02-任务产出/01-实施与验证记录.md`。
- 用户目标要求继续向逐像素还原收敛；并排复核后仅对默认营业执照静态占位图和必要的淡色装饰做第二轮精修，保留条件是主视口像素误差较 `MAE=7.091` 客观下降且不破坏既有验收项。
- 第二轮保留从参考图精确裁取的证照本体静态占位素材；整页 `MAE` 从 `7.089939` 降至 `6.750793`，证照本体局部 `MAE` 从 `20.507688` 降至 `10.054217`，同时未改顶部装饰和已对齐几何。
- 第二轮三视口、脚本哈希、真实上传分支、控制台、资源、类型检查、H5 构建和 `git diff --check` 自检均通过，等待新的独立验收代理复核。

### TASK-004 独立验收与未通过场景收口

**实施标准**
- 由新的独立验收子代理按 `01-验收标准.md` 逐条复核真实页面，不参与实施修改。
- 检查目标视口的视觉相似度、完整性、资源、控制台、输入与上传可操作性，并核对 `<script setup>` 和业务事件未变。
- 发现问题时记录未通过项、现象和重跑入口，修复后重新验收。

**对应技能**
- 独立只读验收

**交付物**
- `02-任务产出/02-独立验收记录.md`

**关联验收项**
- `BA-T009-E2E-001`
- `BA-T009-DATA-001`
- `TA-T009-API-001`
- `TA-T009-QUALITY-001`

**任务产出 / 结果记录**
- 第二轮已由全新的独立验收子代理完成只读复验，四项正式验收标准全部通过，无未通过场景遗留。
- 独立复算确认整页 `MAE=6.750793`，证照本体局部 `MAE=10.054217`，较首轮下降 `50.97%`；三视口均无溢出、重叠或裁切。
- 脚本哈希、六字段、双上传入口、空表单校验、输入读回、真实预览、勾选态、返回入口、类型检查、H5 构建、控制台和资源均通过。
- 最终独立证据见 `02-任务产出/03-第二轮独立验收记录.md`。

### TASK-005 正式验收通过后等待人工审核 / 用户验收

**实施标准**
- 仅在独立验收通过后向用户提交页面入口、最终截图和验收结论。
- 等待用户明确确认，不提前归档。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 用户审核通知与反馈记录

**关联验收项**
- `VC-T009-001`

**任务产出 / 结果记录**
- 第二轮独立验收已通过，现提交用户人工审核并等待明确确认；用户确认前保持在 `02-进行中/`，不归档。

### TASK-006 归档并回写状态

**实施标准**
- 只有正式验收和用户审核均通过后，才回写完成状态并移动到 `03-已完成/`。
- 归档前确认同一任务只存在于一个状态目录。

**对应技能**
- `00-1200-ad-hoc-task-management`

**交付物**
- 已归档任务目录与最终计划状态

**关联验收项**
- `VC-T009-001`

**任务产出 / 结果记录**
- 未开始。
