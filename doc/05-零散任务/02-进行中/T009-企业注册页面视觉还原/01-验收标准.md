# 验收标准

> 本文只写正式标准定义，不写最近一次执行结果、最终结论和阻塞状态。

## 1. 文档信息

- 任务ID：`T009`
- 任务名称：企业注册页面视觉还原
- 计划入口：`00-任务计划.md`
- 视觉基准：`02-任务产出/00-用户参考图.png`（原始文件 `853x1844`）

## 2. 业务验收

| 验收项ID | 业务验收类型 | 任务场景 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `BA-T009-E2E-001` | 端到端业务验收 | 企业用户进入注册页面 | 页面应严格还原参考图的暖白航线背景、深蓝居中标题、左侧返回箭头、白色基础信息卡、六行左右式表单、营业执照上传卡和橙色提交按钮 | `390x844` 主视口的区域顺序、尺寸比例、圆角、阴影、间距、字号、颜色和图标接近参考；`375x812`、`430x932` 下无水平溢出、遮挡、错位或文字截断 | 在真实 `enterprise/register` 页面生成三种视口截图；主视口与参考图按尺寸归一化后并排、叠加和像素统计检查，并补充 DOM 边界测量 | 仍沿用旧蓝色视觉；关键区域缺失；主卡片、表单、上传区或按钮比例明显偏离；存在溢出、重叠、截断或异常空白 |
| `BA-T009-DATA-001` | 数据与交互业务验收 | 用户填写资料、选择营业执照并提交 | 样式改动不得改变现有六个字段、默认文件名、图片预览、拍照/相册选择、表单校验、提交中状态、审核提交或返回/跳转行为 | 六个 `v-model`、输入类型和长度限制、上传预览、两个上传入口、提交禁用/加载态、校验提示和原路由行为全部保留 | 审查模板绑定和脚本差异；在浏览器执行空表单校验、文本录入、上传按钮调用与返回入口检查 | 任一字段、绑定、属性、校验、上传来源、按钮状态、事件或导航语义发生变化 |

## 3. 技术验收

| 验收项ID | 支撑的业务验收项 | 验收类型 | 验收标准说明 | 通过条件 | 验证方式或证据口径 | 不通过判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `TA-T009-API-001` | `BA-T009-DATA-001` | 技术接口验收 | 保留企业注册页面现有状态、服务调用、兼容提交逻辑、存储写入和路由契约 | `<script setup>` 与改动前逐字一致；不得修改或新增请求、接口、状态字段、校验、上传、提交或导航代码 | 保存改动前脚本哈希并与完成后脚本比对；审查 `git diff` | `<script setup>` 内容或任何业务接口与交互契约发生变化 |
| `TA-T009-QUALITY-001` | `BA-T009-E2E-001` | 代码质量验收 | 在现有正式页面单一实现上完成，优先复用项目能力，展示层响应式稳定且无调试残留 | 类型检查和 H5 构建通过；无第二套同义页面；页面资源成功加载；三种视口无溢出、重叠或文本截断；变更限定在前端展示边界 | 审查变更范围；运行 `npm run type-check`、`npm run build:h5`、`git diff --check`；浏览器截图、资源和控制台检查 | 构建失败、资源 404、重复实现、调试残留、跨视口布局异常，或修改业务脚本与后端 |

## 4. 闭环项

| 闭环项ID | 完成标准 | 证据口径 |
| --- | --- | --- |
| `VC-T009-001` | 独立验收通过后提交用户人工审核；只有用户明确确认视觉结果后才允许归档 | 独立验收记录、最终截图、用户明确反馈与任务计划状态 |
