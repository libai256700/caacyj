# T009 企业注册页面视觉还原实施与验证记录

## 1. 实施范围

- 当前动作：`TASK-003` 实施企业注册页面样式还原并沉淀产出。
- 唯一正式页面：`D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/src/pages/enterprise/register.vue`。
- 变更边界：仅调整 `<template>` 中的展示性节点和 `<style lang="scss">`；未修改 `<script setup lang="ts">`、接口、状态、校验、上传、提交或路由逻辑。
- 复用情况：复用全局 easycom 的 `uv-icon`（`arrow-left`、`camera`、`photo`）及现有 `route-top.png`、`route-bottom.png` 航线背景资源；未新增依赖、页面或同义组件。

## 2. 视觉实现

- 页面改为暖白 `#fdfcf8` 航线背景，使用深蓝 `#122942` 正文和橙色 `#fe6313/#fe6817` 强调色。
- 按参考图比例实现居中标题、左侧返回入口、白色圆角阴影卡片和橙色竖条标题。
- 基础信息卡改为六行左右式网格；保留六个原生 `input`，统一灰色边框、尺寸、间距和占位文字样式。
- 营业执照卡实现左侧 `260rpx x 224rpx` 预览、右侧标题/动态文件名及横排拍照和相册按钮。
- 提交按钮改为橙色，页面主内容按约 `1622rpx` 总高排布，在目标视口完整呈现并保留底部安全空白。

## 3. 功能边界证明

- `<script setup lang="ts">` 改动前后 SHA-256 均为 `3eb2f5986dafa31f9e821f5a2a3c3b2b012916a2b67c65f72cf1181432c9a5e3`，哈希计算包含原始标签和 CRLF。
- 六个 `v-model`、`maxlength`、手机号 `type="number"`、全部 placeholder 保留。
- `v-if="licenseFilePath"`、`:src="licenseFilePath"`、`v-if="licenseUploaded"`、动态 `licenseFileName` 保留。
- `chooseLicense('camera')`、`chooseLicense('album')`、`submitRegistration`、`:loading="submitting"`、`:disabled="submitting"` 和动态提交文案保留。
- 浏览器验证：空表单点击提交进入原校验；企业名称可录入并读回；拍照和相册入口均打开文件选择器并可更新预览文件名；返回入口跳转到 `/pages/auth/login?identity=enterprise`。

## 4. 视口与浏览器验证

| 视口 | 文档宽度 | 文档高度 | 提交按钮底部 | 结果 |
| --- | ---: | ---: | ---: | --- |
| `390x844` | `390` | `844` | `804.41` | 无水平溢出，完整首屏 |
| `375x812` | `375` | `812` | `773.50` | 无水平溢出、遮挡或截断 |
| `430x932` | `430` | `932` | `886.89` | 无水平溢出、遮挡或截断 |

- `390x844` DOM 测量：基础卡 `y=76.44..496.59`，上传卡 `y=514.27..709.78`，提交按钮 `y=756.58..804.41`。
- 浏览器控制台：`0 error / 0 warning`。
- 背景资源：`route-top.png`、`route-bottom.png` 返回 `200/304`，未发现 `404` 或加载失败。
- 参考图归一化像素统计：`329160` 像素，`MAE=7.090`、`RMSE=26.819`、平均通道误差不超过 `7.803`，`94.69%` 像素平均通道差不高于 `30`。

## 5. 命令验证

| 命令 | 结果 |
| --- | --- |
| `npm run type-check` | 通过，退出码 `0` |
| `npm run build:h5` | 通过，`DONE Build complete`，退出码 `0` |
| `git diff --check` | 通过，退出码 `0`；仅输出工作区既有 LF/CRLF 转换提示 |
| `git diff --check -- code/develop/yunjikeji/src/pages/enterprise/register.vue` | 通过，退出码 `0`；仅输出 LF/CRLF 转换提示 |

## 6. 截图与对比产物

- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/final-390x844.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/final-375x812.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/final-430x932.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/reference-normalized-390x844.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/compare-side-by-side-390x844.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/compare-overlay-390x844.png`

## 7. 已知差异

- 顶部航线背景复用项目现有浅色飞机航线资源，和参考图中的无人机轮廓不是逐像素同一图形，但色调、层级和构图用途一致。
- 默认营业执照缩略图由真实 DOM/CSS 绘制，没有截取参考图，因此微型证照细节与参考图存在可预期差异；上传后的真实图片预览逻辑保持不变。
- 字体使用系统 `PingFang SC` / `Microsoft YaHei` 回退链，不同操作系统的字形抗锯齿可能有轻微差别。
- 当前实施验证没有已知未通过项；最终独立验收与用户人工视觉确认不在本动作中执行。

## 8. 第二轮静态视觉精修（TASK-003）

### 8.1 保留决策与范围

- 保留第二轮证照占位精修：从用户参考图精确裁取 `x=90, y=1279, width=260, height=195` 的营业执照图案本体，产出 `src/static/enterprise-register/business-license-placeholder.png`。裁取范围包含完整国徽、证照内框和印章，不包含上传卡边框、标题、文件名、按钮或其他 UI。
- 空状态改为在既有预览框内显示页面专用静态 PNG；`v-if="licenseFilePath"`、`:src="licenseFilePath"` 与上传勾选态保持原分支。浏览器选择图片后，静态占位节点计数由 `1` 变为 `0`，真实预览节点计数为 `1`，内部图片源为 `blob:`，文件名与勾选态同步更新。
- 未调整已对齐的卡片、表单、预览外框、上传按钮或提交按钮几何，也未修改功能、交互和脚本。证照替换已使整体与局部指标明显改善，因此按最小变更原则停止扩大到顶部装饰。

### 8.2 像素对比

对比对象均为同一份 `reference-normalized-390x844.png`；整页共 `329160` 像素。

| 区域 | 首轮 MAE | 第二轮 MAE | 首轮 RMSE | 第二轮 RMSE | 平均通道差不超过 30 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 整页 `390x844` | `7.089939` | `6.750793` | `26.819059` | `26.229240` | `94.6868% -> 94.9730%` |
| 证照预览外框 `x=33..167, y=570..686` | `15.226464` | `8.158827` | `32.492369` | `20.092532` | `85.8689% -> 91.8329%` |
| 证照图案本体 `x=41..159, y=585..674` | `20.507688` | `10.054217` | `38.948080` | `23.562229` | `80.3268% -> 89.1223%` |

- 第二轮整页 MAE 比首轮下降 `0.339146`（约 `4.78%`），满足必须低于 `7.091` 的保留硬条件。
- 证照图案本体 MAE 下降约 `50.98%`；人工并排检查中，国徽、证照标题、信息行、二维码与印章均更接近参考图。

### 8.3 三视口与资源验证

| 视口 | `clientWidth` | `scrollWidth` | 提交按钮底部 | 结果 |
| --- | ---: | ---: | ---: | --- |
| `390x844` | `390` | `390` | `804.41` | 无水平溢出、遮挡或截断 |
| `375x812` | `375` | `375` | `773.50` | 无水平溢出、遮挡或截断 |
| `430x932` | `430` | `430` | `886.89` | 无水平溢出、遮挡或截断 |

- 三视口浏览器控制台均为 `0 error / 0 warning`，失败请求均为 `0`。
- 占位资源 `/static/enterprise-register/business-license-placeholder.png` 返回 `200 image/png`，原始尺寸 `260x195`，文件大小 `79912` 字节。
- `<script setup lang="ts">` CRLF 原文 SHA-256 仍为 `3eb2f5986dafa31f9e821f5a2a3c3b2b012916a2b67c65f72cf1181432c9a5e3`。
- `npm run type-check`、`npm run build:h5`、页面定向及全工作区 `git diff --check` 均通过；差异检查仅输出工作区既有 LF/CRLF 转换提示。

### 8.4 第二轮截图

- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/final-v2-390x844.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/final-v2-375x812.png`
- `D:/Workspace/Work/feixingxueyuan/feixingxueyuan/code/develop/yunjikeji/output/playwright/T009-enterprise-register/final-v2-430x932.png`
