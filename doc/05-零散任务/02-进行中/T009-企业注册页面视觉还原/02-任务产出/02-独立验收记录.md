# T009 企业注册页面独立验收记录

## 1. 验收结论

- 独立验收状态：`DONE`。
- 验收日期：`2026-08-14`。
- 验收对象：真实页面 `http://127.0.0.1:5173/yunjikeji/#/pages/enterprise/register`。
- 验收基准：`00-用户参考图.png`，原始尺寸 `853x1844`。
- 验收边界：全程未修改 `register.vue`、样式、资源或业务实现；仅生成本记录和 `acceptance/` 下的独立证据。

| 正式验收项 | 结论 | 核心证据 |
| --- | --- | --- |
| `BA-T009-E2E-001` | 通过 | 三视口真实截图与 DOM 测量无水平滚动、遮挡、错位或隐藏裁切；主视口归一化对比接近参考图 |
| `BA-T009-DATA-001` | 通过 | 六字段属性、空表单校验、文本录入、两个上传入口、预览/文件名、返回入口和提交状态契约均保留 |
| `TA-T009-API-001` | 通过 | `<script setup>` CRLF 原文 SHA-256 精确匹配指定值，脚本无变更 |
| `TA-T009-QUALITY-001` | 通过 | 类型检查、H5 构建、`git diff --check` 均通过；资源、控制台、唯一页面和调试残留检查通过 |

## 2. `BA-T009-E2E-001` 视觉证据

### 2.1 三视口布局

| 视口 | `scrollWidth x scrollHeight` | 基础卡 `x/y/w/h` | 上传卡 `x/y/w/h` | 提交按钮 `x/y/w/h` | 结论 |
| --- | --- | --- | --- | --- | --- |
| `390x844` | `390x844` | `16.63/76.44/356.75/420.16` | `16.63/514.27/356.75/195.52` | `16.63/756.58/356.75/47.83` | 通过 |
| `375x812` | `375x812` | `16/73.5/343/404` | `16/494.5/343/188` | `16/727.5/343/46` | 通过 |
| `430x932` | `430x932` | `18.34/84.27/393.31/463.25` | `18.34/567/393.31/215.56` | `18.34/834.16/393.31/52.73` | 通过 |

- 三个视口均满足 `documentElement.scrollWidth === viewport width`，且页面总高等于视口高。
- 对主卡、六行表单、上传预览/详情、两个上传按钮和提交按钮检查：越出视口元素 `0`，隐藏裁切文本 `0`，相邻区域间距均为正数。
- 最窄 `375x812` 下最长标签“统一社会信用代码”的实际字形右缘与输入框左缘仍保留 `1px`，没有重叠或截断。
- `390x844` 计算样式：页面背景 `rgb(253,252,248)`，正文/标题 `rgb(18,41,66)`，提交按钮 `rgb(254,99,19)`；主卡圆角 `11.44px`，阴影 `rgba(129,91,52,0.11) 0 5.72px 14.04px`；输入框 `214.31x40.55px`、圆角 `5.72px`；提交按钮圆角 `7.28px`、字号 `17.68px`。
- 顶部和底部装饰分别加载 `route-top.png`、`route-bottom.png`，透明度为 `0.68`、`0.62`，暖白航线背景、深蓝标题、橙色强调、白卡阴影、上传区和按钮层级均与参考图一致。

### 2.2 归一化像素复算

- 将参考图独立高质量缩放为 `390x844` 后与新截图比较，共 `329160` 像素。
- `MAE=7.091`，`RMSE=26.819`；R/G/B 通道 MAE 分别为 `6.277/7.189/7.806`。
- 平均通道差不高于 `15` 的像素占 `92.20%`，不高于 `30` 的像素占 `94.69%`。
- 并排图和半透明叠加图显示：标题、两张卡、六行输入、上传区和提交按钮的关键纵向锚点与尺寸比例接近参考图，没有正式标准所列的明显比例偏离或异常空白。

### 2.3 候选差异判定

- 顶部装饰采用项目既有浅色飞机航线，而非参考图无人机轮廓的逐像素复制。正式标准要求的是“暖白航线背景”，现实现满足色调、层级和构图用途，且不影响任何关键区域，因此不构成验收失败。
- 默认证照由 DOM/CSS 绘制，微型文字纹理少于参考图，但证照边框、徽记、标题、文本线、二维码和印章语义完整，尺寸位置接近；选择图片后真实预览和勾选态正常，因此不构成验收失败。

## 3. `BA-T009-DATA-001` 功能边界证据

- 模板静态检查：`v-model` 共 `6` 个，字段依次为企业名称、统一社会信用代码、法人姓名、法人身份证号、联系人姓名、联系人手机号；`maxlength` 依次为 `60/18/20/18/20/11`，手机号 `type="number"`。
- 上传契约保留：`v-if="licenseFilePath"`、`:src="licenseFilePath"`、`v-if="licenseUploaded"`、动态 `licenseFileName`、`chooseLicense('camera')` 和 `chooseLicense('album')` 全部存在。
- 提交契约保留：`@tap="submitRegistration"`、`:loading="submitting"`、`:disabled="submitting"` 和“提交中.../提交审核”动态文案全部存在。
- 浏览器空表单点击提交后出现“请输入企业名称”；未触发网络提交。
- 企业名称录入“独立验收测试企业”后可从真实输入控件读回；浏览器 DOM 再次确认六个输入类型与长度限制正确。
- “拍照上传”和“相册上传”分别弹出文件选择器；选择独立验收图片后文件名先后更新为 `acceptance-390x844.png`、`acceptance-375x812.png`，图片预览节点和上传完成勾选态均出现。
- 返回入口实测跳转到 `/pages/auth/login?identity=enterprise`。
- 按要求未执行真实有效表单提交；审核接口调用、成功存储及 `/pages/enterprise/review?status=reviewing` 跳转由逐字不变的脚本哈希和源码审查证明。

## 4. `TA-T009-API-001` 接口与脚本证据

- 直接从当前文件读取 `<script setup lang="ts">`（包含开始/结束标签）的 CRLF 原文：`6009` 字节、`211` 个 CRLF、`0` 个孤立 LF。
- SHA-256：`3eb2f5986dafa31f9e821f5a2a3c3b2b012916a2b67c65f72cf1181432c9a5e3`，与指定基线完全一致。
- `git diff --unified=0` 的模板变更结束后，下一个变更块直接进入 `style`；脚本区无差异块。请求、状态、校验、上传、兼容提交、存储和导航代码均未变更。

## 5. `TA-T009-QUALITY-001` 质量门禁

| 检查 | 结果 |
| --- | --- |
| `npm run type-check` | 通过，退出码 `0` |
| `npm run build:h5` | 通过，退出码 `0`，输出 `DONE Build complete` |
| `git diff --check` | 通过，退出码 `0`；仅有工作区既有 LF/CRLF 转换提示 |
| `git diff --check -- register.vue` | 通过，退出码 `0`；仅有 LF/CRLF 转换提示 |
| 浏览器控制台 | 共 `5` 条信息，`0 error / 0 warning` |
| 装饰资源 | `route-top.png`、`route-bottom.png` 均为 `200 image/png` |
| 页面唯一性 | `enterprise/register` 正式 Vue 页面计数为 `1` |
| 参考图引用 | `src` 中未匹配参考图文件名、T009 产物路径或参考图背景引用 |
| 调试残留 | 页面无 `console.*`、`debugger`、`TODO`、`FIXME`、测试/演示文案 |

## 6. 记录与截图

- 独立数据：`code/develop/yunjikeji/output/playwright/T009-enterprise-register/acceptance/acceptance-data.json`
- 三视口：`acceptance-390x844.png`、`acceptance-375x812.png`、`acceptance-430x932.png`
- 视觉对比：`acceptance-reference-normalized-390x844.png`、`acceptance-side-by-side-390x844.png`、`acceptance-overlay-390x844.png`
- 交互快照：同目录 `.playwright-cli/` 内保留空表单校验、拍照选择、相册选择和返回跳转的浏览器快照及控制台日志。

## 7. 未通过项与剩余风险

- 正式验收项未发现未通过项。
- 顶部装饰轮廓、默认证照微型纹理和系统字体抗锯齿属于已量化、已人工复核的非关键差异，不降低本次独立验收结论。
- `VC-T009-001` 仍需用户对最终截图明确确认后方可归档；该人工审核闭环不属于本次独立验收失败项。
