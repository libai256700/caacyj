# T009 企业注册页面第二轮独立验收记录

## 1. 验收结论

- 独立验收状态：`DONE`。
- 验收日期：`2026-08-14`。
- 验收对象：`http://127.0.0.1:5173/yunjikeji/#/pages/enterprise/register`。
- 唯一正式标准：`01-验收标准.md`；视觉基准为 `00-用户参考图.png`（`853x1844`）。
- 验收边界：未修改 `register.vue`、CSS、PNG 资源或其他实现；仅在 `acceptance-v2/` 生成独立证据并新增本记录。未执行真实有效表单提交。

| 正式验收项 | 结论 | 独立复核摘要 |
| --- | --- | --- |
| `BA-T009-E2E-001` | 通过 | 全新采集三视口截图和 DOM 测量；无水平溢出、重叠、裁切或文字截断；区域顺序、色彩和比例接近参考图 |
| `BA-T009-DATA-001` | 通过 | 六字段与属性、空表单校验、文本录入、拍照/相册入口、真实预览分支、提交状态契约和返回行为均保留 |
| `TA-T009-API-001` | 通过 | `<script setup>` CRLF 原文 SHA-256 精确匹配指定基线，脚本和业务契约未变 |
| `TA-T009-QUALITY-001` | 通过 | 类型检查、H5 构建和 `git diff --check` 通过；资源、控制台、页面唯一性和调试残留检查通过 |

## 2. 像素与视觉证据

### 2.1 三视口

| 视口 | `clientWidth/scrollWidth` | 基础卡 `x/y/w/h` | 上传卡 `x/y/w/h` | 提交按钮 `x/y/w/h` | 越界/文本裁切 |
| --- | --- | --- | --- | --- | --- |
| `390x844` | `390/390` | `16.63/76.44/356.75/420.16` | `16.63/514.27/356.75/195.52` | `16.63/756.58/356.75/47.83` | `0/0` |
| `375x812` | `375/375` | `16/73.50/343/404` | `16/494.50/343/188` | `16/727.50/343/46` | `0/0` |
| `430x932` | `430/430` | `18.34/84.27/393.31/463.25` | `18.34/567/393.31/215.56` | `18.34/834.16/393.31/52.73` | `0/0` |

- 三个视口的文档高度均等于视口高度；关键区域间距为正，无重叠、遮挡、错位、异常空白或首屏裁切。
- `390x844` 下页面背景为 `rgb(253,252,248)`、正文深蓝为 `rgb(18,41,66)`、提交按钮为 `rgb(254,99,19)`；主卡圆角 `11.44px`，阴影 `rgba(129,91,52,0.11) 0 5.72px 14.04px`。
- 全新主视口截图 SHA-256 为 `d7f626ca37ac8c3be6ae746f53d34d19c22ca8ec01f2367f9ee8512435d526f3`，与实施阶段 `final-v2-390x844.png` 字节一致，证明当前运行页稳定复现第二轮结果。

### 2.2 独立像素 A/B

使用首轮 `final-390x844.png` 与全新 `acceptance-v2-390x844.png` 比较；区域坐标为包含端点口径。

| 对比口径 | 区域 | 首轮 MAE | 第二轮 MAE | MAE 降幅 | 首轮/第二轮 RMSE |
| --- | --- | ---: | ---: | ---: | ---: |
| 任务现有归一化基准 | 整页 `390x844` | `7.089939` | `6.750793` | `4.78%` | `26.819059 / 26.229240` |
| 任务现有归一化基准 | 证照外框 `x=33..167,y=570..686` | `15.226464` | `8.158827` | `46.42%` | `32.492369 / 20.092532` |
| 任务现有归一化基准 | 证照本体 `x=41..159,y=585..674` | `20.507688` | `10.054217` | `50.97%` | `38.948080 / 23.562229` |
| 从原图全新 Lanczos 归一化 | 整页 `390x844` | `7.082001` | `6.742782` | `4.79%` | `26.874709 / 26.291482` |
| 从原图全新 Lanczos 归一化 | 证照本体 `x=41..159,y=585..674` | `20.545658` | `10.095861` | `50.86%` | `38.953599 / 23.754324` |

- 两套独立口径均确认第二轮整页 MAE 严格低于硬条件 `7.091`，且证照本体误差约减半，像素改进成立。
- 任务现有归一化口径中，整页平均通道差不超过 `30` 的像素占比从 `94.6868%` 提升到 `94.9730%`；证照本体从 `80.3268%` 提升到 `89.1223%`。
- 人工检查并排图：标题、两卡、六行表单、上传区和按钮几何保持；国徽、证照标题、信息行、二维码与印章明显更接近参考图。

## 3. 功能边界证据

- 模板与真实 DOM 均为六个字段；`maxlength` 依次为 `60/18/20/18/20/11`，联系人手机号为 `type="number"`，其余为文本输入。
- 空表单点击“提交审核”出现“请输入企业名称”；浏览器动态请求列表为 `0`，未触发真实提交。
- 企业名称录入“第二轮独立验收测试企业”后可从真实输入控件读回。
- 空状态为占位节点 `1`、真实预览 `0`、勾选 `0`，默认文件名“营业执照.jpg”。
- “拍照上传”和“相册上传”均实际打开文件选择器。拍照选择后：占位 `0`、`blob:` 真实预览 `1`、勾选 `1`、文件名 `acceptance-v2-390x844.png`；相册选择后对应文件名更新为 `acceptance-v2-375x812.png`，其他状态相同。
- `:loading="submitting"`、`:disabled="submitting"`、`@tap="submitRegistration"` 及“提交中.../提交审核”动态文案均存在；未以真实有效表单触发审核接口。
- 返回入口实测跳转到 `/pages/auth/login?identity=enterprise`。

## 4. 资源边界

- `business-license-placeholder.png` 为 `260x195`、`79912` 字节、`image/png`，SHA-256 为 `f3706ca10069fac09ff206bc738ecdc918ec424b0d29a6de131b4d139af980ce`。
- 将用户参考图裁取 `x=90,y=1279,width=260,height=195` 后与新 PNG 逐像素比较：差异包围盒为空、四通道差值均为 `0`、像素字节完全一致。
- 直接人工查看该 PNG，仅包含证照图案本体（国徽、证照内框、标题、信息线、二维码和印章），不包含上传卡、按钮、字段、标题栏或整页 UI。
- 浏览器空态内部图片加载完成，自然尺寸为 `260x195`；该资源及 `route-top.png`、`route-bottom.png` 均返回 `200 image/png`。
- 源码仅引用该页面专用占位资源一次；上传真图后空态节点消失，不会覆盖真实 `licenseFilePath` 分支。

## 5. 接口与质量门禁

### 5.1 `TA-T009-API-001`

- 当前 `<script setup lang="ts">`（含开始和结束标签）为 `6009` 字节、`211` 个 CRLF、`0` 个孤立 LF。
- SHA-256：`3eb2f5986dafa31f9e821f5a2a3c3b2b012916a2b67c65f72cf1181432c9a5e3`，与指定基线完全一致。
- 因此状态、校验、上传、服务调用、兼容提交、存储写入和导航逻辑均保持原契约。

### 5.2 `TA-T009-QUALITY-001`

| 检查 | 结果 |
| --- | --- |
| `npm run type-check` | 通过，退出码 `0` |
| `npm run build:h5` | 通过，退出码 `0`，输出 `DONE Build complete` |
| `git diff --check` | 通过，退出码 `0`；仅有工作区既有 LF/CRLF 转换提示 |
| 浏览器控制台 | 共 `5` 条信息，`0 error / 0 warning` |
| 页面唯一性 | `enterprise/register.vue` 正式页面计数 `1` |
| 调试残留 | `console.* / debugger / TODO / FIXME / 测试 / 演示` 命中 `0` |
| 资源请求 | 占位 PNG、顶部航线、底部航线均为 `200 image/png` |

## 6. 记录与截图

- 三视口：`acceptance-v2/acceptance-v2-390x844.png`、`acceptance-v2-375x812.png`、`acceptance-v2-430x932.png`。
- 像素数据：`acceptance-v2/acceptance-v2-pixel-metrics.json`。
- 对比证据：`acceptance-v2-reference-lanczos-390x844.png`、`acceptance-v2-a-b-side-by-side-390x844.png`、`acceptance-v2-overlay-390x844.png`、`acceptance-v2-license-a-b-4x.png`。
- 交互证据：`acceptance-v2/.playwright-cli/` 中保存空表单校验、两次文件选择和返回跳转快照及控制台日志。

## 7. 未通过项与剩余风险

- 四项正式验收未发现未通过项；第二轮像素保留条件成立。
- 按要求未执行真实有效提交；审核接口成功响应、存储写入与审核页跳转由逐字不变的脚本哈希和源码审查证明，不作为本次破坏性联调项。
- `VC-T009-001` 仍需用户对最终截图明确确认后方可归档；该人工审核闭环不构成本次独立验收失败。
