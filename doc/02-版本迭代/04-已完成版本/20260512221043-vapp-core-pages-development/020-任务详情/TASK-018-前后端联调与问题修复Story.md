# TASK-018 前后端联调与问题修复 Story

## 实施标准

1. 本 Story 只作为“前后端联调与穿插人工测试”正式任务容器，实际执行以“Story 子任务清单”为准。
2. 每个子任务最多只绑定 1 个主技能，不得在单子任务内混绑多个主技能。
3. 本 Story 必须在六个页面 Story 都进入可联调状态后推进，并最终为测试阶段 `TASK-011` 提供唯一正式入口。
4. 本 Story 只承接各页面开发后的穿插人工测试、跨页联调、持续回归和测试环境部署前收口，不替代正式测试阶段与发布阶段任务。当前版本所有测试均由人工完成，人工验证结果必须回写到正式验收结论。
5. 当前版本严格执行“开发 + 人工测试 + 开发 + 人工测试”节奏；未完成当前轮人工测试回写前，不得继续堆积后续开发任务。
6. 当前阶段已完成测试环境预备部署独立验收，后续重点转入页面到接口联调、数据一致性校验和跨页持续回归，但每推进一段开发或修复后，必须先完成人工测试再继续。

## 当前前置收口结论

- `TASK-018` 当前状态：`进行中`。
- `TASK-018-01`、`TASK-018-02`、`TASK-018-03` 的文档收口可继续推进，用于补齐联调范围、执行顺序和正式验收入口。
- `TASK-018-04` 之后的部署与联调动作暂不得执行；原因是测试环境部署所需的应用级开发信息尚未补齐。
- 当前已确认测试环境正式主机为 `TEST-3DAPP-MICRO-01`，正式记录入口包括：
  - `C:\Users\13174\.devcenter\Operations\Servers\Test\info.md`
  - `C:\Users\13174\.devcenter\Operations\Servers\Test\TEST-3DAPP-MICRO-01\server-info.md`
  - `C:\Users\13174\.devcenter\Operations\Servers\Test\TEST-3DAPP-MICRO-01\AppList-01.md`
  - `C:\Users\13174\.devcenter\Operations\ApplicationConnections\Test\applications.md`

## 联调范围

### 页面范围

本 Story 联调范围固定覆盖当前版本六个正式页面：

1. `登录页`
2. `首页`
3. `练习开始页`
4. `练习页`
5. `AI深度解答页`
6. `我的页`

`首页/废除` 下旧稿不纳入本 Story 联调、不纳入正式验收。

### 后端能力范围

本 Story 只承接当前版本后端与数据库基线已经定义的能力，不在联调阶段扩写新业务范围：

1. 后端 Spring Boot 应用启动与健康检查。
2. 登录、首页聚合、练习链路、AI 深度解答、我的页聚合所需接口契约。
3. 测试库连接、建表脚本、初始化脚本和数据校验入口。
4. 前端页面调用后端接口后的成功态、失败态、空态和异常提示。

### 联调结果范围

联调完成后必须能支撑后续 `TASK-011`、`TASK-012`：

1. 六个页面均能通过真实运行入口进入。
2. 页面到接口调用链路清晰，接口地址、鉴权方式、返回结构和错误处理有正式记录。
3. 关键业务结果可在测试库或正式结果载体中核对。
4. 联调失败项有明确复跑入口，不以口头说明替代正式记录。

## 执行顺序

1. `TASK-018-01`：收口联调范围、页面顺序与依赖关系。
2. `TASK-018-02`：输出联调程序设计与执行顺序，明确页面、接口、数据、环境之间的依赖。
3. `TASK-018-03`：补齐 `061-验收标准/` 下的正式验收入口和执行详情定义。
4. `TASK-018-04`：在开发信息补齐后，按部署技能部署后端到测试环境预备现场。
5. `TASK-018-05`：后端预备现场成立后，部署前端到测试环境预备现场。
6. `TASK-018-06`：执行当前开发成果对应的页面到接口人工联调验证。
7. `TASK-018-07`：在 `TASK-018-06` 人工验证通过后，执行当前范围的关键数据一致性与库表人工校验。
8. `TASK-018-08`：在上一轮人工校验通过后，再推进下一轮开发结果的六页面持续回归与冒烟（人工执行）。
9. `TASK-018-09`：对人工测试暴露的问题执行修复后，必须先完成目标场景人工重跑复验，再允许继续后续开发或收口。
10. `TASK-018-10`：回写联调证据、人工测试结论与测试阶段入口。

## 当前正式阻塞点

`TASK-018-04` 之前必须补齐以下开发信息；未补齐前不得启动测试环境部署或联调实施：

1. `yunjikeji-server` 应用级部署入口：应用名、部署目录、启动脚本、停止脚本、日志目录、健康检查 URL、运行 profile、外部配置文件路径。
2. `yunjikeji` 前端应用级部署入口：测试环境访问路径、构建产物目录、Nginx 静态资源目录、备份与回滚口径。
3. Nginx 路由：前端静态路由、后端 API 反向代理路径、是否复用 `https://sit.lai-do.com`、是否新增二级路径或独立域名。
4. 端口占用确认：后端服务端口、前端静态服务端口或 Nginx upstream、与 `TEST-3DAPP-MICRO-01` 现有 39 个服务的冲突确认。
5. 测试库正式连接记录：`applications.md` 当前未登记 `yunjikeji` 专属测试库、账号口径、库名、脚本执行入口和只读/变更边界。
6. 鉴权与测试账号：六页面联调使用的登录方式、测试手机号/验证码或账号密码、Token 获取和失效口径。

## 对应技能

- `20-0100-requirements-documentation`
- `30-0200-story-technical-design`
- `20-0200-acceptance-standards-documentation`
- `00-1000-server-deployment-standards`
- `40-0100-project-testing`
- `40-0200-uniapp-cli-run-and-automation`

## 交付物

- 联调范围说明
- 联调程序设计与执行顺序
- 联调与人工测试正式验收项引用
- 测试环境部署前置条件清单
- 联调证据与回归边界
- 测试阶段交接入口

## 分支安排详情

- 沿用当前版本真实代码现场；执行阶段按子任务需要创建或沿用版本分支，本 Story 汇总层不预设多分支。

## 关联验收项

- `BA-VAPP-E2E-001 六页面端到端业务链路验收`
- `BA-VAPP-DATA-001 关键业务数据承接验收`
- `TA-VAPP-API-001 后端核心接口契约验收`
- `TA-VAPP-DB-001 数据库脚本与测试库验证验收`
- `TA-VAPP-QUALITY-001 前后端实现质量验收`
- `VC-VAPP-INTEGRATION-001 TASK-018 联调收口闭环`
- `VC-VAPP-TESTENV-001 测试环境部署门禁`

## Story 子任务清单

| 子任务 ID | 子任务名称 | 主技能 | 前置子任务 | 交付物 | 当前口径 |
| --- | --- | --- | --- | --- | --- |
| `TASK-018-01` | 收口联调范围、页面顺序与依赖关系 | `20-0100-requirements-documentation` | `-` | 联调范围说明 | `可推进` |
| `TASK-018-02` | 输出联调程序设计与执行顺序 | `30-0200-story-technical-design` | `TASK-018-01` | 联调程序设计 | `可推进` |
| `TASK-018-03` | 补充联调与人工测试正式验收项 | `20-0200-acceptance-standards-documentation` | `TASK-018-02` | 联调验收项引用 | `可推进` |
| `TASK-018-04` | 部署后端到测试环境预备现场 | `00-1000-server-deployment-standards` | `TASK-018-03` | 后端测试环境预备结果 | `已完成：TEST-3DAPP-MICRO-01 后端 18080、/api/health、/api/health/db 已验证` |
| `TASK-018-05` | 部署前端到测试环境预备现场 | `00-1000-server-deployment-standards` | `TASK-018-04` | 前端测试环境预备结果 | `已完成：Nginx /yunjikeji/ 与 /yunjikeji-api/api/health 已验证` |
| `TASK-018-06` | 执行当前开发成果的页面到接口人工联调验证 | `40-0100-project-testing` | `TASK-018-05` | 联调验证结果 | `待人工联调验证` |
| `TASK-018-07` | 执行当前范围的关键数据一致性与库表人工校验 | `40-0100-project-testing` | `TASK-018-06` | 数据一致性验证结果 | `待人工联调验证` |
| `TASK-018-08` | 执行通过上一轮人工测试后的持续回归与冒烟（人工执行） | `40-0100-project-testing` | `TASK-018-07` | 持续人工测试结果 | `待人工联调验证` |
| `TASK-018-09` | 收口人工测试暴露问题并完成目标场景人工重跑复验 | `40-0100-project-testing` | `TASK-018-08` | 缺陷重跑与复验结果 | `待人工联调验证` |
| `TASK-018-10` | 回写联调证据、人工测试结论与测试阶段入口 | `40-0100-project-testing` | `TASK-018-09` | Story 结论与交接记录 | `待前序验证完成` |

## 任务产出 / 结果记录

- `2026-05-13`：新增“前后端联调与问题修复 Story”正式详情文件。
- 本 Story 作为开发阶段与测试阶段之间的正式收口任务使用。
- `2026-05-13`：因 `TASK-010` 独立验收通过，本 Story 已按计划切换为当前进行中 Story。
- `2026-05-13`：完成当前可推进的前置文档收口，补齐联调范围、执行顺序、验收入口和 `TASK-018-04` 前置阻塞条件；后续部署与联调需先补齐应用级开发信息。
- `2026-05-13`：完成 `TASK-018-04/05` 测试环境预备部署结果本地收口。已知部署事实为：唯一测试服务器 `TEST-3DAPP-MICRO-01`，备份目录 `/home/soft/backup/yunjikeji-20260513083338`，后端端口 `18080`、`/api/health`、`/api/health/db`、Nginx 前端入口 `/yunjikeji/`、Nginx API 代理入口 `/yunjikeji-api/api/health` 均已完成健康检查。该记录只证明预备部署成立，不代表 `TASK-018` 联调闭环完成，也不代表当前版本正式验收通过。
- `2026-05-14`：最新前端 H5 包已从 `code/develop/yunjikeji` 重新构建并覆盖到测试环境；执行前已完成远程备份，备份目录 `/home/soft/backup/20260514065123/yunjikeji-web`，静态部署目录 `/home/soft/web/yunjikeji`。访问域名 `https://yunjikeji.lai-do.com/` 与抽样 JS 资源均返回 `200`。该记录只代表最新前端包已成功备份并部署，不代表 `TASK-018-06` 到 `TASK-018-10` 已完成。
- `2026-05-13`：`TASK-018-04/05` 已通过独立验收；当前可继续推进 `TASK-018-06` 页面到接口联调验证。
- `2026-05-13 17:18 +08:00`：测试账号 `18171171929` 已完成测试库改密与正式记录回写，独立验收已通过。执行脚本已落位到 `050-执行脚本/TASK-018/20260513171743-backup-yk-user-account-18171171929.sql`、`050-执行脚本/TASK-018/20260513171743-update-yk-user-account-password-18171171929.sql`，备份快照位于 `050-执行脚本/TASK-018/20260513171743-backup-yk-user-account-18171171929.txt`；`C:\Users\13174\.devcenter\Operations\ApplicationConnections\Test\applications.md` 已同步更新为 `18171171929 / wan123456`。该结果只代表联调测试账号环境准备已完成，不代表 `TASK-018-06` 到 `TASK-018-10` 已完成。
- `2026-05-13`：本 Story 推进口径已调整为“开发 + 人工测试 + 开发 + 人工测试”。后续联调、数据校验、持续回归和缺陷复验都必须以人工测试完成并回写为下一步开发的前置门禁，不再允许集中开发后统一测试。
- `2026-05-13`：练习主链后端接口已按当前程序设计与前端调用口径完成准备并落到 `origin/develop@5360ec93de9b116892ed5f53125b8c6a12189600`。当前已具备 `GET /api/practices/current`、`POST /api/practices/{practiceId}/start`、`GET /api/practices/{practiceId}/sessions/{sessionId}/question`、`POST /api/practices/{practiceId}/sessions/{sessionId}/answers`、`GET /api/practices/{practiceId}/sessions/{sessionId}/progress` 五个练习主链接口。
- `2026-05-13`：本轮后端补齐已同步修正 `multiple_choice` 支持、`wrongReview` 语义、匿名 session 访问拦截，并通过独立验证与服务集成测试。该结果只代表练习主链后端接口已准备好且已完成部分技术验收推进，不代表 `TASK-018-06` 到 `TASK-018-10` 的页面联调、人工复测、人工审核与 Story 总收口已完成。

## 进度总结

- `进行中：TASK-018-04/05 预备部署已独立验收通过，最新前端 H5 包已完成测试环境备份后覆盖部署并通过域名访问校验，登录相关联调已通过，练习主链后端接口已准备完成并落远端，测试账号 18171171929 改密与正式记录回写已完成；后续严格按“开发 + 人工测试 + 开发 + 人工测试”推进，TASK-018 其余页面联调/数据校验/持续回归/人工审核/结果回写任务未完成`

## 2026-05-13 login-chain supplement

- `2026-05-13 12:49 +08:00`：登录相关联调项已完成实施与独立验收。测试库正式记录已补齐到 `C:\Users\13174\.devcenter\Operations\ApplicationConnections\Test\applications.md`。
- `2026-05-13 12:49 +08:00`：登录链路相关 `TASK-017 baseline DDL + TASK-005 DDL + TASK-005 seed DML` 已在测试库真实执行完成；账号 `13800138000 / Yk@20260513` 已可用。
- `2026-05-13 12:49 +08:00`：测试环境 `https://sit.lai-do.com/yunjikeji-api/api/auth/login` 使用 `account=13800138000`、`password=Yk@20260513` 返回 `code=00000`；页面 `https://sit.lai-do.com/yunjikeji/` 已可在移动端设备模式下登录并跳转到 `#/pages/home/index`。
- `2026-05-13 12:49 +08:00`：本次只代表 `TASK-018` 中登录相关联调与数据承接已通过，不代表 `TASK-018` 全部页面联调、持续回归、缺陷重跑与 `TASK-018-10` 回写已完成。
- `2026-05-13 12:49 +08:00`：保留风险为接口直调当前仅 `account` 字段口径稳定通过，`mobile` 字段直调仍返回 `A0400 / Mobile number is invalid.`，后续回写不得误写为两种字段口径均通过。
