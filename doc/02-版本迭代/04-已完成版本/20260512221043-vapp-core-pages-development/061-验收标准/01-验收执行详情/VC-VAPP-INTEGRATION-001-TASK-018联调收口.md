# VC-VAPP-INTEGRATION-001 TASK-018 联调收口

## 验收类型

`闭环项`

## 业务场景

`TASK-018` 是开发阶段到测试阶段之间的正式收口任务，必须把联调、人工测试、缺陷复跑和测试阶段入口交代清楚。

## 验收标准说明

`TASK-018` 只有在页面到接口联调、关键数据校验、六页面持续人工测试、缺陷重跑和交接记录全部完成后，才允许作为 `TASK-011` 的前置任务完成。

## 核心对象

- `TASK-018-前后端联调与问题修复Story.md`
- `061-验收标准/`
- 联调执行证据
- 数据校验记录
- 持续人工测试记录
- 缺陷重跑记录
- `TASK-011` 测试环境部署入口

## 前置条件

1. `TASK-018-04` 至 `TASK-018-09` 全部完成。
2. 未通过项已形成明确修复与重跑记录。
3. 验收结论已回写到 `02-验收结论.md`。

## 触发动作

1. 汇总 `TASK-018` 所有子任务交付物。
2. 对照正式验收索引核对业务验收、技术验收和闭环项状态。
3. 回写 `TASK-018-10` 交接记录。
4. 确认是否允许进入 `TASK-011`。

## 期望结果

1. `TASK-018` 有完整联调证据和收口结论。
2. 所有阻塞项都有关闭、降级或待用户决策记录。
3. `TASK-011` 可以基于本文档和验收结论继续分派，不需要重新猜测联调边界。

## 当前执行记录

1. `2026-05-13`：`TASK-018-04/05` 测试环境预备部署已完成，部署目标为 `TEST-3DAPP-MICRO-01`。
2. 已回写预备部署证据：远程备份目录 `/home/soft/backup/yunjikeji-20260513083338`；后端 `18080`、`/api/health`、`/api/health/db`、Nginx `/yunjikeji/`、`/yunjikeji-api/api/health` 已验证通过。
3. 当前仍未完成：`TASK-018-06` 页面到接口联调验证、`TASK-018-07` 数据一致性校验、`TASK-018-08` 六页面持续人工测试、`TASK-018-09` 缺陷重跑、`TASK-018-10` 交接记录。
4. 当前结论：`TASK-018` 仍为进行中；预备部署完成可作为后续独立验收输入，但不能作为 `TASK-018` 或当前版本验收通过结论。
