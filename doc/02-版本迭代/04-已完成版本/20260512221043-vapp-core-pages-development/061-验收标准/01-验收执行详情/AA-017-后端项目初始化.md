# AA-017 后端项目初始化

## 验收目标

- 独立复验 `code/develop/yunjikeji-server/` 已完成 Spring Boot 后端项目初始化，且测试环境数据库配置按正式入口落位。

## 验收入口

- 工程目录：`code/develop/yunjikeji-server/`
- 启动入口：`mvn spring-boot:run`
- 健康检查入口：`http://127.0.0.1:18080/api/health`、`http://127.0.0.1:18080/actuator/health`

## 验收标准

1. 后端工程目录、构建文件、启动类和资源目录齐备。
2. 配置结构已拆分为 `application.yml`、`application-test.yml`、`application-prod.yml`，并支持 jar 外部配置文件覆盖。
3. 明文测试配置已落位到正式配置文件中，且目标为 `114.111.30.111:13306/yunjikeji`。
4. 测试库认证信息为账号 `yunji_test`、密码 `yunji8978_`。
5. 独立启动后可通过健康检查入口确认应用可用。

## 验收记录

- 工程结构已核对通过：存在 `pom.xml`、启动类、`HealthController`、`application.yml`、`application-test.yml`、`application-prod.yml`、测试类。
- `pom.xml` 使用 Spring Boot `2.7.18`，`java.version=1.8`；本机 Maven 使用 Java `1.8.0_162`，兼容通过。
- `application.yml` 与 `application-prod.yml` 不含测试库地址、账号、密码；`application-test.yml` 命中 `114.111.30.111:13306/yunjikeji`、`yunji_test`、`yunji8978_`。
- `mvn clean package` 验证通过，测试 `1` 个，失败 `0`。
- `test` profile 启动后 `Invoke-RestMethod` 访问 `/api/health` 与 `/actuator/health` 均返回 `UP`。
- `prod` profile 使用非测试占位环境变量启动后，两个健康检查均返回 `UP`，未串测试库配置。
- `Test-NetConnection 114.111.30.111 -Port 13306` 返回 `TcpTestSucceeded=True`；独立 MySQL `SELECT 1` 与 `SELECT DATABASE()` 返回 `1` 和 `yunjikeji`。
- `git rev-parse HEAD`、本地 `origin/develop` 与远端 `refs/heads/develop` 一致，均为 `38125987abd55583290ffdff4d015d5714ccd837`。
- 验收结论：`通过 AA-017`。保留项：本轮独立验收未再临时写外部 `config/` 文件做文件级覆盖实跑；外部覆盖口径以实施验证、README 示例和 Spring Boot 默认 `config/` 加载机制共同确认。`/actuator/health` 仍未启用 DB 子项，数据库可用性以独立 MySQL 查询验证为准。
