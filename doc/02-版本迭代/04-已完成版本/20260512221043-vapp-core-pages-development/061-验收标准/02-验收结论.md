# 验收结论

## 1. 当前结论

1. `[REQ-001]` 当前状态：`进行中，TASK-005 登录页开发已完成，TASK-005A 登录页人工测试与审核进行中，TASK-008 / TASK-008A 保持进行中，TASK-018 / TASK-018A 保持进行中，TASK-011 已完成，TASK-012 已进入人工正式验收中`。
2. 当前版本尚未进入测试环境正式验收通过状态，不得宣称版本验收通过。
3. 当前已确认 `AA-017 后端项目初始化` 独立验收通过；该结论只覆盖后端项目初始化、配置拆分、健康检查和本地/测试库连通验证，不等同于六页面联调通过。
4. 当前 `TASK-018-04/05` 测试环境预备部署已完成并独立验收通过，已回写部署事实与健康检查结果。
5. 当前练习主链后端接口已完成准备并推送到 `origin/develop@5360ec93de9b116892ed5f53125b8c6a12189600`，已覆盖 `GET /api/practices/current`、`POST /api/practices/{practiceId}/start`、`GET /api/practices/{practiceId}/sessions/{sessionId}/question`、`POST /api/practices/{practiceId}/sessions/{sessionId}/answers`、`GET /api/practices/{practiceId}/sessions/{sessionId}/progress`，并已完成独立验证与服务集成测试；该结论只代表后端接口准备与部分技术验收推进成立。
6. 当前 `TASK-018 / TASK-018A` 尚未完成首页、练习开始页、练习页、AI 深度解答页、我的页的联调、关键数据一致性、六页面持续人工测试、缺陷重跑和交接记录；后续页面级验证必须由人类执行并形成正式记录后才能回写状态。
7. 当前 `TASK-005 登录页 Story` 已完成账号密码登录链路的独立复验，结论为可进入人工审核；该结论代表登录页在测试环境下的真实接口与真实页面登录闭环通过，但不等同 `TASK-018` 全量联调通过或版本验收通过。

## 2. 业务验收状态

### 已通过

1. 当前无。

### 未通过

1. 当前无。

### 未执行

1. `BA-VAPP-E2E-001`：六页面端到端业务链路尚未全部在测试环境执行；当前仅登录页账号密码链路已独立验收通过。
2. `BA-VAPP-DATA-001`：关键业务数据承接尚未全部执行；当前仅登录链路测试库记录、账号种子数据、`last_login_at` 与 `yk_login_audit` 成功记录已独立验收通过。

## 2.1 页面级补充记录

1. `TASK-005 登录页 Story`：已按移动端设备模式完成独立视觉验收，设备参数为 `390x844`、`deviceScaleFactor=3`、`mobile=true`、`touch=true`、`portrait`、`iPhone Safari UA`；运行态截图证据为 `C:\Users\13174\AppData\Local\Temp\TASK-005-mobile-1778641476791.png`。
2. 本轮已使用本机浏览器移动端设备模式完成人工验收。
3. 本轮结论：登录页背景中的无人机和底部蓝色波纹已明显呈现，整体蓝白氛围、标题区、白色大卡片、主按钮、分隔线、微信入口和协议区层级基本齐全，微信绿色 logo 清晰可见；开发任务保持 `已完成`，继续由 `TASK-005A` 承接人工审核。
4. 剩余风险：本轮未实际提交登录表单验证后端响应；背景中的山体存在感相对更弱，但不构成当前进入人工审核的阻塞项。

## 3. 技术验收状态

### 已通过

1. `AA-017`：后端项目初始化独立验收通过，详见 `01-验收执行详情/AA-017-后端项目初始化.md`。
2. `TASK-018-04/05 预备部署健康检查`：唯一测试服务器 `TEST-3DAPP-MICRO-01` 上，后端 `18080`、`/api/health`、`/api/health/db`、Nginx `/yunjikeji/`、`/yunjikeji-api/api/health` 已验证通过；备份目录为 `/home/soft/backup/yunjikeji-20260513083338`。该结论只覆盖测试环境预备部署健康检查。
3. `TA-VAPP-API-001 / practice-mainline supplement`：练习主链后端接口已准备完成并落到 `origin/develop@5360ec93de9b116892ed5f53125b8c6a12189600`，当前至少包含 `GET /api/practices/current`、`POST /api/practices/{practiceId}/start`、`GET /api/practices/{practiceId}/sessions/{sessionId}/question`、`POST /api/practices/{practiceId}/sessions/{sessionId}/answers`、`GET /api/practices/{practiceId}/sessions/{sessionId}/progress`。本轮并已修正 `multiple_choice` 支持、`wrongReview` 语义和匿名 session 访问拦截，独立验证与服务集成测试通过；该结论不等同页面联调与人工验收通过。

### 未通过

1. 当前无。

### 未执行

1. `TA-VAPP-API-001`：后端健康检查与 Nginx API 代理预备入口已验证；登录接口账号密码链路已独立验收通过；练习主链五个后端接口已准备完成、已落远端并通过独立验证与服务集成测试；其余页面业务接口契约仍待联调验收。
2. `TA-VAPP-QUALITY-001`：登录链路前后端真实实现已独立复验通过；练习主链后端已确认未退回假数据方案，`multiple_choice`、`wrongReview` 和匿名 session 访问边界已按真实实现修正；其余页面实现质量需在联调与持续人工测试后复验。

## 4. 版本闭环项状态

### 已完成

1. 当前无。

### 未完成

1. `VC-VAPP-INTEGRATION-001`：`TASK-018 / TASK-018A` 仍在进行中；`TASK-018-04/05` 预备部署已完成，登录相关联调已独立验收通过，但其余页面联调验证、数据校验、持续人工测试、缺陷重跑和交接记录尚未完成。
2. `VC-VAPP-TESTENV-001`：测试环境正式部署已完成，当前访问入口、后端健康检查和测试服现有 `fly-llm` 运行入口均已具备继续验收条件，且用户已反馈“现在测试环境的效果是对的”；但 `TASK-012` 仍在人工正式验收中，尚不等同版本正式验收通过。
3. `VC-VAPP-DEFECT-001`：尚未进入正式验收缺陷收口阶段。
4. `VC-VAPP-RELEASE-001`：尚未进入发布准备与发布验证阶段。
5. `VC-VAPP-SYNC-001`：尚未进入发布后同步阶段。

## 5. 测试环境预备部署执行结果

1. 执行日期：`2026-05-13`。
2. 测试服务器：`TEST-3DAPP-MICRO-01`。
3. 备份目录：`/home/soft/backup/yunjikeji-20260513083338`。
4. 后端预备部署健康检查：端口 `18080`、`/api/health`、`/api/health/db` 已验证通过。
5. 前端与 Nginx 预备入口健康检查：`/yunjikeji/`、`/yunjikeji-api/api/health` 已验证通过。
6. 本轮结论：测试环境预备部署成立且独立验收通过，可进入 `TASK-018` 联调与 `TASK-018A` 人工复验；不得据此宣称当前版本验收通过。

## 6. 当前仍缺的正式验收内容

1. 首页、练习开始页、练习页、AI 深度解答页、我的页的端到端业务链路验收。
2. 上述非登录页面的关键业务数据承接与测试库数据核对。
3. 上述非登录页面的后端完整业务接口契约联调验收。
4. 非登录页面的前后端实现质量复验。
5. 缺陷记录、修复与目标场景重跑。
6. `TASK-018-10` 联调证据、测试结论与测试阶段入口交接记录。

## 2026-05-13 TASK-008-06 数据库执行补充

1. 正式执行时间：`2026-05-13 18:36-18:40`。
2. 正式执行入口：`mysql -h114.111.30.111 -P13306 -uyunji_test -pyunji8978_ yunjikeji`。
3. 执行前备份：
   - `050-执行脚本/TASK-008/20260513183611-backup-object-presence.txt`：执行前仅 `yk_question`、`yk_practice_record` 存在，其余 3 张目标表缺失。
   - `050-执行脚本/TASK-008/20260513183611-backup-question-and-practice-record.sql`：执行前导出 `yk_question`、`yk_practice_record`。
   - `050-执行脚本/TASK-008/20260513183611-backup-preexisting-structures.txt`：执行后补抓 5 张目标表 `SHOW CREATE TABLE` 结果，用于对照已落库结构。
4. 本轮按顺序执行：
   - `20260513190000-ddl-question-bank-schema.sql`
   - `20260513191000-dml-question-category-seed.sql`
   - `20260513192000-dml-question-import-template.sql`
5. 只读核对结果：
   - `yk_question_category` 已落库 11 个分类，分类编码齐全；终端回显存在中文编码显示异常，但脚本写入与排序结果已形成记录。
   - `yk_question_category`、`yk_question_option`、`yk_practice_session` 三张新表已创建。
   - `yk_question.category_id / question_code / correct_answer / score / sort_no / source_ref` 已落库。
   - `yk_practice_record.session_id` 已落库，`session_id` 承载结构在 `yk_practice_record` 与 `yk_practice_session` 均已存在。
   - `uk_yk_question_code`、`idx_yk_question_category_status`、`uk_yk_practice_record_session_question`、`idx_yk_practice_record_session_time` 已落库。
   - 导入模板脚本已写入 1 条示例题 `Q-OVERVIEW-0001` 和 4 条选项；该脚本当前会直接写入带 `TODO_STEM`、`TODO_ANALYSIS`、`SOURCE_REF` 的占位样例，只适合作为导入模板验证，不应直接当作正式题库数据导入。
6. 第一条路径失败证据：
   - `050-执行脚本/TASK-008/20260513183611-exec-ddl-output.txt` 记录了直接报错：`ERROR 1419 (HY000): You do not have the SUPER privilege and binary logging is enabled`。
7. 第二条路径改造结果：
   - `050-执行脚本/TASK-008/20260513190000-ddl-question-bank-schema.sql` 已改为无触发器方案。
   - `050-执行脚本/TASK-008/20260513195800-exec-ddl-native-constraints-output.txt` 表明新 DDL 已在当前账号下执行完成，未再出现 `ERROR 1419`。
   - `yk_question.question_type` 已收敛为 `ENUM('single_choice','multiple_choice','judge')`。
   - `yk_practice_session.mode` 已收敛为 `ENUM('standard','wrongReview')`，`status` 已收敛为 `ENUM('in_progress','completed','abandoned')`。
   - 已建立外键：
     - `yk_question.category_id -> yk_question_category.id`
     - `yk_question_option.question_id -> yk_question.id`
     - `yk_practice_session.user_id -> yk_user_account.id`
     - `yk_practice_session.category_id -> yk_question_category.id`
     - `yk_practice_record.question_id -> yk_question.id`
     - `yk_practice_record(session_id, user_id) -> yk_practice_session(session_id, user_id)`
8. 第二条路径只读核对结果：
   - 11 个分类已全部落库。
   - `sessionId` 承载结构已在 `yk_practice_session` 与 `yk_practice_record.session_id` 落库，并由复合唯一 / 复合外键承接会话级一致性。
   - 模板脚本已落 1 条示例题 `Q-OVERVIEW-0001` 和 4 条选项；该数据仍是占位模板验证样例，不应直接当作正式题库导入。
9. 当前剩余边界：
   - `yk_practice_record.correct_flag` 仍为 `TINYINT(1)`，当前 MySQL 5.7 下不以 `CHECK` 作为正式约束。
   - 空字符串拒绝规则仍依赖正式模板与业务写入口，不由当前库原生约束单独承担。
10. 本轮正式结论：`TA-VAPP-DB-001 通过`。当前测试库已在无触发器方案下完成正式执行、只读核对和结果回写，可作为当前账号权限边界下的正式可执行方案。

## 7. 正式结论

1. 当前版本结论：`未通过，仍处于 TASK-018 / TASK-018A 登录链路已通过但全量联调未完成的待验收阶段`。
2. 后续只有在测试环境部署门禁满足、`TASK-018` 其余页面联调闭环完成、业务验收与技术验收全部通过后，才允许把当前版本推进到测试环境验收通过；页面级验证可以人工完成，但仍需形成正式记录后才能回写状态。
3. 当前所有新增回写仅证明“练习主链后端接口已准备好并完成部分技术验收推进”；不证明 `TASK-018` 完成，不证明 `TASK-018A` 人工测试完成，也不证明版本正式验收通过。

## 2026-05-13 login-chain supplement

1. `TASK-005` 登录页账号密码登录链路已在测试环境完成独立验收，当前保持 `已完成`，并由 `TASK-005A` 单独承接人工审核。
2. 本轮独立验收已确认：`POST https://sit.lai-do.com/yunjikeji-api/api/auth/login` 使用 `account=13800138000`、`password=Yk@20260513` 返回 `code=00000`；移动端设备模式下页面可从 `#/` 登录并跳转到 `#/pages/home/index`。
3. 本轮独立验收已确认：测试库中账号 `13800138000` 存在，`yk_user_account.last_login_at` 与 `yk_login_audit` 已出现最新成功记录。

## 2026-05-13 local-login-review-retest supplement

1. `TASK-005` 已完成本地人工审核问题修复后的独立复验，当前继续保持 `已完成`，并由 `TASK-005A` 单独承接人工审核。
2. 本轮独立验收使用 `iPhone 13` 移动端设备模式在 `http://localhost:5173/#/` 复验，首屏未再出现“登录服务不可用”，背景图与微信 logo 正常显示且未见变形。
3. 本轮独立验收确认：账号/密码输入框纵向间距已拉开，登录按钮视觉已收窄；使用 `13800138000 / Yk@20260513` 勾选协议后，本地 `POST /dev-api/api/auth/login` 返回 `200 + code=00000`，并从 `#/` 跳转到 `#/pages/home/index`。
4. 本轮保留风险：本轮改用本机浏览器移动端模拟进行人工复验；控制台仍有 1 条 `404` 资源日志，当前不阻塞本 Story 进入人工审核。

## 2026-05-13 local-login-review-retest-round3 supplement

1. `TASK-005` 已完成第三轮人工审核问题修复后的独立复验，当前继续保持 `已完成`，并由 `TASK-005A` 单独承接人工审核。
2. 本轮独立验收已使用本机浏览器的 `iPhone 12` 设备模式，对真实本地地址完成现场复验；实际本地端口已由 `5173` 自动切换为 `5174`。
3. 本轮独立验收已现场确认：短密码场景提示为中文 `登录密码长度需为 6-32 位`，未再出现英文 `Password length must be 6 to 32 characters.`；微信图标在运行态中已明显放大；登录按钮继续收窄后的视觉满足本轮人工审核方向。
4. 本轮独立验收已现场确认：使用 `13800138000 / Yk@20260513` 勾选协议后，本地 `POST http://127.0.0.1:5174/dev-api/api/auth/login` 返回 `200 + code=00000`，页面成功跳转到首页。
5. 本轮保留风险：本地登录成功依赖当前 `5174` 与 `/dev-api` 代理链路在线，若端口或代理切换需重新复验。
4. 本轮保留风险：接口直调当前仅 `account` 字段口径稳定通过，`mobile` 字段直调仍返回 `A0400 / Mobile number is invalid.`；因此本轮只确认“登录链路真实通过”，不确认“两种字段口径均通过”。
5. `TASK-018 / TASK-018A` 仍不得回写为完成；当前仅登录相关联调与数据承接已通过，其余页面联调、关键数据一致性、持续人工测试、缺陷重跑与结果回写仍待继续推进。

## 2026-05-13 TASK-008-06 数据库执行补充二次收口

1. 本轮正式收口以“当前测试库实时结构取证”为准，不再以历史 rerun 命令输出代替真实落库结论。
2. 当前保留的正式执行成功证据为 `050-执行脚本/TASK-008/20260513195800-exec-ddl-native-constraints-output.txt`。
3. 本轮采用的正式取证文件：
   - `050-执行脚本/TASK-008/20260513204058-postcheck-results.txt`：证明 6 张关键表已存在、11 个分类已落库、当前正式题目表/选项表/会话表/错题表行数可读。
   - `050-执行脚本/TASK-008/20260513233000-postcheck-structure-results.txt`：证明关键字段、枚举类型、错题库唯一键、异常清单计数均已在当前测试库真实存在。
4. 本轮明确剔除的无效成功证据：
   - `050-执行脚本/TASK-008/20260513224500-exec-ddl-rerun-output.txt` 仅为 `mysql` 帮助输出，不是有效 DDL 成功执行证明；`20260513224500-exec-ddl-rerun-error.txt` 为空，不构成成功结论。
5. 当前测试库实时结构取证结论：
   - 6 张关键表 `yk_question_category`、`yk_question`、`yk_question_option`、`yk_practice_session`、`yk_practice_record`、`yk_wrong_question_book` 全部存在。
   - `yk_question.question_type` 已真实落为 `ENUM('single_choice','multiple_choice','judge')`。
   - `yk_practice_session.mode` 已真实落为 `ENUM('standard','wrongReview')`，`status` 已真实落为 `ENUM('in_progress','completed','abandoned')`。
   - `yk_practice_record` 已具备 `session_id`、`selected_answer`、`question_code_snapshot`、`question_type_snapshot`、`question_stem_snapshot`、`standard_answer_snapshot`、`question_analysis_snapshot`、`category_id_snapshot`、`category_code_snapshot`、`category_name_snapshot`、`options_snapshot_json`、`status`、`delete_status`。
   - `yk_wrong_question_book` 已具备 `user_id`、`question_id`、`latest_practice_record_id`、`latest_session_id`、`latest_selected_answer`、`wrong_count`、`status`、`delete_status`，并已存在唯一键 `uk_yk_wrong_question_book_user_question (user_id, question_id)`。
6. 当前测试库实时异常清单取证结论：
   - `invalid_question_type_count = 0`。
   - `legacy_type_inferred_count = 0`。
   - `pending_manual_confirm_count = 0`。
7. 当前数据落地现状补充：
   - `yk_question_category` 已存在 11 个正式分类。
   - 当前正式题目表为 1 条模板验证题，选项表为 4 条模板验证选项；这证明表结构与导入模板链路可用，但不等同于完整 1512 题正式导入已完成。
8. 当前剩余边界：
   - `yk_practice_record.correct_flag` 仍为 `TINYINT(1)`，当前按 MySQL 原生约束路线不以 `CHECK` 作为正式边界。
   - 完整题库内容导入仍属于后续题库内容整理动作，不属于本次 `TASK-008-06 postcheck 取证与正式回写收口` 的完成范围；本次正式结构范围不再包含 staging 表。
9. 本轮正式结论：`TA-VAPP-DB-001 通过`。含义是“题库相关库表设计整改已真实落地到测试库，且正式取证与文档回写已完成”；不含义“当前版本整体验收通过”。

## 2026-05-14 TASK-008 题库缺失补录补充

1. 本轮正式执行目标不是重做题库结构，而是把此前未成功导入的正式题库内容补录到当前测试库 `yunjikeji`。
2. 本轮补录范围共 `163` 题：
   - `无人机教员`：`155` 题，对应 `Q-INST-0001` 至 `Q-INST-0155`
   - `概述`：`5` 题，对应 `Q-OVERVIEW-0004`、`Q-OVERVIEW-0012`、`Q-OVERVIEW-0026`、`Q-OVERVIEW-0030`、`Q-OVERVIEW-0048`
   - `无人机操作注意事项`：`2` 题，对应 `Q-OP-0021`、`Q-OP-0023`
   - `无人机飞行手册、法律法规及其他`：`1` 题，对应 `Q-FMR-0050`
3. 本轮资产修正结果：
   - `163` 条题目记录已修正为可导入状态
   - `8` 道选项文本粘连异常题已按原始来源重建选项
   - 共重建 `489` 条正式选项记录
4. 本轮正式证据文件：
   - `050-执行脚本/TASK-008/20260514004446-remaining-question-asset-fix-report.md`
   - `050-执行脚本/TASK-008/20260514004610-before-target-rowcounts.txt`
   - `050-执行脚本/TASK-008/20260514004610-exec-direct-import-fixed-output.txt`
   - `050-执行脚本/TASK-008/20260514004610-postcheck-direct-import-fixed-output.txt`
   - `050-执行脚本/TASK-008/20260514004610-after-target-rowcounts.txt`
   - `050-执行脚本/TASK-008/20260514004610-remaining-question-direct-import-evidence.md`
5. 本轮导入前后结果对比：
   - 目标范围导入前激活题数：`0`
   - 目标范围导入后激活题数：`163`
   - 目标范围导入前激活选项数：`0`
   - 目标范围导入后激活选项数：`489`
   - 测试库总题数：`1310 -> 1473`
   - 测试库总选项数：`3916 -> 4405`
   - 分类总数：保持 `11`
6. 本轮 postcheck 结果：
   - `active_excluded_question_rows=0`
   - `active_duplicate_option_rows=0`
   - `missing_category_mapping_rows=0`
   - `missing_option_question_mapping_rows=0`
   - 历史重复 option key 组仍有 `5` 组，但未在当前激活数据中生效，不构成本轮目标 `163` 题的导入阻塞
7. 本轮正式结论：`TA-VAPP-DB-001 继续通过`。含义补充为“题库相关正式表结构可用，且剩余 `163` 题已在测试库真实补录完成”；不含义“练习页页面联调已完成”，也不含义“当前版本正式验收通过”。

## 2026-05-14 TASK-008 题库补录对版本结论的影响

1. 当前 `TASK-008` 相关测试库题库补录已完成，但这只解决了练习页前置题库数据缺口。
2. 练习页页面级人工测试、人工审核、前后端联调和整版测试阶段门禁仍未完成。
3. 因此，当前版本正式结论继续保持 `未通过`，本轮补录结果不构成 `TASK-018`、`TASK-018A` 或整版验收通过的依据。

## 2026-05-14 测试环境效果确认与 develop 基线补充

1. 当前测试环境部署链路已具备正式人工验收入口：`https://yunjikeji.lai-do.com/`、`https://yunjikeji.lai-do.com/yunjikeji/`、`https://yunjikeji.lai-do.com/yunjikeji-api/api/health`、`http://127.0.0.1:18080/api/health`、`http://127.0.0.1:18080/api/health/db`、`http://127.0.0.1:8088/health`。
2. `2026-05-14` 用户已明确反馈“现在测试环境的效果是对的”。当前将该反馈视为 `TASK-012` 已开始执行的人类验收事实，不视为全部正式验收项已经完成。
3. 本轮 AI 深度解答相关代码已提交并推送到 `origin/develop@6c05dca`，提交说明为 `feat: sync ai answer flow to develop`；该事实用于锁定当前测试环境联调所对应的代码基线。
4. 当前版本正式结论继续保持 `未通过`。只有在 `TASK-012` 按 `061-验收标准/` 全量回写完成后，才允许继续推进 `TASK-013`。
