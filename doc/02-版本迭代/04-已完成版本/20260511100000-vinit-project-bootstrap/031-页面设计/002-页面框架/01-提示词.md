根据基础色调指导图和页面背景参考图生成页面框架图

【参考图说明】

图片1:
基础色调指导图

用途:
用于继承页面框架的主色系统、状态色分工、层级对比和无人机教学培训产品的整体视觉口径。

图片2:
重点氛围背景图

用途:
用于在通用弱氛围背景缺失时补足页面背景参考，继承更偏品牌氛围的背景层次，但仍只作为框架承载背景参考，不替代框架结构。

【全局固定标准·所有页面100%通用、无需修改】

产品名称:
云技科技飞行学院无人机教学培训平台

产品端口:
移动端 App ，优先按竖屏手机触控体验设计。

设计规范:
页面框架阶段只保留可复用的结构骨架、信息层级、控件承载位、状态反馈区和安全留白区；不写任何具体页面内容、具体业务入口或具体学习场景。

【当前需要生成图片的具体要求】

页面框架自身要求:
输出一张移动端带底部 tab 的框架图，作为可直接使用的高保真页面框架。主体内容区用统一的浅灰色矩形块占位，干净简洁，底部操作区与底部 Tab 需要分层明确，避免混成单一工具栏。

底部 Tab 样式:
底部 Tab 每个项采用图标在上、文字在下的上下结构（图标 ⊥ 文字），三个 Tab 项"首页 / 练习 / 我的"均需同时包含图标和文字

画面应干净、可承载前景 UI、无水印、无文字、无边框、无设备外壳、无截图样式。

【严格禁止项】

禁止文字标注:
画面中不得出现任何说明性文字、标注、箭头、引导线、注释或功能说明。不得在任何区域添加"这是导航栏""这是内容区""这是 Tab 栏"等解释性文字。整体画面保持干净，如同最终产品截图。

禁止手机外壳:
画面不得出现手机外框、手机壳、设备模型或任何模拟真实手机外观的装饰元素。输出内容就是页面本身，不是套在手机模型里的效果图。

禁止骨架屏占位:
主体内容区不得使用骨架屏（Skeleton Screen）样式的占位块、闪烁动画感图形、圆形/方形交替占位或任何模拟内容加载态的视觉元素。主体内容区统一为干净的浅灰色矩形块。

【尺寸要求】

比例:
9:16

分辨率:
780x1387
