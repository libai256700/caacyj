# 验收标准索引

## 验收范围

1. 应创建目录全部存在。
2. 应创建入口文件全部存在。
3. 不存在项目级 `AGENTS.md`。
4. 不存在 `.git`。
5. 未创建真实业务代码仓库，只存在 `code/develop/` 与 `code/worktree/` 根目录。
6. `02-版本详细计划.md` 包含版本代码现场、正式验收入口、目录结构、六阶段任务总表和初始化任务当前结果。

## 验收执行入口

| 验收项 | 执行入口 | 期望结果 |
| --- | --- | --- |
| 目录清单 | `01-验收执行详情/` | 全部存在 |
| 入口文件清单 | `01-验收执行详情/` | 全部存在 |
| 禁建项 | `01-验收执行详情/` | `AGENTS.md` 和 `.git` 不存在 |
| 计划关键章节 | `02-版本详细计划.md` | 关键章节完整 |
| UI 设计目录规范 | `031-页面设计/` | 固定根目录、参考图来源、9:16 / 780x1387 入参、核心页面文件、校验脚本 exit code 0 均满足要求 |

## 当前状态

- 已完成本次独立验收：答题页提示词满足“以展示内容为主、布局交给生图智能体决定”的要求；本轮无必须同步开发中心事项。
