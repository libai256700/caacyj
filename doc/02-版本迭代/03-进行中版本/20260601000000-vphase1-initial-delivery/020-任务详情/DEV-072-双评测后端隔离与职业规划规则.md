# DEV-072 双评测后端隔离与职业规划规则

## 实施标准

- 上游规格：`REQ-052`、`PD-052`、接口/数据/规则验收项，以及DEV-074/077真实初始化回执。
- 分类13旧无参 completed/latest-status（或当前真实latest入口）/reset/start/record/report/regenerate 的请求、响应和语义，以及`SelfAssessmentV3RuleEngine`和 `ASSESSMENT_AGENT_INFO_ID=2L`路径全部保持不动。
- 分类14新增平行 completed/latest-status/reset/start/record/report/regenerate、末题统一提交和报告能力；不得改旧无参latest，不得实现逐题实时提交、翻页等待保存或续答恢复。末题统一提交只复用时机，不能复用分类13选填放行、答案校验或报告语义。
- 所有按步骤取数、步骤详情、步骤下题目、题目反查步骤及 `step_id` 关联必须使用 `yj_practice_step.category_id` 过滤；分类13固定13并保持原结果，分类14固定14。分类14题目必须指向分类14步骤，缺少步骤或跨分类关联时拒绝启动、提交和报告。
- 不得以运行时代码兼容测试库已确认的18错误题干、6缺失步骤和47个空`step_id`；这些数据必须先由DEV-071/074在成功备份后手工原子修复并回读。
- 分类判断经 `result.record_id -> record.category_id(catalogBatchId) -> catalog_batch.category_id`；不得直接比较record.category_id与13/14。
- 分类14实现独立职业规划规则：按源 `formVersion=2` 将47题固定重建为1-9基本情况、10-13经历与成果、14-25 RIASEC、26兴趣方向、27-38工作风格、39-40价值观、41-47目标计划；仅姓名、成果经历、12题RIASEC和恰3项价值必填。工作风格12题按源题目配置作选填处理，已作答题目必须合法，未作答题目不得被分类13通用 `is_required` 分支拦成“答案不能为空”；least不得与Top3重复，其他字段按源schema可选。不得使用分类13通用 `is_required` 分支判定分类14空答，也不得让源必填空答进入写入并报“答案不能为空”。
- 服务端必须由独立 `CareerPlanningRuleEngine`（或等价独立策略）从原始答案重建`mode=self/formVersion=2`，按源规则重算RIASEC和四维风格，拒绝客户端派生结论；生成报告时只将重算事实放入`<submitted_data>`，使用id=3 `SELF_SYSTEM_PROMPT`和既有`practice_assessment`模型。模型HTML必须通过白名单、职业报告三固定标题、300-3500可见字符和源安全规则校验；有警告或错误即待复核、不发布。
- DEV-077完成合法id=3初始化和回读后，运行时只以该有效前置记录为依据，固定按 `CAREER_ASSESSMENT_AGENT_INFO_ID=3L`读取。禁止按agent_id/name选择，运行时不执行Agent冲突判断、插入、更新或换号。
- 模型继续来自 `yj_ai_model_config` 的 `practice_assessment` 场景。

## 对应技能

- `30-0100-development-task-breakdown`
- `40-0100-project-testing`

## 交付物

- 分类14后端平行接口/服务、职业规划源表单重建与独立规则/报告校验、固定主键Agent只读调用和目标自动化测试；分类13零回归证据。

## 分支安排详情

- 前置任务：DEV-070、重新完成的DEV-071、DEV-074（含源 `web` 步骤/题目重复/`step_id`回读）和DEV-077；不改 `uni_modules/**`，不引用任一初始化工具。

## 关联验收项

- `AC-CAREER-ASSESS-002/101/202/301`。

## 任务产出 / 结果记录

- 返工中；已发现现有分类14实现错误复用分类13选填规则，导致空答案进入后端并出现“答案不能为空”。在完成源表单映射、职业规划专属规则引擎、报告校验及分类13回归前，现有实现不得视为完成；DEV-074分类14源步骤/题干/`step_id`回读和DEV-077合法id=3回读仍是实施门禁。
- 2026-08-30 新增报告读取性能返工：实测已生成职业报告首次进入约 `13.9s`、单次详情请求约 `6.69s`，根因为职业报告专用路由仍复用完整 `getRecordDetail` 并重建统计与逐题答案。先建立失败契约，再把职业报告路由改为分类14轻量投影；不得调用完整详情、题目统计或逐题答案重建，保持用户归属、分类14、完成态、`PENDING/SUCCESS/FAILED` 与重试语义；完成后由独立验收执行连续三次真实接口计时。

## 进度总结

- 当前状态：进行中（返工）。
