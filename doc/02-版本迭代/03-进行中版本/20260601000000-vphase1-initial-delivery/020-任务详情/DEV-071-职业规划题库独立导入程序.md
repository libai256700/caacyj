# DEV-071 职业规划题库独立导入程序

## 实施标准

- 上游规格：`PD-052`第2.5、6.1节和 `AC-CAREER-ASSESS-201/301`。
- 新增独立Python手工程序，从 `D:\飞机\-APP--main (4)\-APP--main\skills\career-planning-coach` 提取分类14源 `web` 步骤、题目、选项和规则映射；每个题目节点必须回填至同分类步骤的 `step_id`。
- 程序可放项目仓库独立人工工具目录，但不得进入业务源码模块，也不得被启动、依赖注入、migration、build hook、资源初始化、定时、发布或业务接口引用。
- 程序只连接当前程序同库 `114.111.30.111:13306/yunjikeji`；连接参数由人工显式传入，凭据不入代码、回执或Git。
- 已确认源 `web/index.html:27-34,40-108` 有6步骤，题序区间依次为1-9、10-13、14-26、27-38、39-40、41-47；现库分类14 47题 `step_id` 全为空。
- 已确认“姓名”排序2-7、9-13、41-47共18条并非源重复，而是 `career_question_import.py` 跨label正则错误。重开本任务必须实现并验收：备份成功后才可显式 apply，以单事务原子修复18条题干、写入6步骤、回填47个 `step_id`；备份失败或回读差异必须零修改/整体回滚，不得由运行时处理掩盖。

## 对应技能

- `00-0350-sql-script-standards-definition`
- `30-0100-development-task-breakdown`

## 交付物

- 独立题库与步骤修复程序、6步骤/18题干/47关联解析测试、备份成功前置、原子apply说明和回执模板；本任务不执行数据库写入。

## 分支安排详情

- 前置任务：DEV-070正确红灯；不修改业务源码、数据库连接配置、分类表或 `uni_modules/**`。

## 关联验收项

- `AC-CAREER-ASSESS-201/301`。

## 任务产出 / 结果记录

- DEV-070第三轮独立验收PASS后，本任务曾完成仅题目/选项范围的独立Python人工工具实现。
- 用户新增 `yj_practice_step.category_id` 隔离、源 `web` 步骤写入和题目 `step_id` 回填要求；源和测试库只读证据已确认6步骤、47个空`step_id`及18条跨label正则错误题干，原工具未覆盖该范围，故重新打开本任务。
- 本轮只允许补齐工具和静态/解析验证；不得连接数据库、执行dry-run或apply，不得进入业务源码、启动、构建或运行链路。

## 进度总结

- 当前状态：进行中；完成后才重新放行DEV-074的源核验、备份与人工 apply。
