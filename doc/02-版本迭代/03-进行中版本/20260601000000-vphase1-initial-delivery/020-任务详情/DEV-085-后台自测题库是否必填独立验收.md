# DEV-085 后台自测题库是否必填独立验收

## 实施标准

- 上游规格：`DEV-084` 与 `AC-ASSESS-REQUIRED-001/101/201/301/401`。
- 独立验收代理不得修改业务实现，只复跑接口、数据、代码质量和浏览器交互验证。
- 浏览器验证使用 Playwright；覆盖列表“是否必填”列、编辑回显、切换保存刷新、新增默认值与现有操作回归。
- 未取得测试环境所需账号或入口时，按正式记录核对；正式记录缺失则请求用户干预，不猜测凭据。

## 对应技能

- `40-0100-project-testing`
- `00-1150-main-subagent-governance`

## 交付物

- 独立验收结论、命令回执、页面交互证据和未关闭缺陷清单。

## 分支安排详情

- 前置任务：`DEV-084`。
- 执行责任：不同于 DEV-084 实施代理的独立验收代理。

## 关联验收项

- `AC-ASSESS-REQUIRED-001/101/201/301/401`。

## 任务产出 / 结果记录

- 独立复跑后端聚焦测试 `4/4 PASS`、`pnpm build:local`、`git diff --check` 和 `uni_modules/**` 边界检查，均通过。
- `pnpm ts:check` 因仓库既有跨模块错误退出 2，本任务 `config.ts/index.vue` 精确筛选零命中。
- Playwright 受控接口桩验证真实前端的列表列、是/否行值、编辑回显、下拉选项、false 载荷、新增默认“是”和原操作渲染，退出码 0。
- 正式连接记录无管理后台测试账号/登录步骤，本机 48080 后端未运行；未调用真实 create/update、未连接数据库、无真实数据变更。
- 正式证据：`061-验收标准/03-测试验证/DEV-085/`。

## 进度总结

- 当前状态：待测试环境验收；`AC-ASSESS-REQUIRED-201/401` 未完成，不能宣称整体闭环。
