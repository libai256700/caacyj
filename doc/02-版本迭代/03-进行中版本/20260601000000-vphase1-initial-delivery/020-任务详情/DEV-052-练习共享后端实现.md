# DEV-052 练习共享后端实现

## 实施标准
- 按 `PD-050` 最小修改七个现有 DO、通用服务、后台/APP Service、Mapper、原生 JDBC 与动态资源。
- 9 张运行时表 SQL 不再使用或写入 tenant；保留用户/测评结果业务关系、认证门禁和非目标表租户机制。
- 更新当前版本 `050-执行脚本/20260610-yj-practice-data-migration.sql`，移除目标表 INSERT 的 tenant 列和值；旧零散任务历史 SQL / 回滚不改。
- 为错题唯一键及存在且继续保留的兼容表新增随版幂等 DDL、校验和回滚入口，不执行数据库、不新增兼容表 Java 路径。
- 错题重复门禁未通过时不得实施唯一键调整或清理数据。
## 对应技能
- `30-0100-development-task-breakdown`
## 交付物
- 后端实现、当前版本迁移 SQL 修订、必要的随版幂等 DDL/回滚/校验、实现自检记录。
## 分支安排详情
- 复杂模式；代码现场 `code/develop/`；当前分支 `feature/20260601000000-vphase1-initial-delivery`；禁止修改 `uni_modules/**`。
## 关联验收项
- `AC-PRACTICE-SHARED-101`、`201`、`301`
## 任务产出 / 结果记录
- 七个目标 DO 已切换为 `BaseDO + @TenantIgnore`；后台、APP、Mapper、原生 JDBC 与动态资源中的目标租户条件和插入赋值已移除。
- 当前版本迁移 SQL 已移除目标表 `tenant_id` 写入；随版前向/回滚 DDL 已补齐错题唯一键调整、全量重复门禁和兼容表未知复合索引保护。
- 用户/测评结果业务关系、认证门禁、`agent_info` 及其他非目标表租户机制保持不变；未执行数据库。
## 进度总结
- 当前状态：已完成。
- 当前结论：实现与 SQL 静态自检及本地独立验收通过；数据库脚本仍须经备份、审批并在目标环境执行。
