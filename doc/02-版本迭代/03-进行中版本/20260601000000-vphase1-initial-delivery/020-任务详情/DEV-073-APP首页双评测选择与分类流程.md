# DEV-073 APP首页双评测选择与分类流程

## 实施标准

- 上游规格：`REQ-052`、`PD-052`和 `AC-CAREER-ASSESS-001/002/101/301`。
- 首页继续只保留原单个“开始评测”按钮；点击后才在当前首页弹出选择层，“自我评测”直接调用修改前 `startSelfTest`，“职业规划评测”调用分类14平行入口，不新增首页常驻并列按钮或独立选择页面。
- 首页继续只保留原单个“评测结果/自测报告”入口；点击后才在当前首页弹出报告选择层，“自我评测报告”直接调用修改前 `openLatestSelfTestReport`，“职业规划评测报告”调用分类14报告入口。两层关闭、取消或点击遮罩均不得触发查询、导航、重试、重置或其他业务动作。
- 两类答题均一题一页、非末题只 `goNext`、末题统一提交全部答案；不得新增翻页保存、续答或恢复逻辑。分类14页面按源 `formVersion=2` 的题序映射显示和校验：姓名、成果经历、12题兴趣、恰3项价值必须在前端阻止空提交；工作风格题目按源题目配置作选填处理，已作答题目必须合法，未作答题目不得拦成分类13必填错误；末题点击后直接统一提交分类14自身答案，提交请求发出后立即显示与分类13一致的等待弹框，等待期间的关闭、成功、失败和可否重新进入行为严格对照当前分类13生效代码；least不得与Top3重复；其余字段按源schema可选。分类13现有`is_required`选填语义不进入分类14。
- 保护分类13原页面、参数、请求顺序、提示、跳转、报告空状态/失败/重试行为；分类14显示和请求只使用自身分类上下文，并只展示经分类14步骤/题目 `step_id` 回读核验的数据。
- 三档移动视口下原单入口、弹层和层内选项不重叠；浏览器验证按项目规则优先Playwright；不修改 `uni_modules/**`。

## 对应技能

- `00-1600-frontend-ui-implementation`
- `40-0100-project-testing`

## 交付物

- 两个原首页单入口点击后出现的选择层、分类14评测/报告平行入口、分类13保护性回归、类型检查、构建和浏览器交互测试。

## 分支安排详情

- 前置任务：DEV-070、重新完成的DEV-071、DEV-074（源 `web` 步骤、题干节点和`step_id`回读）、DEV-077及DEV-072；前端只改首页、评测相关页面/服务白名单。分类14不得用题干文本猜字段或在前端计算兴趣/风格结论，后端为唯一权威计算端。

## 关联验收项

- `AC-CAREER-ASSESS-001/002/101/301`。

## 任务产出 / 结果记录

- 返工中；现有选择层后职业规划流程错误放行源必填空答，未通过分类14源表单验收，不能作为完成产出。

## 进度总结

- 当前状态：进行中（返工）。
