# DEV-034 APP首页系统栏快捷栏移除与暖色替换

## 实施标准

- 从真实首页删除页面内部模拟的时间、信号、Wi-Fi、电量状态栏；APP 系统状态栏由设备系统负责，不在页面重复绘制。
- 删除“回访话术 / 知识库 / 公众号 / 图片生成 / 数字分身”五项快捷入口整行，并清理只服务该行的无用数据与样式。
- 首页背景保持现有 `linear-gradient(180deg, ... 0, ... 33%, ... 76%)` 函数、方向、色标位置和作用范围不变，只把蓝色颜色字面量替换为登录页正式暖色值；外围底色同步使用登录页暖白色值。
- 除上述两行删除和背景色值替换外，不得改变卡片结构、边缘、圆角、配图、间距、文案、真实导航、三项底栏或独立参考预览页。
- 在 `360x800`、`390x844`、`430x932` 三档视口验证无异常空白、重叠、裁切、横向滚动或底栏遮挡。

## 对应技能

- `00-1600-frontend-ui-implementation`
- `40-0100-project-testing`
- `visual-verdict`

## 交付物

- 最小范围首页样式与结构补丁。
- 三档截图、结构断言、样式差异和质量命令证据。

## 分支安排详情

- 沿用 `feature/20260601000000-vphase1-initial-delivery`。
- 实施白名单：`code/develop/yunjikeji/src/pages/home.vue` 与 `DEV-034` 正式计划、验收、测试证据。
- 禁止修改 `home-reference-preview.vue`、参考页专用素材、旧首页、登录页、业务服务和 `uni_modules/**`。

## 关联验收项

- 业务验收：`AC-HOMETONE-001`、`AC-HOMETONE-002`
- 技术验收：`AC-HOMETONE-101`
- 闭环项：`AC-HOMETONE-201`

## 任务产出 / 结果记录

- 2026-07-18 用户要求移除页面模拟系统状态栏和五项快捷入口整行，并将蓝色背景换成登录页橙色，同时只允许改变颜色编码，不影响渐变等其他背景样式。
- 实施仅修改 `code/develop/yunjikeji/src/pages/home.vue`：删除两块目标 DOM、对应数据与专用样式，收紧对应占用空间；外围改为 `#F5F0EA`，三段渐变精确保持 `180deg` 与 `0 / 33% / 76%`，颜色替换为 `#F7A16A / #F8DCC8 / #F5F0EA`。
- 独立验收完成 Chrome DevTools `390x844` 与 Playwright `360x800`、`390x844`、`430x932`；目标 DOM 和六项精确文本计数均为 `0`，三档无异常空白、重叠、裁切、溢出或底栏遮挡，视觉判定 `96/pass`。
- `pnpm type-check`、`pnpm run build:h5`、`git diff --check` 均退出码 `0`；证据位于 `061-验收标准/03-测试验证/DEV-034/`。

## 进度总结

- 当前状态：已完成。
- 当前结论：`AC-HOMETONE-001` 至 `AC-HOMETONE-201` 全部通过，无未关闭缺陷；功能与正式证据提交 `e8adb0e1` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`。
