# DEV-082 企业端教员我的页真实资料最小实现

## 实施标准

- 上游规格：`REQ-053`、`PD-053`、`AC-PROFILE-001/101/201/301` 与 DEV-081 正确红灯证据。
- 后端原位扩展 `/app-api/yj/company-auth/me`：在当前企业账号已绑定 tenantId 上下文按登录手机号唯一查询 `AdminUserDO`，返回昵称并按其 `deptId` 返回部门名称；缺失关联受控为空。
- 禁止复用 `/post-codes` 跨租户候选逻辑；不改数据库、不改账号绑定。
- 前端只在企业分支调用 `/me` 并显示 `nickname/deptName`；手机号继续使用 session 与现有 mask；学员及其他角色不变。
- 仅做使 DEV-081 红灯转绿的最小实现，不修改 `uni_modules/**`。

## 交付物

- 后端接口最小改动、前端企业分支最小改动、红灯转绿记录、类型检查和差异白名单。

## 分支安排详情

- 前置任务：`DEV-081` 正确红灯确认。
- 执行责任：实施代理；不得由该代理承担 DEV-083 独立验收。

## 关联验收项

- `AC-PROFILE-001/101/201/301`。

## 任务产出 / 结果记录

- 尚未执行；当前业务代码零改动。

## 进度总结

- 当前状态：未开始。
