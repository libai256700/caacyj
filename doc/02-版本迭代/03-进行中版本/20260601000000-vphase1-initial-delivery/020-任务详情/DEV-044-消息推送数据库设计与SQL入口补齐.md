# DEV-044 消息推送数据库设计与SQL入口补齐

## 实施标准

- 数据库设计必须覆盖消息主表、收件人表、设备表、投递记录表以及必要索引、状态字段与租户隔离字段。
- SQL 脚本入口必须继续复用当前版本 `050-执行脚本/` 正式入口，不另建临时 SQL 目录。
- 未落实表结构、幂等规则和测试库验证前，不得推进后端正式实现。

## 对应技能

- `30-0500-database-management`
- `00-0350-sql-script-standards-definition`

## 交付物

- 消息推送库表设计说明
- `050-执行脚本/20260809120000-ddl-create_yj_push_tables.sql`
- `050-执行脚本/00-脚本索引.md` 对应登记事实（只读核对，不在本任务越权修改）

## 分支安排详情

- 当前版本代码现场模式：`复杂模式`
- 当前代码现场：`code/develop/`
- 当前分支：`feature/20260601000000-vphase1-initial-delivery`
- 本任务要求同步维护 SQL 正式入口，不接受散落脚本

## 关联验收项

- `AC-PUSH-101`
- `AC-PUSH-102`

## 任务产出 / 结果记录

- 历史初始状态：2026-08-09 首次定位 SQL 入口 `050-执行脚本/20260809120000-ddl-create_yj_push_tables.sql` 时，文件仍为待填写骨架；对应脚本索引也尚未登记。
- 当前静态资产事实：SQL 入口、脚本索引与程序设计引用链路已完成静态核对并通过 `VERDICT PASS`；当前阻断点不在静态资产缺失，而在测试库正式执行入口未落位。
- 2026-08-10 当前静态技术资产独立验收已通过：`PD-042` 与 DDL 入口骨架可定位，静态结构核对 `VERDICT PASS`。
- 2026-08-10 测试库执行仍未完成：正式 Test 记录已具备目标库与认证字段，但实际可执行入口仍是 `<JdbcRunner>` 占位；当前缺少可执行 Runner / 连接命令，以及备份、DDL 执行、幂等重跑、后置校验四类正式命令，当前不得宣称数据库实施闭环完成。
- 当前硬门禁：未补齐 Test 正式入口并完成真实测试库执行前，不得把 `DEV-044` 回写为 `已完成`，也不得启动 `DEV-045`。

## 进度总结

- 当前状态：进行中。
- 当前结论：`DEV-044` 已进入数据库门禁处理阶段，静态验收通过但真实测试库执行被正式入口占位阻断；后续人工依赖是补齐 Test 正式入口与执行命令，`DEV-045` 及之后任务继续保持 `未开始`。
