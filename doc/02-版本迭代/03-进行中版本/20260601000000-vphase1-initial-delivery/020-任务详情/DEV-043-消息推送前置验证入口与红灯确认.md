# DEV-043 消息推送前置验证入口与红灯确认

## 实施标准

- 必须先建立消息发送、设备绑定、消息列表、已读回流、推送点击回流等前置验证入口，再进入实现。
- 红灯结果必须能准确说明当前实现缺失点，不能拿空脚本或伪造返回冒充 TDD。
- 如后端接口未具备，至少先落脚本级或接口契约级红灯，再推进 `DEV-045`。

## 对应技能

- `40-0100-project-testing`
- `30-0100-development-task-breakdown`

## 交付物

- 消息推送前置验证入口清单
- 红灯执行命令与预期失败原因

## 分支安排详情

- 当前版本代码现场模式：`复杂模式`
- 当前代码现场：`code/develop/`
- 当前分支：`feature/20260601000000-vphase1-initial-delivery`
- 红灯验证只允许建立在真实代码与真实配置边界上

## 关联验收项

- `AC-PUSH-101`
- `AC-PUSH-102`

## 任务产出 / 结果记录

- 历史初始状态：在本任务开始前尚未建立消息推送红灯验证脚本；任务详情先行锁定该门禁，避免后续直接跳过前置验证。
- 历史推进记录：2026-08-10 `TASK-016` 与 `DEV-042` 完成后，本任务先回写为 `进行中`，随后围绕消息发送、设备绑定、消息列表、已读回流与点击回流补齐前置验证入口并完成独立验收。

## 进度总结

- 当前状态：已完成。
- 当前结论：`DEV-043` 最终独立验收 `VERDICT PASS`；红灯验证资产、AST 结果、动态正反例与脚本退出码均符合“红灯阶段完成”口径，允许推进 `DEV-044`；功能验收项 `AC-PUSH-*` 仍不得标记通过。

## 2026-08-10 结果记录

- 已在 `061-验收标准/03-测试验证/DEV-043/` 建立正式测试资产：`TC-CODE-PUSH-001-消息推送源码与API契约红灯.md`、`notification-contract-checklist.md`、`verify_notification_contract.py`、`red-result.json`、`test-conclusion.md`。
- 已同步 `061-验收标准/03-测试验证/03-代码级测试索引.md` 与 `00-测试验证索引.md`。
- 执行 `python -m py_compile E:\huiyitechworkspace\feixingxueyuan\doc\02-版本迭代\03-进行中版本\20260601000000-vphase1-initial-delivery\061-验收标准\03-测试验证\DEV-043\verify_notification_contract.py`，退出码 `0`。
- 执行 `python E:\huiyitechworkspace\feixingxueyuan\doc\02-版本迭代\03-进行中版本\20260601000000-vphase1-initial-delivery\061-验收标准\03-测试验证\DEV-043\verify_notification_contract.py --workspace-root E:\huiyitechworkspace\feixingxueyuan --output E:\huiyitechworkspace\feixingxueyuan\doc\02-版本迭代\03-进行中版本\20260601000000-vphase1-initial-delivery\061-验收标准\03-测试验证\DEV-043\red-result.json`，退出码预期并实际为 `1`。
- 当前 `red-result.json` 统计：`16` 项检查，`0` 项通过，`16` 项未通过。
- Provider 正反例验证：反例 `NoopPushProvider.java + FooBridge.java` 返回 `False`；正例 `PushProvider.java + NoopPushProvider.java + UniCloudHttpPushProvider.java` 返回 `True`。
- 本轮已修正独立验收指出的误判缺陷：`PushProvider` 改为 basename 精确匹配；管理端前端 API 补齐 `get/retry`；App 契约改为 PD 正式 REST 路径 `/{id}`、`/{id}/read`、`/{id}/open`；后端契约判定兼容 Spring 类级+方法级映射；管理页面判定改为候选路径+特征。
- 红灯失败项来自真实缺失实现：后端父 POM 未声明 `yunjikeji-module-notification`、模块 POM/控制器/service/API 路径缺失，且未见 `PushProvider`、`NoopPushProvider`、`UniCloudHttpPushProvider/Bridge`；管理平台推送 API 与页面缺失；App 消息中心服务、页面注册、`clientId + onPushMessage` 生命周期、manifest 推送配置与 `uniCloud-aliyun/cloudfunctions/yj-push-bridge/index.js` 缺失；`yj_push_message`、`yj_push_recipient`、`yj_push_device`、`yj_push_delivery` 四表未落仓。
- 本轮生成的 `DEV-043\\__pycache__\\verify_notification_contract.cpython-312.pyc` 已删除，空 `__pycache__` 目录已移除。
- 当前门禁结论：允许进入 `DEV-044` 继续处理数据库门禁，但不得把 `AC-PUSH-101/102/201/301` 标记为通过；在 `DEV-044` 完成真实测试库执行前，不得启动 `DEV-045`。
- 最终独立验收结论：`DONE / VERDICT PASS`；AST 差异 `0`、动态正例 `True` / 反例 `False`、正式红灯脚本退出码 `1` 且结果 `0/16`、无残留 `pyc`，满足“前置验证入口与红灯确认任务完成、功能实现未开始”的正式口径。
