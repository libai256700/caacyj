# DEV-084 后台自测题库是否必填展示与编辑

## 实施标准

- 上游规格：`010-原始需求/第一期需求与报价清单.md` 中“用户自测题库管理”的列表与新增/编辑要求，以及用户 2026-09-02 明确提出的“列表显示是否必填、编辑弹窗下拉回显”。
- 正式页面：`http://localhost:3000/yj/assessment/question`；复用现有通用资源页，不新增平行页面或弹窗。
- 真实数据源：`yj_practice_exercises.is_required`；列表、详情、创建、更新必须形成完整读写链路，数据库结构不变。
- 列表列名为“是否必填”，值显示“是 / 否”；新增/编辑使用下拉，编辑回显真实值，新增默认“是”。
- 必须先补聚焦契约红灯并确认因字段链路缺失而失败，再做最小实现转绿；不得修改 `uni_modules/**`，不得覆盖工作区其他改动。

## 对应技能

- `30-0100-development-task-breakdown`
- `00-1600-frontend-ui-implementation`
- `40-0100-project-testing`

## 交付物

- 管理端资源字段配置、列表列与新增/编辑下拉回显。
- 后端 assessment-question 分页、详情、创建、更新的 `is_required` 透传与持久化。
- 聚焦契约测试、红绿证据、前端类型检查与构建回执。

## 分支安排详情

- 当前代码现场：`code/develop/`；沿用当前版本分支 `feature/20260601000000-vphase1-initial-delivery`。
- 实施责任：实施子代理；`DEV-085` 必须由不同的独立验收子代理执行。
- 提交时只纳入本任务相关文件，并复核 `uni_modules/**` 零改动；远程可用时推送当前版本分支。

## 关联验收项

- `AC-ASSESS-REQUIRED-001/101/201/301/401`。

## 任务产出 / 结果记录

- 先建立 `YjAdminAssessmentQuestionRequiredContractTest`，准确红灯覆盖字段白名单、分页/详情投影、camelCase/snake_case 写入映射、false 保留与新增默认值。
- 最小实现已接通管理端“是否必填”列表列、新增/编辑下拉及默认“是”，并接通后端 assessment-question 专用 CRUD 的 `is_required` 读写。
- 后端聚焦测试 `4 tests / 0 failures / 0 errors`，`pnpm build:local`、`git diff --check` 通过，`uni_modules/**` 零改动。
- `pnpm ts:check` 仍被仓库既有跨 BPM、mall、pay 等模块错误阻断；精确筛选本任务 `config.ts/index.vue` 零错误命中。

## 进度总结

- 当前状态：待测试环境验收；本地实现与独立受控前端交互通过，真实接口保存及数据库回读尚未执行。
