# TC-UI-HOMEMAP-001 参考首页业务映射

- 对应验收项：`AC-HOMEMAP-001`、`AC-HOMEMAP-002`、`AC-HOMEMAP-003`、`AC-HOMEMAP-101`、`AC-HOMEMAP-102`
- 页面入口：`http://127.0.0.1:5173/yunjikeji/#/pages/home`
- 冻结对照：`http://127.0.0.1:5173/yunjikeji/#/pages/home-reference-preview`
- 自动化脚本：`dev033-home-mapping.spec.cjs`
- 正式视口：`360x800`、`390x844`、`430x932`

## 验证范围

1. 八项业务文案完整且旧标题不再作为标题或底栏项显示。
2. 状态栏、欢迎区、三张创作卡、五个快捷入口、三张常用功能卡、建议按钮和输入框完整。
3. 底栏仅“首页 / AI助手 / 我的”，三等宽，中间使用 `/static/brand/jixiangwu-logo.png` 并上浮。
4. 自测、考题、错题、岗位、客服、个人资料、AI 助手和我的调用正式页面路由。
5. 客服按默认/学员身份进入默认会话，按企业身份进入企业客服页。
6. 三档真实首页与冻结预览页的非业务差异几何、背景和连续间距一致。
7. 三档无横向溢出、文字/配图遮挡、控制台错误、页面错误或资源失败。
8. 冻结预览页、专用素材、旧首页、登录页和 `uni_modules/**` 不产生 DEV-033 差异。

## 执行结果

- Chrome DevTools：`390x844` 门禁通过；DOM、console、80项资源请求、8项默认点击和企业客服点击均已现场验证。
- Playwright：最终退出码 `0`；三档 `allPassed=true`。
- 几何：三档 `geometryDiffs=[]`；真实页与冻结页背景值完全一致。
- 连续流：三档智能助理到建议按钮间距均为 `10px`。
- 底栏：三档宽差为 `0.015625/0.015625/0px`；猫头鹰外径 `62px`。
- 遮挡：三档朋友圈标题与素材实际文字范围重叠面积均为 `0`。
- 浏览器：三档 console error、page error、failed request 均为 `0`。
- 导航：默认8项与企业客服1项全部与预期一致。
- 视觉：`98/pass`。
- 质量：`pnpm type-check`、`pnpm run build:h5`、`git diff --check` 均退出码 `0`。

## 测试边界

- 接口级：不涉及后端接口调用与返回契约变更；验证的是前端真实路由契约。
- 数据级：不涉及数据库或业务数据变更；仅临时切换本地身份值验证客服分支，测试结束已恢复为空。
- 交互级：Chrome DevTools 与 Playwright 均已执行。
- 本验收代理只创建 DEV-033 测试证据，未修改应用源码、素材、计划或验收标准定义。
