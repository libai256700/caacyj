# 测试执行结论

## 2026-09-02 DEV-011 用户练习统计与错题本（本地代码级通过，待测试环境验收）

- 当前结论：`部分通过 / 待测试环境验收`。本轮已验证源码契约、后端 `6/6`、前端 `eslint`、`build:testhost` 与 `git diff --check`；`AC-ADMIN-WRONG-001` 的 Playwright / 测试环境浏览器交互未执行，因此不能把 DEV-011 判成整体通过。
- 后端结果：`mvn '-Dtest=UserPracticeExercisesRecordSqlContractTest' test` 通过，后端 `6/6`，`false` 与空串分支同时被断言。
- 前端结果：`pnpm exec eslint src/views/yj/resource/config.ts src/views/yj/resource/index.vue` 通过；`pnpm build:testhost` 通过；首次/重置默认 `is_correct=false` 的代码级验证成立，默认值仅落在错题管理资源链路。
- 数据结果：`python verify_wrong_question_default_filter.py` 通过，`queryDefaultValue=false` 只存在于 `practice-record-detail.is_correct`，未扩散到其他资源页。
- 交互缺口：Playwright / 测试环境浏览器交互未执行，当前不能证明无登录即可进入正式错题管理页。
- 当前状态：`DEV-011` 保持 `待测试环境验收`，等待正式浏览器交互复验后再决定是否推进到后续闭环。

## 2026-08-31 DEV-078 双评测提交保存状态门禁独立验收

- 当前结论：`PASS / 待用户审核`。独立验收已覆盖 `AC-ASSESSMENT-SAVE-001/101/201/301` 并通过；`AC-ASSESSMENT-SAVE-401` 仍等待用户审核，不据此宣称 DEV-078 或版本整体完成。
- 后端结果：DEV-078 聚焦验证 `23/23` 通过；成功路径在原完成流程结束后单独只写 `status=2`，失败路径在原事务回滚后以独立事务只补偿 `status=2` 并继续返回原异常，`is_completed` 零改动。
- 前端结果：保存状态契约 `3/3` 通过，`type-check` 与 H5 build 均通过；分类13/14均只在 `status=1` 时显示“上次评测正在初始化分析，请稍后”，`status=2` 继续原 completed/restart/reset/start/navigation 流程。
- 浏览器结果：使用 `chrome-devtools-mcp` 验证分类13/14状态1均只显示保存中弹窗且不导航，状态2继续原流程；保存中弹窗点击遮罩只关闭弹窗，不触发导航或原重测 resolver。
- 保护边界：DEV-078 仅管理 `status`，未修改 `is_completed`；未触碰 `uni_modules/**`。当前无未关闭的 DEV-078 独立验收缺陷，下一节点为用户审核。

## 2026-08-28 DEV-049 内容审计误阻断与 HTML 安全绕过返工（实施完成，待独立验收）

- 原 `75/75` 结果属于遗漏修复前版本，未覆盖必答题完整性、单选整值匹配、旧统计隔离、前端展示和当前数据库提示词回执，原 PASS 保持撤销。
- 本轮代码结果：正式 JDK 8（`1.8.0_151`）下，`FrontPracticeServiceImplAssessmentHtmlTest` 57 项、`FrontPracticeV3ContentAuditNoMockTest` 9 项、`SelfAssessmentV3RuleEngineTest` 32 项、`DeepSeekOpenAiClientTest` 7 项，合计 `105/105` 通过，失败、错误、跳过均为 `0`；无 mock 用例精确覆盖错序闭合 `tag_order` 与 HTML 注释 `unparseable_tail` 拒绝；`mvn -nsu -DskipTests compile` 为 `BUILD SUCCESS`，234 个源文件按 target 1.8 编译。
- 安全与边界：正文业务偏差仅审计；自闭合 `br/hr` 尾部属性、危险标签、文档/元数据标签、任意属性和非法闭合结构均有硬拒绝回归。`git diff --check` 通过，项目 `uni_modules/**` 无变更。
- 数据库结果：`DATABASE_V3_PROMPT=PASS`。测试库 `yj_agent_info.id=2` 当前为 `2627` 个 Unicode 码点、`6770` 个 UTF-8 字节、SHA-256 `3f2a53619b12ea47e94978d47aff1028781ea52903fe2e845362027f8160dbc1`；直接前驱为 `2440 / 6243 / 974ef05f4cda6fe0e07d6076c06e0c87b33986461dcaf2d39feda7c1e2d8d414`。
- 备份证据：`database-backup-yj_agent_info-id2-before-v3-20260827.sql` 与 restore-safe 文件均已留存；restore-safe SHA-256 为 `1625ade26898b5ad937ad0dbaae266a261e35681c3763abe584b28da08f4ad45`。
- 日志边界：完整 `systemPrompt`、`userPrompt`、`rawResult` INFO 日志按用户明确要求保留，不整改、不列为本轮阻塞。
- 当前结论：`DEV-049` 保持进行中；实施与数据库提示词专项验证完成，等待独立验收。真实 DeepSeek 与测试环境完整报告链路仍未验收，不得宣称 DEV-049 整体完成。

## 2026-08-18 DEV-060 答题正确结果解析展示（已完成）

- 当前结论：`PASS / 已完成`。业务交互与代码边界验收全部通过，无未关闭缺陷；功能与证据提交 `2a04783b` 已推送远程。
- TDD 证据：实现前 Playwright 退出码 `1`，准确命中 `resultTitleCount=0` 且 `questionGetCount=2`；删除正确提交后的自动 `goNext()` 分支后复跑退出码 `0`。
- 交互结果：正确单选、多选、文本、普通题、末题、错误回归和快速重复提交共 9 项检查通过；普通题点击下一题前 GET 为 `1`、点击后为 `2`；末题点击完成前未跳转，点击后进入 `/pages/home`。
- 视觉结果：`360x800`、`390x844`、`430x932` 三档结果区非空，无横向溢出或关键内容遮挡。
- 命令结果：`pnpm type-check`、`pnpm run build:h5`、指定 `git diff --check` 均退出码 `0`；工作区和暂存区 `uni_modules/**` 均为 `0` 文件。
- 边界结果：DEV-060 仅删除 `answer.vue` 正确提交后的自动跳题分支，事前三块并行改动完整保留；DEV-038 历史资产无修改。
- 证据入口：`DEV-060/TC-ANSWERCORRECT-001-正确结果态与回归.md`、`DEV-060/red-evidence.md`、`DEV-060/playwright-results.json`、`DEV-060/independent-acceptance.md`。

## 2026-08-16 DEV-024 企业注册营业执照百度 BOS 直传恢复（本地通过，待测试环境）

- 当前结论：`PARTIAL / 待测试环境验收`。本地代码、静态脚本、后端最小测试、前端类型检查、H5 构建和差异检查通过；真实 BOS 上传尚未在测试环境复验。
- 后端结果：`BaiduBosUtil` 已恢复 `2310754d` 的直传语义，移除静默回退 `FileApi`；`FrontComanpanyAuditController` 新增 `POST /app-api/yj/enterprise-audit/license/upload`，固定目录 `enterprise/license`，BOS 未配置时对企业端返回掩码化失败信息。
- 前端结果：`customerAuth.ts` 为企业营业执照新增专用上传路径 `/app-api/yj/enterprise-audit/license/upload`；`register.vue` 继续保持先上传、后保存、再提交的顺序，上传失败直接中断，不进入保存/提交。
- 配置结果：`application.yaml` 中 `huiyitech.bos.baidu` 保留 `enabled / endpoint / bucket-name / prefix-root` 默认语义，AK/SK 仅绑定 `BAIDU_BOS_ACCESS_KEY_ID`、`BAIDU_BOS_SECRET_ACCESS_KEY`；核对 `2310754d` 时未发现 BOS AK/SK 明文写入。
- 测试环境缺口：必须由运维注入 `BAIDU_BOS_ENABLED=true`、`BAIDU_BOS_ACCESS_KEY_ID`、`BAIDU_BOS_SECRET_ACCESS_KEY`、`BAIDU_BOS_ENDPOINT`、`BAIDU_BOS_BUCKET_NAME`、`BAIDU_BOS_PREFIX_ROOT` 后，重跑真实营业执照上传、`save`、`submit` 完整链路。
- 证据入口：`DEV-024/TC-CODE-ENTERPRISE-LICENSE-BOS-001-企业注册营业执照BOS上传.md`、`DEV-024/front-static-check.json`、`DEV-024/command-results.json`、`05-自动化测试脚本/check-enterprise-license-upload.mjs`。

## 2026-08-15 DEV-049 MJS 提示词测试库应用（历史旧六章证据，当前 V3 PASS 已撤销）

- 当前结论：历史旧六章曾 `APPLIED / PASS`；该结论只用于证明当前迁移脚本的真实前驱，不证明当前 V3 已应用。
- 执行门禁：执行前目标为已确认旧值 `3988` 字符、`8315` 字节、SHA-256 `4172c6...a436`；完整目标行已通过 `mysqldump` 备份，回滚脚本已就绪。
- 脚本结果：`precheck=READY`、`affected_rows=1`、`postcheck=PASS`。
- 独立查询：实际值 `4285` 字符、`9305` 字节、SHA-256 `c8f480...79f75`；第一章 `11` 行、固定标签 `11/11`、六章 `6/6`、禁止字段 `0`、`data-brand=` HTML 标记 `0`。
- 安全边界：连接密码仅通过进程环境变量注入，命令和证据未记录密码、完整提示词、答题内容或个人资料。
- 未完成边界：尚未部署测试服务器并生成新报告，报告持久化、按记录 ID 查看和页面端到端仍保持未执行。
- 证据入口：`DEV-049/TC-DATA-ASSESSMENT-PROMPT-001-MJS模板应用.md`、`DEV-049/database-prompt-application-result.json`、`DEV-049/20260815150000-backup-yj_agent_info-id-2-before-mjs-prompt.sql`。

## 2026-08-15 DEV-049 MJS 基准缺口返工后独立复验（历史结论，当前已撤销）

- 当前结论：当时旧六章静态结论已失效；当前 V3 脚本须重新独立验收后才可按“测试库先备份、再执行 DML、再生成新回执”放行。
- 缺陷关闭：Java 与参数化测试已精确增加 `自测题量`、`综合得分`，并新增合法 `综合评分` 正向用例，未误伤 MJS 画像评分结构。
- 独立复跑：目标测试 `36/36`、联合测试 `40/40`、39 模块编译全部成功；`git diff --check` 通过，`uni_modules/**` 无变更。
- MJS/Java：品牌区保持 `1186` 字符、`2367` 字节、SHA-256 `e1eaf926...3241d` 且逐字节一致。
- SQL：新 Prompt `4285` 字符、`9305` 字节、SHA-256 `c8f480...79f75`；旧 Prompt `3988` 字符、`8315` 字节、SHA-256 `4172c6...a436`；正向与回滚保护条件正确，六章和第一章 11 行与 Downloads MJS 一致。
- 执行边界：本轮未执行任何数据库 DML；DML 执行结果、新报告记录和页面端到端不包含在本次静态放行结论中。
- 证据入口：`DEV-049/TC-CODE-ASSESSMENT-NO-FALLBACK-001-禁止简版兜底.md`、`DEV-049/independent-no-fallback-verification.json`。

## 2026-08-15 DEV-049 MJS 唯一结构基准独立复验（未通过）

- 当前结论：`FAIL`，暂不放行测试库 DML。
- 已通过：Java 品牌常量与 Downloads MJS `BRAND_CONTENT` 逐字节一致；正向 SQL 的 SYSTEM_PROMPT 前缀、六章结构、11 行概览、4285 字符及 `c8f480...79f75` 哈希正确；回滚旧值 3988 字符及 `4172c6...a436` 哈希正确；目标测试 `33/33`、联合测试 `37/37`、39 模块编译均成功。
- 阻断缺陷：Java 禁止字段及参数化测试漏掉正式验收明确禁止的 `自测题量`，同时未覆盖 SQL 后置校验禁止的第一章 `综合得分` 标签；上述 AI 输出可能通过最终校验并持久化。
- 执行边界：本轮只做静态验收，未执行 `UPDATE` 或其他数据库 DML。实施补齐两个禁止标签及回归测试后，必须重新执行独立验收。
- 证据入口：`DEV-049/TC-CODE-ASSESSMENT-NO-FALLBACK-001-禁止简版兜底.md`、`DEV-049/independent-no-fallback-verification.json`。

## 2026-08-15 DEV-049 禁止简版兜底独立验收（历史结论，当前已撤销）

- 当前结论：该代码级 PASS 未覆盖当前规则引擎返工项，已撤销并等待本轮独立复验。
- `AC-ASSESSMENT-AI-301`：`通过`。运行时简版构造、固定 HTML 和 fallback 日志均为零命中；联合测试 `25/25` 通过，39 模块编译成功，`git diff --check` 通过，`uni_modules/**` 无变更。
- `AC-ASSESSMENT-AI-101`：代码级 `通过`、测试服务器运行态 `待验收`。AI 禁用、缺 Key、配置查询失败、上游异常、空/短/非 HTML、六章节任一缺失及缺品牌区均抛统一业务异常；新增日志未记录 Key、Prompt、答题正文、响应正文或底层异常消息。
- `AC-ASSESSMENT-AI-201`：代码级 `通过`、测试数据库 `待验收`。完整 HTML 写库前执行六章节加品牌校验；双兼容 INSERT 均失败时接口失败；详情接口拒绝历史简版并保留完整历史报告；完成态查询逐条过滤不合格报告。
- `AC-ASSESSMENT-AI-001`：`未执行`。尚未在测试服务器验证真实提交失败停留答题页、新生成完整 AI 报告及两个入口页面展示，不能用本地单测替代页面端到端证据。
- 通用兼容：`DeepSeekOpenAiClient` 的 Anthropic 和原 OpenAI 分支均保留，联合测试包含旧 OpenAI 兼容路径。
- 证据入口：`DEV-049/TC-CODE-ASSESSMENT-NO-FALLBACK-001-禁止简版兜底.md`、`DEV-049/independent-no-fallback-verification.json`。

## 2026-08-15 DEV-049 自测报告返回首页独立验收

- 当前结论：`部分通过 / PARTIAL`。两个入口、唯一报告页、返回首页契约、类型检查、H5 构建、差异格式和 `uni_modules/**` 边界均通过，未发现实现缺陷。
- 入口证据：首页读取最新记录 ID 后进入 `/pages/center/self-test-report?id=...`；答题提交后读取最新记录 ID 并进入同一页面。
- 页面契约：只保留一个 `report-action` 按钮，文案为“返回首页”，点击调用 `goHome`；旧文案与 `goCenter` 零命中；顶部返回失败复用 `goHome`；最终 `reLaunch` 目标为 `/pages/home`。
- 命令证据：`check-self-test-report-return-home.mjs`、`npm run type-check`、`npm run build:h5`、`git diff --check` 均退出码 `0`；H5 构建输出 `DONE Build complete.`。
- 未覆盖项：本机 H5 服务监听 `5173`，但现有浏览器未开放 CDP，且没有可直接复用的 Playwright 合法登录上下文；未伪造账号、Token 或报告数据，因此两个入口实际点击与返回按钮点击保持未执行。
- 证据入口：`DEV-049/TC-UI-ASSESSMENT-RETURN-001-报告返回首页.md`、`DEV-049/return-home-verification.json`、`05-自动化测试脚本/check-self-test-report-return-home.mjs`。

## 2026-08-15 DEV-049 DeepSeek Anthropic 改造独立验收（代码通过，待测试环境）

- 当前结论：`部分通过`。代码与供应商接口验收 `PASS`；测试服务器尚未部署，因此完整业务与数据端到端保持待验收，不能判定 DEV-049 整体完成。
- `AC-ASSESSMENT-AI-101`：代码与供应商契约部分 `通过`。真实请求 HTTP `200`，响应模型 `deepseek-v4-flash`，包含 1 个非空 text 内容块；`agent_config_query_failure`、`empty_response`、`validation_failure`、`non_html`、`too_short` 五类 warn 已可区分，且只记录原因、长度、租户/智能体编号和异常类型，不记录响应正文、异常消息、密钥或答题内容。测试服务器运行态日志待部署后复核。
- `AC-ASSESSMENT-AI-301`：`通过`。联合目标测试共 `9` 项，失败 `0`、错误 `0`、跳过 `0`；覆盖 Anthropic 成功、HTTP 错误、无 text 内容块、短报告回退、旧 OpenAI 兼容分支及五类脱敏日志；39 模块 Maven 测试与独立编译均为 `BUILD SUCCESS`。
- `AC-ASSESSMENT-AI-001`：`未执行`。修复尚未部署到测试服务器，未生成新的完整 AI 报告，不能以真实上游冒烟代替页面验收。
- `AC-ASSESSMENT-AI-201`：`未执行`。尚无新记录用于校验数据库模板变量、同记录持久化和详情接口一致性。
- 配置审计：运行时自测链路为 `https://api.deepseek.com/anthropic/v1/messages`；旧代理地址无命中；配置密钥精确值仅在 `application.yaml` 一处出现，并由环境变量覆盖后被知识库配置引用。通用 Spring AI 的 `deepseek-chat` 配置与保留的旧 OpenAI 兼容分支不属于自测报告当前 Anthropic 调用路径。
- 安全审计：真实请求和证据均已脱敏，未输出完整 Key 或个人答题内容；`git diff --check` 退出码 `0`；工作区与暂存区 `uni_modules/**` 变更数为 `0`。
- 证据入口：`DEV-049/TC-API-ASSESSMENT-AI-001-DeepSeek-Anthropic协议.md`、`DEV-049/test-data.json`、`DEV-049/execution-results.json`。

## 2026-07-18 DEV-032 88分返工后独立复验

- 结论：通过，进入待用户审核。正式 Playwright 三档脚本退出码 `0`，`test-results.json` 为 `allPassed=true`。
- AC 结果：`AC-REFHOME-001`、`AC-REFHOME-101` 通过；`AC-REFHOME-201` 仅推进到待用户审核。
- 返工量化：`390/430` 创作面板为 `289px`，`360` 为 `278px`；三档智能助理到建议按钮间距均为 `10px`；底栏五等分宽差均为 `0.015625px`；悬浮助手为 `62px`，上浮 `26px`。
- 视觉结果：机器人软边无矩形硬边，三类创作素材无明显矩形底且不侵入文字，悬浮助手比例接近参考；`visual-verdict.json` 为 `95/pass`。
- 回归结果：全部原文案、五张素材、三张创作卡、五个快捷入口、智能助理三卡、建议按钮、输入框和五入口底栏完整；三档无横向溢出、裁切、console error 或资源失败。
- 质量命令：`pnpm type-check`、`pnpm run build:h5`、`git diff --check` 均退出码 `0`。
- 边界：本轮只更新 DEV-032 正式测试与验收证据，未修改应用源码、素材或 `uni_modules/**`；Chrome DevTools MCP 因共享配置目录锁定未能接管，已由 Playwright 完成等价浏览器检查并保留阻塞记录。

## 2026-07-18 DEV-031 第二轮返工最终复验

- 结论：通过。`check-home-visual.mjs` 与 `check-home-page.mjs` 均退出码 `0`，三档截图、`playwright-results.json` 与 `navigation-results.json` 已覆盖更新。
- AC 结果：`AC-HOME-001`、`AC-HOME-002`、`AC-HOME-003`、`AC-HOME-004`、`AC-HOME-101`、`AC-HOME-102` 全部通过。
- 测评几何：左列比 `44.6%-44.7%`；gap `9.60/9.36/10.32px`；三档左卡与右侧两卡加 gap 等高。
- 插画证据：五卡均有多层插画；右侧两卡可见面积约 `29.7%-30.8%`，其余三卡均不少于 25%。
- 底栏证据：标签为“首页 / AI工作台 / 我的”，字号 `11.04-13.76px`；左右图标 `20.17-24.09px`；白环 `56.63/61.36/67.64px`；上浮 `14.84/16.16/17.92px`；AI 标签与猫头鹰重叠为 `0`。
- 回归证据：真实导航全部通过；三档无横向溢出、文字裁切、底栏遮挡、console error 或 page error；顶部猫头鹰无灰色矩形底。
- 视觉结论：`visual-verdict.json` 为 `96/pass`，达到 `95/pass` 门禁。
- 质量命令：`pnpm type-check`、`pnpm run build:h5`、`git diff --check` 均退出码 `0`。
- 边界：独立验收只更新正式测试脚本和证据，未修改 `home.vue`、`pages.json`、业务源码或 `uni_modules/**`。

## 2026-07-18 DEV-031 返工后独立复验

- 结论：未通过。返工后 `check-home-visual.mjs` 退出码 `0`，`check-home-page.mjs` 退出码 `1`；三档截图、`playwright-results.json` 与 `navigation-results.json` 均已刷新。
- AC 结果：`AC-HOME-001` 通过；`AC-HOME-002` 未通过；`AC-HOME-003` 未通过；`AC-HOME-004` 未通过；`AC-HOME-101` 通过；`AC-HOME-102` 未通过。
- 已通过：测评列比 `44.7%-44.9%`；三档左右组合等高；390/430 五卡插画可见面积均不少于 25%；三标签文本正确；真实导航全部通过；无横向溢出、文字裁切、console 或 page error；顶部猫头鹰不再有灰色矩形底。
- 未通过：360 gap `7.68px`；360 考题/错题插画各约 `23.4%`；左右底栏标签在 360/390 为 `9.6/10.4px`；AI工作台被白环覆盖 `4.33/4.67/5.16px`；430 白环外径 `73.38px`。
- 视觉结论：`visual-verdict.json` 为 `92/fail`，未达到 `95/pass`。
- 质量命令：`pnpm type-check`、`pnpm run build:h5`、`git diff --check` 均退出码 `0`。
- 边界：只更新正式测试脚本和测试/验收证据，未修改 `home.vue`、`pages.json`、业务源码或 `uni_modules/**`。

## 2026-07-18 DEV-031 最新参考图返工独立复验

- 结论：未通过。正式 `check-home-visual.mjs` 与 `check-home-page.mjs` 均退出码 `1`，已覆盖生成三档最新截图、`playwright-results.json` 与 `navigation-results.json`。
- AC 结果：`AC-HOME-001` 未通过；`AC-HOME-002` 未通过；`AC-HOME-003` 未通过；`AC-HOME-004` 未通过；`AC-HOME-101` 通过；`AC-HOME-102` 未通过。
- 几何证据：左列占比为 `54.8%-55.2%`，超出 `42%-48%`；360 档列间距仅 `5.76px`；左卡高度与右侧两卡加间距不等；常用功能插画可见区域不足卡片约 25%。
- 底栏证据：三等分与真实导航存在，但中间移动端截图未显示“AI工作台”，源码/桌面文本为“AI助手”；390 档猫头鹰下部存在裁切感。顶部猫头鹰有明显灰色矩形底。
- 导航证据：自我评测、考题测评、错题重做、岗位招聘、默认客服、AI 入口、我的及企业客服源码分支全部通过有界拦截验证。
- 视觉结论：`visual-verdict.json` 为 `72/fail`，低于正式 `95/pass` 门禁。
- 质量命令：`pnpm type-check` 退出码 `0`；`pnpm run build:h5` 退出码 `0`；`git diff --check` 退出码 `0`。
- 边界：本轮未修改 `home.vue`、`pages.json`、旧首页、共享底栏、登录页、业务页、services 或 `uni_modules/**`。

## 1. 当前结论

- 当前场景：`DEV-030 APP 登录页双 Tab 中线无缝连接复验`
- 当前测试时间：`2026-07-17`（返工后独立复验）
- 当前结论：`已通过`。当前源码 `5173` 服务的 Playwright 三视口 × 双状态退出码 `0`；未选中虚化层下穿白色圆弧 `58px`，`gap=0px`、可见 `seam=0px`，六张原图目视无透明竖带、硬线或背景断层，学员态与企业态镜像成立，`visual-verdict=96/pass`。
- 协议门禁：`已通过`；学员、企业两种角色均为未勾白底无勾，勾选后白底 `rgb(26,26,26)` 黑勾，第二次点击恢复无勾。状态、登录校验、提交和跳转逻辑守护不变。

### 后端 protection 模块编译产物修复

- 当前结论：`通过（原故障与完整启动关注项均已关闭）`；目标模块限定清理重编与独立复编均为 `exit 0 / BUILD SUCCESS`，`target/classes` 已恢复 29 个 `.class`，用户已启动进程的完整运行态复验通过。
- 通过门禁：仅清理 protection 模块 `target`；Maven 编译 `exit 0` 且 `BUILD SUCCESS`；目标 `.class` 恢复；启动越过该自动配置元数据读取错误；源码、POM、自动配置清单和 `uni_modules/**` 不变。
- 独立证据：目标 Configuration、`core/aop/IdempotentAspect.class` 与 imports 清单存在，`javap` 可解析；Java PID `56620` 监听 `0.0.0.0:48080` 与 `[::]:48080`，`GET /actuator/health` 返回 HTTP `200 / UP`，`GET /` 返回 HTTP `200`。
- 历史关注（已关闭）：此前 60 秒观察窗内未出现完整 `Started`/`Tomcat started`，当时只关闭缺类错误；当前持续运行进程的监听和 HTTP 健康证据已证明完整应用对外服务，本次未操作用户进程。
- 正式入口：`00-验收标准索引.md` 的 `AC-BACKEND-BOOT-001 / AC-BACKEND-BOOT-101`，测试证据承接至 `BUILD-FIX-001/TC-CODE-IDEMPOTENT-001-幂等自动配置编译与启动.md`。

## 2. 接口级结果

- 已通过：0；本任务不涉及接口变更。
- 未通过：0。
- 未执行：0；接口级不适用，未伪造真实登录结果。

## 3. 数据级结果

- 已通过：0；本任务不涉及数据库、状态映射、日志或配置变更。
- 未通过：0。
- 未执行：0；数据级不适用。

## 4. 交互级结果

- 自动化结构检查通过：1；`TC-UI-LOGIN-001` 已覆盖展示文案、逻辑边界、三档等宽几何、虚化层下穿圆弧、中线 `gap/seam`、两角色协议黑勾及响应式断言。
- 正式独立验收：已通过；`AC-LOGIN-002` 与 `TC-UI-LOGIN-001` 恢复为已通过，其余既有验收项必要关联回归通过。
- 结果摘要：“学员登录 / 企业登录”与“账号 / 验证码 / 请输入验证码 / 获取验证码”可见，不显示密码文案；“获取验证码”为无点击属性的 `UNI-VIEW`；`script setup` 与 HEAD 完全一致，内部 password 校验和提交未改。
- 视觉摘要：左右两个 Tab 均严格占角色区 `50%`；选中面为纯白且无 `filter/backdrop-filter`，未选中面为半透明虚化面，可透出后方猫头鹰；切换后两面与相交圆弧完整镜像互换；Tab 本身无边缘，不存在描边小按钮或胶囊。猫头鹰轮廓、暖色纵向渐变、分界位置、底部 `36px` 圆角和 `16px` 暖色留白未回退。
- 响应式摘要：实际 `window` 为 `360×800`、`390×844`、`430×932`；左右 Tab 实测宽度分别为 `170/170px`、`183/183px`、`203/203px`，每档学员态与企业态最终可见区域均为 `50/50`，虚化层下穿 `58px`、`gap=0px`、外缘偏差和横向溢出均为 `0`，文案中心分别位于容器 `25%/75%`；页面滚动宽度等于视口宽度，正文裁切和浏览器错误列表均为空。
- 视觉结论：已通过；未选中虚化层越过中线并下穿白色圆弧，由白色面覆盖，圆弧边缘与虚化区域连续，六张原图均无透明空隙、未虚化竖带、硬竖线或背景断层，`visual-verdict=96/pass`。

## 5. 失败记录

- 本轮环境入口失败：按任务指定的 `05-自动化测试脚本/` 工作目录直接执行时，首次报错 `Cannot find module 'playwright'`；复用 APP 现有依赖后继续从该目录执行，报错 `ENOENT ... 05-自动化测试脚本/src/pages/auth/login.vue`。脚本以 `process.cwd()` 同时解析依赖和 `src/pages/auth/login.vue`，因此正式可执行入口必须位于 `code/develop/yunjikeji/`；从 APP 根目录调用同一脚本后退出码 `0`，三视口 `PASSED`。
- 已关闭脚本失败 1：新增静态文案正则跨度过窄，未进入浏览器断言；改为分别核对标签和占位文案后关闭。
- 已关闭脚本失败 2：透明顶部构图使旧 `panelBackgroundColor=white` 断言失效；改为核对透明背景色和白色主体渐变后关闭。
- 已关闭视觉失败 3：上一轮三档截图中猫头鹰下部只呈现均匀橙色阴影/色块，`visual-verdict=86/revise`；返工后重新生成三档证据，均可辨身体边缘与暗部细节，复验为 `92/pass`，`AC-LOGIN-002` 和 `TC-UI-LOGIN-001` 转为已通过。
- 已关闭视觉失败 4：上一轮将“学员 / 企业”实现为两个带 `1px` 边缘的小 Tab，偏离用户要求的完整白色/虚化表面；本轮改为“学员登录 / 企业登录”完整半区并完成双状态镜像复验，`visual-verdict=94/pass`。
- 已关闭视觉失败 5：返工后虚化层在双状态均下穿圆弧 `58px`，三档 `gap=0px`，六张原图无未虚化竖带或硬接缝。
- 已关闭视觉失败 6：返工后协议控件在学员、企业两角色均为白底 `rgb(26,26,26)` 黑勾，二次点击可取消。
- 已关闭构建失败 7：仅清理 protection 模块 `target` 后恢复 29 个 `.class`；限定复编和独立启动复验均证明原 `YudaoIdempotentConfiguration` 元数据读取错误不再出现，`AC-BACKEND-BOOT-001 / AC-BACKEND-BOOT-101` 转为通过。
- 已关闭启动关注 8：PID `56620` 持续监听 `48080`，health 为 HTTP `200 / UP`、根路径为 HTTP `200`；此前未获得完整 `Started` 输出的历史关注已由运行态证据关闭。
- 未关闭失败：0。
- 重测入口：`05-自动化测试脚本/check-login-page.mjs`；本轮已生成三档双状态六图及 `playwright-results.json`、`tab-state-results.json`。

## 6. 验证命令与后续动作

- 本轮：从 APP 根目录执行 `node <check-login-page.mjs绝对路径> <DEV-030证据目录绝对路径>`，退出码 `0`，三视口 × 双状态 `PASSED`。
- 本轮：`pnpm run type-check` 退出码 `0`。
- 本轮：`pnpm run build:h5` 退出码 `0`，输出 `DONE Build complete.`。
- 真实账号登录接口集成测试仍不在本次纯视觉返工范围；`scriptSetupUnchanged=true`、`internalPasswordInputUnchanged=true`、`studentLoginFlowPresent=true`、`enterpriseLoginFlowPresent=true`、`roleBranchUnchanged=true`、`noSmsFlowAdded=true`，内部 password 校验、学员/企业登录提交及跳转流程未改。

## 2026-07-18 DEV-031 独立首页验收

- 结论：通过。`check-home-page.mjs` 在用户已启动的 `http://127.0.0.1:5173/yunjikeji/` 执行退出码为 `0`，`pnpm type-check` 退出码为 `0`。
- 交互证据：三个视口均确认 3 个底部入口、2 个练习块、独立招聘块、独立客服块；无横向溢出、无受检文字裁切、固定底栏稳定、控制台和页面异常均为空。
- 导航证据：首页 `/pages/home`、AI `/pages/center`、我的 `/pages/profile`、练习 `/pages/practice`、招聘 `/pages/jobs`、默认客服 `/pages/service/customer-service-chat?conversationId=default` 全部通过拦截调用验证；企业客服源码分支为 `/pages/service/customer-service`。
- 视觉证据：`DEV-031/visual-verdict.json` 为 `93/pass`，三档截图为 `home-360x800.png`、`home-390x844.png`、`home-430x932.png`。
- 覆盖结论：`AC-HOME-001`、`AC-HOME-002`、`AC-HOME-003`、`AC-HOME-004`、`AC-HOME-101`、`AC-HOME-102` 全部通过。
- 说明：正式导航脚本本轮修正了两处测试定位问题：探活保留用户 URL 的末尾斜杠，且“我的”采用唯一 `aria-label` 选择器；未改动应用源码。
- 构建证据：在 `code/develop/yunjikeji` 执行 `pnpm run build:h5`，退出码 `0`，关键输出为 `DONE Build complete.`；`git diff --check` 退出码 `0`。
- 边界证据：DEV-031 的应用源码范围为新增 `src/pages/home.vue` 与 `src/pages.json` 中新增的 `pages/home` 注册。`src/pages/index.vue`、`src/components/AppDynamicTabBar.vue`、`src/pages/auth/login.vue` 和 `uni_modules/**` 在工作区及暂存区均无差异。工作区另有 `center` 和 `services` 改动，经差异复核为自测评测、报告和登录错误文案内容，不包含 DEV-031 首页、底部导航或客服块逻辑，未归属本任务。

## 2026-08-17 DEV-051 ~ DEV-053 练习数据跨组织共享实施自检

- 当前结论：`本地独立验收通过，待测试环境验收`；`DEV-051` ~ `DEV-053` 已完成，`DEV-054` 仅推进到待测试环境验收，不得据此宣称 Story-050 已完成。
- 红灯证据：实现前 `PracticeSharedTenantContractTest` 退出码 `1`，真实失败为七个目标 DO 仍继承 `TenantBaseDO`、共享资源仍写入 `tenant_id`、错题删除仍使用租户键；入口为 `DEV-051/red-evidence.md`。
- 独立验收返工红灯：新增 SQL 资产契约后 `PracticeSharedTenantContractTest` 共 `7` 项、失败 `2`、退出码 `1`；失败准确对应迁移回滚文件缺失和兼容表字段状态校验缺失，入口为 `DEV-053/independent-review-red-evidence.md`。
- 绿灯证据：`PracticeSharedTenantContractTest` 与 `FrontPracticeServiceImplAssessmentHtmlTest` 合计 `43` 项，失败 `0`、错误 `0`，退出码 `0 / BUILD SUCCESS`。
- 编译证据：`mvn -pl yunjikeji-admin-server -am -DskipTests compile` 覆盖 `39` 个模块，退出码 `0 / BUILD SUCCESS`。
- 静态证据：`git diff --check` 退出码 `0`；工作区与暂存区 `uni_modules/**` 差异检查均退出码 `0`；正式执行结果为 `DEV-053/execution-results.json`。
- SQL 证据：迁移 SQL 的五个目标 INSERT 已移除 `tenant_id`，并在插入前固化五张不可变候选 ID 快照；`20260817113200-dml-rollback_yj_practice_data_migration.sql` 按快照精确回滚本次新增行，已登记脚本索引。前向 DDL 对错题唯一键执行全量重复门禁，对兼容表未知复合索引执行保护，并直接输出两个兼容表的 `tenant_id` 列状态；未执行数据库。
- 保护边界：未修改前端、依赖项目或 `uni_modules/**`；`application.yaml` 保持任务开始前已有的用户改动，本任务未编辑。
- 验收项边界：本地证据支持 `AC-PRACTICE-SHARED-101`、`301` 通过；不支持将 `AC-PRACTICE-SHARED-001`、`201`、`401` 整体标记为通过。
- 提交与推送证据：功能提交 `22aded0e feat: remove tenant isolation from practice data`；远端同步合并后本地 `HEAD` 与远端均为 `90f8e9277cbe2fccf9f517d94cd3ec5fa09ba110`，已推送 `origin/feature/20260601000000-vphase1-initial-delivery`。提交精确包含 49 个任务文件、禁区文件 0 个，`application.yaml` 及无关文件未提交。
- 待办：测试环境数据库脚本、双租户双用户数据验证仍需按备份、审批和正式环境入口另行执行；用户复验尚未完成，`AC-PRACTICE-SHARED-001`、`201`、`401` 整体不得标记为通过。

## 2026-08-30 DEV-049 / DEV-072 双评测报告轻量读取实施自检

- 当前结论：`本地实施自检通过 / 待独立验收与运行态计时`。本节只证明分类13/14轻量读取源码、目标测试与前端构建成立，不把 DEV-049、DEV-072 或 Story-052 标记为完成。
- 失败基线：职业报告已存在 `SUCCESS` 正文时，修复前实测首次进入约 `13.9s`、单次详情请求约 `6.69s`；根因是职业路由调用通用 `getRecordDetail`，自测报告页也调用通用 `/records/{recordId}`，触发统计与逐题答案重建。
- 红灯证据：实现前执行 `AssessmentReportReadLightweightContractTest`，`3` 项中 `1` 通过、`2` 失败；首个失败为分类13 `/assessment-result/self/{recordId}` 不存在，第二个失败为分类13轻量服务方法不存在。通用详情保留 `answers` 的保护项已通过。
- 绿灯证据：实现后同一契约先转为 `3/3`，补充专用 DTO 精确五字段保护后为 `4/4`；扩大执行 `AssessmentReportReadLightweightContractTest,FrontPracticeServiceImplCareerReportReadTest,CareerAssessmentIsolationContractTest,FrontPracticeAssessmentResetContractTest` 共 `25/25`，失败 `0`、错误 `0`，`BUILD SUCCESS`。
- 接口与代码证据：分类13新增专用轻量路由；分类14原报告路由改为同一专用五字段 DTO。两个路径只执行本人记录、对应分类完成批次和最新评测结果读取，不调用 `getRecordDetail`、`listExerciseStatsForCategoryPage` 或 `buildRecordAnswers`；通用 `/records/{recordId}` 继续返回完整 `answers`。
- 前端证据：仅 `self-test-report.vue` 切换到新分类13轻量 service；`self-test-record.vue` 仍使用原完整详情。`npm run type-check` 退出码 `0`；`npm run build:h5` 退出码 `0`，输出 `DONE Build complete.`。
- 卫生与禁区：两侧 `git diff --check` 退出码 `0`；两侧 `git status --short -- uni_modules` 均无输出；未修改数据库、未新增脚本、未重启或部署服务。
- 未决验证：因本实施任务明确禁止重启当前后端，本轮未对新代码执行连续三次真实接口计时，也未复跑浏览器三态/重试交互。独立验收须在加载新构建的服务上分别核对分类13/14 `PENDING/SUCCESS/FAILED`、失败重试、响应不含 `answers`，并对职业 `588` 或等价已生成样本连续计时三次，与 `6.69s/13.9s` 基线比较。

## 2026-08-31 DEV-028 双身份客服会话修复独立验收

- 独立结论：`FAIL`。`AC-CS-004`、`AC-CS-104`、`AC-CS-202` 未通过；`AC-CS-005`、`AC-CS-105`、`AC-CS-106` 保持待测试环境重跑。
- 正式缺陷：企业客服学员列表只固定请求 `pageNo=1&pageSize=100`，既不符合正式 `pageSize=50`，也没有上一页 / 下一页、页码或末页判定。教员无法浏览首批结果以后的全部审核通过学员。
- 分页重验：以 101 名可区分审核通过学员验证 `pageSize=50` 时形成 3 页；使用 `chrome-devtools-mcp` 检查上一页 / 下一页、`pageNo` 与页码、首末页禁用、翻页数据隔离和每页点击的 `studentId/conversationId`。
- 后端证据：`CustomerMessageServiceImplTest` 完整 `10/10`、退出码 `0`；`mvn -pl yunjikeji-admin-server -am -DskipTests compile` 的 39 个模块全部 `SUCCESS`、退出码 `0`。
- 前端证据：DEV-028 静态契约、`pnpm run type-check`、`pnpm run build:h5` 均退出码 `0`；H5 构建输出 `DONE Build complete.`。
- 差异证据：`git diff --check`、`git diff --cached --check` 均退出码 `0`；SQL 和 `uni_modules/**` 工作区 / 暂存区差异为空；未发现项目级 `AGENTS.md`。
- 浏览器证据：仅使用 `chrome-devtools-mcp`。最新本地 H5 中两个键盘 API 均为 `undefined`；分别直达学员参数与企业学员参数的共用会话页，页面在无登录态下回到登录页，load / unload 未产生对应 TypeError，控制台与捕获的 `error/unhandledrejection` 均为空。该结果不替代真实双身份业务验收。
- 证据缺口：实施阶段资产仍将转绿 / 构建记录为“待执行”，没有实施聚焦 `4/4` 的精确命令；现有自动化未覆盖可操作分页。
- 人工依赖：当前 H5 无登录会话，测试环境正式连接记录未提供 H5 测试入口、企业 / 学员账号、验证码或可复用登录材料。本轮未连接数据库、未伪造 token 或接口通过；真实列表、已有会话、无会话首次发送、tenant 存储、WS 与学员端退出重进待返工后在测试环境重验。
- 详细入口：`DEV-028/02-独立验收结论.md`。
