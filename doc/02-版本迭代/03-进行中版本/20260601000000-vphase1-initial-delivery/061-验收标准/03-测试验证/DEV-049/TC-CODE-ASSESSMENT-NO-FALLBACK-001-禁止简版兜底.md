# TC-CODE-ASSESSMENT-NO-FALLBACK-001 禁止简版自测报告兜底

## 当前状态

- 2026-08-15 基于旧六章和流式改造的 PASS 结论已失效，不得作为当前 V3 验收结论。
- 当前实现已经恢复规则引擎并完成内容审计与 HTML 安全返工；本文件登记本轮实施自检和待独立复验入口，实施代理不得自判正式验收 PASS。
- 数据库当前回执为 `2627 / 6770 / 3f2a5361...dbc1`，直接前驱为 `2440 / 6243 / 974ef05f...d414`；`2284 / 5809 / d7794b51...3c16ce` 与 `4285 / 9305 / c8f4803d...79f75` 仅是更早历史阶段。

## 当前 V3 检查项

| 检查项 | 当前通过条件 |
| --- | --- |
| 分类 13 输入完整性 | 只要检测到稳定题号，就必须存在 Q1-Q17、Q47、Q48；缺任一题均终止生成，旧 21 题无稳定 ID 路径保持兼容。 |
| 单选映射 | 规范化后整值相等；同意与不同意分别唯一映射，我同意、未知值和拼接多值均失败。 |
| 规则事实 | 五维分数、双指数、画像和方向匹配只由规则引擎生成；AI 不重算。 |
| 旧统计隔离 | V3 AI 请求、元数据、提交响应、记录列表和自测记录页均不得生成或展示答对、答错、正确率、题量或旧综合得分语义。 |
| AI 正文字数 | 纯 AI 可见正文以 2500-3800 Unicode 码点为生成目标；越界记录审计，不阻断安全 HTML 持久化。 |
| 报告结构 | 默认 8 章和动态 A-G 是提示词生成目标；标题、编号、顺序、数量和唯一性偏差记录审计，不阻断安全 HTML 持久化。 |
| 组装顺序 | 方法论 -> 规则评分仪表盘 -> AI 正文 -> 唯一品牌区。 |
| 数据库提示词 | 版本脚本链从直接前驱 `2440 / 6243 / 974ef05f...d414` 迁移至当前 `2627 / 6770 / 3f2a5361...dbc1`；更早回执不得标为当前 V3 PASS。 |
| 失败行为 | 映射、AI 调用或超时、响应解析、不安全 HTML 标签/属性/结构、固定区块组装或持久化失败均不得构造、保存或返回简版报告；字数、章节和正文事实偏差只审计。 |
| 保护边界 | 不修改 uni_modules；生产库禁止直接执行。 |

## 待执行验证

- 后端：`SelfAssessmentV3RuleEngineTest`、`FrontPracticeServiceImplAssessmentHtmlTest`、`FrontPracticeV3ContentAuditNoMockTest`、`DeepSeekOpenAiClientTest`；当前 JDK 8 实施自检合计 `105/105` 通过，其中无 mock V3 文件 `9/9` 覆盖错序闭合和 HTML 注释拒绝。
- 前端：类型检查或等价构建检查，确认自测适配器和记录页不再重算、展示旧统计。
- 静态：git diff --check，以及工作区、暂存区 uni_modules 差异均为零。
- 数据：测试库已按备份、正向 DML、回读顺序生成 `2627 / 6770 / 3f2a5361...dbc1` 实际回执；独立验收须核对回执、直接前驱和 restore-safe 证据，不重复盲目执行 DML。

## 验收边界

- 本轮只能回报本地实施自检完成并等待独立验收。
- 未取得独立验收和真实报告端到端证据前，不得宣称 DEV-049 整体完成。
