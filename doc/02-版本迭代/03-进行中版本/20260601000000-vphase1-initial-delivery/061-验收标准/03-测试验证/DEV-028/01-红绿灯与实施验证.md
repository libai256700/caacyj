# DEV-028 红绿灯与实施验证

## 红灯

- 状态：已建立初始红灯；产品语义已校准为“保留无会话学员并显式首次 start”，对应红灯将在实施前重跑。
- 后端命令：`mvn -pl yunjikeji-admin-server -am "-Dtest=CustomerMessageServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`。
- 后端结果：退出码 `1`，`Tests run: 6, Failures: 5, Errors: 0`，`BUILD FAILURE`。
- 后端失败断言：缺少显式 `studentId + conversationId` 企业历史方法；缺少显式双键 HTTP 发送方法；缺少 WebSocket conversation 参数；学员发送新增 `messageId=999` 时错误返回历史首屏尾项 `id=100`；当前无真实 session 行会合成 conversation。
- 前端命令：`node <DEV-028>/05-自动化测试脚本/dev028_frontend_contract.mjs E:\huiyitechworkspace\feixingxueyuan\code\develop`。
- 前端结果：退出码 `1`；首个失败断言为 `H5 must guard the missing keyboard subscribe API`，对应 `uni.onKeyboardHeightChange is not a function` 根因。
- 备注：首次后端调用因 PowerShell 未保护带点号的 Maven `-D` 参数而失败，属于无效命令行取证，未计入红灯；以上为加引号后的有效测试结果。

### 企业学员列表分页返工红灯

- 首次分页契约执行同一 `dev028_frontend_contract.mjs` 命令，退出码 `1`；首个失败断言为 `enterprise list must declare one fixed, visible-page size`，证明独立验收指出的固定首屏实现没有可见分页页态。
- 用户最终明确每页 50 人后，再次先改测试、后改实现；参数红灯退出码 `1`，失败断言为 `enterprise visible pagination must show exactly 50 students per page`，实际值 `20`、期望值 `50`。
- 最新夹具固定 101 名学员，三页长度必须依次为 `[50, 50, 1]`；此前 20 或 100 人口径均已作废。

## 转绿与构建

- 状态：分页返工后的实施阶段代码级验证已通过；历史独立验收 `FAIL` 保留，待不同代理基于最新实现重新独立验收。
- 后端聚焦命令：在 `code/develop/yunjikeji-admin-server` 执行 `mvn -pl yunjikeji-admin-server -am "-Dtest=CustomerMessageServiceImplTest#getCompanyStudentConversations_shouldKeepUnstartedStudentWithoutSyntheticIdAndEveryRealConversation+startCurrentCompanyStudentConversation_shouldReuseUnboundStudentSessionTenantRule+getCurrentCompanyStudentMessages_shouldUseSelectedConversationAndItsPersistedTenant+sendCurrentCompanyStudentMessage_shouldStayInSelectedConversationAndReturnInsertedMessage" "-Dsurefire.failIfNoSpecifiedTests=false" test`。
- 后端聚焦结果：退出码 `0`；`Tests run: 4, Failures: 0, Errors: 0, Skipped: 0`；`BUILD SUCCESS`。
- 后端完整命令：在 `code/develop/yunjikeji-admin-server` 执行 `mvn -pl yunjikeji-admin-server -am "-Dtest=CustomerMessageServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`。
- 后端完整结果：退出码 `0`；`Tests run: 10, Failures: 0, Errors: 0, Skipped: 0`；`BUILD SUCCESS`，同时完成目标后端编译。
- 前端静态契约：执行 `node <DEV-028>/05-自动化测试脚本/dev028_frontend_contract.mjs E:\huiyitechworkspace\feixingxueyuan\code\develop`，退出码 `0`，输出 `DEV-028 frontend/backend contract checks passed`。覆盖 101 名学员的 50/50/1 三页、`pageNo + 1/-1`、首末页禁用、整页替换不混数据、空后一页保留末个非空页、重新显示回第 1 页，以及学员点击继续携带 `studentId + conversationId`。
- 前端 type-check：在 `code/develop/yunjikeji` 执行 `pnpm run type-check`，退出码 `0`，无类型错误。
- 前端 H5 build：在 `code/develop/yunjikeji` 执行 `pnpm run build:h5`，退出码 `0`，输出 `DONE Build complete.`；仅保留与 DEV-028 无关的 `practice-topics-grid-reference.png` 构建期解析警告。
- 差异与禁区：`git diff --check` 退出码 `0`，仅有 LF/CRLF 提示；`git status --short -- '*.sql'` 与 `git status --short -- ':(glob)**/uni_modules/**'` 均输出为空，`rg --files -g 'AGENTS.md' .` 无结果，即 SQL 变更 `0`、`uni_modules/**` 变更 `0`、项目级 `AGENTS.md` `0`。

## 独立验收门禁

- `chrome-devtools-mcp` 测试环境交互验收：待不同代理执行。
- 本文件只记录实施阶段红绿灯和代码级验证，不替代正式独立验收结论。

## 2026-08-31 独立复跑

- 实施证据完整性：本文件实施区截至独立验收时仍将“转绿与构建”记为“待执行”，没有记录实施代理所述聚焦 `4/4` 的精确命令与转绿回执；该缺口不能由聊天结论替代。
- 后端完整目标类：在 `code/develop/yunjikeji-admin-server` 执行 `mvn -pl yunjikeji-admin-server -am "-Dtest=CustomerMessageServiceImplTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`，退出码 `0`，`Tests run: 10, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- 后端编译：执行 `mvn -pl yunjikeji-admin-server -am -DskipTests compile`，39 个 Reactor 模块全部 `SUCCESS`，退出码 `0`，`BUILD SUCCESS`。
- 前端静态契约：执行 `node <DEV-028>/05-自动化测试脚本/dev028_frontend_contract.mjs E:\huiyitechworkspace\feixingxueyuan\code\develop`，退出码 `0`，输出 `DEV-028 frontend/backend contract checks passed`。
- 前端质量：在 `code/develop/yunjikeji` 执行 `pnpm run type-check` 和 `pnpm run build:h5`，退出码均为 `0`；构建输出 `DONE Build complete.`。
- 差异与禁区：`git diff --check`、`git diff --cached --check` 均退出码 `0`；SQL、`uni_modules/**` 工作区与暂存区差异均为空；未发现项目级 `AGENTS.md`。
- `chrome-devtools-mcp`：在最新本地 H5 `http://127.0.0.1:5173/` 中，分别直达学员参数页 `conversationId=default` 和企业参数页 `studentId=123`。两次均在无登录会话下由共用会话页返回登录页；此时 `uni.onKeyboardHeightChange`、`uni.offKeyboardHeightChange` 均为 `undefined`，控制台以及捕获的 `error` / `unhandledrejection` 均无对应 TypeError。该证据只证明无登录 load / unload 能力保护，不证明真实会话业务通过。
- 正式缺陷：`customer-service.vue` 仅调用一次无参数 `fetchEnterpriseStudentConversations()`，该函数默认固定请求 `pageNo=1&pageSize=100`，既不符合正式 `pageSize=50`，页面也没有上一页 / 下一页、页码状态或末页判定；超过首批结果后无法继续浏览，违反“全局看到所有符合条件学员”的正式口径。
- 独立结论：`FAIL`。代码级命令全绿不能抵消分页缺陷；真实企业 / 学员账号、测试 H5 入口及验证码未见于测试环境正式连接记录，双身份真实列表、已有会话、无会话首次发送、数据归属、WS 与退出重进仍受人工依赖门禁。
