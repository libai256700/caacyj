# DEV-053 独立验收返工红灯证据

- 日期：2026-08-17。
- 触发来源：`DEV-054` 独立验收不通过。
- 红灯入口：`PracticeSharedTenantContractTest`。
- 有效红灯：`7` 项测试、失败 `2`、错误 `0`，退出码 `1 / BUILD FAILURE`。
- 缺陷一：`20260610-yj-practice-data-migration.sql` 没有实际备份对象及一一对应的可执行回滚文件。
- 缺陷二：`20260817113000-ddl-normalize_practice_shared_indexes.sql` 后置校验没有直接输出两个兼容表的 `tenant_id` 列状态。
- 非业务入口失败：首次新增契约误用了 Java 9 API，项目 Java 8 编译失败；修正为 Java 8 API 后重新取得上述有效红灯，该次入口错误不计为需求红灯。
- 绿灯：补齐 SQL 资产后同一测试 `7` 项全部通过，失败 `0`、错误 `0`，退出码 `0 / BUILD SUCCESS`。

## 第二轮回滚完整性返工

- 缺口：首次候选 ID 快照已固定，但五段前向 `INSERT ... SELECT` 没有绑定快照，重跑可能导入快照创建后新增的源数据，而回滚不会覆盖这些行。
- 有效红灯：为五段迁移增加逐段快照绑定断言后，`7` 项测试失败 `1`、错误 `0`，退出码 `1 / BUILD FAILURE`。
- 修复：五段前向迁移分别 `INNER JOIN` 对应首次候选 ID 快照；回滚继续只按同一快照删除，前向与回滚范围完全一致。
- 绿灯：同一静态契约 `7` 项全部通过，失败 `0`、错误 `0`，退出码 `0 / BUILD SUCCESS`。
