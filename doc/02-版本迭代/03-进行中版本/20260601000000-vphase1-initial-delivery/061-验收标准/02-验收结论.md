# 验收结论

## 2026-09-04 TASK-026 生产Nginx切换 xiaojiapp 到新服务器并完成发布验证

- 当前结论：`PASS / 已完成`。`AC-PROD-NGINX-001`、`AC-PROD-NGINX-101`、`AC-PROD-NGINX-201`、`AC-PROD-NGINX-301` 已验证，闭环项 `AC-PROD-NGINX-401` 已完成；实施与独立验收由不同代理执行。
- 旧映射事实：生产机 `106.13.128.217` 上 `xiaojiapp.caacyj.com` 的旧根路径为本地静态内容，旧 admin / api 分别指向 `114.111.30.111:18081` 与 `114.111.30.111:18080`。
- 本次变更：仅将根、admin、api 三处反代统一切到 `https://106.13.71.85`，SNI 固定 `yunjikeji.lai-do.com`；两个隐私相关路径保留本地，DNS 继续指向 `106.13.128.217`。
- 变更与恢复：变更前完整备份位于 `/data/backup/nginx/conf/TASK-026-20260904175724`；`nginx -t` 通过后于 `2026-09-04 17:58:54` 执行平滑 `reload`，未触发 `restart` 或回滚。
- 验证结果：外部域名 HTTPS、关键静态资源、相关 API 与新机日志核对通过，其他生产站点回归通过；生产 Java 应用未重启。
- 状态边界：本结论仅关闭 `TASK-026`；当前版本仍有其他未完成任务，版本整体继续保持 `进行中`。

## 2026-09-04 TASK-025 新测试服务器环境安装与应用迁移

- 当前结论：`PASS / 已完成`。`AC-NEWSERVER-001`、`AC-NEWSERVER-101`、`AC-NEWSERVER-201`、`AC-NEWSERVER-301` 已验证，闭环项 `AC-NEWSERVER-401` 已完成；实施与独立核验由不同代理执行。
- 基础与依赖：新机 `106.13.71.85` 已具备受控 SSH、Java、Docker、Docker Compose、Nginx 与必要运行依赖；Redis 为新机本地 `6379` 新空持久化实例。后台直接连接内网数据库 `172.16.32.2:3306/yunjikeji_test`，未保留本地 `13306` 或 `127.0.0.1:3306` 代理。
- 工程状态：`knowledge-graph` 的 Neo4j 数据与运行态、`QwenPaw` 的容器和必要运行卷已迁移并健康运行；`yunjikeji` H5、`yunjikeji-admin-server`、`yunjikeji-admin-ui` 已由当前代码产物部署并通过入口与健康检查。
- 入口验证：未切 DNS；IP + Host / `curl --resolve` 外部访问返回 `HTTP 200`。旧测试服务器在迁移备份和必要的短暂停机窗口后恢复并继续保活。
- 保护边界：未部署 App、uniCloud 或 `yunjikeji-server`，未触碰 `uni_modules/**`，正式记录不包含用户名、密码或密钥。
- 状态边界：本结论仅关闭 `TASK-025`；当前版本仍有其他未完成任务，版本整体继续保持 `进行中`，版本台账无需迁移或归档。

## 2026-09-02 DEV-011 用户练习统计与错题本（本地代码级通过，待测试环境验收）

- 当前结论：`部分通过 / 待测试环境验收`。`AC-ADMIN-WRONG-101`、`AC-ADMIN-WRONG-201`、`AC-ADMIN-WRONG-301` 已有本地正式证据；`AC-ADMIN-WRONG-001` 仍缺 Playwright / 测试环境浏览器交互，因此不能把 DEV-011 写成已完成。
- 代码级结果：源码契约通过，后端 `6/6` 通过，前端 `eslint` 与 `build:testhost` 通过；首次进入与重置默认 `is_correct=false` 的代码级断言已成立。
- 交互缺口：Playwright / 测试环境浏览器交互未执行，当前只证明本地页面骨架可达，未证明无登录即可进入正式错题管理页。
- 结论边界：`is_correct` 默认值仅作用于错题管理链路，不扩散到其他资源页；用户主动选择“不限”时仍按空条件查询。
- 当前状态：`DEV-011` 保持 `待测试环境验收`，不得写为已完成。

## 2026-08-31 DEV-078 双评测提交保存状态门禁（独立验收通过，待用户审核）

- 当前结论：`独立验收通过 / 待用户审核`。`AC-ASSESSMENT-SAVE-001/101/201/301` 已通过，`AC-ASSESSMENT-SAVE-401` 仅推进到待用户审核；尚无用户审核通过结论，不宣称 DEV-078 或版本整体完成。
- 自动验证：后端聚焦验证 `23/23`、前端保存状态契约 `3/3` 通过，前端 `type-check` 与 H5 build 通过。
- 交互验证：`chrome-devtools-mcp` 已验证分类13/14的 `status=1` 均只显示保存中弹窗且无导航，`status=2` 继续原流程；保存中弹窗点击遮罩只关闭弹窗，不导航、不触发原重测逻辑。
- 数据边界：成功与失败状态动作均只更新 `status`，失败补偿不随原事务回滚且继续返回原异常；`is_completed` 零改动。
- 当前状态：`DEV-078` 待用户审核；未取得用户审核结论前不得回写为已完成。

## 2026-08-31 DEV-079 APP答题AI深度解答与AI助手历史隔离（本地独立验收通过，待测试环境验收）

- 当前结论：`本地独立验收通过 / 待测试环境验收`。`AC-AI-CONTEXT-001`、`AC-AI-CONTEXT-101`、`AC-AI-CONTEXT-201`、`AC-AI-CONTEXT-301` 的本地证据已跑通，但测试环境/真机链路尚未执行，因此不能把本任务判成最终完成。
- 复跑结果：`mvn -pl yunjikeji-admin-server -Dtest=FrontKnowledgeServiceImplHistoryIsolationContractTest test` 实际执行 `4/4` 通过；`npm run type-check` 通过。
- 差异边界：答题页 AI 深度解答继续保留当前页即时展示与追问体验，前端未见可见 UI 改动；后端通过 `source` 分流决定是否写 `yj_ai_center`，并且 `practice_ai_answer` 跳过历史落库，`ai_assistant` 和缺省来源继续兼容写入。
- 兼容性与保护：`AppKnowledgeAskReqVO` 已不再携带多余变更，`center.vue` 未改，`uni_modules/**` 未触碰；既有污染记录不删除，AI 助手自身历史回看保持不变。
- 当前状态：本地已过，待测试环境验收，尚未宣称版本完成。

## 2026-08-28 DEV-049 内容审计误阻断与 HTML 安全绕过返工（实施完成，待独立验收）

- 验收范围：`AC-ASSESSMENT-AI-001`、`AC-ASSESSMENT-AI-101`、`AC-ASSESSMENT-AI-201`、`AC-ASSESSMENT-AI-301`。
- 返工原因：恢复规则引擎后的既有 PASS 未覆盖分类 13 必答题完整性、单选整值匹配、旧答题统计隔离和数据库提示词新版本回执；后续又发现 V3 把正文内容偏差误作硬门禁，并存在自闭合 `br/hr` 尾部属性绕过，因此原结论持续撤销。
- 当前口径：默认 8 章、动态 A-G 和 AI 可见正文 `2500-3800` Unicode 码点均为生成目标；分数、等级、标题/章节、字数、训练、金额、证书和方向等正文偏差仅审计，危险标签、任意属性、文档/元数据标签、注释和非法闭合结构仍硬拒绝。评分仪表盘只采用本地规则引擎事实。
- 数据库结果：`DATABASE_V3_PROMPT=PASS`。测试库 `yj_agent_info.id=2` 当前为 `2627` 个 Unicode 码点、`6770` 个 UTF-8 字节、SHA-256 `3f2a53619b12ea47e94978d47aff1028781ea52903fe2e845362027f8160dbc1`；直接前驱为 `2440 / 6243 / 974ef05f4cda6fe0e07d6076c06e0c87b33986461dcaf2d39feda7c1e2d8d414`。`2284 / 5809 / d7794b51...` 是更早证照阶段，`4285 / 9305 / c8f4803d...` 是更早旧六章历史。
- 备份证据：执行前完整快照与 restore-safe 文件均已留存；restore-safe SHA-256 为 `1625ade26898b5ad937ad0dbaae266a261e35681c3763abe584b28da08f4ad45`。
- 本地结果：正式 JDK 8（`1.8.0_151`）下，报告 HTML 57 项、无 mock 内容审计/安全回归 9 项、规则引擎 32 项、AI 客户端 7 项，合计 `105/105` 通过；无 mock 用例已精确覆盖错序闭合与 HTML 注释拒绝，234 个源文件按 target 1.8 编译成功，`git diff --check` 通过，`uni_modules/**` 无变更。
- 日志边界：完整 `systemPrompt`、`userPrompt`、`rawResult` INFO 日志按用户明确要求保留，不整改、不列为本轮阻塞。
- 当前状态：`DEV-049` 保持进行中；本地实施与数据库专项验证已完成，等待独立验收。真实 DeepSeek 和测试环境完整生成、持久化、详情查询及 APP 展示链路尚未验收，不得宣称 DEV-049 整体完成。

## 2026-08-18 DEV-060 APP答题正确结果解析展示独立验收

- 当前结论：`PASS / 已完成`。`AC-ANSWERCORRECT-001`、`AC-ANSWERCORRECT-101`、`AC-ANSWERCORRECT-201` 全部通过。
- 行为结果：正确单选、多选、文本题提交后均保留当前题结果态，显示“回答正确”、正确答案、试题详解和 AI 深度解答入口；普通题只在显式点击“下一题”后加载下一题，末题先显示解析，再由“完成练习”进入 `/pages/home`。
- 回归结果：错误结果态、显式下一题、单选点击提交、多选与文本显式提交、快速重复提交防重均通过；Playwright 共 9 项检查，浏览器错误为 `0`。
- 视觉结果：`360x800`、`390x844`、`430x932` 三档结果区非空，无横向溢出或关键内容遮挡；末题正确解析与完成按钮可见。
- 代码边界：DEV-060 仅删除 `answer.vue` 正确提交后的自动 `goNext()` 分支；文件事前已有三块并行改动完整保留；services、后端、auth、其他页面、DEV-038 和 `uni_modules/**` 均无本任务改动。
- 质量结果：`pnpm type-check`、`pnpm run build:h5`、指定 `git diff --check` 均退出码 `0`。
- 证据入口：`03-测试验证/DEV-060/`。
- 提交与推送：功能与证据提交 `2a04783b` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`；提交精确排除 `uni_modules/**` 和其他并行任务改动。

## 2026-08-16 DEV-024 企业注册营业执照百度 BOS 直传恢复

- 当前结论：`部分通过 / PARTIAL`。本地已恢复百度 BOS 直传链路并切走企业注册的通用七牛示例上传入口；真实 BOS 上传、审核保存与提交仍待测试环境复验。
- 已通过：`AC-ENTERPRISE-BOS-101`、`AC-ENTERPRISE-BOS-201`、`AC-ENTERPRISE-BOS-301` 的本地代码与构建门禁；企业营业执照上传不再调用 `/app-api/infra/file/upload`，`BaiduBosUtil` 不再静默回退 `FileApi`。
- 待验收：`AC-ENTERPRISE-BOS-001` 的真实上传成功链路，以及 `AC-ENTERPRISE-BOS-401` 的测试环境闭环；必须在测试环境提供有效 `BAIDU_BOS_*` 环境变量后复验。
- 2310754d 对照：已恢复 `huiyitech.bos.baidu` 配置结构、默认 endpoint `bj.bcebos.com`、默认 bucket `yunjikeji`、默认 prefixRoot `yunjikeji`、AK/SK 环境变量名 `BAIDU_BOS_ACCESS_KEY_ID / BAIDU_BOS_SECRET_ACCESS_KEY` 和 `BaiduBosUtil` 直传语义；本次保留的偏差只有企业专用上传接口、前端专用上传路径和企业端错误掩码。
- 安全结论：核对 `2310754d` 的 BOS 配置段未发现 BOS 明文 AK/SK；当前实现也未把 AK/SK 写入 Git、日志或正式测试结果。
- 证据入口：`03-测试验证/DEV-024/`、`03-测试验证/04-测试执行结论.md`。

## 2026-08-15 DEV-049 自测 AI 报告故障修复独立验收（历史结论，当前已撤销）

- 验收范围：`AC-ASSESSMENT-AI-001`、`AC-ASSESSMENT-AI-101`、`AC-ASSESSMENT-AI-201`、`AC-ASSESSMENT-AI-301`。
- 当前结论：该结论只反映当时旧六章实现，不能作为当前 V3 PASS；当前 V3 须按 2026-08-27 返工口径重新独立验收。
- 已通过：Anthropic `/v1/messages` 协议、指定模型真实请求、错误分类脱敏日志、联合测试 `9/9`、39 模块构建、旧代理清理、Key 单一来源、`git diff --check` 与 `uni_modules/**` 边界审计。
- 数据库历史证据：当时启用记录 `yj_agent_info.id=2` 为 `4285` Unicode 码点、`9305` 字节、SHA-256 `c8f4803d...79f75`；该值现仅作为旧六章历史，不代表当前 `2627 / 6770 / 3f2a5361...dbc1` V3 已应用。
- 待验收：`AC-ASSESSMENT-AI-001`，以及 `AC-ASSESSMENT-AI-101` 的测试服务器运行态；必须部署后新建自测记录并确认完整 AI 报告，不得使用既有回退记录作为证据。
- 报告页返回入口：代码与构建验收通过；首页查看最新报告和答题提交后查看报告均进入同一报告页，按钮显示“返回首页”并 `reLaunch` 到 `/pages/home`。真实登录态下的两个入口与按钮点击仍待测试环境 Playwright 复验。
- 2026-08-15 重新打开：用户明确禁止任何简版兜底报告；现有结论不再覆盖 fallback 删除、历史简版拦截和 AI 失败提交行为，`AC-ASSESSMENT-AI-001/101/201/301` 均需按更新后的标准重新实施与独立验收。
- 2026-08-15 模板口径更新：用户指定 Downloads 下 `eval_ai_transform_redacted.mjs` 为唯一结构基准；数据库提示词与后端固定品牌区已经按该基准实施并完成代码/数据库独立验收，禁止沿用 7 月 3 日加入自测题量、正确情况和自测得分的改写模板；最终运行态仍须以测试服务器新生成报告验收。
- 证据入口：`03-测试验证/DEV-049/`、`03-测试验证/04-测试执行结论.md`。

## 2026-08-09 Story-042 消息推送与消息中心验收定义

- 当前结论：已建立 `REQ-042` 正式需求文档与 `AC-PUSH-001` 至 `AC-PUSH-301` 八项正式验收定义；当前仅完成需求与验收口径收口，等待后续开发、独立验收和人工审核，不代表功能已通过。
- 覆盖范围：管理平台 App 推送管理、APP 统一消息中心、定时/取消/失败统计、多租户与双身份隔离、接口契约、设备生命周期、四表审计和独立 Maven 模块边界。
- 人工审核关注：H5 首期只承接站内消息；客服即时通讯继续保持独立；UniPush、厂商通道和 APNs 凭证属于后续人工依赖，不在本轮需求与验收定义中视为已完成。

## 2026-07-18 DEV-040 APP新首页正式业务接入启动

- 当前结论：2026-07-19 短信业务手工合并冲突解决通过，H5/Playwright 待环境复验；DEV-040 尚未完成闭环，`AC-PAGEROUTE-201` 保持未完成。
- 运行态缺陷记录（2026-07-19）：登录前共用发码接口 `/app-api/member/auth/send-sms-code` 不携带 `tenant-id`，原先在进入 Controller 前被租户过滤器以业务 `400` 拦截；当前已仅给 `AppAuthController.sendSmsCode` 添加方法级 `@TenantIgnore`，类级与其他方法保持不变，Maven 39/39 模块编译通过，等待 Chrome 真实无 tenant 请求独立复验后关闭缺陷。
- 两条链路边界：登录前发码继续不写 tenant；学员/企业 `yj/*-auth/login-or-register` 继续沿用原密码登录业务链路、仅将凭证改为短信验证码，并由既有类级 `@TenantIgnore` 承接；登录返回实际租户后，企业 refresh-token 才按既有逻辑条件写入 `tenant-id`。
- 验收范围：冷启动、登录原分流与默认首页、受保护页 redirect、新首页三入口、测评/答题返回完成、退出登录、控件条件与短信登录并发集成保留。
- 实施边界：DEV-040 路由/门禁仍限于 `login.vue`、`home.vue`、`index.vue`、`answer.vue`；本轮另合入同事提交 `1f50accd` 的 7 个短信业务文件，并保留任务前 DeepSeek/assessment 本地配置；未覆盖 `uni_modules/**`、锁文件或范围外并发改动。
- 正式证据入口：`03-测试验证/DEV-040/`；真实账号登录联调保留为用户复验关注项。
- 独立验收：登录短信发送、60 秒倒计时、互斥与禁用态、验证码错误语义、原审核/注册/绑定分流、redirect 优先和三处首页默认落点完整；登录视觉相对 `HEAD 2687d5da` 等价并保留 `.login-hero`；后端四文件及 `customerAuth.ts` 与同事提交一致。
- 质量与 Git：`pnpm type-check`、后端 39/39 模块 Maven 编译、`git diff --check` 均通过；无活动 Git 操作、U 文件、stage 冲突、冲突标记或暂存文件。`AC-PAGEROUTE-005/101` 按代码口径通过。
- 环境边界：原 esbuild binary 与 Temp 副本执行 `--version` 均超时，属操作系统级执行故障；未重复 H5 构建，最新 Playwright 未执行。`AC-PAGEROUTE-102` 保持待验收，真实短信账号联调待用户复验。

## 2026-07-18 DEV-039 APP答题页背景暖色基值替换独立验收

- 当前结论：通过并完成；DEV-039 已完成提交与远程推送闭环，`AC-ANSWERBG-001`、`AC-ANSWERBG-101`、`AC-ANSWERBG-201` 全部通过。
- 结构门禁：全文件归一化对比为 `COLOR_MAP_ONLY=1`、`STRUCTURE_PRESERVED=1`、`exactExpectedMatch=true`；仅三行六个颜色字面量变化，3 增/3 删，`linear-gradient`、`180deg`、四个色标与选择器均未变化，`#F3AD7B` 计数为 `0`。
- 浏览器证据：Playwright `360x800`、`390x844`、`430x932` 全部通过；computed 背景与四个暖色色标准确，DEV-038 关键几何最大差 `0px`，三档非空、无横向溢出，console/page errors 均为 `0`。
- 质量与边界：`pnpm type-check`、`pnpm run build:h5`、`git diff --check` 均 exit `0`；暖色映射相关应用 diff 仅 `answer.vue`，`person-map.svg` 与 `uni_modules/**` 无差异，工作区其他并行改动未归入 DEV-039且验收未触碰。
- 正式证据：`03-测试验证/DEV-039/`；功能与证据提交 `2687d5da87dfe8c096857379997ca5b5be793486` 已通过无冲突集成提交 `4215c171599bace9da3345b448c24ec02ddd0bf0` 推送到 `origin/feature/20260601000000-vphase1-initial-delivery`。

## 2026-07-18 DEV-038 APP答题页参考问卷样式还原独立验收

- 当前结论：通过；DEV-038 已完成提交与远程推送闭环，`AC-ANSWERUI-001` 至 `AC-ANSWERUI-201` 全部通过。
- 浏览器证据：Playwright 三档基础 12 场景及文本题、快速双击防重、390/360 长选项和长解析滚动特别补测全部通过，0 failures、0 console/page/request errors；三档非空、无横向溢出、重叠、底栏遮挡或安全区冲突。
- 视觉门禁：对照参考图仅取手机屏幕内部，390 单选与多选独立 visual-verdict 均为 `95/pass`，`category_match=true`。
- 逻辑证据：单选点击即提交、多选可反选并显式提交、文本非空提交、提交防重、正确/错误状态、解析、AI 入口、答错下一题、答对自动下一题、末题完成和上一题真实 index 均通过；上一题不伪造历史选中态。
- 完成机制：HEAD 基线与当前均无 `completePractice` 导出或调用；既有完成机制保持为 `completed=true` 后 `reLaunch('/pages/practice')`，未删除、改变或虚构服务契约。
- 质量与边界：`pnpm type-check`、`pnpm run build:h5`、`git diff --check` 均 exit `0`；DEV-038 应用实现仅 `answer.vue` 与新增 `person-map.svg`，夹具未进入应用白名单，`uni_modules/**` 无差异；工作区其他并行任务差异未归入 DEV-038，验收未触碰。
- 正式证据：`03-测试验证/DEV-038/`；功能与证据提交 `39035c61` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`。

## 2026-07-18 DEV-037 考题测评页暖色背景基值替换闭环

- 当前结论：通过；DEV-037 已完成提交与远程推送闭环。
- 实施范围：只按指定映射替换 `exam-assessment.vue` 两行中的四个背景色值；渐变结构、透明度、色标、背景图片及其余页面内容保持不变。
- 验收范围：`AC-EXAMBG-001`、`AC-EXAMBG-101`、`AC-EXAMBG-201`。
- 当前门禁：`AC-EXAMBG-001`、`AC-EXAMBG-101`、`AC-EXAMBG-201` 全部通过，无未关闭缺陷。
- 独立验收：Playwright 390x844 页面非空、无横向溢出，暖色背景生效且布局几何与 DEV-036 基线一致；console/page error 为 `0`。
- 代码与质量：应用源码只变更两行四个目标色值，`COLOR_MAP_ONLY=1`、`STRUCTURE_PRESERVED=1`；类型检查、H5 构建和差异检查均退出码 `0`。
- 正式证据：`03-测试验证/DEV-037/`。
- 提交与推送：功能与证据提交 `1fc972ba` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`。

## 2026-07-18 DEV-036 考题测评分类联动页启动

- 当前结论：二次视觉返工后独立验收通过，DEV-036 已完成提交与推送闭环。
- 用户要求：首页考题测评进入独立页面；上部复刻参考图并使用真实题型分类横滑，分类变化后重新查询刷新下方原练习确认信息和真实练习动作。
- 验收范围：`AC-EXAMASSESS-001` 至 `AC-EXAMASSESS-201`；正式证据写入 `03-测试验证/DEV-036/`。
- 数据边界：分类、题量、总分、标准练习和错题复练必须走现有真实接口；接口为空不得显示硬编码假数据。
- 实施边界：不修改后端、services、center、参考页、登录页或 `uni_modules/**`。
- 首轮通过：三档响应式、五分类左右真实 touch、查询联动、竞态保护、loading/empty/error/ready、标准/错题动作防重及旧开始页共享卡回归通过；类型检查、H5 构建和差异检查通过。
- 环境限制：真实 current 请求已携带 Bearer Token 发出，但专用 Chrome 登录态失效，服务端返回业务 `401 账号未登录`；真实账号端到端集成尚未复验。
- 视觉失败：分类标签外存在参考图一没有的白色“题型分类”面板；下方在图四确认卡前存在多余“确认练习信息”标题与说明。`AC-EXAMASSESS-001`、`AC-EXAMASSESS-004` 标记未通过，首轮 `96/pass` 视觉结论撤销。
- 返工门禁：分类标签直接位于浅蓝顶部应用区，下方直接承接共享确认卡，不改变已经通过的真实数据、滑动、竞态、动作和旧页复用逻辑。
- 第一轮返工结果：分类 heading 和白色外壳已关闭，但 `.practice-start__intro` 因 scoped CSS 未穿透仍可见，视觉 `84/revise`；`AC-EXAMASSESS-001`、`AC-EXAMASSESS-004`、`AC-EXAMASSESS-101` 未通过。
- 二次返工要求：共享组件新增默认开启的正式介绍区显示属性；新页显式关闭，旧开始页保持默认开启。移除父页无效样式后，重新执行三档视觉与全量功能回归。
- 最终验收：`AC-EXAMASSESS-001`、`AC-EXAMASSESS-002`、`AC-EXAMASSESS-003`、`AC-EXAMASSESS-004`、`AC-EXAMASSESS-005`、`AC-EXAMASSESS-101`、`AC-EXAMASSESS-102` 全部通过；`AC-EXAMASSESS-201` 已完成。
- 视觉关闭：新页 intro/heading/tip 均为 `0`，旧页 intro 为 `1`；swiper 到共享卡体间距 `3.83~4.58px`，视觉 `97/pass`，符合“上部图一 + 下部图四”组合。
- 功能证据：五分类左右真实 touch `A→B→A`、分类查询联动、慢旧/快新竞态最终保留最新 C、四态、标准/错题动作防重、无错题不 POST 及旧页共享组件回归全部通过。
- 质量与边界：Playwright、类型检查、H5 构建和 `git diff --check` 均退出码 `0`；应用源码只涉及五个白名单文件，services、后端、center、参考页、登录页和 `uni_modules/**` 无本任务差异。
- 真实账号关注：专用 Chrome 的真实 current 请求已携带 Bearer Token，但凭证失效返回业务 `401 账号未登录`；未用 mock 结果冒充真实账号端到端集成通过，等待有效登录态复验。
- 提交与推送：功能、正式验收资产和测试证据提交 `2ec70263` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`；闭环文档随后的独立提交继续推送同一分支。

## 2026-07-18 DEV-035 首页位置恢复与入口校正启动

- 当前结论：最小返工后通过并完成。
- 用户要求：删除打卡、金币和 `X`，但删除内容不得带动后续内容上移；恢复原位置并校正四项真实页面入口。
- 验收范围：`AC-HOMEPOS-001` 至 `AC-HOMEPOS-201`；正式证据写入 `03-测试验证/DEV-035/`。
- 实施边界：暖色背景、参考预览页、登录页、旧首页、业务服务和 `uni_modules/**` 不得修改。
- 首轮结果：`AC-HOMEPOS-001`、`AC-HOMEPOS-003` 通过；`AC-HOMEPOS-002` 未通过；`AC-HOMEPOS-101` 未判通过，`AC-HOMEPOS-201` 未闭环。
- 失败证据：`360x800` 的常用功能/建议/输入框为 `381/579/624px`，基线 `443/641/686px`；`390x844` 与 `430x932` 为 `403/601/646px`，基线 `465/663/708px`，三档统一提前 `62px`。
- 返工门禁：不得恢复任何快捷入口 DOM、图标或文字；只允许保留其原 `62px` 不可见布局高度，使后续块体位置差归零，再完整重跑。
- 最终结果：`AC-HOMEPOS-001`、`AC-HOMEPOS-002`、`AC-HOMEPOS-003`、`AC-HOMEPOS-101`、`AC-HOMEPOS-201` 全部通过。
- 失败关闭：最小返工后 `360x800` 的常用功能/建议/输入框为 `443/641/686px`，`390x844` 与 `430x932` 为 `465/663/708px`，与 `DEV-033` 基线差全部为 `0`；Playwright 由 `exit1` 覆盖为 `exit0`。
- 路由证据：默认和企业身份下四项目标均通过，客服联系统一进入 `/pages/service/customer-service-chat?conversationId=default`。
- 视觉与质量：`visual-verdict=97/pass`；三档无幽灵内容、重叠、溢出或浏览器错误；类型检查、H5 构建、`git diff --check` 均退出码 `0`。
- 提交与推送：功能与正式证据提交 `2509fc66` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`。

## 2026-07-18 DEV-034 首页精简与暖色替换启动

- 当前结论：通过并完成。
- 用户要求：删除页面模拟系统状态栏和五项快捷入口整行；背景只替换颜色编码为登录页橙色，不改变渐变等其他样式。
- 验收结果：`AC-HOMETONE-001`、`AC-HOMETONE-002`、`AC-HOMETONE-101`、`AC-HOMETONE-201` 全部通过。
- 浏览器证据：Chrome DevTools `390x844` 门禁通过；Playwright 三档全部通过，模拟系统栏、快捷栏及六项精确文本计数均为 `0`，卡片、底栏、五张配图和八项导航保持可用，无 console、page、request 或资源错误。
- 视觉门禁：`03-测试验证/DEV-034/visual-verdict.json` 为 `96/pass`；删除后连续间距为 `0 / 10 / 10px`，机器人等比且保持原 `7px` 裁切关系，无明显变形或截断。
- 背景边界：外围为 `#F5F0EA`，三段渐变保持 `linear-gradient(180deg, ... 0, ... 33%, ... 76%)` 不变，只替换为登录页暖色色值。
- 质量证据：类型检查、H5 构建、`git diff --check` 均退出码 `0`。
- 实施边界：参考预览页、登录页、旧首页、业务服务和 `uni_modules/**` 不得修改。
- 提交与推送：功能与正式证据提交 `e8adb0e1` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`。

## 2026-07-18 DEV-033 首页业务映射启动

- 当前结论：通过并完成。
- 用户确认：已明确确认 `DEV-032` 的参考页布局，并要求保持该布局不变后映射指定文案、真实入口和三项底栏。
- 验收结果：`AC-HOMEMAP-001`、`AC-HOMEMAP-002`、`AC-HOMEMAP-003`、`AC-HOMEMAP-101`、`AC-HOMEMAP-102`、`AC-HOMEMAP-201` 全部通过。
- 浏览器证据：Chrome DevTools `390x844` 门禁通过；Playwright 三档全部通过，默认八条路由和企业客服分流通过，无 console、page、request 或资源错误。
- 视觉门禁：`03-测试验证/DEV-033/visual-verdict.json` 为 `98/pass`；三档真实首页与冻结参考页 `geometryDiffs=[]`，仅存在验收允许的八项文案和五项改三项底栏差异。
- 质量证据：类型检查、H5 构建、`git diff --check` 均退出码 `0`。
- 实施边界：独立参考预览页、旧首页、登录页、业务服务和 `uni_modules/**` 不得修改。
- 提交与推送：功能与正式证据提交 `b1d65089` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`。

## 2026-07-18 DEV-032 88分返工后独立复验

- 验收结论：通过并完成。
- 逐项结果：`AC-REFHOME-001`、`AC-REFHOME-101`、`AC-REFHOME-201` 全部通过；用户已明确确认布局。
- 视觉门禁：`03-测试验证/DEV-032/visual-verdict.json` 为 `95/pass`，达到正式门禁。
- 关键证据：三档截图、连续流 `10px`、创作面板 `278/289/289px`、悬浮助手 `62px`/上浮 `26px`、五等分宽差 `0.015625px`、五素材加载和 DOM 文本检查均通过。
- 质量证据：类型检查、H5 构建、`git diff --check` 均退出码 `0`。
- 人工门禁：已由用户明确确认关闭；第二阶段由 `DEV-033` 独立承接，预览页继续冻结保留。

## 2026-07-18 DEV-031 第二轮返工最终复验

- 验收结论：通过。
- 逐项结果：`AC-HOME-001`、`AC-HOME-002`、`AC-HOME-003`、`AC-HOME-004`、`AC-HOME-101`、`AC-HOME-102` 全部通过。
- 视觉门禁：`03-测试验证/DEV-031/visual-verdict.json` 为 `96/pass`，达到 `95/pass`。
- 关键证据：三档列比、gap、等高、五卡插画面积、三标签、图标字号、猫头鹰白环/上浮/无遮挡、真实导航、无溢出与浏览器错误全部通过。
- 质量证据：类型检查、H5 构建、`git diff --check` 均退出码 `0`。
- 边界：最终复验未修改应用源码或 `uni_modules/**`。

## 2026-07-18 DEV-031 返工后独立复验

- 验收结论：未通过，继续返工。
- 逐项结果：`AC-HOME-001`、`AC-HOME-101` 通过；`AC-HOME-002`、`AC-HOME-003`、`AC-HOME-004`、`AC-HOME-102` 未通过。
- 视觉门禁：`03-测试验证/DEV-031/visual-verdict.json` 为 `92/fail`，低于 `95/pass`。
- 量化失败：360 gap `7.68px`；360 两张右卡插画约 `23.4%`；360/390 左右标签仅 `9.6/10.4px`；AI工作台与猫头鹰重叠 `4.33-5.16px`；430 猫头鹰白环外径 `73.38px`。
- 正向证据：布局、文案、顶部透明融合、真实导航、列比、等高、无溢出/裁切/浏览器错误以及质量命令均通过。
- 边界：独立验收未修改应用源码或 `uni_modules/**`。

## 2026-07-18 DEV-031 最新参考图返工独立复验

- 验收结论：未通过，退回返工。
- 逐项结果：`AC-HOME-001`、`AC-HOME-002`、`AC-HOME-003`、`AC-HOME-004`、`AC-HOME-102` 未通过；`AC-HOME-101` 通过。
- 失败证据：测评左列实测 `54.8%-55.2%`，360 档 gap `5.76px`；常用功能插画未达到约 25% 可见面积；底栏缺少可见“AI工作台”标签，390 档中间猫头鹰下部有裁切感；顶部猫头鹰带灰色矩形底。
- 视觉门禁：`03-测试验证/DEV-031/visual-verdict.json` 为 `72/fail`，未达到 `95/pass`。
- 正向证据：五个入口及三项底栏结构均存在，真实导航全部通过；三档无横向溢出，console/page errors 为空；类型检查、H5 构建、`git diff --check` 均退出码 `0`。
- 边界：独立验收仅更新正式测试脚本、截图、JSON 与结论，未修改任何应用源码或 `uni_modules/**`。

## 2026-07-18 DEV-031 最新参考图返工重开

- 当前结论：未通过 / 待返工。
- 用户复验反馈：上一轮首页块状结构、内部配图、字体、边缘、边距与三入口底栏未达到最新参考图的一致性要求；旧 `93/pass` 结论失效。
- 新通过门禁：测评面板须为左大右二布局并包含“自我评测 / 考题测评 / 错题重做”完整配图；常用功能面板须包含带配图的“岗位招聘 / 联系客服”；底栏须复刻三等分、字体、图标比例和中间猫头鹰上浮结构；整体颜色只采用登录页暖色体系。
- 重跑要求：`360x800`、`390x844`、`430x932` 三档 Playwright、Chrome DevTools 直调、`visual-verdict >= 95/pass`、类型检查、H5 构建和导航回归全部重新执行，不沿用旧截图判定通过。

## 2026-07-18 DEV-031 首页样式与导航独立验收

- 验收结论：通过。
- 已通过：`AC-HOME-001`、`AC-HOME-002`、`AC-HOME-003`、`AC-HOME-004`、`AC-HOME-101`、`AC-HOME-102`。
- 证据：`03-测试验证/DEV-031/navigation-results.json`、三档首页截图、`03-测试验证/DEV-031/visual-verdict.json`（`93/pass`）及 `TC-UI-HOME-001-首页视觉与交互.md`。
- 边界：本轮独立验收仅修正测试脚本的 URL 探活和唯一元素选择器；未修改应用实现、旧首页、登录页、业务页面、服务层或 `uni_modules/**`。
- 构建与质量：`pnpm run build:h5` 退出码 `0`（`DONE Build complete.`），`pnpm type-check` 退出码 `0`，`git diff --check` 退出码 `0`。
- 应用源码边界：DEV-031 仅涉及新增 `src/pages/home.vue` 和 `src/pages.json` 的 `pages/home` 注册。旧首页 `src/pages/index.vue`、`src/components/AppDynamicTabBar.vue`、`src/pages/auth/login.vue` 与 `uni_modules/**` 无工作区或暂存区差异；工作区已存在的 `center`、`services` 差异不含本任务首页/导航/客服实现，未纳入本次验收。
- 提交与推送：`50e7a48c feat: add independent app home` 已推送到 `origin/feature/20260601000000-vphase1-initial-delivery`，`AC-HOME-201` 通过，`DEV-031` 闭环完成。

## 2026-07-17 APP 登录页协议黑勾与双 Tab 中线返工独立验收

- 验收范围：`DEV-030 / M14` 的 `AC-LOGIN-002 / TC-UI-LOGIN-001`，并回归既有登录逻辑边界。
- 验收结论：通过；当前源码 `5173` 服务的正式 Playwright 三视口 × 双 Tab、`pnpm run type-check`、`pnpm run build:h5` 均退出码 `0`，`visual-verdict=96/pass`。
- 中线证据：两侧最终可见区域严格 `50/50`；未选中虚化层下穿白色圆弧 `58px`，三档双状态 `gap=0px`、外缘偏差 `0px`，六张原图无透明竖带、硬线或背景断层，学员/企业状态完整镜像。
- 协议证据：学员、企业两角色均为“未勾白底无勾 → 勾选白底 `rgb(26,26,26)` 黑勾 → 二次点击恢复无勾”，active class 与状态同步。
- 逻辑边界：`scriptSetupUnchanged=true`、`internalPasswordInputUnchanged=true`、`studentLoginFlowPresent=true`、`enterpriseLoginFlowPresent=true`、`roleBranchUnchanged=true`、`noSmsFlowAdded=true`；既有协议状态、内部 password 校验、登录提交及跳转流程未改。
- 证据入口：`03-测试验证/DEV-030/playwright-results.json`、`tab-state-results.json`、`visual-verdict.json` 及三档双状态六张截图。

## 2026-07-17 APP 登录页协议黑色勾号返工待实施（历史，已关闭）

- 验收范围：`DEV-030 / M14` 的协议勾选视觉，复用 `AC-LOGIN-002 / TC-UI-LOGIN-001`；双 Tab 中线连续虚化失败仍保持未关闭。
- 当前结论：未通过 / 待实施；协议状态实际可切换，但勾选后的白色勾号被后置白底覆盖，视觉上无法确认已勾选。
- 通过门禁：学员、企业两种角色下，未勾选为无勾号白底；勾选后仍为白底并显示黑色/等效近黑色勾号（各 RGB 通道不高于 `32`）；再次点击恢复未勾选；既有协议状态、登录校验、提交和跳转逻辑不变。
- 重跑入口：`03-测试验证/DEV-030/TC-UI-LOGIN-001-登录页视觉与交互.md` 与 `03-测试验证/05-自动化测试脚本/check-login-page.mjs`；本轮尚未执行修复后重测，不得沿用历史视觉分数判定通过。

## 2026-07-17 后端 protection 模块编译产物修复独立验收

- 验收范围：独立的后端编译产物修复，对应 `AC-BACKEND-BOOT-001 / AC-BACKEND-BOOT-101`；不混入 `DEV-030` 纯视觉验收。
- 当前结论：通过；目标模块限定清理重编与独立复编均 `exit 0 / BUILD SUCCESS`，29 个 `.class` 恢复，目标 Configuration、Aspect、imports 与 `javap` 复核通过；用户已启动进程的完整运行态复验同步通过。
- 通过门禁：只清理该模块 `target`；重新编译退出码 `0` 且输出 `BUILD SUCCESS`；目标 `YudaoIdempotentConfiguration.class` 和相关 Java 类恢复；启动越过原自动配置元数据读取错误；源码、POM、自动配置清单和 `uni_modules/**` 不变。
- 完整运行证据：Java PID `56620` 持续存活并监听 `0.0.0.0:48080` 与 `[::]:48080`；`GET /actuator/health` 返回 HTTP `200 / {"status":"UP"}`，`GET /` 返回 HTTP `200`。原 `Unable to read meta-data`、目标类 `FileNotFoundException`、`IllegalStateException` 不可能继续阻断当前运行进程的启动主链路。
- 历史关注（已关闭）：独立 JDK 17 argfile 启动曾到达 `Tomcat initialized with port(s): 48081`，但 60 秒观察窗内未输出完整 `Started`，当时未声明完整启动；现已由 PID `56620` 的端口监听和 HTTP 健康证据关闭。
- 边界证据：protection 源码、POM、自动配置清单工作区与暂存区均无本次变化；临时启动结束后仅停止本次 PID，未影响用户与 IDEA 原有进程。
- 重跑入口：`03-测试验证/BUILD-FIX-001/TC-CODE-IDEMPOTENT-001-幂等自动配置编译与启动.md`。

## 2026-07-17 APP 登录页双 Tab 中线无缝连接复验失败（历史，已关闭）

- 验收范围：`DEV-030 / M14` 的登录角色双 Tab 中线连接，重点复验 `AC-LOGIN-002` 与 `TC-UI-LOGIN-001`；既有登录逻辑不在修改范围。
- 当前结论：未通过 / 待返工；用户现场截图显示学员登录选中时，白色圆弧与右侧企业登录虚化面之间出现未虚化竖带和硬接缝，企业登录选中时须按镜像状态一并修复。
- 新增门禁：两侧最终可见区域继续严格各占 `50%`；未选中虚化层必须在层叠上越过中线并延伸至选中白色圆弧下方，由白色面覆盖；中线几何 `gap=0px`、可见 `seam=0px`，不得出现透明空隙、未虚化竖带、硬竖线或背景断层；两状态完整镜像。
- 历史保留：上一轮 `visual-verdict=96/pass` 及三档等宽数据保留，用于证明当时 `50/50` 和既有交互检查；该结果未覆盖本次新增的虚化下穿与中线无缝口径，不能沿用为当前通过结论。
- 重跑入口：`03-测试验证/05-自动化测试脚本/check-login-page.mjs`；返工后须在 `360×800`、`390×844`、`430×932` 三档执行双状态检查，并补充中线 `gap/seam` 数值及圆弧下方连续虚化的截图/像素证据。

## 2026-07-17 APP 登录页双 Tab 严格 50/50 独立验收

- 验收范围：`DEV-030 / M14` 的登录角色双 Tab 等宽布局与双状态切换，重点复验 `AC-LOGIN-002`，并回归 `AC-LOGIN-001`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`。
- 验收结论：通过；正式 Playwright 三视口、双状态点击检查、`pnpm run type-check`、`pnpm run build:h5` 和 `git diff --check` 均退出码 `0`，`visual-verdict=96/pass`，达到 90 分视觉门禁。
- 严格等宽证据：`360×800` 的左右 Tab 为 `170/170px`，`390×844` 为 `183/183px`，`430×932` 为 `203/203px`；学员态与企业态的宽差、重叠、中线偏差和横向溢出均为 `0`，两侧文案中心分别位于容器 `25%/75%`。
- 状态证据：选中 Tab 为纯白清晰面且无 `filter/backdrop-filter`，未选中 Tab 为半透明虚化面；切换角色后左右表面、文案激活态和相交圆弧完整镜像互换，不存在描边小 Tab 或胶囊。
- 逻辑边界：内部 password 校验、学员/企业提交与既有登录流程未改；本轮只收紧双 Tab 几何布局，`uni_modules/**` 无变更。
- 证据入口：`03-测试验证/DEV-030/playwright-results.json`、`03-测试验证/DEV-030/visual-verdict.json`、三档 `login-*.png` 与 `login-enterprise-*.png`。上一轮 `94/pass` 结论保留在下方作为历史记录。

## 2026-07-17 APP 登录页完整双 Tab 表面返工独立验收

- 验收范围：`DEV-030 / M14` 的登录角色双 Tab 表面与切换状态，重点复验 `AC-LOGIN-002`，并回归 `AC-LOGIN-001`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`。
- 验收结论：通过；正式 Playwright 三视口、双状态点击检查、`pnpm run type-check`、`pnpm run build:h5` 和 `git diff --check` 均退出码 `0`，`visual-verdict=94/pass`，达到 90 分视觉门禁。
- 关键视觉证据：“学员登录 / 企业登录”构成左右两个完整半区 Tab；当前选中 Tab 为纯白清晰面，`filter` 和 `backdrop-filter` 均为 `none`；未选中 Tab 为半透明虚化面，可透出后方猫头鹰；切换角色后左右表面、文案激活态和相交圆弧完整镜像互换。
- 反向门禁：Tab 自身边缘为 `0px/none`，不存在 `1px` 描边小 Tab 或胶囊；后续不得退回上一轮描边按钮方案。
- 响应式与逻辑边界：`360×800`、`390×844`、`430×932` 的学员态和企业态全部通过且无横向溢出；`scriptSetupUnchanged=true`、`internalPasswordInputUnchanged=true`、`roleBranchUnchanged=true`、`noSmsFlowAdded=true`，内部 password 校验、两类角色提交与既有登录流程未改；`uni_modules/**` 无变更。
- 证据入口：`03-测试验证/DEV-030/tab-state-results.json`、`03-测试验证/DEV-030/playwright-results.json`、`03-测试验证/DEV-030/visual-verdict.json`、三档 `login-*.png` 与 `login-enterprise-*.png`。

## 2026-07-17 APP 登录页猫头鹰遮罩可见性返工后独立复验

- 验收范围：`DEV-030 / M14` 的猫头鹰半透明遮罩可见性，重点复验 `AC-LOGIN-002`，并回归 `AC-LOGIN-001`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`。
- 验收结论：通过；正式 Playwright 三视口、`pnpm run type-check`、`pnpm run build:h5` 和 `git diff --check` 均退出码 `0`，严格目视对照后 `visual-verdict=92/pass`，达到 90 分视觉门禁。
- 返工后证据：`360×800`、`390×844`、`430×932` 三档遮罩内均可肉眼辨认猫头鹰两侧翅膀外轮廓、腹部圆弧下缘和脚部暗影，不再表现为均匀橙色色块；下部仍位于前景遮罩后并略微柔化，上部保持清晰。
- 回归结果：白色 Tab 弧线方向、暖色纵向渐变、上下分界位置、账号区域、底部圆角和响应式表现均未回退；三档无横向溢出、异常重叠、正文裁切或浏览器错误。
- 逻辑边界：自动化静态检查 `scriptSetupUnchanged=true`、`internalPasswordInputUnchanged=true`，内部 password 校验、学员/企业提交、微信、协议、加载态和登录后跳转流程未改；`uni_modules/**` 无变更。
- 证据入口：`03-测试验证/DEV-030/playwright-results.json`、`03-测试验证/DEV-030/visual-verdict.json`、`03-测试验证/DEV-030/login-360x800.png`、`login-390x844.png`、`login-430x932.png`。

## 2026-07-17 APP 登录页猫头鹰遮罩可见性独立复验（历史，已关闭）

- 验收范围：`DEV-030 / M14` 的猫头鹰半透明遮罩可见性，重点复验 `AC-LOGIN-002`，并回归 `AC-LOGIN-001`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`。
- 验收结论：未通过；正式 Playwright 三视口、`pnpm run type-check`、`pnpm run build:h5` 和 `git diff --check` 均退出码 `0`，但严格目视对照后 `visual-verdict=86/revise`，未达到 90 分视觉门禁。
- 失败证据：`360×800`、`390×844`、`430×932` 三档截图中，猫头鹰下部在半透明区域内主要显示为均匀橙色阴影/色块，无法像参考图一样辨认身体或暗部轮廓；`26px` 重叠、alpha `0.16` 和 `blur(3px)` 参数存在不能替代肉眼可见性。
- 回归结果：猫头鹰上部清晰；白色 Tab 弧线方向、暖色纵向渐变、分界位置、底部圆角、响应式无溢出和登录逻辑边界均未回退。
- 逻辑边界：自动化静态检查 `scriptSetupUnchanged=true`、`internalPasswordInputUnchanged=true`；内部 password 校验和学员/企业提交未改；`uni_modules/**` 无变更。
- 证据入口：`03-测试验证/DEV-030/playwright-results.json`、`03-测试验证/DEV-030/visual-verdict.json`、`03-测试验证/DEV-030/login-360x800.png`、`login-390x844.png`、`login-430x932.png`。

## 2026-07-17 APP 登录页第三轮视觉修正独立验收

- 验收范围：`DEV-030 / M14` 的 `AC-LOGIN-001`、`AC-LOGIN-002`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`；提交推送闭环由主代理处理。
- 验收结论：通过；正式 Playwright 三视口、`pnpm run type-check` 和 `pnpm run build:h5` 均退出码 `0`，`visual-verdict` 为 `93/pass`，达到 90 分视觉门禁。
- 关键视觉证据：白色学员 Tab 区从左上较窄处向右下展开，曲线方向与参考一致；猫头鹰上部清晰、下部约 `18px` 位于 `blur(12px)`、alpha `0.54` 的半透明前景层后；暖色背景具有纵向渐变，上下区域分界形成柔和虚化/光晕，虚化带延伸到账号标签上方约 `16px`。
- 响应式与逻辑边界：`360×800`、`390×844`、`430×932` 均无横向溢出、异常重叠、正文裁切或浏览器错误；`script setup` 与 HEAD 一致，内部 password 校验、学员/企业提交、微信、协议、加载态和登录后跳转流程未改；`uni_modules/**` 无变更。
- 轻微差异：猫头鹰进入虚化层的占比和暖色虚化带宽度仍略小于参考图，不影响“仅一小部分位于虚化层后”、曲线方向、渐变和分界位置的正式验收点。
- 证据入口：`03-测试验证/DEV-030/playwright-results.json`、`03-测试验证/DEV-030/visual-verdict.json`、`03-测试验证/DEV-030/login-360x800.png`、`login-390x844.png`、`login-430x932.png`。

## 2026-07-17 APP 登录页第二次视觉返工独立验收

- 验收范围：`DEV-030 / M14` 的 `AC-LOGIN-001`、`AC-LOGIN-002`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`、`AC-LOGIN-201`。
- 验收结论：通过；正式 Playwright 三视口、`pnpm run type-check` 和 `pnpm run build:h5` 均退出码 `0`，`visual-verdict` 为 `92/pass`，达到 90 分视觉门禁；提交 `ea170194` 已推送，`DEV-030` 已完成。
- 关键证据：账号/验证码 label 均为 `16px / 600 / rgb(98, 91, 86)`；“获取验证码”为无点击属性的 `UNI-VIEW`；吉祥物与虚化层重叠 `18px`，虚化层 `blur(12px)`、alpha `0.54`、底边圆角 `0`；白色页面底部圆角 `36px`，暖色留白 `16px`。
- 逻辑边界：`script setup` 与 HEAD 一致；无验证码请求、倒计时或短信逻辑；内部 `verifyCode`、password 校验、学生/企业登录接口、微信、协议、加载态和跳转流程未改变；`uni_modules/**` 无变更。
- 视觉复核：两张用户局部参考图与最新 `login-390x844.png` 已实际查看；右侧虚化区略窄、第三方图标略大为轻微差异，不影响本轮正式验收点。
- 证据入口：`03-测试验证/DEV-030/playwright-results.json`、`03-测试验证/DEV-030/visual-verdict.json`、`03-测试验证/DEV-030/login-360x800.png`、`login-390x844.png`、`login-430x932.png`。

## 2026-07-17 APP 登录页第二次视觉返工重开（历史，已关闭）

- 验收范围：`DEV-030 / M14`，对应 `AC-LOGIN-001`、`AC-LOGIN-002`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`、`AC-LOGIN-201`。
- 当前结论：未通过；用户最新复验指出账号/验证码标签样式、半透明虚化层与吉祥物遮挡关系、白色页面底部大圆角和验证码展示文案仍不符合参考图，旧 `93/pass` 结论失效。
- 失败影响：页面视觉与展示文案；现有登录、角色分流、微信、协议、加载态和登录后跳转逻辑不得调整。
- 实施进度：第二次视觉返工已完成实施自检；正式 Playwright、type-check 和 H5 构建通过，最新证据已更新，等待独立验收代理复跑后再调整本结论。
- 重跑入口：`03-测试验证/05-自动化测试脚本/check-login-page.mjs`；返工实现完成后由独立验收代理重新判定，不沿用旧证据宣称通过。

## 2026-07-17 APP 登录页严格复刻返工

- 验收范围：`DEV-030 / M14`，对应 `AC-LOGIN-001`、`AC-LOGIN-002`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`、`AC-LOGIN-201`。
- 验收结论：通过；2026-07-17 返工独立复验中，正式 Playwright、类型检查和 H5 构建均通过，`visual-verdict` 为 `93/pass`，达到 90 分视觉门禁；返工提交 `8976695d` 已推送，`DEV-030` 已完成。
- 验收项结果：`AC-LOGIN-001`、`AC-LOGIN-002`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`、`AC-LOGIN-201` 全部通过。
- 复验结果：品牌 `y=104`、副标题 `y=153`，进入参考图欢迎层级；Apple 标识清晰，`iosTextContent=''`，容器无按钮、点击、导航、请求或 handler。
- 影响范围：APP 登录页展示结构与视觉；现有登录、角色分流、微信、协议、加载态和登录后跳转逻辑不在返工范围。
- 独立验收证据：`03-测试验证/DEV-030/visual-verdict.json`、`03-测试验证/DEV-030/visual-compare-390x844.png`、`03-测试验证/DEV-030/playwright-results.json`。
- 重跑入口：`03-测试验证/05-自动化测试脚本/check-login-page.mjs`；本次三档视口、构建与 90 分视觉门禁均已通过。

## 2026-06-13 客服会话实时联动

- 验收范围：`DEV-015 / M12` 客服会话实时刷新、会话名称与聊天头像展示。
- 验收结论：通过；未执行真实 APP 到管理端 WebSocket 联调，需测试环境登录态复验实时链路。
- 关键证据：后端学员发消息与后台客服回复均广播 `yj_customer_service_message` 到 ADMIN；管理端客服会话页监听 `/infra/ws` 并在收到消息后刷新会话列表和当前消息；会话标题显示“学员名称 的对话”；消息前显示学员或客服头像框与名称。
- 验证命令：`mvn -pl yunjikeji-admin-server -am -DskipTests compile`，结果 `BUILD SUCCESS`；`pnpm exec eslint src/views/yj/resource/index.vue src/views/yj/resource/config.ts`，结果通过；`git diff --check`，结果通过。
- 边界检查：`git diff --name-only -- uni_modules ../yunjikeji-admin-ui/uni_modules ../yunjikeji-admin-server/uni_modules` 无输出，未改动 `uni_modules/**`。
- 遗留事项：真实 WebSocket 联动依赖测试环境后端、管理端登录态和 APP 学员端登录态，本轮未启动完整联调环境。

## 2026-06-11 企业审核重复提交拦截

- 验收范围：`DEV-001 / DEV-024 / DEV-025` 企业审核重复提交后端拦截。
- 验收结论：通过。
- 关键证据：`saveAudit` 与 `submitAudit` 两条路径均已加入相同手机号 + 相同企业名称 + 审核中状态的重复校验，并排除当前记录自身。
- 验证命令：`mvn -f yunjikeji-admin-server/pom.xml -pl yunjikeji-admin-server -am -DskipTests compile`，结果 `BUILD SUCCESS`。
- 边界检查：`git diff --name-only -- uni_modules` 无输出，未改动 `uni_modules/**`。
- 遗留事项：无。

## 2026-07-16 APP 登录页样式改造

- 验收范围：`DEV-030 / M14`，对应 `AC-LOGIN-001`、`AC-LOGIN-002`、`AC-LOGIN-003`、`AC-LOGIN-101`、`AC-LOGIN-102`。
- 验收结论：通过；闭环项 `AC-LOGIN-201` 已由提交 `c204c7fe` 及远程推送完成，`DEV-030` 已完成。
- 关键证据：Playwright 最终退出码 `0`；实际视口 `360×800`、`390×844`、`430×932`，三档背景均为 `rgb(245, 240, 234)`，滚动宽度等于视口宽度，重叠、正文截断和浏览器错误列表均为空；三张非空截图已人工复核。
- 业务入口：品牌、副标题、学员/企业切换、手机号、密码、登录/加载态、微信登录、协议勾选、用户协议、隐私协议和 ICP 均保留；390 档实际打开两类协议页面成功。
- 构建结果：`npm run type-check` 退出码 `0`；`npm run build:h5` 退出码 `0` 并输出 `DONE Build complete.`。
- 边界检查：提交与推送前暂存区 `uni_modules/**` 无变更；登录页提交包含并保留工作区此前已有的密码登录改动；独立验收代理未修改正式页面实现。
- 证据入口：`03-测试验证/DEV-030/playwright-results.json`、`03-测试验证/04-测试执行结论.md`、`03-测试验证/05-自动化测试脚本/check-login-page.mjs`。
- 未覆盖边界：未执行真实账号登录接口集成测试；本任务为隔离 H5 环境的静态 UI 样式验收，不伪造登录结果。
## 2026-08-17 Story-050 练习数据跨组织共享本地独立验收

- 验收范围：`DEV-051` ~ `DEV-054` 本地证据，覆盖红灯真实性、9 张运行时表与 2 张条件性兼容表矩阵、实现与 SQL 静态契约、回滚完整性、代码测试、39 模块编译及保护边界。
- 当前结论：本地独立验收通过，`DEV-051` ~ `DEV-053` 已完成，`DEV-054` 进入待测试环境验收；Story-050 尚未完成。
- 验收项结果：本地证据支持 `AC-PRACTICE-SHARED-101`、`301` 通过；`AC-PRACTICE-SHARED-001`、`201`、`401` 不得标记为整体通过。
- 提交与推送：子项已完成；功能提交 `22aded0e feat: remove tenant isolation from practice data`，远端同步合并后本地 `HEAD` 与远端均为 `90f8e9277cbe2fccf9f517d94cd3ec5fa09ba110`，已推送 `origin/feature/20260601000000-vphase1-initial-delivery`。提交精确包含 49 个任务文件、禁区文件 0 个，`application.yaml` 及无关文件未提交。
- 未完成门禁：数据库脚本执行、测试环境双租户双用户数据验证和用户复验尚未完成；`AC-PRACTICE-SHARED-001`、`201`、`401` 整体仍不得标记为通过。
- 证据入口：`03-测试验证/DEV-051/`、`03-测试验证/DEV-053/`、`03-测试验证/04-测试执行结论.md`。

## 2026-08-31 Story-053 企业端教员我的页真实资料正式验收门禁

- 验收范围：`REQ-053 / PD-053 / DEV-080..083 / AC-PROFILE-001/101/201/301`，覆盖企业页面真实姓名与部门、接口契约、tenantId+mobile 唯一关联、缺失关联受控空值和角色回归。
- 当前结论：未执行。当前只完成 DEV-080 程序设计资产；DEV-081 红灯、DEV-082 业务实现和 DEV-083 独立验收均未开始。
- 业务验收状态：`AC-PROFILE-001` 未执行。
- 技术验收状态：`AC-PROFILE-101`、`AC-PROFILE-201`、`AC-PROFILE-301` 未执行。
- 门禁：DEV-081 正确红灯形成前不得启动 DEV-082；DEV-082 与 DEV-083 必须由不同代理执行。任何任务不得修改数据库或 `uni_modules/**`。
- 正式结论：Story-053 尚未通过，当前不得宣称页面、接口或租户隔离已修复。

## 2026-08-31 DEV-028 企业端教员选学员会话与学员端客服兼容验收门禁

- 验收范围：`DEV-028 / AC-CS-004/005/104/105/106/202`，覆盖 `pageSize=50` 的分页全局可见、101 人三页、上一页 / 下一页、首末页禁用、`pageNo=1/2/3`、翻页数据隔离、任意页真实双键且不一次加载全部；同时覆盖账号 `tenantId=null`、有 / 无会话、历史 GET 不创建新 session 且允许合法接收方绑定、持久 tenant 规范化、学员端兼容、H5 能力保护及独立验收闭环。
- 原始路径：企业端教员登录 -> 底部客服 -> 全局所有符合既有审核通过条件的学员分页列表 -> 点击任一页具体学员 -> 与该学员的会话页。列表以学员为主 LEFT JOIN 会话，包括尚未加入组织且账号 `tenantId=null` 的学员；全局可见通过分页到达，不要求一次自动加载全部。无会话学员是合法结果且保留教员首次主动联系能力。
- 学员端兼容路径：学员即使尚未加入组织且 `tenantId=null`，仍可从 `profile.vue` 原“联系客服”入口进入共用会话页；目标为保持原会话创建 / 复用、历史加载、发送、退出重进及既有未入组织创建存储规则。
- 当前结论：`FAIL`。2026-08-31 不同代理独立复跑后，后端目标类 `10/10`、39 模块编译、前端静态契约、type-check、H5 build、diff 与禁区检查均通过；最新本地 H5 中两个键盘 API 均为 `undefined`，学员参数和企业参数两次无登录直达 load / unload 均未出现对应 TypeError。但企业客服列表只固定请求 `pageNo=1&pageSize=100`，既不符合正式 `pageSize=50`，也没有上一页 / 下一页或页码，无法浏览首批结果以后学员，属于正式未关闭缺陷。
- 验收项状态：`AC-CS-004`、`AC-CS-104`、`AC-CS-202` 未通过；`AC-CS-005`、`AC-CS-105`、`AC-CS-106` 仅有本地代码级部分证据，保持待测试环境重跑，不得标记通过。
- 会话分支：已有会话必须进入并复用所选学员同一真实 conversation，不得创建重复 / 空 / default；无会话允许进入空态，空态、历史 GET 和首次发送均不得以教员或学员 tenant 非空为前置条件。历史 GET 不得创建新 session；对 `conversationId + studentId` 已校验的既有 session，允许沿用 `bindSessionReceiver` 绑定当前教员接收方以保持 WebSocket 实时收件，该绑定不得被误判为隐式建会话。首次发送必须显式 start/create 并取得真实 `conversationId` 后再发送，后续历史与 WS 均使用该 ID。禁止 synthetic conversationId。
- 可见、分页与权限：企业教员必须通过 `pageSize=50` 的正式分页 UI 看到所有符合既有审核通过条件的学员，包括尚未加入组织且账号 `tenantId=null` 的学员；101 人形成 3 页，上一页 / 下一页可操作，首页禁用上一页、末页禁用下一页、中间页两者可用，`pageNo=1/2/3` 与展示页一致。每次只展示当前页响应，翻页与返回不得残留、追加、重复或混入其他页数据；点击任一页任一行须传递该行真实 `studentId` 与已有真实 `conversationId`。全局可见不得解释为一次自动加载全部，列表与会话链路也不得以 `tenant_id` 作为可见或权限过滤条件。
- tenant 存储与归属：tenant 仅为存储事实；已有 session 与消息沿用各自实际非空持久 `tenant_id`。尚未加入组织的学员首次 start/create 产生的新 session 与消息复用学员端既有规范化规则，将账号 `tenantId=null` 持久化为 `tenant_id=1`；不得把账号空值传播到 session/message，也不得新增或臆造其他默认 tenant。历史与新增消息以 `studentId + conversationId` 归属所选学员，防止串学员。
- 学员端保护边界：企业端跨租户共享修复不得改变学员端既有 `/app-api/yj/customer-service/**` 的会话创建 / 复用、历史查询、发送或退出重进语义。
- 数据库与残余风险：本次禁止数据库变更，不新增唯一索引或约束。当前无数据库唯一约束，并发首次联系同一无会话学员仍可能创建重复 session；该项为已登记残余风险，不得声称已保证强并发唯一。
- 证据完整性门禁：实施阶段 `01-红绿灯与实施验证.md` 截至本次独立验收仍将转绿 / 构建记录为“待执行”，没有记录所述聚焦 `4/4` 的精确命令；独立 `10/10` 复跑不能代替缺失的实施转绿回执。
- 重跑入口：返工后先以 101 名可区分审核通过学员验证 `pageSize=50` 的 3 页、`pageNo=1/2/3`、首中末页控件状态、翻页前后行集合、任意页点击上下文且不一次加载全部，再覆盖账号 tenant 有值 / `null`、有会话 / 无会话、持久 tenant 规范化、学员端回归和独立验收结论；统一落入 `03-测试验证/DEV-028/`。真实接口、数据与浏览器交互验收只能在测试环境使用 `chrome-devtools-mcp` 执行。
- 人工依赖门禁：当前 Chrome H5 无 `yunjikeji-user-session`；测试环境正式连接记录未提供 H5 测试入口、企业 / 学员账号、验证码或可复用登录材料。本轮未连接数据库、未伪造 token 或接口通过，真实列表、已有会话、无会话首次发送、tenant 存储、WS 与退出重进均待人工补齐环境和账号后复验。
- 正式结论：DEV-028 保持进行中并退回分页 UI 返工；当前不得提交完成或推送完成结论。独立证据入口为 `03-测试验证/DEV-028/02-独立验收结论.md`。
