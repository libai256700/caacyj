# AC-PROD-NGINX-301 生产变更边界与执行分工保护

- 类型：技术-代码级
- 关联任务：`TASK-026`
- 正式入口：生产机上与 `xiaojiapp.caacyj.com` 直接相关的 Nginx 配置文件、实施记录和独立验收记录。
- 支撑的业务结果：本次生产切换保持最小影响面，只改变目标域名的上游映射，并由不同执行方完成实施与验收。
- 技术边界：不修改项目源码、不修改数据库、不改 DNS、不触碰无关站点；优先 `reload` 而非硬 `restart`，只有 `reload` 明确不适用时才允许 `restart` 并记录原因。
- 通过条件：最终差异仅落在 `xiaojiapp.caacyj.com` 相关 Nginx 配置；实施与独立验收由不同代理或人类完成；无无关服务、无关配置或源码被一并改动。
- 证据承接方式：配置差异白名单、实施记录、独立验收记录与最终任务回写共同证明。

## 验收结果

- 状态：已验证。
- 结果：通过。最终变更仅落在 `xiaojiapp.caacyj.com` 相关生产 Nginx 配置；项目源码、数据库、DNS 和其他无关站点未改动，生产 Java 未重启。实施与独立验收由不同代理完成，独立验收结论为 `PASS`。
