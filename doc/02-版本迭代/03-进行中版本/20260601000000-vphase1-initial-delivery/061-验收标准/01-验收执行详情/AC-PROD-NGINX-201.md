# AC-PROD-NGINX-201 生产Nginx备份、回滚与敏感信息保护

- 类型：技术-数据级
- 关联任务：`TASK-026`
- 正式入口：生产机当前生效 Nginx 主配置、相关 include 文件、证书引用信息、备份目录和回滚步骤。
- 支撑的业务结果：生产 Nginx 变更失败时可以快速恢复旧入口，不因配置切换导致长时间中断或敏感信息泄露。
- 技术边界：变更前必须备份完整生效配置与证书引用信息；文档、日志和仓库不得回显私钥、证书内容或其他敏感凭据；不改数据库，不执行生产库写入。
- 通过条件：备份路径和回滚步骤明确可用；`nginx -t` 先于加载动作执行；若 `reload` 或外部验证失败，能按备份恢复旧配置并确认入口恢复；整个过程不泄露敏感信息。
- 证据承接方式：备份目录记录、回滚命令或步骤、`nginx -t` 回执、异常时的恢复记录与验证结果统一写入正式验收目录。

## 验收结果

- 状态：已验证。
- 结果：通过。变更前完整备份位于 `/data/backup/nginx/conf/TASK-026-20260904175724`；`nginx -t` 已通过，并于 `2026-09-04 17:58:54` 平滑 `reload`。本次未触发回滚，回滚入口保留有效，正式记录未写入私钥、证书内容或其他敏感凭据。
