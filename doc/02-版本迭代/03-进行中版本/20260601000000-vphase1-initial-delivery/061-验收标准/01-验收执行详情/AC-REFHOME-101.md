# AC-REFHOME-101 独立实现与隔离边界

- 当前状态：通过。独立结构、构建和隔离边界复验通过。
- 类型：技术-代码级
- 关联任务：`DEV-032`
- 正式入口：独立预览页面路由。
- 技术边界：只允许新增预览页、预览页专用素材和单个页面注册项；当前首页、旧首页、共享底栏、登录页、服务文件和 `uni_modules/**` 不得产生任务差异。
- 通过条件：页面由可维护的 Vue 模板和样式组成，文本为真实 DOM 文本，卡片和底栏为独立结构；整张参考截图不得作为页面背景或整页图片。
- 证据承接：类型检查、H5 构建、Git 边界检查与浏览器 DOM 检查全部通过。

## 独立复验

- 页面由 Vue 模板、真实 DOM 文本、独立卡片和底栏组成；五张局部专用素材均加载，不存在整页截图背景。
- `pnpm type-check`、`pnpm run build:h5`、`git diff --check` 均退出码 `0`。
- 当前首页、旧首页、共享底栏、登录页和 `uni_modules/**` 无 DEV-032 差异；工作区既有 `services/**` 差异未纳入本任务。
- Chrome DevTools MCP 被共享配置目录锁定，正式 Playwright 已完成 DOM、console、资源和三档几何复核。
