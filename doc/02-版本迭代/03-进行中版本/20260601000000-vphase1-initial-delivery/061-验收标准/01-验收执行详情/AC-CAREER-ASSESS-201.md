# AC-CAREER-ASSESS-201 两个独立人工初始化工具与数据回读

- 类型：技术-数据级
- 正式入口：独立题库与步骤 Python 工具、独立 Agent Python 工具、`yj_practice_step`、`yj_practice_exercises`、`yj_practice_exercises_answer`、`yj_agent_info`及人工执行回执。
- 支撑业务结果：分类14具备完整职业规划题库和专用 Agent；分类13题库与 `yj_agent_info.id=2` 不变。
- 技术边界：两个工具必须是不同文件和命令入口，默认 dry-run、显式 apply、写前备份、事务、冲突中止、异常回滚和写后回读；只连接当前程序同库 `114.111.30.111:13306/yunjikeji`，连接参数由人工显式传入且凭据不入Git。题库与步骤工具从职业规划源 `web` 解析步骤和每个题目源节点，写入分类14步骤并回填同分类题目 `step_id`，同时保留题目源schema的选填属性，不得把源选填题改写成分类13必答。
- 通过条件：源 `web/index.html:27-34,40-108` 的6步骤必须按题序1-9、10-13、14-26、27-38、39-40、41-47写入分类14；现库全部47题必须回读到同分类步骤的 `step_id`。已确认“姓名”排序2-7、9-13、41-47共18条是 `career_question_import.py` 跨label正则错误，工具只能在备份成功后用单事务原子修复18条题干、6步骤和47个 `step_id`，备份失败或回读差异时零修改/整体回滚；不得以运行时兼容掩盖。Agent工具固定插入id=3、tenant_id=1、agent_id=NULL及指定核心字段并回读 `SELF_SYSTEM_PROMPT` hash；apply时只要id=3已存在，无论内容是否相同，本次apply都安全中止且零修改，不更新、不换号。
- 证据承接：两个独立 dry-run 回执、分类14步骤/题目/选项备份哈希、18题干/6步骤/47关联修复事务回执、写后回读、分类13步骤/题库和Agent id=2不变反证及凭据扫描。
- 当前状态：待执行；已确认现库分类14为47题/111答案、全部47题 `step_id`为空，源并无重复“姓名”，18条错误题干待受备份保护的手工原子修复；Agent id=3仍为空。
