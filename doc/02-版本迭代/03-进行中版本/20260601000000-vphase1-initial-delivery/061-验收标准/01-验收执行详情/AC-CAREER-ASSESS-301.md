# AC-CAREER-ASSESS-301 分类13保护边界与实现隔离

- 类型：技术-代码级
- 正式入口：APP首页原单个开始入口和原单个报告入口点击后出现的选择层、分类14评测/报告平行代码、职业规划规则、固定主键 Agent 调用和两个独立人工工具。
- 支撑业务结果：新增职业规划评测的同时，分类13开始、答题、末题提交和报告行为保持修改前完全一致；分类14独立执行职业规划源规则引擎。
- 源规则隔离：分类14必须独立承接固定题序答案映射、`formVersion=2` schema校验、RIASEC/四维风格服务端重算和职业报告HTML校验；禁止调用分类13 `is_required` 空答分支、`SelfAssessmentV3RuleEngine`、分类13报告模板或Agent2。
- 技术边界：首页继续保留原单个开始入口和原单个报告入口，选择层只在点击对应入口后出现；分类13 `startSelfTest`、`openLatestSelfTestReport`及旧无参completed/latest-status/reset/start/record/report/regenerate调用链为保护边界，不新增逐题保存或续答逻辑。分类14题序14-25按组页展示 12 题，其余题序按单题页显示；分类14末题点击后直接统一提交自身答案，提交请求发出后立即显示与分类13一致的等待弹框，题间切换不显示全屏 loading，等待期间的关闭、成功、失败和可否重新进入行为严格对照当前分类13生效代码；所有业务源码中的步骤关联/查询必须显式按 `yj_practice_step.category_id` 过滤，分类13为13且结果不变、分类14为14；分类14规则和对已合法预初始化 `CAREER_ASSESSMENT_AGENT_INFO_ID=3L` 的固定读取独立。分类13/14报告页各自使用轻量报告查询；两个轻量路径均不得调用通用 `getRecordDetail`、题目统计或逐题答案重建，查询次数不得随题目数增长；通用 `/records/{recordId}` 继续返回完整 `answers`。两个工具不进入业务源码模块、启动、依赖注入、migration、build hook、资源初始化、定时或业务接口；不修改 `uni_modules/**`。
- 通过条件：两个原单入口仍存在且关闭选择层无业务动作；分类13保护边界调用序列和请求参数基线一致；分类14题序14-25按组页正确承载，所有记录判断经批次反查、所有步骤关联均有同分类过滤；分类14末题提交在交互层先发请求后立即出现等待弹框，等待弹框的关闭、成功、失败和重新进入行为与分类13当前生效代码一致；题间切换不显示全屏 loading；业务代码不引用工具；未运行工具时项目可正常构建/启动；类型检查、构建、目标测试、`git diff --check`和禁区审计通过。
- 证据承接：源码契约扫描、基线对比、依赖引用扫描、后端目标测试、前端类型检查/构建、工具隔离与凭据扫描。
- 当前状态：返工中；现有分类14实现错误沿用分类13选填空答分支，不符合本验收项。
