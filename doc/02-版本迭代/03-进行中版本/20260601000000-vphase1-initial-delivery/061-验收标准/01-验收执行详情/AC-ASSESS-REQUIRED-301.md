# AC-ASSESS-REQUIRED-301 自测题库是否必填实现边界与回归

- 类型：技术-代码级
- 关联任务：`DEV-084`、`DEV-085`
- 正式入口：管理端通用资源配置、后端 assessment-question 专用 CRUD 与聚焦测试。
- 支撑的业务结果：目标功能以最小改动接入现有列表和表单，不产生第二套页面或字段语义。
- 技术边界：复用 Element Plus 表格和下拉、通用资源页与既有 API；只扩展目标资源的 `is_required` 链路；不修改 `uni_modules/**`，不覆盖工作区无关改动。
- 通过条件：存在字段缺失导致的正确红灯与实现后的绿灯；后端聚焦测试、前端类型检查与构建、`git diff --check` 通过；列表、编辑、新增、状态、题型、自测答案和删除入口无回归。
- 证据承接方式：红绿回执、命令结果、差异清单及独立验收结论写入正式测试验证目录。
