# AC-PROD-NGINX-101 生产Nginx目标映射盘点与上游切换精确性

- 类型：技术-接口级
- 关联任务：`TASK-026`
- 正式入口：生产机 `106.13.128.217` 的 `nginx -T` 输出、`xiaojiapp.caacyj.com` 对应 `server_name` / `location` / `proxy_pass` / `upstream` 配置，以及新机 `106.13.71.85` 对应 Host/SNI 的实际响应。
- 支撑的业务结果：外部访问 `xiaojiapp.caacyj.com` 时，生产机能把命中的请求稳定转发到新服务器而不误伤其他站点或路径。
- 技术边界：必须先盘点真实端口、协议、header、Host、path 和证书引用，再按旧配置语义逐项切换；禁止把所有 `location` 机械替换成同一 `proxy_pass`，禁止把 HTTPS 入口改成简单外部跳转。
- 通过条件：`nginx -T` 已形成可复核的映射表；新机对每个命中路径的 Host/SNI、端口和协议都已先验证可响应；变更后相关 `location` 仅切到 `106.13.71.85`，其余语义保持与旧配置一致。
- 证据承接方式：生产机只读盘点记录、变更前后配置差异、生产机到新机的 Host/SNI 连通性结果和外部请求验证记录写入正式测试验证目录。

## 验收结果

- 状态：已验证。
- 结果：通过。生产机已按 `nginx -T` 盘点旧映射：旧根路径为本地静态，旧 admin / api 分别指向 `114.111.30.111:18081` 与 `114.111.30.111:18080`；本次仅将根、admin、api 三处改为 `proxy https://106.13.71.85`，SNI 固定 `yunjikeji.lai-do.com`，两个隐私相关路径保留本地。目标 Host/SNI 预验与外部访问、新机日志核对均通过。
