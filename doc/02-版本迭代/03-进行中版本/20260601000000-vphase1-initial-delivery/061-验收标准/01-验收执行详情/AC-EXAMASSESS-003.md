# AC-EXAMASSESS-003 分类切换查询联动

- 类型：业务-交互级
- 前置条件：至少两个分类返回不同题量或总分；可控制两次请求先后完成顺序。
- 触发动作：依次选择不同分类，并快速连续选择造成前一请求晚于后一请求返回。
- 期望结果：每次选择按 `fieldType || id` 重新查询；下方分类、题量、总分随最后选中项刷新；过期响应不覆盖最新分类结果。
- 核对方式：请求参数、调用次数、最终 DOM 内容和过期响应保护断言。
