# AC-ASSESSMENT-AI-301 自测 AI 报告修复代码质量

- 类型：技术-代码级
- 正式入口：自测报告生成服务、AI 客户端、配置与对应自动化测试。
- 支撑结果：修复对当前上游兼容且失败可控，不依赖硬编码模板或人工改库维持。
- 通过条件：自测链路不存在简版 fallback 构造、返回或持久化路径；真实48题映射优先按稳定题目 ID，题干用于漂移校验，禁止模糊题干和静默默认掩盖错误；固定 fixture 保持 83.8/63.3/高潜力准备型/航拍传媒。AI 正文先对允许标签、任意属性、标签闭合、文档/元数据标签和注释执行 HTML 安全硬校验；随后对 2499/2500/3800/3801 Unicode 码点、默认8章及动态A-G标题/顺序/数量、分数、等级、画像、五维、主方向、证据、限制、训练、金额和证照等内容偏差只记录审计，不阻断安全正文持久化。可信后端模板始终按“方法论 -> 评分仪表盘 -> 正文 -> 唯一品牌区”组装，评分仪表盘只采用规则引擎事实。自动化测试至少覆盖真实48题、未知/冲突映射、内容审计不阻断、自闭合 `br/hr` 属性绕过、危险标签、文档/元数据标签、非法闭合结构、固定块顺序与唯一性、上游错误、空/非HTML、历史简版、持久化失败及 DML 真实前驱；通用知识库客户端兼容能力不被误删。分类13轻量报告查询不得调用通用 `getRecordDetail`、题目统计或逐题答案重建，查询次数不得随题目数增长；通用 `/records/{recordId}` 仍保持原详情与 `answers` 契约。代码不得硬编码数据库完整提示词或真实密钥，允许以可信常量/构造器保存固定方法论、仪表盘和品牌模板，不修改 `uni_modules/**`；JDK 8 编译、目标测试与 `git diff --check` 通过。
- 证据承接：测试命令、通过结果、变更白名单和边界审计统一回写 `061-验收标准/03-测试验证/DEV-049/`。
- 2026-08-27 历史证据：流式改造撤回后曾在 `2aa987f4` 规则引擎基线取得主要回归 `75/75` 和全 reactor `41/41`；该历史结果不覆盖后续内容审计与 HTML 安全验收条件。2026-08-28 本地实施自检已取得 JDK 8 相关回归 `105/105` 与 target 1.8 编译通过，其中无 mock V3 安全回归 `9/9` 覆盖错序闭合与 HTML 注释拒绝；仍须由独立验收代理复跑，且不得冒充测试环境真实完整链路结果。
