# AC-NEWSERVER-201 新测试服务器数据与配置迁移

- 类型：技术-数据级
- 正式入口：`knowledge-graph` 旧机数据与配置、`QwenPaw` 旧机容器和运行卷、新机 Redis，以及 `E:\huiyitechworkspace\feixingxueyuan\服务器信息.txt`、`E:\huiyitechworkspace\feixingxueyuan\数据库信息.txt`。
- 支撑结果：新机 Redis 作为默认新空实例本地持久化部署，旧 Redis 保持不停止，`knowledge-graph` 的 Neo4j 当前数据 / config 与旧机现状一致，`QwenPaw` 的容器、必要运行卷和配置与旧机现状一致，应用直连内网数据库 `172.16.32.2:3306/yunjikeji_test`。
- 技术边界：不把密码、密钥或临时口令写入文档、仓库或日志；不改 DNS，不停旧机；不迁未点名工程的数据与配置。
- 通过条件：迁移后的数据、配置和外部依赖承接关系可回读、可核对、可追溯，新机 Redis 端口 `6379` 的新空实例事实可证实，应用直接使用内网数据库 `172.16.32.2:3306/yunjikeji_test` 且不存在本地数据库代理，旧 Redis 未停止，且没有把临时接入信息变成长期明文配置。
- 证据承接：配置差异、数据快照、容器 / 卷列表、迁移回执和配置回读摘要。

## 验收结果

- 状态：已验证。
- 结果：通过。新机 Redis 为本地 `6379` 新空持久化实例，未迁移旧缓存或会话；Neo4j 数据与运行配置、QwenPaw 必要运行卷与配置已完成迁移并通过运行态核验。
- 数据库：应用直连 `172.16.32.2:3306/yunjikeji_test`，未在新机部署 MySQL，也未设置本地数据库端口代理。
- 一致性与保活：迁移前已完成必要备份；Neo4j 与 QwenPaw 在一致性复制时仅短暂停止旧机对应服务，复制结束后立即恢复，旧 Redis 与旧机应用继续运行。
- 安全边界：正式回写未包含用户名、密码、密钥或临时口令。
