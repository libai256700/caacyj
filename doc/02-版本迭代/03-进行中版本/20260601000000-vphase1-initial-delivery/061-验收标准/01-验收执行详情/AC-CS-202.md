# AC-CS-202 DEV-028 修复闭环

- 类型：闭环项
- 正式入口：DEV-028 企业端与学员端正确红灯、最小实现转绿、接口级 / 数据级 / `chrome-devtools-mcp` 双身份浏览器交互级测试、独立验收、正式验收结论及 Git 提交与远程推送记录。
- 通过条件：`AC-CS-004/005/104/105/106` 在测试环境全部通过且无未关闭缺陷；正确红灯覆盖教员与学员账号 tenant 有值 / `null` 的全局审核通过学员可见、101 名学员在 `pageSize=50` 下形成 3 页、分页 UI 的上一页 / 下一页与 `pageNo`、页码、首末页禁用、翻页数据隔离和每页点击参数、尚未加入组织学员的空态与首次发送、列表 / 会话 / 历史 / 发送零账号 tenant 依赖和权限过滤、有 / 无会话双分支、账号空 tenant 按既有规则规范化为持久 `tenant_id=1`、既有 session/message 持久 tenant 非空、无空值传播或新增默认 tenant、`studentId + conversationId` 归属、学员端兼容和 H5 能力保护；实施与独立验收代理不同，检查通过后提交并推送。
- 失败边界：`pageSize` 不是 50、101 人未形成 3 页、缺少可操作前后页、固定只读第 1 页、`pageNo=1/2/3` 或首末页禁用错误、翻页残留 / 追加 / 重复 / 混入其他页数据、一次自动加载全部，或任意页点击参数不属于当前行；以及 tenant 权限过滤、双键不一致放行、历史 GET 新建 session、合法接收方绑定缺失、持久 tenant 规范化错误、学员端回退，均不得判定 DEV-028 完成。
- 残余风险：本次禁止数据库变更，不新增唯一索引或约束；当前无数据库唯一约束下，并发首次联系同一无会话学员仍可能创建重复 session。本任务只登记并在结论中披露该风险，不得声称强并发唯一已解决。
- 证据承接：`061-验收标准/03-测试验证/DEV-028/`、三层测试索引、`pageSize=50` 与 `pageNo=1/2/3` 请求 / 控件矩阵、三页行集合与往返翻页结果、任意页点击上下文、账号 tenant 有值 / `null`、GET 前后 session 数量、持久 tenant 结果、双键归属、WS、学员端回归、正式结论、提交哈希、远端一致性与 `uni_modules/**` 禁区审计。
