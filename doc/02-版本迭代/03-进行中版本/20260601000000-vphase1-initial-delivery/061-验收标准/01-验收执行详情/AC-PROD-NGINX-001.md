# AC-PROD-NGINX-001 xiaojiapp 生产入口切换后可正常访问

- 类型：业务-交互级
- 业务起点：外部用户通过 `https://xiaojiapp.caacyj.com/` 访问生产入口，DNS 继续指向生产机 `106.13.128.217`。
- 触发动作：访问首页，并按真实页面引用继续访问关键静态资源和本次受影响的 API 路径。
- 关键交互：生产机继续承接 TLS/HTTPS，请求再按原有反代语义转发到新服务器 `106.13.71.85`；访问过程中不得落入默认站点、错误 Host 或旧上游。
- 业务终点：首页、关键静态资源和目标 API 返回与新服务器承载内容一致，状态码和页面行为符合旧入口预期。
- 结果承接：只切换 `xiaojiapp.caacyj.com` 的相关上游；无关域名、无关站点和 DNS 保持原样。
- 核对方式：外部浏览器或 `curl` 以真实域名复核 HTTPS、首页、静态资源和目标 API；必要时结合生产机与新机日志做只读比对。

## 验收结果

- 状态：已验证。
- 结果：通过。`xiaojiapp.caacyj.com` 继续由 `106.13.128.217` 承接 HTTPS，首页、关键静态资源与受影响 API 已按原路径语义转发到 `https://106.13.71.85`；外部域名访问未落默认站点或旧上游，DNS 保持不变。
