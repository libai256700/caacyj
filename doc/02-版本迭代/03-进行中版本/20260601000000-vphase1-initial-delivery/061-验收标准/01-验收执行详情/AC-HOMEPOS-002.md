# AC-HOMEPOS-002 纵向位置恢复

- 类型：业务-交互级
- 前置条件：以提交 `232fcf3a` 和 `DEV-033` 三档截图作为删除前正式位置基线。
- 触发动作：分别在 `360x800`、`390x844`、`430x932` 测量顶部区域、欢迎标题、机器人、考题面板和后续块体位置。
- 期望结果：常规顶部区域为 `166px`、小屏为 `155px`，内容区高度计算分别恢复 `233px / 222px` 扣减，机器人恢复 `140x146px / 128x136px`；考题面板顶部与 `DEV-033` 同视口基线一致，其他内容不因元素删除上移。
- 核对方式：DOM 边界测量、基线 JSON/截图对比和视觉判定。
